# 2026-08-31 TIL — 기초 통계: 확률분포와 표본추출

## 학습 범위

- 2장 2강: 정규분포와 표준화
- 2장 3강: 이항분포와 기대값
- 3장 1강: 모집단과 표본, 표본추출

## 사용 라이브러리

```python
import pandas as pd
import numpy as np
import matplotlib.pyplot as plt
from scipy.stats import norm, shapiro, binom
```

---

## 2장 2강. 정규분포와 표준화

### 1. 정규분포란?

정규분포는 평균을 중심으로 좌우가 대칭인 종 모양의 연속확률분포다. 키, 시험점수, 측정오차처럼 평균 주변에 값이 많이 모이고 평균에서 멀어질수록 값이 적어지는 데이터에서 자주 나타난다.

정규분포는 다음 두 모수로 모양이 결정된다.

- 평균 `μ`: 분포의 중심 위치
- 표준편차 `σ`: 데이터가 평균을 중심으로 퍼진 정도

표준편차가 작으면 분포가 좁고 높아지고, 표준편차가 크면 넓고 낮아진다.

```python
mu = 70
sigma = 5

print("평균 ± 1σ:", mu - sigma, "~", mu + sigma)
print("평균 ± 2σ:", mu - 2 * sigma, "~", mu + 2 * sigma)
print("평균 ± 3σ:", mu - 3 * sigma, "~", mu + 3 * sigma)
```

### 2. 68–95–99.7 규칙

정규분포를 따르는 데이터는 대략 다음 비율로 분포한다.

| 범위 | 포함되는 데이터 비율 |
| --- | ---: |
| 평균 ± 1σ | 약 68% |
| 평균 ± 2σ | 약 95% |
| 평균 ± 3σ | 약 99.7% |

따라서 평균에서 표준편차 3개 이상 떨어진 값은 매우 드문 값으로 볼 수 있다.

### 3. 표준정규분포

표준정규분포는 평균이 0이고 표준편차가 1인 정규분포다. 단위와 범위가 서로 다른 데이터를 같은 기준으로 비교하기 위해 사용한다.

### 4. Z-score 표준화

Z-score는 개별 값이 평균에서 표준편차 몇 개만큼 떨어져 있는지를 나타낸다.

\[
Z=\frac{X-\mu}{\sigma}
\]

- `X`: 관측값
- `μ`: 평균
- `σ`: 표준편차

#### Z-score 해석

- `Z = 0`: 평균과 같은 값
- `Z > 0`: 평균보다 큰 값
- `Z < 0`: 평균보다 작은 값
- `Z = 2`: 평균보다 표준편차 2개만큼 큼
- `Z = -2`: 평균보다 표준편차 2개만큼 작음

```python
a_sales = 550
a_mean = 450
a_std = 50

b_sales = 800
b_mean = 700
b_std = 100

a_z = (a_sales - a_mean) / a_std
b_z = (b_sales - b_mean) / b_std

print("A지점 Z-score:", a_z)
print("B지점 Z-score:", b_z)
```

단위나 평균 규모가 다른 데이터도 Z-score로 변환하면 동일한 기준에서 상대적 위치를 비교할 수 있다.

### 5. Z-score와 표준편차의 차이

| 구분 | 표준편차 | Z-score |
| --- | --- | --- |
| 대상 | 데이터 전체 | 개별 관측값 |
| 의미 | 데이터가 평균 주변에 퍼진 정도 | 값이 평균에서 얼마나 떨어졌는지 |
| 단위 | 원래 데이터와 같은 단위 | 단위 없음 |
| 활용 | 전체 변동성 파악 | 표준화, 상대 비교, 이상치 탐지 |

### 6. 정규성 확인

`shapiro()`는 데이터가 정규분포를 따르는지 검정할 때 사용할 수 있다.

```python
from scipy.stats import shapiro

statistic, p_value = shapiro(data)
print(statistic, p_value)
```

일반적으로 유의수준을 0.05로 설정했다면 다음처럼 해석한다.

- `p-value > 0.05`: 정규성을 기각하기 어려움
- `p-value ≤ 0.05`: 정규분포를 따른다고 보기 어려움

> 정규분포를 눈으로 확인하는 것과 통계적 검정 결과를 함께 살펴보는 것이 좋다.

### 7. 코딩테스트 사전 지식: DFS

DFS(Depth-First Search)는 한 경로를 끝까지 탐색한 뒤 다른 경로로 이동하는 깊이 우선 탐색 방식이다. 스택 또는 재귀함수로 구현할 수 있다.

```python
graph = {
    "A": ["B", "C"],
    "B": ["D"],
    "C": [],
    "D": []
}

stack = ["A"]
visited = set()

while stack:
    current = stack.pop()

    if current not in visited:
        visited.add(current)
        print(current)

        for next_node in graph[current]:
            stack.append(next_node)
```

---

## 2장 3강. 이항분포와 기대값

### 1. 이산확률분포와 연속확률분포

| 구분 | 이산확률분포 | 연속확률분포 |
| --- | --- | --- |
| 값의 형태 | 셀 수 있는 값 | 특정 구간의 모든 실수 |
| 예시 | 주문 건수, 클릭 횟수 | 키, 무게, 대기시간 |
| 확률 계산 | 특정 값의 확률 계산 가능 | 특정 구간의 확률 계산 |

결제수단이나 고객등급처럼 종류를 나타내는 값은 수치형 확률변수가 아니라 범주형 데이터다.

### 2. 이항분포란?

이항분포는 독립적인 시행을 여러 번 반복했을 때 성공 횟수의 확률분포다.

\[
X\sim Bin(n,p)
\]

- `n`: 전체 시행 횟수
- `p`: 한 번 시행에서 성공할 확률
- `k`: 알고 싶은 성공 횟수

#### 이항분포의 조건

1. 시행 횟수 `n`이 정해져 있다.
2. 각 시행의 결과는 성공과 실패 두 가지다.
3. 각 시행의 성공확률 `p`가 일정하다.
4. 각 시행은 서로 독립이다.

### 3. 정확히 k번 성공할 확률

\[
P(X=k)={n\choose k}p^k(1-p)^{n-k}
\]

SciPy에서는 `binom.pmf()`로 계산한다.

```python
from scipy.stats import binom

n = 20
p = 0.1
k = 3

probability = binom.pmf(k, n, p)
print(f"정확히 3명이 클릭할 확률: {probability:.4f}")
```

### 4. PMF와 CDF

- `binom.pmf(k, n, p)`: 정확히 `k`번 성공할 확률
- `binom.cdf(k, n, p)`: `k`번 이하로 성공할 누적확률
- `1 - binom.cdf(k - 1, n, p)`: `k`번 이상 성공할 확률

```python
exactly_3 = binom.pmf(3, 20, 0.1)
at_most_3 = binom.cdf(3, 20, 0.1)
at_least_3 = 1 - binom.cdf(2, 20, 0.1)
```

### 5. 기대값과 분산

이항분포의 기대값은 시행을 장기간 반복했을 때 기대되는 평균 성공 횟수다.

\[
E(X)=np
\]

이항분포의 분산은 다음과 같다.

\[
Var(X)=np(1-p)
\]

```python
n = 200
p = 0.1

expected_value = n * p
variance = n * p * (1 - p)

print("기대값:", expected_value)
print("분산:", variance)
```

SciPy의 `binom.stats()`를 이용할 수도 있다.

```python
mean, variance = binom.stats(n, p)
print(mean, variance)
```

### 6. 이항분포의 정규근사

시행 횟수가 충분히 크면 이항분포를 평균과 분산이 같은 정규분포로 근사할 수 있다.

\[
\mu=np
\]

\[
\sigma=\sqrt{np(1-p)}
\]

정규근사는 일반적으로 `np`와 `n(1-p)`가 충분히 클 때 사용한다. 이산형 이항분포를 연속형 정규분포로 근사하므로 정확도를 높이기 위해 `±0.5`의 연속성 보정을 적용할 수 있다.

```python
from scipy.stats import binom, norm
import numpy as np

n = 1000
p = 0.2
k = 220

exact_probability = binom.cdf(k, n, p)

mean = n * p
std = np.sqrt(n * p * (1 - p))
approx_probability = norm.cdf(k + 0.5, loc=mean, scale=std)

print("이항분포:", exact_probability)
print("정규근사:", approx_probability)
```

---

## 3장 1강. 모집단과 표본, 표본추출

### 1. 모집단과 표본

- **모집단(Population)**: 분석하거나 알고 싶은 전체 대상
- **표본(Sample)**: 모집단에서 실제로 조사하기 위해 선택한 일부 대상

모집단 전체를 조사하는 전수조사는 정확도가 높지만 시간과 비용이 많이 든다. 따라서 모집단을 잘 대표하는 표본을 추출하여 조사하는 경우가 많다.

```python
population = pd.Series([
    32000, 45000, 28000, 51000, 39000,
    62000, 47000, 35000, 41000, 55000
])

sample = population.sample(n=5, random_state=42)

population_mean = population.mean()
sample_mean = sample.mean()

print("모집단 평균:", population_mean)
print("표본 평균:", sample_mean)
```

### 2. 모수와 통계량

- **모수(Parameter)**: 모집단의 특성을 나타내는 값
- **통계량(Statistic)**: 표본에서 계산한 값

| 대상 | 대표값 | 예시 |
| --- | --- | --- |
| 모집단 | 모수 | 전체 고객의 실제 평균 만족도 |
| 표본 | 통계량 | 조사한 500명의 평균 만족도 4.2점 |

표본에서 계산한 통계량을 이용해 모집단의 모수를 추정한다.

### 3. 표본추출 방법

#### 단순 무작위 추출(Simple Random Sampling)

모집단의 모든 대상이 동일한 확률로 선택되는 방법이다.

```python
sample = customers.sample(n=5, random_state=42)
```

#### 층화 추출(Stratified Sampling)

모집단을 성별, 연령대, 지역 등 중요한 기준에 따라 여러 층으로 나눈 뒤 각 층에서 표본을 추출한다. 모든 주요 집단을 골고루 반영할 수 있다.

```python
sample = (
    df.groupby("gender", group_keys=False)
      .apply(lambda group: group.sample(n=1, random_state=42))
)
```

#### 군집 추출(Cluster Sampling)

모집단을 자연스럽게 형성된 군집으로 나눈 뒤 일부 군집을 선택하여 그 군집의 전체 또는 일부를 조사한다.

예: 전국 매장 중 특정 매장 몇 곳을 선택하여 해당 매장의 고객을 조사한다.

#### 계통 추출(Systematic Sampling)

임의의 시작점을 정한 뒤 일정한 간격으로 표본을 선택한다.

```python
k = len(df) // 100
sample = df.iloc[::k]
```

### 4. 표본추출 방법 비교

| 방법 | 핵심 | 적합한 상황 | 주의점 |
| --- | --- | --- | --- |
| 단순 무작위 | 모두에게 같은 선택 기회 | 모집단이 비교적 균일함 | 소수 집단이 빠질 수 있음 |
| 층화 추출 | 각 층에서 골고루 선택 | 성별·연령 등 집단 반영이 중요함 | 층 구분 기준이 필요함 |
| 군집 추출 | 일부 군집을 선택 | 전국 매장처럼 범위가 넓음 | 선택한 군집이 편향될 수 있음 |
| 계통 추출 | 일정 간격으로 선택 | 목록에서 빠르게 추출 | 데이터의 주기성과 겹치면 편향 가능 |

### 5. 표본의 크기와 대표성

표본이 크다고 해서 반드시 모집단을 잘 대표하는 것은 아니다. 표본의 크기보다 모집단의 다양한 특성이 편향 없이 반영되도록 **어떻게 추출했는지**가 중요하다.

예를 들어 전국 고객을 조사하면서 서울 고객만 많이 포함했다면 표본 수가 많아도 전국 고객을 대표한다고 보기 어렵다.

### 6. 표본추출 편향(Sampling Bias)

표본추출 편향은 특정 집단이 표본에 과도하게 포함되거나 제외되어 표본이 모집단을 제대로 대표하지 못하는 문제다.

대표적인 원인:

- 접근하기 쉬운 대상만 조사함
- 자발적으로 응답한 사람만 포함함
- 특정 시간이나 지역에만 조사함
- 모집단의 일부 집단을 누락함

편향된 표본에서는 표본 통계량을 모집단 전체로 일반화하기 어렵다.

---

## 오늘의 오류와 해결

### 1. SciPy 모듈 설치 오류

#### 오류

```text
ModuleNotFoundError: No module named 'scipy'
```

#### 원인

현재 Jupyter 가상환경에 SciPy가 설치되어 있지 않았다.

#### 해결

```python
%pip install scipy matplotlib
```

Jupyter에서는 현재 커널 환경에 설치하기 위해 `%pip` 사용을 권장한다.

### 2. Smart App Control의 DLL 차단

#### 오류

```text
ImportError: DLL load failed
애플리케이션 제어 정책에서 이 파일을 차단했습니다.
```

#### 원인

SciPy 설치는 완료되었지만 Windows Smart App Control이 SciPy 내부 실행 파일을 차단했다.

#### 해결 과정

1. Windows 보안의 Smart App Control 설정 확인
2. 설정 변경
3. Jupyter 커널만 재시작하는 것이 아니라 PC 전체 재시작
4. 라이브러리 불러오기 재실행
5. 연습문제 출력까지 확인

```python
from scipy.stats import norm, shapiro, binom
import matplotlib.pyplot as plt

print("불러오기 성공!")
```

> 보안 설정 변경은 시스템 보호 수준에 영향을 줄 수 있으므로 신뢰할 수 있는 패키지인지 확인한 후 신중하게 결정해야 한다.

### 3. 미완성 대입문으로 인한 SyntaxError

#### 오류 코드

```python
probability =
```

#### 원인

대입 연산자 `=` 오른쪽에 저장할 값이나 계산식이 없었다.

#### 해결

```python
probability = binom.pmf(k, n, p)
```

### 4. Code 셀에 서술형 답안을 입력한 오류

#### 오류

```text
SyntaxError: invalid character '→'
```

#### 원인

사람이 읽는 설명 문장을 Python이 실행하는 Code 셀에 입력했다.

#### 해결

- 설명, 제목, 서술형 정답, 결과 해석 → `Markdown` 셀
- `import`, 변수, 계산, 함수, `print()` → `Code` 셀
- Code 셀 안의 메모 → 앞에 `#`을 붙여 주석 처리

```python
# 전체 고객 10,000명은 모집단이다.
```

### Jupyter 셀 판단 기준

> 컴퓨터가 실행할 내용이면 Code, 사람이 읽을 내용이면 Markdown

| 문제 표현 | 셀 종류 |
| --- | --- |
| 계산하세요, 출력하세요, 코드를 작성하세요 | Code |
| 설명하세요, 분류하세요, 해석하세요 | Markdown |

---

## 헷갈리기 쉬운 개념 비교

### 표준편차 vs Z-score

- 표준편차: 데이터 전체의 퍼짐 정도
- Z-score: 개별 값이 평균에서 표준편차 몇 개만큼 떨어졌는지 나타내는 값

### 모집단·표본 vs 모수·통계량

- 모집단과 표본: 사람이나 데이터의 **대상 범위**
- 모수와 통계량: 대상에서 계산한 평균·분산 등의 **숫자**

```text
모집단 → 모수
표본   → 통계량
```

### PMF vs CDF

- `PMF`: 정확히 특정 횟수일 확률
- `CDF`: 특정 횟수 이하의 누적확률

### 단순 무작위 추출 vs 층화 추출

- 단순 무작위 추출: 모두에게 동일한 추출 기회를 줌
- 층화 추출: 중요한 집단별로 나눈 뒤 각 집단에서 추출함

---

## 암기 포인트

1. 정규분포는 평균을 중심으로 좌우 대칭인 종 모양이다.
2. 정규분포의 모양은 평균 `μ`와 표준편차 `σ`가 결정한다.
3. 평균 ± 1σ, 2σ, 3σ 안에는 약 68%, 95%, 99.7%가 포함된다.
4. 표준정규분포는 평균 0, 표준편차 1이다.
5. `Z = (X - 평균) ÷ 표준편차`
6. 이항분포는 시행 횟수 고정, 성공·실패, 일정한 성공확률, 독립 시행이 조건이다.
7. 정확히 `k`번 성공할 확률은 `binom.pmf(k, n, p)`다.
8. `k`번 이하의 확률은 `binom.cdf(k, n, p)`다.
9. 이항분포의 기대값은 `np`, 분산은 `np(1-p)`다.
10. 모집단의 특성값은 모수, 표본의 특성값은 통계량이다.
11. 표본이 크다고 무조건 대표성이 높은 것은 아니며 추출 방법이 중요하다.
12. 컴퓨터가 실행할 내용은 Code, 사람이 읽을 설명은 Markdown이다.

### 함수 암기표

| 목적 | 코드 |
| --- | --- |
| 정규분포 누적확률 | `norm.cdf(x, loc=평균, scale=표준편차)` |
| 정규분포 확률밀도 | `norm.pdf(x, loc=평균, scale=표준편차)` |
| 정규성 검정 | `shapiro(data)` |
| 이항분포 정확히 k번 | `binom.pmf(k, n, p)` |
| 이항분포 k번 이하 | `binom.cdf(k, n, p)` |
| 이항분포 평균·분산 | `binom.stats(n, p)` |
| 무작위 표본추출 | `df.sample(n=개수, random_state=42)` |

---

## 🌟 최종정리

정규분포는 평균과 표준편차를 이용해 연속형 데이터의 분포를 설명하고, Z-score는 서로 다른 단위와 규모의 데이터를 같은 기준으로 비교하게 해준다. 이항분포는 성공과 실패로 나뉘는 독립 시행에서 성공 횟수의 확률을 계산하며, 기대값과 분산으로 장기적인 평균과 변동성을 파악할 수 있다. 모집단 전체를 조사하기 어려울 때는 표본을 사용하지만, 올바른 추론을 위해서는 표본의 크기뿐 아니라 모집단을 대표하도록 편향 없이 추출하는 과정이 중요하다.

## 💡 느낀점

표준편차와 Z-score, 모집단과 모수처럼 비슷하게 들리는 용어도 각각 데이터 전체, 개별 값, 조사 대상, 계산된 숫자처럼 역할이 분명히 다르다는 점을 알게 되었다. 또한 표본이 많기만 하면 좋은 것이 아니라 어떤 방법으로 뽑았는지가 분석 결과의 신뢰도를 결정한다는 점이 중요하게 느껴졌다. 실습에서는 오류 메시지가 반드시 어려운 설치 문제를 뜻하는 것이 아니며, 미완성 코드나 셀 종류처럼 기본 설정도 먼저 확인해야 한다는 점을 배웠다.

## 📝 한줄

같은 기준으로 비교하고, 올바르게 추출해야 데이터에서 믿을 수 있는 결론을 얻을 수 있다.

## 🌱 목표

정규분포·Z-score·이항분포의 핵심 공식과 SciPy 함수를 구분해서 사용하고, 분석 목적과 모집단의 구조에 맞는 표본추출 방법을 스스로 선택할 수 있도록 연습한다.
