# 📚 TIL - SQL & 데이터베이스 기초부터 데이터 조회까지

## 📌 오늘의 학습 범위

- 1장 1강 : SQL, Data 기초
- 1장 2강 : 데이터베이스 구조
- 1장 3강 : 테이블 간 관계와 ERD 읽기
- 1장 4강 : 테이블 생성, 데이터 입력 문법
- 2장 1강 : 데이터 조회하기

---

# 1️⃣ 1장 1강 - SQL, Data 기초

## 1. 데이터 사이언티스트의 역할

데이터 사이언티스트는 단순히 데이터를 보는 사람이 아니라 데이터를 활용하여 문제를 발견하고 해결하는 역할을 한다.

주요 역량은 다음과 같다.

- SQL을 이용하여 필요한 데이터를 가져오는 능력
- Python 등을 이용하여 데이터를 분석하는 능력
- 통계 및 머신러닝을 활용하는 능력
- 데이터를 시각화하여 의미를 전달하는 능력
- 비즈니스 문제를 데이터와 연결하는 능력

### 핵심 흐름

```text
문제 정의
→ 데이터 수집
→ 데이터 분석
→ 결과 해석
→ 의사결정 지원
```

---

## 2. Data / Database / DBMS / SQL / Query

### Data

데이터는 문자, 숫자, 날짜 등 저장되고 분석될 수 있는 정보를 의미한다.

---

### Database

데이터를 일정한 구조로 저장하고 관리하는 공간이다.

```text
Database
→ 여러 데이터를 체계적으로 저장하는 공간
```

---

### DBMS

DBMS는 `Database Management System`의 약자로 데이터베이스를 관리하는 소프트웨어이다.

대표적인 DBMS:

| DBMS | 특징 |
|---|---|
| PostgreSQL | 오픈소스 관계형 DBMS |
| MySQL | 웹 서비스에서 많이 사용 |
| Oracle | 대규모 기업 환경에서 많이 사용 |
| SQL Server | Microsoft 기반 DBMS |
| SQLite | 가볍고 별도 서버 없이 사용 가능 |
| MariaDB | MySQL 기반 오픈소스 DBMS |

### 핵심

```text
Database → 데이터가 저장되는 곳
DBMS     → Database를 관리하는 프로그램
```

---

### Query

Query는 데이터베이스에 보내는 **요청**을 의미한다.

예:

```text
"2026년 주문 데이터를 보여줘"
```

이러한 요청을 SQL 문법으로 작성할 수 있다.

---

### SQL

SQL은 `Structured Query Language`의 약자로 관계형 데이터베이스에 명령을 전달하는 언어이다.

예:

```sql
SELECT *
FROM orders;
```

### 핵심

```text
Query → DB에 보내는 요청
SQL   → 그 요청을 작성하는 언어
```

---

## 3. PostgreSQL과 DBeaver

이번 실습에서는 다음 프로그램을 사용했다.

```text
PostgreSQL
→ 실제 데이터베이스를 관리하는 DBMS

DBeaver
→ PostgreSQL 등에 접속하여 SQL을 작성하고 실행하는 GUI 도구
```

### 환경 구축

1. PostgreSQL 설치
2. pgAdmin 설치 확인
3. DBeaver 설치
4. PostgreSQL JDBC Driver 설치
5. DBeaver에서 PostgreSQL 연결

연결 정보:

```text
Host     : localhost
Port     : 5432
Database : postgres
Username : postgres
Password : PostgreSQL 설치 시 설정한 비밀번호
```

`Test Connection`에서 `Connected`가 출력되면 정상 연결된 것이다.

---

## 4. 관계형 데이터베이스의 데이터 저장 구조

관계형 데이터베이스는 데이터를 **테이블(Table)** 형태로 저장한다.

예:

| customer_id | name | email |
|---|---|---|
| 1 | 김가영 | user1@email.com |
| 2 | 홍길동 | user2@email.com |

### 기본 용어

```text
Table  → 데이터를 저장하는 표
Row    → 한 개의 데이터 / 레코드
Column → 데이터의 속성
```

예를 들어:

```text
Table  → customers
Row    → 고객 한 명
Column → name, email, customer_id
```

---

## 5. SQL 작성 순서와 실행 순서

SQL은 작성하는 순서와 실제 데이터베이스가 처리하는 순서가 다르다.

### SQL 작성 순서

```text
SELECT
→ FROM
→ WHERE
→ GROUP BY
→ HAVING
→ ORDER BY
```

### ⭐ SQL 실제 실행 순서

```text
FROM
→ WHERE
→ GROUP BY
→ HAVING
→ SELECT
→ ORDER BY
```

### ⭐ 암기

```text
작성 : SELECT → FROM → WHERE → GROUP BY → HAVING → ORDER BY

실행 : FROM → WHERE → GROUP BY → HAVING → SELECT → ORDER BY
```

---

# 2️⃣ 1장 2강 - 데이터베이스 구조

## 1. 데이터 타입

데이터베이스의 각 컬럼에는 저장할 데이터의 형태에 맞는 데이터 타입을 지정한다.

| 데이터 타입 | 의미 | 예 |
|---|---|---|
| `INT` | 정수 | 100 |
| `VARCHAR(n)` | 길이가 제한된 문자열 | 이름, 이메일 |
| `TEXT` | 길이가 긴 문자열 | 설명 |
| `DATE` | 날짜 | 2026-08-19 |
| `DECIMAL(p,s)` | 소수점이 있는 숫자 | 가격 |
| `BOOLEAN` | 참/거짓 | TRUE / FALSE |

---

## 2. VARCHAR

```sql
VARCHAR(50)
```

은 최대 50자의 문자열을 저장하도록 설정한 것이다.

### `VARCHAR` vs `TEXT`

```text
VARCHAR(n)
→ 최대 길이를 지정

TEXT
→ 긴 문자열 저장 가능
```

---

## 3. 스키마 Schema

스키마는 데이터베이스 안에서 테이블 등을 논리적으로 묶어 관리하는 구조이다.

PostgreSQL에서는 기본적으로 `public` 스키마를 사용한다.

예:

```text
Database
└─ Schema
   ├─ customers
   ├─ orders
   └─ products
```

---

## 4. 제약조건 Constraint

데이터를 저장할 때 반드시 지켜야 하는 규칙이다.

| 제약조건 | 의미 |
|---|---|
| `PRIMARY KEY` | 행을 유일하게 식별 |
| `FOREIGN KEY` | 다른 테이블의 키 참조 |
| `NOT NULL` | NULL 금지 |
| `UNIQUE` | 중복 값 금지 |
| `CHECK` | 지정 조건 확인 |
| `DEFAULT` | 기본값 지정 |

---

## 5. PRIMARY KEY

PK는 각 행을 유일하게 식별하는 값이다.

```sql
customer_id INT PRIMARY KEY
```

### 특징

```text
중복 X
NULL X
행마다 고유한 값
```

---

## 6. FOREIGN KEY

FK는 다른 테이블의 PK를 참조하여 테이블 간 관계를 연결한다.

예:

```sql
CREATE TABLE orders (
    order_id INT PRIMARY KEY,
    customer_id INT,
    FOREIGN KEY (customer_id)
        REFERENCES customers(customer_id)
);
```

여기서:

```text
customers.customer_id → PK
orders.customer_id    → FK
```

---

## ⭐ PK vs FK

| 구분 | PK | FK |
|---|---|---|
| 역할 | 행을 유일하게 식별 | 다른 테이블 연결 |
| 중복 | 불가능 | 가능 |
| NULL | 불가능 | 설정에 따라 가능 |
| 대상 | 자신의 테이블 | 다른 테이블의 PK |

---

## 7. 데이터 무결성

데이터 무결성이란 데이터가 정확하고 일관된 상태를 유지하는 것을 의미한다.

대표적인 제약조건:

```text
PRIMARY KEY
NOT NULL
UNIQUE
FOREIGN KEY
CHECK
```

이러한 제약조건을 이용하여 잘못된 데이터가 저장되는 것을 막을 수 있다.

---

## 8. 참조 무결성

FK를 이용하여 연결된 데이터가 실제로 존재하도록 유지하는 규칙이다.

예를 들어:

```text
products 테이블에 product_id = 999가 없음
```

그런데 orders 테이블에

```text
product_id = 999
```

를 입력하려고 하면 FK 오류가 발생할 수 있다.

### 핵심

```text
FK 값은 참조 대상 테이블에 실제로 존재해야 한다.
```

---

## 9. 정규화 Normalization

정규화는 데이터 중복을 줄이고 테이블을 효율적으로 나누는 과정이다.

정규화 전:

| order_id | customer_name | customer_email | product |
|---|---|---|---|
| 101 | 가영 | a@email.com | 노트북 |
| 102 | 가영 | a@email.com | 마우스 |

고객 정보가 반복된다.

정규화 후:

### customers

| customer_id | customer_name | customer_email |
|---|---|---|
| 1 | 가영 | a@email.com |

### orders

| order_id | customer_id | product |
|---|---|---|
| 101 | 1 | 노트북 |
| 102 | 1 | 마우스 |

### 핵심

```text
정규화
→ 데이터 중복 감소
→ 데이터 일관성 향상
→ 필요한 경우 JOIN으로 다시 연결
```

---

# 3️⃣ 1장 3강 - 테이블 간 관계와 ERD 읽기

## 1. 테이블 관계

관계형 데이터베이스에서는 여러 테이블이 PK와 FK를 통해 연결된다.

대표적인 관계:

```text
1:1
1:N
N:M
```

---

## 2. 1:1 관계

한 테이블의 한 행이 다른 테이블의 한 행과 연결되는 관계이다.

예:

```text
사용자 1명 ↔ 사용자 프로필 1개
```

```text
users 1 : 1 user_profiles
```

---

## 3. 1:N 관계

한 테이블의 하나의 행이 다른 테이블의 여러 행과 연결되는 관계이다.

예:

```text
고객 1명
↓
여러 개의 주문
```

```text
customers 1 : N orders
```

예:

```text
customers.customer_id → PK
orders.customer_id    → FK
```

---

## 4. N:M 관계

양쪽 테이블의 여러 행이 서로 여러 행과 연결되는 관계이다.

예:

```text
학생 여러 명 ↔ 강의 여러 개
```

하지만 관계형 데이터베이스에서는 N:M 관계를 직접 표현하기보다 **중간 테이블**을 이용한다.

```text
students
   ↓
enrollments
   ↑
courses
```

관계:

```text
students 1:N enrollments
courses  1:N enrollments
```

결과적으로:

```text
students N:M courses
```

### ⭐ N:M 핵심

```text
N:M 관계
→ 중간 테이블을 만든다.
```

중간 테이블에는 보통 양쪽 테이블의 FK가 들어간다.

---

## 5. ERD

ERD는 `Entity Relationship Diagram`의 약자로 데이터베이스 구조와 테이블 간 관계를 그림으로 표현한 것이다.

### 주요 구성요소

```text
Entity       → 테이블
Attribute    → 컬럼
Relationship → 테이블 간 관계
```

---

## 6. ERD 읽는 순서

ERD를 볼 때는 다음 순서로 확인한다.

```text
1. 테이블 확인
2. PK 확인
3. FK 확인
4. FK가 어느 PK를 참조하는지 확인
5. 1:1 / 1:N / N:M 관계 판단
6. 필요한 JOIN 경로 확인
```

---

## 7. Crow's Foot 표기

ERD에서는 테이블 관계를 나타내기 위해 Crow's Foot 표기법을 사용한다.

기본 개념:

```text
| → 하나
O → 0개 가능
< → 여러 개
```

예:

```text
|---<
```

는 기본적으로:

```text
1 : N
```

관계를 표현한다.

---

## 8. ERD와 JOIN

ERD를 읽을 수 있으면 어떤 컬럼을 이용하여 테이블을 JOIN해야 하는지 알 수 있다.

예:

```text
customers.customer_id = orders.customer_id
orders.order_id        = order_items.order_id
```

SQL:

```sql
SELECT
    c.name,
    o.order_id
FROM customers AS c
JOIN orders AS o
    ON c.customer_id = o.customer_id;
```

### ⭐ 핵심

```text
PK / FK 관계 확인
→ 어떤 테이블끼리 연결되는지 파악
→ JOIN 조건 작성
```

---

# 4️⃣ 1장 4강 - 테이블 생성과 데이터 입력

## 1. DDL과 DML

SQL 명령은 역할에 따라 여러 종류로 나뉜다.

### DDL

DDL은 `Data Definition Language`로 데이터베이스의 **구조를 정의**한다.

대표 명령:

```text
CREATE
ALTER
DROP
```

---

### DML

DML은 `Data Manipulation Language`로 실제 데이터를 다룬다.

대표 명령:

```text
SELECT
INSERT
UPDATE
DELETE
```

---

## ⭐ DDL vs DML

| 구분 | 역할 | 주요 명령 |
|---|---|---|
| DDL | 구조 정의 | CREATE, ALTER, DROP |
| DML | 데이터 처리 | SELECT, INSERT, UPDATE, DELETE |

### 암기

```text
DDL → 구조
DML → 데이터
```

---

## 2. CREATE TABLE

새로운 테이블을 생성한다.

기본 구조:

```sql
CREATE TABLE 테이블명 (
    컬럼명 데이터타입 제약조건,
    컬럼명 데이터타입 제약조건
);
```

예:

```sql
CREATE TABLE users (
    user_id INT PRIMARY KEY,
    username VARCHAR(50) NOT NULL,
    email VARCHAR(100) UNIQUE,
    created_at DATE NOT NULL
);
```

---

## 3. DEFAULT

값을 입력하지 않았을 때 자동으로 사용할 기본값을 지정할 수 있다.

```sql
CREATE TABLE customers (
    customer_id INT PRIMARY KEY,
    active BOOLEAN DEFAULT TRUE
);
```

---

## 4. REFERENCES

FK를 생성할 때 `REFERENCES`를 사용한다.

```sql
CREATE TABLE orders (
    order_id INT PRIMARY KEY,
    customer_id INT,
    FOREIGN KEY (customer_id)
        REFERENCES customers(customer_id)
);
```

---

## 5. DROP TABLE

테이블 자체를 삭제한다.

```sql
DROP TABLE test_events;
```

### ⭐ DROP TABLE vs DELETE FROM

```text
DROP TABLE
→ 테이블 구조 + 데이터 전부 삭제

DELETE FROM
→ 테이블은 남겨두고 데이터만 삭제
```

---

## 6. INSERT INTO

테이블에 새로운 데이터를 추가한다.

기본 구조:

```sql
INSERT INTO 테이블명 (
    컬럼1,
    컬럼2,
    컬럼3
)
VALUES (
    값1,
    값2,
    값3
);
```

예:

```sql
INSERT INTO products (
    product_id,
    product_name,
    list_price
)
VALUES (
    1,
    'Mountain Bike',
    1200.00
);
```

### ⭐ 중요

```text
지정한 컬럼 순서
=
VALUES 값의 순서
```

---

## 7. 여러 행 한 번에 INSERT

여러 데이터를 한 번에 입력할 수 있다.

```sql
INSERT INTO products (
    product_id,
    product_name,
    list_price
)
VALUES
    (1, 'Mountain Bike', 1200.00),
    (2, 'Road Bike', 1800.00),
    (3, 'Electric Bike', 2500.00);
```

### 핵심

```text
VALUES 뒤에
(값...), (값...), (값...)
형태로 연결
```

---

# 🧠 SQL 1장 암기노트

## ⭐ 기본 용어

```text
Data     → 분석·저장할 정보
Database → 데이터를 구조적으로 저장하는 공간
DBMS     → Database를 관리하는 프로그램
SQL      → Database에 명령을 전달하는 언어
Query    → Database에 보내는 요청
```

---

## ⭐ 테이블 구조

```text
Table  → 표
Row    → 한 행 / 하나의 데이터
Column → 데이터 속성
```

---

## ⭐ PK / FK

```text
PK
→ 행을 유일하게 식별
→ 중복 X
→ NULL X

FK
→ 다른 테이블의 PK 참조
→ 테이블 관계 연결
```

---

## ⭐ 주요 제약조건

```text
PRIMARY KEY → 고유 식별
FOREIGN KEY → 다른 테이블 참조
NOT NULL    → NULL 금지
UNIQUE      → 중복 금지
CHECK       → 조건 확인
DEFAULT     → 기본값
```

---

## ⭐ 관계

```text
1:1 → 하나 ↔ 하나

1:N → 하나 ↔ 여러 개

N:M → 여러 개 ↔ 여러 개
      → 중간 테이블 필요
```

---

## ⭐ ERD

```text
Entity       → 테이블
Attribute    → 컬럼
Relationship → 테이블 관계
```

ERD 읽기:

```text
PK 찾기
→ FK 찾기
→ 어떤 PK를 참조하는지 확인
→ 관계 파악
→ JOIN 경로 파악
```

---

## ⭐ DDL / DML

```text
DDL → 구조 정의
CREATE
ALTER
DROP

DML → 데이터 처리
SELECT
INSERT
UPDATE
DELETE
```

---

## ⭐ CREATE / DROP / INSERT

```text
CREATE TABLE → 테이블 생성

DROP TABLE → 테이블 자체 삭제

INSERT INTO → 데이터 추가
```

---

## ⭐ SQL 작성 순서

```text
SELECT
FROM
WHERE
GROUP BY
HAVING
ORDER BY
```

## ⭐ SQL 실행 순서

```text
FROM
WHERE
GROUP BY
HAVING
SELECT
ORDER BY
```

---

# 5️⃣ 2장 1강 - 데이터 조회하기

## 1. 실습 데이터

이번 실습에서는 Kaggle의 Bike Store Sample Database 데이터를 PostgreSQL에 불러와 사용했다.

주요 테이블:

```text
customers
orders
order_items
products
staffs
stocks
```

테이블 간 관계는 PK와 FK를 기준으로 연결된다.

예:

```text
customers → orders → order_items → products
```

---

## 2. 데이터 개수 확인

테이블에 데이터가 정상적으로 들어갔는지 확인할 때 `COUNT(*)`를 사용할 수 있다.

```sql
SELECT COUNT(*) FROM customers;
```

### 실제 실습 확인 결과

```text
customers → 1,445건
products  → 321건
stocks    → 939건
```

### 핵심

```text
테이블이 존재한다
≠
데이터가 들어 있다
```

따라서 Import 후에는 `COUNT(*)`로 행 개수를 확인하는 것이 좋다.

---

# ⚠️ 실습 중 발생한 오류와 해결방법

## 1. CSV Import 중 Batch Insert 오류

DBeaver에서 `products.csv`를 Import하던 중 다음 오류가 발생했다.

```text
Error occurred during batch insert

character varying(50) 자료형에
너무 긴 자료를 담으려고 합니다.
```

---

## 원인

`products` 테이블의 `product_name` 컬럼이 다음과 같이 설정되어 있었다.

```sql
VARCHAR(50)
```

하지만 CSV에 저장된 일부 `product_name` 데이터가 50자를 초과하여 INSERT에 실패했다.

이 때문에:

```sql
SELECT COUNT(*) FROM products;
```

실행 결과가:

```text
0
```

으로 나타났다.

즉:

```text
테이블은 생성됨
BUT
데이터 입력은 실패
```

상태였다.

---

## 해결

`ALTER TABLE`을 사용하여 `product_name` 컬럼의 길이를 늘렸다.

```sql
ALTER TABLE products
ALTER COLUMN product_name TYPE VARCHAR(255);
```

그 후:

```text
products.csv 다시 Import
```

하였다.

---

## 결과 확인

다시 실행:

```sql
SELECT COUNT(*) FROM products;
```

결과:

```text
321
```

### ✅ 해결 완료

```text
오류 발생
→ 오류 메시지 확인
→ VARCHAR 길이 부족 확인
→ ALTER TABLE로 VARCHAR(255) 변경
→ CSV 재Import
→ COUNT(*)로 확인
→ 321건 정상 입력
```

---

## ⭐ 오류 해결 메모

```text
VARCHAR(n)보다 긴 문자열을 넣으면
INSERT 오류가 발생할 수 있다.

해결:
ALTER TABLE
→ 컬럼 타입/길이 수정
→ 데이터 재입력
→ COUNT(*) 확인
```

---

# ⭐ UNION ALL 별표 메모

여러 테이블의 `COUNT(*)` 결과를 한 화면에서 확인하고 싶을 때 `UNION ALL`을 사용할 수 있다.

```sql
SELECT 'customers' AS table_name, COUNT(*) AS count
FROM customers

UNION ALL

SELECT 'orders', COUNT(*)
FROM orders

UNION ALL

SELECT 'order_items', COUNT(*)
FROM order_items

UNION ALL

SELECT 'products', COUNT(*)
FROM products

UNION ALL

SELECT 'staffs', COUNT(*)
FROM staffs

UNION ALL

SELECT 'stocks', COUNT(*)
FROM stocks;
```

결과는 다음처럼 한 표로 확인할 수 있다.

```text
customers    1445
orders       ...
order_items  ...
products      321
staffs        ...
stocks        939
```

## ⭐ UNION vs UNION ALL

```text
UNION
→ 여러 SELECT 결과를 세로로 합침
→ 중복 제거

UNION ALL
→ 여러 SELECT 결과를 세로로 합침
→ 중복 제거하지 않음
```

### 암기

```text
UNION     → 합치고 중복 제거
UNION ALL → 그냥 전부 합치기
```

---

# 6️⃣ SELECT와 FROM

데이터를 조회할 때 가장 기본적인 명령이다.

기본 구조:

```sql
SELECT 컬럼명
FROM 테이블명;
```

예:

```sql
SELECT first_name, last_name, email
FROM customers;
```

### 의미

```text
SELECT → 어떤 컬럼을 가져올지 선택
FROM   → 어느 테이블에서 가져올지 지정
```

---

## 모든 컬럼 조회

```sql
SELECT *
FROM customers;
```

`*`는 모든 컬럼을 의미한다.

### 주의

실무에서는 필요한 컬럼만 명시하는 것이 더 좋은 경우가 많다.

```sql
SELECT customer_id, first_name, email
FROM customers;
```

---

# 7️⃣ DISTINCT - 중복 제거

중복된 값을 제거하여 조회할 때 사용한다.

```sql
SELECT DISTINCT store_id
FROM orders;
```

### 핵심

```text
DISTINCT
→ 조회 결과의 중복 제거
```

예를 들어 원본:

```text
1
1
1
2
2
```

`DISTINCT` 적용:

```text
1
2
```

---

# 8️⃣ WHERE - 조건으로 데이터 필터링

조건을 만족하는 행만 조회한다.

```sql
SELECT product_name, list_price
FROM products
WHERE list_price > 2000;
```

### 핵심

```text
WHERE
→ Row를 조건으로 필터링
```

---

## 비교 연산자

| 연산자 | 의미 |
|---|---|
| `=` | 같다 |
| `!=`, `<>` | 같지 않다 |
| `>` | 크다 |
| `<` | 작다 |
| `>=` | 크거나 같다 |
| `<=` | 작거나 같다 |

---

## AND

여러 조건을 모두 만족해야 한다.

```sql
SELECT *
FROM products
WHERE list_price >= 1000
AND list_price <= 2000;
```

### 암기

```text
AND → 모든 조건 만족
```

---

## OR

여러 조건 중 하나라도 만족하면 된다.

```sql
SELECT *
FROM products
WHERE brand_id = 1
OR brand_id = 2;
```

### 암기

```text
OR → 하나라도 만족
```

---

# 9️⃣ CASE WHEN

조건에 따라 값을 분류하거나 새로운 값을 만들 때 사용한다.

Python의 `if / elif / else`와 비슷한 역할을 한다.

```sql
SELECT
    product_name,
    list_price,
    CASE
        WHEN list_price < 1000 THEN '저가'
        WHEN list_price < 2000 THEN '중간'
        ELSE '고가'
    END AS product_grade
FROM products;
```

### 핵심

```text
CASE
WHEN 조건 THEN 결과
WHEN 조건 THEN 결과
ELSE 결과
END
```

### ⭐ Python과 비교

```text
Python
if / elif / else

SQL
CASE WHEN / WHEN / ELSE / END
```

---

# 🔟 자주 사용하는 WHERE 연산자

## BETWEEN

범위 안에 있는 값을 찾는다.

```sql
SELECT *
FROM products
WHERE list_price BETWEEN 1000 AND 2000;
```

### 핵심

```text
BETWEEN A AND B
→ A 이상 B 이하
```

---

## IN

여러 값 중 하나에 해당하는 데이터를 찾는다.

```sql
SELECT *
FROM products
WHERE brand_id IN (1, 2, 3);
```

### 핵심

```text
IN (A, B, C)
→ A 또는 B 또는 C
```

---

## LIKE

문자열 패턴을 검색한다.

```sql
SELECT *
FROM products
WHERE product_name LIKE '%Bike%';
```

### `%`

```text
% → 글자 수와 관계없이 모든 문자열
```

예:

```sql
LIKE 'Bike%'
```

```text
Bike로 시작
```

```sql
LIKE '%Bike'
```

```text
Bike로 끝남
```

```sql
LIKE '%Bike%'
```

```text
중간에 Bike 포함
```

---

## IS NULL

NULL 값을 찾을 때 사용한다.

```sql
SELECT *
FROM customers
WHERE phone IS NULL;
```

NULL이 아닌 값:

```sql
SELECT *
FROM customers
WHERE phone IS NOT NULL;
```

### 중요

NULL은 일반 값처럼:

```sql
phone = NULL
```

로 비교하지 않고:

```sql
IS NULL
```

을 사용한다.

---

## NOT

조건의 반대를 의미한다.

```sql
SELECT *
FROM products
WHERE brand_id NOT IN (1, 2);
```

---

# 1️⃣1️⃣ WHERE 연산자 암기표

| 연산자 | 의미 |
|---|---|
| `=` | 같다 |
| `!=`, `<>` | 다르다 |
| `>` | 초과 |
| `<` | 미만 |
| `>=` | 이상 |
| `<=` | 이하 |
| `AND` | 조건 모두 만족 |
| `OR` | 하나 이상 만족 |
| `NOT` | 반대 |
| `BETWEEN` | 범위 |
| `IN` | 여러 값 중 하나 |
| `LIKE` | 문자열 패턴 |
| `IS NULL` | NULL 확인 |
| `IS NOT NULL` | NULL이 아닌 값 확인 |

---

# 1️⃣2️⃣ ORDER BY - 결과 정렬

조회 결과를 특정 컬럼을 기준으로 정렬한다.

기본 구조:

```sql
SELECT *
FROM orders
ORDER BY order_date;
```

기본 정렬은 오름차순이다.

---

## ASC

오름차순:

```sql
ORDER BY order_date ASC;
```

```text
숫자 → 작은 값에서 큰 값
날짜 → 과거에서 최근
문자 → 오름차순
```

---

## DESC

내림차순:

```sql
ORDER BY order_date DESC;
```

```text
숫자 → 큰 값에서 작은 값
날짜 → 최근에서 과거
```

### ⭐ 암기

```text
ASC  → 오름차순
DESC → 내림차순
```

---

## 여러 컬럼으로 정렬

```sql
SELECT *
FROM orders
ORDER BY customer_id ASC, order_date DESC;
```

의미:

```text
1차 정렬 → customer_id 오름차순
2차 정렬 → 같은 customer_id 안에서 order_date 내림차순
```

---

# 🧠 2장 1강 핵심 암기노트

```text
SELECT
→ 조회할 컬럼 선택

FROM
→ 조회할 테이블

DISTINCT
→ 중복 제거

WHERE
→ 조건에 맞는 Row 필터링

CASE WHEN
→ 조건에 따라 값 분류

BETWEEN
→ 범위

IN
→ 여러 값 중 하나

LIKE
→ 문자열 패턴 검색

IS NULL
→ NULL 확인

ORDER BY
→ 결과 정렬

ASC
→ 오름차순

DESC
→ 내림차순
```

---

## ⭐ SQL 조회 기본 뼈대

```sql
SELECT 컬럼
FROM 테이블
WHERE 조건
ORDER BY 컬럼;
```

---

## ⭐ WHERE 조건 암기

```text
AND → 모두 만족
OR  → 하나라도 만족

BETWEEN A AND B
→ A 이상 B 이하

IN (A, B, C)
→ A/B/C 중 하나

LIKE '%문자%'
→ 특정 문자열 포함

IS NULL
→ NULL 값
```

---

## ⭐ 오늘의 별표

```text
UNION ALL
→ 여러 SELECT 결과를
  중복 제거 없이 세로로 합치기
```

---

# 🌟 오늘 최종 정리

이번 학습에서는 SQL을 사용하기 위한 기본적인 데이터베이스 개념부터 실제 데이터를 조회하는 방법까지 학습했다.

먼저 데이터베이스는 데이터를 구조적으로 저장하는 공간이며, DBMS는 이러한 데이터베이스를 관리하는 프로그램이다. SQL은 관계형 데이터베이스에 필요한 요청을 전달하는 언어이며 이번 실습에서는 PostgreSQL과 DBeaver를 사용했다.

관계형 데이터베이스는 데이터를 Table 형태로 저장하고, Table은 Row와 Column으로 구성된다. 각 데이터를 정확하게 관리하기 위해 PRIMARY KEY, FOREIGN KEY, NOT NULL, UNIQUE 등의 제약조건을 사용할 수 있다.

PK는 한 행을 유일하게 식별하고, FK는 다른 테이블의 PK를 참조하여 테이블 간 관계를 만든다.

테이블 관계는 크게 다음과 같이 나뉜다.

```text
1:1
1:N
N:M
```

특히 N:M 관계는 중간 테이블을 만들어 두 개의 1:N 관계로 변환하는 것이 중요하다.

ERD에서는 이러한 관계를 시각적으로 확인할 수 있으며, PK와 FK를 확인하면 어떤 테이블끼리 JOIN할 수 있는지 파악할 수 있다.

테이블 구조를 생성하거나 변경할 때는 DDL을 사용하고 실제 데이터를 조회하거나 추가, 수정, 삭제할 때는 DML을 사용한다.

```text
DDL → CREATE / ALTER / DROP
DML → SELECT / INSERT / UPDATE / DELETE
```

2장에서는 실제 Bike Store 데이터를 PostgreSQL에 불러와 데이터를 조회했다.

기본 조회는:

```sql
SELECT 컬럼
FROM 테이블;
```

형태로 작성하며 `DISTINCT`, `WHERE`, `CASE WHEN`, `BETWEEN`, `IN`, `LIKE`, `IS NULL`, `ORDER BY` 등을 이용하면 원하는 데이터만 추출할 수 있다.

또한 실제 CSV Import 과정에서 `VARCHAR(50)`보다 긴 문자열 때문에 Batch Insert 오류가 발생했고, 다음과 같이 해결했다.

```sql
ALTER TABLE products
ALTER COLUMN product_name TYPE VARCHAR(255);
```

이후 CSV를 다시 Import한 후:

```sql
SELECT COUNT(*) FROM products;
```

를 실행하여 총 `321`건의 데이터가 정상적으로 입력된 것을 확인했다.

여러 테이블의 행 개수를 한 번에 확인할 때는 `UNION ALL`을 이용하면 여러 SELECT 결과를 하나의 결과표로 합칠 수 있다는 것도 알게 되었다.

전체 흐름은 다음과 같다.

```text
DB 구조 이해
→ Table / Row / Column 이해
→ PK / FK로 테이블 관계 파악
→ ERD 읽기
→ CREATE TABLE로 구조 생성
→ INSERT INTO로 데이터 입력
→ CSV 데이터 Import
→ COUNT(*)로 데이터 확인
→ SELECT / WHERE로 필요한 데이터 조회
→ ORDER BY로 결과 정렬
```

---

# 💡 오늘 느낀 점

SQL은 처음에는 명령어와 작성 순서를 모두 암기해야 하는 것처럼 느껴졌지만, 실제로 실습해보니 반복해서 사용하는 기본 구조가 있다는 것을 알게 되었다.

특히 단순히 문법을 보는 것보다 직접 테이블을 만들고 데이터를 넣고, CSV를 Import하면서 오류까지 해결해보니 데이터베이스의 구조가 더 잘 이해되었다.

`products.csv`를 불러오는 과정에서 데이터 타입과 컬럼 길이가 실제 데이터와 맞지 않으면 데이터 입력 자체가 실패할 수 있다는 것도 직접 확인했다. 오류 메시지를 보고 원인을 찾은 뒤 `ALTER TABLE`로 컬럼 구조를 수정하고 다시 데이터를 넣은 과정이 기억에 가장 잘 남았다.

앞으로는 SQL 문법을 무조건 통째로 외우기보다 문제를 보고 필요한 기능을 연결해서 생각해야겠다.

```text
중복 제거 → DISTINCT
조건 조회 → WHERE
범위 → BETWEEN
여러 값 → IN
문자 검색 → LIKE
조건 분류 → CASE WHEN
정렬 → ORDER BY
```

이런 식으로 문제와 명령어를 연결해서 익히면 SQL을 훨씬 쉽게 사용할 수 있을 것 같다.

---

# 📝 오늘의 한 줄

**SQL은 문법을 외우는 것에서 끝나는 것이 아니라, 데이터 구조를 이해하고 필요한 데이터를 정확하게 꺼내는 방법을 익히는 과정이다.**

---

# 🌱 앞으로의 목표

- SQL 기본 명령어와 역할을 반복해서 익히기
- DDL과 DML을 확실하게 구분하기
- PK와 FK를 보고 테이블 관계를 판단할 수 있도록 연습하기
- 1:1 / 1:N / N:M 관계를 바로 구분할 수 있도록 익히기
- ERD에서 PK와 FK를 먼저 찾는 습관 만들기
- `SELECT → FROM → WHERE → ORDER BY` 기본 구조를 손에 익히기
- `DISTINCT`, `BETWEEN`, `IN`, `LIKE`, `IS NULL`의 사용 상황 구분하기
- `CASE WHEN`을 Python의 조건문과 연결해서 이해하기
- `ASC`와 `DESC`를 헷갈리지 않기
- 오류 발생 시 에러 메시지 → 원인 → 수정 → 재실행 → 결과 확인 순서로 해결하기
- 새 SQL 문법을 배울 때마다 TIL 암기포인트에 누적하고 단원이 끝나면 별도의 암기노트로 정리하기

---

# ⚠️ SQL 실습 오류 & 해결방법 요약

## 1. DBeaver SQL 실행 시 `No active connection`

### 오류 메시지

```text
No active connection
```

또는

```text
Not connected to database
Connect to a database
```

### 원인

SQL 파일은 열려 있었지만 현재 SQL Editor가 PostgreSQL과 연결되지 않은 상태였다.

화면 상단에 다음처럼 표시되었다.

```text
<N/A>
```

### 해결방법

```text
Connections
→ postgres 우클릭
→ SQL 편집기
→ 새 SQL 스크립트
```

새 SQL 창에서 상단에 아래처럼 보이는지 확인한다.

```text
postgres
public@postgres
```

### 결과

DB 연결 후 SQL이 정상 실행되었다.

### ⭐ 메모

```text
SQL 문법이 맞는데 실행이 안 되면
→ 먼저 DB 연결 상태부터 확인
```

---

## 2. CSV Import 중 `VARCHAR(50)` 길이 초과 오류

### 오류 메시지

```text
Error occurred during batch insert

character varying(50) 자료형에
너무 긴 자료를 담으려고 합니다.
```

### 발생 상황

`products.csv` Import 과정에서 오류가 발생했다.

확인:

```sql
SELECT COUNT(*)
FROM products;
```

결과:

```text
0
```

즉 테이블은 생성되었지만 데이터는 들어가지 않은 상태였다.

### 원인

`product_name` 컬럼이 다음처럼 설정되어 있었다.

```sql
VARCHAR(50)
```

하지만 CSV 안에 50자를 초과하는 상품명이 존재했다.

### 해결방법

```sql
ALTER TABLE products
ALTER COLUMN product_name TYPE VARCHAR(255);
```

그 후 `products.csv`를 다시 Import했다.

### 결과 확인

```sql
SELECT COUNT(*)
FROM products;
```

결과:

```text
321
```

### ⭐ 해결 흐름

```text
Batch Insert 오류
→ VARCHAR 길이 부족 확인
→ 컬럼 길이 증가
→ CSV 재Import
→ COUNT(*)로 확인
```

---

## 3. `INT` 컬럼에 문자열 입력 오류

### 잘못된 입력

```sql
INSERT INTO ddl_test_products (
    product_id,
    product_name,
    list_price
)
VALUES (
    '상품번호',
    'Error Bike',
    999.00
);
```

### 오류 메시지

```text
invalid input syntax for type integer
```

### 원인

`product_id`는 `INT` 타입인데 문자열 `'상품번호'`를 입력했기 때문이다.

```text
product_id → INT
'상품번호' → 문자열
```

### 해결방법

정수값으로 수정한다.

```sql
INSERT INTO ddl_test_products (
    product_id,
    product_name,
    list_price
)
VALUES (
    11,
    'Error Bike',
    999.00
);
```

### ⭐ 메모

```text
컬럼 데이터 타입
=
입력값 데이터 타입
```

---

## 4. CSV Import 중 문자열 ID를 숫자로 해석하는 오류

### 오류 메시지

```text
Can't parse numeric value
Invalid argument: For input string: "e5f2d"
```

실제 값:

```text
e5f2d52b802189ee658865ca93d83a8f
```

### 원인

해당 값은 숫자가 아니라 문자열 형태의 ID인데,
일부 PostgreSQL Target 컬럼이 `integer`로 생성되어 있었다.

문제가 있던 컬럼:

```text
customers.customer_id
order_items.order_id
order_items.product_id
orders.customer_id
orders.order_id
products.product_id
```

### 실제 타입 확인

```sql
SELECT
    table_name,
    column_name,
    data_type
FROM information_schema.columns
WHERE table_schema = 'public';
```

확인 결과 일부 ID 컬럼이 다음처럼 되어 있었다.

```text
integer
```

### 해결방법

문자열 ID 컬럼을 `VARCHAR(50)`으로 변경했다.

```sql
ALTER TABLE order_items
ALTER COLUMN order_id TYPE VARCHAR(50)
USING order_id::VARCHAR;

ALTER TABLE order_items
ALTER COLUMN product_id TYPE VARCHAR(50)
USING product_id::VARCHAR;

ALTER TABLE orders
ALTER COLUMN order_id TYPE VARCHAR(50)
USING order_id::VARCHAR;

ALTER TABLE orders
ALTER COLUMN customer_id TYPE VARCHAR(50)
USING customer_id::VARCHAR;

ALTER TABLE products
ALTER COLUMN product_id TYPE VARCHAR(50)
USING product_id::VARCHAR;

ALTER TABLE customers
ALTER COLUMN customer_id TYPE VARCHAR(50)
USING customer_id::VARCHAR;
```

### 수정 결과 확인

```sql
SELECT
    table_name,
    column_name,
    data_type
FROM information_schema.columns
WHERE table_schema = 'public'
  AND (
       (table_name = 'order_items'
        AND column_name IN ('order_id', 'product_id'))

    OR (table_name = 'orders'
        AND column_name IN ('order_id', 'customer_id'))

    OR (table_name = 'products'
        AND column_name = 'product_id')

    OR (table_name = 'customers'
        AND column_name = 'customer_id')
  )
ORDER BY table_name, column_name;
```

최종 결과:

```text
customers.customer_id      → character varying
order_items.order_id       → character varying
order_items.product_id     → character varying
orders.customer_id         → character varying
orders.order_id            → character varying
products.product_id        → character varying
```

### 결과

PostgreSQL 컬럼 타입 자체는 정상적으로 `character varying`으로 수정되었다.

하지만 DBeaver Data Transfer에서는 동일한:

```text
Can't parse numeric value
```

오류가 계속 발생했다.

### 현재 결론

```text
DB 컬럼 타입 수정 완료
→ character varying 확인 완료
→ 그러나 DBeaver Import 단계에서 동일 오류 반복
→ CSV 전체 Import는 보류
```

---

## 5. SQL Editor에서 `character varying`만 단독 실행한 오류

### 오류 메시지

```text
구문 오류, "character" 부근
```

### 원인

확인용으로 적어둔:

```text
character varying
```

문장을 SQL 명령어처럼 단독 실행했기 때문이다.

### 해결방법

해당 줄을 삭제하거나 주석 처리한다.

```sql
-- character varying
```

### ⭐ 메모

```text
DBeaver에서 Ctrl + Enter
→ 커서가 있는 현재 SQL 문장을 실행할 수 있음
→ 확인용 텍스트를 실행하지 않도록 주의
```

---

## ⭐ 오늘 오류 해결 핵심 흐름

```text
오류 메시지 확인
→ 원인 추정
→ 현재 DB 상태 조회
→ ALTER TABLE 등으로 수정
→ 다시 조회해서 수정 여부 확인
→ 재실행
→ 동일 오류가 나면 다른 원인으로 범위 좁히기
```

## ⭐ 오늘 기억할 오류 포인트

```text
No active connection
→ DB 연결 문제

VARCHAR 길이 초과
→ 컬럼 길이 증가

INT에 문자열 입력
→ 데이터 타입 불일치

Can't parse numeric value
→ 문자열 ID를 숫자로 해석

수정 후에도 동일 오류
→ DB 타입 외에 DBeaver Import 매핑/변환 문제 가능
```
