# 🧠 Python 암기노트

## 1️⃣ 변수와 자료형

### 기본 자료형

```python
int      # 정수
float    # 실수
str      # 문자열
bool     # True / False
```

### 자료형 확인

```python
type(값)
```

예시

```python
type(10)        # int
type(3.14)      # float
type("Python")  # str
type(True)      # bool
```

---

# 2️⃣ 형변환

```python
int()
float()
str()
bool()
```

예시

```python
int("10")
float("3.14")
str(100)
```

### 핵심

```text
int()   → 정수로 변환
float() → 실수로 변환
str()   → 문자열로 변환
bool()  → True / False로 변환
```

---

# 3️⃣ 연산자

## 산술 연산자

| 연산자 | 의미 |
|---|---|
| `+` | 더하기 |
| `-` | 빼기 |
| `*` | 곱하기 |
| `/` | 나누기 |
| `//` | 몫 |
| `%` | 나머지 |
| `**` | 거듭제곱 |

예시

```python
10 // 3   # 3
10 % 3    # 1
2 ** 3    # 8
```

---

## 비교 연산자

```text
==  같다
!=  다르다
>   크다
<   작다
>=  크거나 같다
<=  작거나 같다
```

---

## 논리 연산자

```text
and → 모두 True여야 True
or  → 하나라도 True면 True
not → 반대로 바꿈
```

### 핵심 암기

```text
and = 모두
or  = 하나라도
```

---

# 4️⃣ 문자열

## 문자열 인덱싱

```python
text = "Python"
```

```python
text[0]    # P
text[1]    # y
text[-1]   # n
```

### 핵심

```text
앞에서 시작 → 0
뒤에서 시작 → -1
```

---

# 5️⃣ 슬라이싱

```python
text[start:end]
```

예시

```python
text = "Python"

text[1:4]
```

결과

```text
yth
```

## ⭐ 핵심 규칙

```text
시작 인덱스는 포함
끝 인덱스는 미포함
```

즉,

```python
text[1:4]
```

는

```text
1번, 2번, 3번 인덱스
```

까지만 가져온다.

---

## 슬라이싱 기본형

```python
[start:end:step]
```

예시

```python
text[:3]
text[2:]
text[::2]
text[::-1]
```

### 핵심

```text
[::-1] → 역순
```

---

# 6️⃣ 리스트 List

여러 값을 순서대로 저장한다.

```python
numbers = [10, 20, 30]
```

### 특징

```text
순서 있음
중복 가능
수정 가능
```

### 값 추가

```python
numbers.append(40)
```

### 값 삭제

```python
numbers.remove(20)
```

### 특정 위치 삭제

```python
del numbers[0]
```

### 길이 확인

```python
len(numbers)
```

---

# 7️⃣ 튜플 Tuple

```python
numbers = (10, 20, 30)
```

### 특징

```text
순서 있음
중복 가능
수정 불가능
```

---

## 🔍 리스트 vs 튜플

| 구분 | List | Tuple |
|---|---|---|
| 기호 | `[]` | `()` |
| 순서 | 있음 | 있음 |
| 중복 | 가능 | 가능 |
| 수정 | 가능 | 불가능 |

### 암기

```text
리스트 = 가변
튜플   = 불변
```

---

# 8️⃣ 딕셔너리 Dictionary

Key와 Value 구조로 데이터를 저장한다.

```python
user = {
    "name": "가영",
    "age": 29
}
```

### 값 가져오기

```python
user["name"]
```

### 값 추가 / 수정

```python
user["age"] = 30
```

### 값 삭제

```python
del user["age"]
```

### 주요 메서드

```python
user.keys()
user.values()
user.items()
```

### 핵심

```text
Dictionary = Key → Value
```

---

# 9️⃣ 집합 Set

```python
numbers = {1, 2, 3}
```

### 특징

```text
중복 제거
순서 없음
```

예시

```python
numbers = {1, 1, 2, 2, 3}
```

결과

```python
{1, 2, 3}
```

### 핵심

```text
Set = 중복 제거
```

---

## 🔍 Dictionary vs Set

| 구분 | Dictionary | Set |
|---|---|---|
| 구조 | Key : Value | 값 |
| 중복 | Key 중복 불가 | 값 중복 불가 |
| 용도 | Key로 값 찾기 | 중복 제거 |

---

# 🔟 조건문 if

기본 구조

```python
if 조건:
    실행문
elif 조건:
    실행문
else:
    실행문
```

예시

```python
score = 80

if score >= 90:
    print("A")
elif score >= 80:
    print("B")
else:
    print("C")
```

## ⭐ 핵심

```text
if → elif → else 순서로 위에서 아래로 확인
처음 True가 나온 곳만 실행
```

---

# 1️⃣1️⃣ 반복문 for

```python
for 변수 in 반복가능한값:
    실행문
```

예시

```python
for i in range(5):
    print(i)
```

결과

```text
0
1
2
3
4
```

---

## range()

```python
range(start, end, step)
```

### 핵심

```text
start 포함
end 미포함
```

예시

```python
range(1, 5)
```

결과

```text
1, 2, 3, 4
```

---

# 1️⃣2️⃣ while 반복문

조건이 True인 동안 반복한다.

```python
while 조건:
    실행문
```

예시

```python
count = 1

while count <= 5:
    print(count)
    count += 1
```

---

## 🔍 for vs while

| for | while |
|---|---|
| 반복 횟수가 명확할 때 | 조건 중심 반복 |
| 리스트 등 순회 | 조건이 True인 동안 반복 |

---

# 1️⃣3️⃣ break / continue

## break

반복문을 즉시 종료한다.

```python
for i in range(10):
    if i == 5:
        break
```

---

## continue

현재 반복만 건너뛰고 다음 반복으로 이동한다.

```python
for i in range(5):
    if i == 2:
        continue

    print(i)
```

### 핵심

```text
break    → 반복 종료
continue → 이번만 건너뜀
```

---

# 1️⃣4️⃣ enumerate()

반복할 때 번호와 값을 함께 가져온다.

```python
names = ["가영", "민경", "규진"]

for index, name in enumerate(names):
    print(index, name)
```

### 번호를 1부터 시작

```python
enumerate(names, start=1)
```

### 핵심

```text
enumerate() = 번호 + 값
```

---

# 1️⃣5️⃣ 함수 Function

기본 구조

```python
def 함수명():
    실행문
```

예시

```python
def hello():
    print("Hello")
```

함수 호출

```python
hello()
```

---

## 매개변수

```python
def hello(name):
    print(name)
```

호출

```python
hello("가영")
```

---

# 1️⃣6️⃣ return

함수의 결과값을 밖으로 돌려준다.

```python
def add(a, b):
    return a + b
```

```python
result = add(10, 20)
```

### 핵심

```text
print()  → 화면에 출력
return   → 결과값 반환
```

---

## 🔍 print vs return

| print | return |
|---|---|
| 화면에 보여줌 | 결과값을 함수 밖으로 전달 |
| 값 재사용 어려움 | 결과값 재사용 가능 |

---

# 1️⃣7️⃣ 지역 변수와 전역 변수

## 지역 변수

함수 안에서 만들어진 변수

```python
def test():
    x = 10
```

함수 밖에서는 사용할 수 없다.

---

## 전역 변수

함수 밖에서 만든 변수

```python
x = 10
```

프로그램 여러 곳에서 사용할 수 있다.

### 핵심

```text
지역 변수 = 함수 안
전역 변수 = 함수 밖
```

---

# 1️⃣8️⃣ 파일 입출력

파일 열기

```python
open("파일명", "모드")
```

### 파일 모드

```text
r → 읽기
w → 쓰기
a → 추가
```

---

## with open()

```python
with open("test.txt", "r", encoding="utf-8") as file:
    content = file.read()
```

### 핵심

```text
with open() → 작업 후 자동으로 파일 닫기
```

---

# 1️⃣9️⃣ 파일 읽기 함수

```python
read()
readline()
readlines()
```

## 차이

| 함수 | 의미 |
|---|---|
| `read()` | 전체 읽기 |
| `readline()` | 한 줄 읽기 |
| `readlines()` | 전체 줄을 리스트로 |

### 암기

```text
read      → 전체
readline  → 한 줄
readlines → 전체 줄 리스트
```

---

# 2️⃣0️⃣ CSV

CSV는 쉼표로 데이터를 구분하는 표 형태의 파일이다.

```text
이름,나이,지역
가영,29,인천
```

### CSV 모듈

```python
import csv
```

읽기

```python
csv.reader()
```

쓰기

```python
csv.writer()
```

한 행 작성

```python
writer.writerow()
```

여러 행 작성

```python
writer.writerows()
```

---

# 2️⃣1️⃣ JSON

JSON은 Key-Value 구조의 데이터 형식이다.

```json
{
    "name": "가영",
    "age": 29
}
```

### Python에서는

```python
import json
```

---

## JSON 함수 암기

```text
dump  → 파일에 저장
load  → 파일에서 읽기

dumps → 문자열로 변환
loads → 문자열에서 변환
```

## ⭐ 핵심

```text
s가 붙으면 String
```

---

# 2️⃣2️⃣ 직렬화 / 역직렬화

```text
직렬화
Python 객체 → 저장 가능한 데이터

역직렬화
저장된 데이터 → Python 객체
```

### JSON 기준

```text
dump / dumps → 직렬화
load / loads → 역직렬화
```

---

# 2️⃣3️⃣ 모듈과 패키지

## 모듈

Python 코드가 들어있는 하나의 `.py` 파일

```python
import math
```

---

## 패키지

여러 모듈을 모아놓은 구조

### 핵심

```text
모듈 = 파일
패키지 = 모듈 묶음
```

---

# 2️⃣4️⃣ import

전체 모듈 가져오기

```python
import math
```

사용

```python
math.sqrt(16)
```

---

## 특정 기능 가져오기

```python
from math import sqrt
```

사용

```python
sqrt(16)
```

---

## 별칭 사용

```python
import pandas as pd
import numpy as np
```

---

# 2️⃣5️⃣ 클래스 Class

클래스는 객체를 만들기 위한 설계도이다.

```python
class Car:
    pass
```

### 인스턴스 생성

```python
my_car = Car()
```

### 핵심

```text
Class    → 설계도
Instance → 설계도로 만든 실제 객체
```

---

# 2️⃣6️⃣ 속성과 메서드

```python
class Car:

    def __init__(self, color):
        self.color = color

    def drive(self):
        print("주행합니다.")
```

### 구분

```text
color → 속성 Attribute
drive → 메서드 Method
```

---

## 클래스 / 인스턴스 / 속성 / 메서드

```python
class Car:                  # 클래스

    def __init__(self, color):
        self.color = color  # 속성

    def drive(self):        # 메서드
        print("주행")

my_car = Car("Red")         # 인스턴스
```

### 암기

```text
클래스   = 설계도
인스턴스 = 실제 객체
속성     = 객체의 정보
메서드   = 객체의 기능
```

---

# 2️⃣7️⃣ 상속

기존 클래스의 기능을 새로운 클래스가 물려받는다.

```python
class Animal:
    def speak(self):
        print("소리")

class Dog(Animal):
    pass
```

### 핵심

```text
상속 = 기존 클래스 기능 재사용
```

---

# 2️⃣8️⃣ 예외 처리

프로그램 실행 중 오류가 발생했을 때 처리한다.

```python
try:
    실행할 코드

except:
    오류 발생 시 실행
```

예시

```python
try:
    num = int(input())

except ValueError:
    print("숫자를 입력하세요.")
```

---

## try / except / else / finally

```python
try:
    실행

except:
    오류 처리

else:
    오류 없을 때 실행

finally:
    오류 여부와 상관없이 실행
```

### 핵심

```text
try     → 일단 실행
except  → 오류 처리
else    → 성공했을 때
finally → 무조건 실행
```

---

# 2️⃣9️⃣ Pandas

```python
import pandas as pd
```

## Series

```text
1차원 데이터
```

```python
pd.Series()
```

## DataFrame

```text
2차원 표 데이터
```

```python
pd.DataFrame()
```

---

## 🔍 Series vs DataFrame

| Series | DataFrame |
|---|---|
| 1차원 | 2차원 |
| 하나의 열과 비슷 | 행과 열로 구성된 표 |

---

# 3️⃣0️⃣ Pandas 기본 확인

## info()

```python
df.info()
```

```text
데이터 구조
컬럼
자료형
결측치
```

---

## describe()

```python
df.describe()
```

```text
count
mean
std
min
25%
50%
75%
max
```

### 핵심

```text
info()     → 구조 확인
describe() → 통계 확인
```

---

# 3️⃣1️⃣ NumPy

```python
import numpy as np
```

주로 수치 계산에 사용한다.

```python
np.array()
np.zeros()
np.ones()
```

### 기본 확인

```python
arr.shape
arr.dtype
```

```text
shape → 배열의 형태
dtype → 데이터 타입
```

---

# 3️⃣2️⃣ Matplotlib

```python
import matplotlib.pyplot as plt
```

### 선 그래프

```python
plt.plot(x, y)
```

### 제목

```python
plt.title("제목")
```

### X축

```python
plt.xlabel("X축")
```

### Y축

```python
plt.ylabel("Y축")
```

### 격자

```python
plt.grid(True)
```

### 범례

```python
plt.legend()
```

### 출력

```python
plt.show()
```

---

# 🔥 시험 전 최우선 암기

```text
인덱스
앞 → 0
뒤 → -1
```

```text
슬라이싱
시작 포함
끝 미포함
```

```text
List  → 가변
Tuple → 불변
```

```text
Dictionary → Key : Value
Set        → 중복 제거
```

```text
if → elif → else
위에서 아래
최초 True만 실행
```

```text
and → 모두
or  → 하나라도
```

```text
break    → 반복 종료
continue → 현재 반복만 건너뜀
```

```text
print  → 출력
return → 값 반환
```

```text
read      → 전체
readline  → 한 줄
readlines → 전체 줄 리스트
```

```text
dump / load   → 파일
dumps / loads → 문자열
```

```text
Series    → 1차원
DataFrame → 2차원
```

```text
info()     → 구조
describe() → 통계
```

```text
클래스   → 설계도
인스턴스 → 실제 객체
속성     → 정보
메서드   → 기능
```

---

# ⭐ 헷갈리는 것만 초압축

| 개념 1 | 개념 2 | 차이 |
|---|---|---|
| List | Tuple | 수정 가능 / 수정 불가 |
| Dictionary | Set | Key-Value / 중복 제거 |
| `break` | `continue` | 반복 종료 / 현재만 건너뜀 |
| `print()` | `return` | 출력 / 반환 |
| `read()` | `readlines()` | 문자열 / 리스트 |
| `dump()` | `dumps()` | 파일 / 문자열 |
| `load()` | `loads()` | 파일 / 문자열 |
| Series | DataFrame | 1차원 / 2차원 |
| `info()` | `describe()` | 구조 / 통계 |
| Class | Instance | 설계도 / 실제 객체 |
| Attribute | Method | 정보 / 기능 |

---

# 📝 한 줄 암기

**파이썬은 자료를 저장하고 → 조건을 판단하고 → 반복하고 → 함수로 묶고 → 객체와 파일·데이터로 확장하는 언어다.**
