# 📚 TIL - 3장 데이터 시각화

## 오늘의 학습 범위

- 3장 1강: 시각화의 목적과 차트 선택 프레임워크
- 3장 2강: 분포 파악 차트 — 히스토그램, KDE, 박스플롯
- 3장 3강: 관계 파악 차트 — 산점도, 상관관계 히트맵
- 3장 4강: 비교 차트 — 막대그래프, 그룹 막대
- 3장 5강: 추세 차트 — 선 그래프, 이동평균

---

# 1. 시각화의 목적과 차트 선택 프레임워크

## 1-1. 데이터 시각화란?

데이터 시각화는 데이터를 단순히 예쁘게 표현하는 것이 아니라,  
**질문과 목적에 맞는 차트를 선택하여 데이터의 의미를 빠르고 정확하게 전달하는 과정**이다.

좋은 시각화는 차트를 먼저 고르는 것이 아니라 먼저 질문을 정해야 한다.

> 무엇을 알고 싶은가?  
> 어떤 관계를 보여주고 싶은가?  
> 어떤 메시지를 전달하고 싶은가?

---

## 1-2. 시각화 목적의 대표적인 4가지

### ① 분포
데이터가 어느 구간에 얼마나 몰려 있는지 확인한다.

대표 차트:
- 히스토그램
- KDE
- 박스플롯

### ② 관계
두 변수 또는 여러 변수 사이에 어떤 관계가 있는지 확인한다.

대표 차트:
- 산점도
- 상관관계 히트맵

### ③ 비교
범주별 값의 크기를 비교한다.

대표 차트:
- 막대그래프
- 그룹 막대그래프

### ④ 추세
시간이 흐르면서 값이 어떻게 변하는지 확인한다.

대표 차트:
- 선 그래프
- 이동평균

---

## 1-3. 차트 선택 순서

차트를 선택할 때는 다음 순서로 생각하면 좋다.

1. **시각화 목적 확인**
2. **데이터 유형 확인**
3. **시간 흐름이 있는지 확인**
4. **비교 대상의 수 확인**
5. **가장 단순하고 해석하기 쉬운 차트 선택**

---

## 1-4. 같은 데이터라도 질문에 따라 차트는 달라진다

같은 데이터를 가지고 있어도 질문이 다르면 선택해야 하는 차트도 달라진다.

예시:

- 고객별 주문 금액이 어디에 몰려 있는가?  
  → 히스토그램

- 주문 금액과 배송 기간 사이에 관계가 있는가?  
  → 산점도

- 지역별 매출을 비교하고 싶은가?  
  → 막대그래프

- 월별 매출 변화가 궁금한가?  
  → 선 그래프

---

## 1-5. Greedy(탐욕법)

Greedy는 매 순간 현재 상황에서 가장 좋아 보이는 선택을 하는 방식이다.

예시:

```python
def solution(models, limit):
    models = sorted(models)

    used_memory = 0
    count = 0

    for memory in models:
        if used_memory + memory <= limit:
            used_memory += memory
            count += 1
        else:
            break

    return count
```

### 코드 핵심

```python
models = sorted(models)
```

- `sorted()`는 파이썬 내장 함수
- 리스트를 오름차순으로 정렬한 새 리스트를 반환한다.

예:

```python
models = [4, 2, 5, 1]
sorted(models)
```

결과:

```python
[1, 2, 4, 5]
```

작은 메모리 모델부터 선택해야 제한된 메모리 안에서 더 많은 모델을 넣을 수 있다.

### break

```python
break
```

- 반복문을 즉시 종료한다.
- 데이터를 삭제하는 것이 아니다.

암기:

> break = 반복문 강제 종료

연상 암기:

> break up = 헤어지다 → 관계를 끝내다 → break = 반복을 끝내다

### continue와 차이

- `break` → 반복문 전체 종료
- `continue` → 이번 반복만 건너뛰고 다음 반복 진행

---

# 2. 분포 파악 차트

분포 차트는 데이터가 어느 값에 많이 몰려 있는지, 얼마나 퍼져 있는지, 이상치가 있는지 확인할 때 사용한다.

---

# 2-1. 히스토그램

## 히스토그램이란?

수치형 데이터를 여러 구간으로 나누고  
각 구간에 데이터가 몇 개 들어 있는지 막대로 표현하는 그래프이다.

```python
plt.hist(data, bins=10)
```

---

## 히스토그램의 X축과 Y축

### X축
**분포를 확인하고 싶은 실제 수치 데이터**

예:
- 평균 주문 금액
- 배송 기간
- 나이
- 매출
- 점수

### Y축
**각 구간에 포함된 데이터의 개수 = 빈도**

즉, 히스토그램에서는 Y축 데이터를 따로 지정하지 않는다.

```python
plt.hist(df["avg_order_value"], bins=15)
```

이 코드에서:

- X축 → `avg_order_value`
- Y축 → matplotlib이 자동으로 계산한 빈도

---

## 예시: 평균 주문 금액

문제:

> 고객들의 평균 주문 금액이 어느 수준에 많이 분포하는지 확인한다.

축 설정:

```python
plt.xlabel("평균 주문 금액")
plt.ylabel("고객 수")
```

여기서 `고객 수`는 각 평균 주문 금액 구간에 포함된 고객의 수이므로  
히스토그램의 빈도를 의미한다.

---

## 예시: 배송 기간

```python
plt.hist(
    df["delivery_days"].dropna(),
    bins=5,
    edgecolor="white"
)

plt.xlabel("배송기간")
plt.ylabel("주문수")
plt.title("배송 기간 분포 (bins=5)")
plt.show()
```

두 번째 그래프:

```python
plt.hist(
    df["delivery_days"].dropna(),
    bins=20,
    edgecolor="white"
)

plt.xlabel("배송기간")
plt.ylabel("주문수")
plt.title("배송 기간 분포 (bins=20)")
plt.show()
```

---

## bins란?

`bins`는 히스토그램에서 데이터를 몇 개의 구간으로 나눌지 결정하는 값이다.

```python
plt.hist(data, bins=5)
```

→ 5개 구간

```python
plt.hist(data, bins=20)
```

→ 20개 구간

### bins가 작으면
- 구간 수가 적다.
- 전체적인 분포 형태를 쉽게 볼 수 있다.
- 세부 정보가 많이 사라질 수 있다.

### bins가 크면
- 구간 수가 많다.
- 세부적인 분포를 볼 수 있다.
- 지나치게 많으면 그래프가 복잡해질 수 있다.

암기:

> bins가 작다 = 크게 묶어 본다  
> bins가 크다 = 잘게 나눠 본다

---

## 히스토그램 해석

예를 들어 평균 주문 금액 그래프에서 낮은 금액 구간에 고객이 많고  
금액이 커질수록 고객 수가 줄어드는 경우:

> 낮은 평균 주문 금액 구간에 고객이 많이 분포하며, 높은 금액대로 갈수록 고객 수가 감소한다.

오른쪽 방향으로 꼬리가 길면:

> 우측 편향 분포

---

# 2-2. 한글 폰트 설정

matplotlib에서 한글이 깨질 경우 운영체제에 맞는 한글 폰트를 지정한다.

```python
import matplotlib.pyplot as plt
import platform

if platform.system() == "Windows":
    plt.rcParams["font.family"] = "Malgun Gothic"
elif platform.system() == "Darwin":
    plt.rcParams["font.family"] = "AppleGothic"

plt.rcParams["axes.unicode_minus"] = False
```

### 의미

```python
platform.system()
```

→ 현재 운영체제를 확인한다.

```python
plt.rcParams["font.family"] = "Malgun Gothic"
```

→ Windows에서 맑은 고딕을 사용한다.

```python
plt.rcParams["axes.unicode_minus"] = False
```

→ 마이너스 기호가 깨지는 것을 방지한다.

### 진짜 글자 깨짐의 예

- `□`
- `�`
- 특정 글자가 사라짐
- `Glyph missing` 경고

글자 모양이 조금 얇거나 이상하게 보이는 정도는 화면 축소나 렌더링 차이일 수 있다.

---

# 2-3. KDE

## KDE란?

KDE(Kernel Density Estimation)는 히스토그램처럼 분포를 확인하지만  
막대가 아니라 **부드러운 곡선**으로 분포를 표현한다.

```python
sns.kdeplot(data, fill=True)
```

---

## KDE의 X축과 Y축

### X축
실제 수치형 데이터 값

예:
- 체류 시간
- 주문 금액
- 배송 기간

### Y축
밀도(Density)

즉, 해당 값 주변에 데이터가 얼마나 몰려 있는지를 나타낸다.

중요:

> KDE의 Y축은 고객 수나 실제 데이터 값이 아니라 밀도이다.

---

## KDE 예시

```python
import seaborn as sns
import matplotlib.pyplot as plt

store_a = [20, 22, 23, 25, 25, 27, 28, 30, 31, 33, 35, 36, 38, 40, 42]
store_b = [30, 32, 34, 35, 37, 38, 40, 42, 43, 45, 47, 48, 50, 52, 55]

sns.kdeplot(store_a, label="A 매장", fill=True)
sns.kdeplot(store_b, label="B 매장", fill=True)

plt.xlabel("체류 시간(분)")
plt.ylabel("밀도")
plt.title("매장별 고객 체류 시간 분포")
plt.legend()
plt.show()
```

해석:

- A 매장은 약 20~30분대에 많이 분포
- B 매장은 A보다 더 긴 체류 시간 구간에 분포
- 두 그룹의 전체적인 분포 모양을 비교하기 좋다.

---

## seaborn 설치 오류

오류:

```text
ModuleNotFoundError: No module named 'seaborn'
```

뜻:

> 현재 Python 환경에 seaborn이 설치되어 있지 않다.

Jupyter에서는 다음처럼 설치하면 현재 커널 환경에 설치하기 편하다.

```python
%pip install seaborn
```

설치 후 필요하면 커널을 재시작한다.

중요:

> 예전에 설치했는데도 없다고 뜨는 경우, 다른 Python 또는 다른 가상환경에 설치되어 있을 수 있다.

---

# 2-4. 박스플롯

박스플롯은 데이터의 중앙값, 사분위수, 범위, 이상치를 한눈에 확인할 수 있는 그래프이다.

```python
plt.boxplot(data)
```

---

## 박스플롯 주요 요소

- Q1: 1사분위수, 25%
- 중앙값(Median): 50%
- Q3: 3사분위수, 75%
- IQR: Q3 - Q1
- 이상치 후보: 수염 밖에 위치한 값

### IQR

```text
IQR = Q3 - Q1
```

이상치 후보 기준:

```text
Q1 - 1.5 × IQR 미만
Q3 + 1.5 × IQR 초과
```

암기:

> 박스플롯 = 중앙값 + 사분위수 + IQR + 이상치

---

# 2-5. 히스토그램 / KDE / 박스플롯 비교

| 차트 | 주 목적 | 특징 |
|---|---|---|
| 히스토그램 | 어느 구간에 많이 몰렸는지 | 구간별 빈도 |
| KDE | 분포 모양 비교 | 부드러운 밀도 곡선 |
| 박스플롯 | 중앙값, 범위, 이상치 | 요약 통계 확인 |

암기:

> 히스토그램 = 어디에 많이 몰렸나  
> KDE = 분포 모양이 어떻게 생겼나  
> 박스플롯 = 중앙값·범위·이상치가 어떤가

---

# 3. 관계 파악 차트

# 3-1. 산점도

산점도는 두 수치형 변수 사이의 관계를 점으로 표현한다.

```python
plt.scatter(x, y)
```

### 해석

- 점들이 우상향 → 양의 관계
- 점들이 우하향 → 음의 관계
- 일정한 패턴이 없음 → 관계가 약할 가능성

---

## 산점도에서 확인할 것

- 방향
- 관계의 강도
- 이상치
- 군집
- 비선형 패턴

---

# 3-2. 피어슨 상관계수

피어슨 상관계수는 두 변수의 선형 관계 정도를 숫자로 표현한다.

범위:

```text
-1 ≤ r ≤ 1
```

### 해석

- `r = 1` → 완벽한 양의 상관
- `r`이 1에 가까움 → 강한 양의 상관
- `r = 0` → 선형 관계가 거의 없음
- `r`이 -1에 가까움 → 강한 음의 상관
- `r = -1` → 완벽한 음의 상관

예:

```python
df.corr()
```

---

# 3-3. 상관관계 히트맵

여러 변수의 상관계수를 색으로 표현하여 한눈에 비교할 수 있다.

```python
corr = df.corr()

sns.heatmap(
    corr,
    annot=True,
    cmap="coolwarm"
)
```

### 주요 옵션

```python
annot=True
```

→ 각 칸에 상관계수 숫자 표시

```python
cmap="coolwarm"
```

→ 색상 테마 지정

---

# 3-4. 상관관계와 인과관계

매우 중요:

> 상관관계 ≠ 인과관계

두 변수가 함께 증가하거나 감소한다고 해서  
한 변수가 다른 변수의 원인이라는 뜻은 아니다.

따라서 상관계수가 높더라도 인과관계라고 단정하면 안 된다.

---

## 암기

> 산점도 = 관계의 모양 확인  
> 상관계수 = 관계의 방향과 강도를 숫자로 확인  
> 히트맵 = 여러 변수의 상관관계를 한눈에 확인

---

# 4. 비교 차트

# 4-1. 막대그래프

막대그래프는 범주별 값의 크기를 비교할 때 사용한다.

세로 막대:

```python
plt.bar(categories, values)
```

가로 막대:

```python
plt.barh(categories, values)
```

---

## 수직 막대 vs 수평 막대

### 수직 막대그래프
적합:
- 범주가 적을 때
- 범주 이름이 짧을 때

### 수평 막대그래프
적합:
- 범주가 많을 때
- 범주 이름이 길 때

---

# 4-2. 정렬의 중요성

막대그래프는 값을 정렬하면 비교가 더 쉬워진다.

```python
df.sort_values("sales", ascending=False)
```

- `ascending=False` → 큰 값부터
- `ascending=True` → 작은 값부터

---

# 4-3. Bar Plot과 Histogram 차이

모양이 비슷하지만 목적이 완전히 다르다.

### Bar Plot
- 범주형 데이터 비교
- 각 범주별 값의 크기 비교

예:
- 지역별 매출
- 상품별 주문 수
- 부서별 인원

### Histogram
- 수치형 데이터의 분포 확인
- 값을 구간으로 나누어 빈도 확인

예:
- 주문 금액 분포
- 배송 기간 분포
- 나이 분포

암기:

> Bar Plot = 범주 간 크기 비교  
> Histogram = 수치형 데이터의 분포 확인

---

# 4-4. 그룹 막대그래프

하나의 범주 안에서 여러 그룹의 값을 나란히 비교한다.

예:
- 계절별 A/B 지역 매출
- 연령대별 남/여 구매 수

```python
sns.barplot(
    data=df,
    x="season",
    y="sales",
    hue="region"
)
```

### hue

```python
hue="region"
```

→ 하나의 범주 안에서 그룹을 색으로 나누어 비교

---

# 4-5. Stacked Bar

누적 막대그래프는 전체 크기와 내부 구성 비율을 함께 볼 때 사용한다.

장점:
- 전체 규모 확인
- 구성 비율 확인

단점:
- 중간에 쌓인 항목끼리 정확한 크기를 비교하기 어려울 수 있다.

---

# 5. 추세 차트

# 5-1. 선 그래프

선 그래프는 시간에 따른 값의 변화를 확인할 때 사용한다.

```python
plt.plot(x, y)
```

예:
- 일별 매출
- 월별 주문 수
- 연도별 고객 수

---

## 선 그래프 기본 예시

```python
import pandas as pd
import matplotlib.pyplot as plt

sales = pd.DataFrame({
    "month": ["1월", "2월", "3월", "4월", "5월"],
    "sales": [120, 125, 118, 150, 170]
})

plt.plot(
    sales["month"],
    sales["sales"],
    marker="o"
)

plt.xlabel("Month")
plt.ylabel("Sales")
plt.title("Monthly Sales")
plt.show()
```

---

# 5-2. 선 그래프 디자인 옵션

matplotlib 그래프는 기본 디자인만 사용하는 것이 아니라 다양한 옵션을 직접 설정할 수 있다.

```python
plt.plot(
    sales["month"],
    sales["sales"],
    color="purple",
    linestyle="--",
    linewidth=2,
    marker="o",
    markersize=7,
    alpha=0.8
)
```

### 자주 쓰는 옵션

- `color` → 선 색상
- `linestyle` → 선 모양
- `linewidth` → 선 굵기
- `marker` → 점 모양
- `markersize` → 점 크기
- `alpha` → 투명도
- `figsize` → 그래프 크기
- `grid` → 격자
- `legend` → 범례

예:

```python
plt.figure(figsize=(8, 5))
plt.grid(True)
plt.legend()
```

---

# 5-3. 선 그래프 여러 개 비교

같은 시간축에서 여러 값의 변화를 비교할 수 있다.

```python
plt.plot(months, sales_a, label="A")
plt.plot(months, sales_b, label="B")

plt.legend()
plt.show()
```

---

# 5-4. 이동평균

이동평균(Rolling Average)은 값이 너무 많이 출렁일 때  
일정 기간의 평균을 계산하여 전체 추세를 더 부드럽게 확인하는 방법이다.

```python
df["moving_avg"] = df["sales"].rolling(window=3).mean()
```

---

## window

`window`는 몇 개의 데이터를 묶어서 평균을 계산할지 정하는 값이다.

### window가 작으면
- 원본 변화에 민감
- 세부 움직임이 많이 남음

### window가 크면
- 그래프가 더 부드러워짐
- 단기 변동이 많이 제거됨
- 너무 크면 세부 변화가 사라질 수 있음

암기:

> window 작음 = 민감  
> window 큼 = 부드러움

---

## 이동평균과 NaN

이동평균은 처음 몇 개의 값에서 계산할 데이터가 부족하기 때문에 NaN이 생길 수 있다.

예:

```python
rolling(window=3)
```

→ 처음 2개 값은 평균을 계산할 3개 데이터가 부족하므로 NaN이 될 수 있다.

---

# 6. 차트별 X축 / Y축 정리

| 차트 | X축 | Y축 |
|---|---|---|
| 히스토그램 | 실제 수치값 구간 | 빈도 |
| KDE | 실제 수치값 | 밀도 |
| 산점도 | 첫 번째 수치 변수 | 두 번째 수치 변수 |
| 막대그래프 | 범주 | 비교할 값 |
| 선 그래프 | 시간 또는 순서 | 변화하는 실제 값 |
| 박스플롯 | 그룹/범주 | 수치형 값 |

---

# 7. 오늘의 핵심 암기 포인트

## 차트 목적

```text
분포 → 히스토그램 / KDE / 박스플롯
관계 → 산점도 / 상관관계 히트맵
비교 → 막대그래프 / 그룹 막대그래프
추세 → 선 그래프 / 이동평균
```

---

## 히스토그램

```text
X축 = 실제 수치형 데이터
Y축 = 빈도
bins = 구간 개수
```

```text
bins 작음 → 크게 묶어서 전체적인 형태 확인
bins 큼 → 잘게 나눠 세부적인 형태 확인
```

---

## KDE

```text
X축 = 실제 값
Y축 = 밀도
```

---

## 박스플롯

```text
IQR = Q3 - Q1
```

이상치 후보:

```text
Q1 - 1.5 × IQR 미만
Q3 + 1.5 × IQR 초과
```

---

## 상관관계

```text
-1 ≤ r ≤ 1
```

```text
1에 가까움 → 강한 양의 상관
0에 가까움 → 선형 관계 약함
-1에 가까움 → 강한 음의 상관
```

반드시 기억:

```text
상관관계 ≠ 인과관계
```

---

## 막대그래프 vs 히스토그램

```text
막대그래프 = 범주 비교
히스토그램 = 수치형 데이터 분포
```

---

## 선 그래프

```text
선 그래프 = 시간에 따른 변화
이동평균 = 단기 변동을 줄이고 전체 추세 확인
```

```text
window 작음 = 민감
window 큼 = 부드러움
```

---

## 파이썬 명령어

```text
sorted() = 정렬된 새 리스트 반환
break = 반복문 종료
continue = 이번 반복만 건너뛰기
return = 결과 반환 후 함수 종료
```

---

## 한글 폰트

```python
plt.rcParams["font.family"] = "Malgun Gothic"
plt.rcParams["axes.unicode_minus"] = False
```

---

## 패키지 오류

```text
ModuleNotFoundError
= 현재 Python 환경에 패키지가 없음
```

Jupyter:

```python
%pip install seaborn
```

---

# 8. 이번 장 전체 흐름

```text
1강: 어떤 차트를 고를까?
        ↓
2강: 데이터가 어떻게 분포되어 있을까?
        ↓
3강: 변수끼리 어떤 관계가 있을까?
        ↓
4강: 범주별 값은 얼마나 차이 날까?
        ↓
5강: 시간에 따라 값이 어떻게 변할까?
```

한 줄로 정리:

> 목적을 먼저 정하고, 분포·관계·비교·추세에 맞는 차트를 선택한다.

---

# 🌟 오늘 최종 정리

오늘은 데이터 시각화의 목적을 먼저 정한 뒤, 목적에 따라 적절한 차트를 선택하는 방법을 학습했다.  
분포를 확인할 때는 히스토그램, KDE, 박스플롯을 사용하며, 히스토그램은 구간별 빈도, KDE는 밀도 곡선, 박스플롯은 중앙값과 사분위수 및 이상치를 확인하는 데 적합하다.

두 수치형 변수의 관계는 산점도로 시각적으로 확인하고, 피어슨 상관계수와 상관관계 히트맵으로 관계의 방향과 강도를 파악할 수 있다. 단, 상관관계가 높다고 해서 반드시 인과관계가 있는 것은 아니다.

범주별 값을 비교할 때는 막대그래프를 사용하며, 여러 그룹을 함께 비교할 때는 그룹 막대그래프를 사용할 수 있다. 막대그래프와 히스토그램은 모양은 비슷하지만 각각 범주 비교와 수치형 데이터의 분포 확인이라는 서로 다른 목적을 가진다.

시간에 따른 변화는 선 그래프로 확인하고, 값의 단기적인 출렁임이 클 경우 이동평균을 사용하여 전체적인 추세를 부드럽게 확인할 수 있다.

또한 matplotlib의 그래프는 색상, 선 모양, 마커, 굵기, 폰트, 격자 등 다양한 디자인 요소를 조절할 수 있으며, 한글이 깨질 경우 운영체제에 맞는 폰트를 설정해야 한다.

# 💡 오늘 느낀 점

차트마다 단순히 모양이 다른 것이 아니라 각각 목적이 다르다는 점이 가장 중요하게 느껴졌다.  
특히 히스토그램은 X축에 실제 수치형 데이터를 넣으면 Y축의 빈도를 자동으로 계산해준다는 점과, KDE에서는 Y축이 빈도가 아니라 밀도라는 점을 구분해서 기억해야겠다.

예시 답안과 내가 작성한 코드가 조금 달라도 문제에서 요구한 조건을 만족하고 올바른 결과가 나온다면 정답이 될 수 있다는 점도 알게 되었다. 앞으로는 예시 코드를 그대로 외우기보다 코드가 어떤 역할을 하는지 이해하면서 작성해야겠다.

# 📝 오늘의 한 줄

**좋은 시각화는 차트를 먼저 고르는 것이 아니라, 질문과 목적을 먼저 정하는 것에서 시작한다.**

# 🌱 앞으로의 목표

- 분포 / 관계 / 비교 / 추세에 맞는 차트를 바로 떠올릴 수 있도록 반복해서 익힌다.
- 히스토그램, KDE, 박스플롯의 차이를 정확하게 구분한다.
- 차트별 X축과 Y축의 의미를 헷갈리지 않도록 암기한다.
- matplotlib과 seaborn의 기본 문법을 직접 작성해보며 익힌다.
- 그래프 결과를 단순히 출력하는 데서 끝내지 않고, 데이터가 의미하는 내용을 문장으로 해석하는 연습을 한다.
