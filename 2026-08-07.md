# 📚 TIL - Python 기초

> **학습일 : 2026.08.07**
>
> 개발 환경과 파이썬 가상환경 구축 -> uv init을 활용한 모던 프로젝트 시작 -> 프로젝트 가상환경 활성화와 기본 실행 방식 -> VS Code와 주피터 노트북 커널 연결 (사용 프로그램 VS Code, python, Jupyter Extension) (사용 라이브러리 python 3.14 , ipykernel) 이 과정을 통해 개발의 필요한 기초 프로그램 설치, 연동을 완료하고 기초세팅을 완료
>
---
>  # TIL - Python 개발 환경과 가상환경 구축

## 오늘 배운 내용

오늘은 Python을 본격적으로 사용하기 전에 개발 환경을 구축하고,
프로젝트별로 독립적인 Python 환경을 만드는 방법을 학습했다.

### 1. Python 프로젝트 시작하기

Python 프로젝트를 진행할 때는 먼저 프로젝트를 저장할 폴더를 만들고,
VS Code에서 해당 폴더를 열어 작업한다.

이번 실습에서는 프로젝트 폴더를 만든 뒤 VS Code에서 폴더를 열어
Python 개발 환경을 구성했다.

---

## 2. 가상환경(Virtual Environment)

가상환경은 프로젝트마다 필요한 Python 패키지를
독립적으로 관리하기 위한 공간이다.

쉽게 생각하면,

> 프로젝트마다 따로 사용하는 Python 전용 작업 공간

이라고 이해할 수 있다.

프로젝트마다 필요한 라이브러리의 버전이 다를 수 있기 때문에
가상환경을 사용하면 다른 프로젝트와 충돌하는 것을 방지할 수 있다.

---

### 가상환경 생성

VS Code 터미널에서 다음 명령어를 입력했다.

```bash
python -m venv .venv
```

✅ python : Python 실행
-m venv : Python의 가상환경 생성 기능 사용
.venv : 생성할 가상환경 폴더 이름

명령어 실행 후 프로젝트 폴더 내부에 .venv 폴더가 생성되었다.

---


## Jupyter Notebook

데이터 사이언스에서는 Python 코드를 한 번에 실행하기보다
코드를 셀(Cell) 단위로 실행할 수 있는 Jupyter Notebook을 많이 사용한다.

VS Code에 Python과 Jupyter 확장 프로그램을 설치한 후
.ipynb 파일을 생성하여 Notebook을 사용할 수 있다.

Jupyter Notebook에서도 실행에 사용할 Python 환경이 필요하며,
이를 Kernel(커널) 이라고 한다.

VS Code에서 생성한 .venv를 Kernel로 선택하면
현재 프로젝트의 가상환경에서 Notebook 코드를 실행할 수 있다.

테스트 코드
print("연결 완료!")

코드가 정상적으로 실행된다면

VS Code → Python → 가상환경 → Jupyter Notebook

연결이 정상적으로 완료된 것이다.

---

# 🌟 오늘의 핵심 정리
프로젝트마다 별도의 폴더를 만들어 관리한다.
가상환경은 프로젝트별 Python 실행 환경이다.
.venv를 사용하면 프로젝트별 라이브러리를 독립적으로 관리할 수 있다.
VS Code에서는 사용할 Python Interpreter를 선택해야 한다.
Jupyter Notebook에서는 실행할 Kernel을 선택해야 한다.
가상환경과 Kernel을 연결하면 같은 Python 환경에서 코드를 실행할 수 있다.

---

## 💡 오늘 느낀 점

처음에는 가상환경이라는 개념 자체가 생소해서
'폴더를 왜 만들고 Python을 왜 다시 연결해야 하지?'라는 생각이 들었다.

직접 프로젝트 폴더를 만들고 .venv를 생성한 뒤
VS Code와 Jupyter Notebook까지 연결해보면서,

프로젝트마다 독립적인 Python 작업 공간을 만들어 사용하는 과정

이라고 생각하니 조금 더 이해하기 쉬웠다.

아직 명령어는 익숙하지 않지만,
개발환경을 직접 한 번 구축해봤다는 것에 의미가 있었다.

---

## 📝 오늘의 한 줄

> 개발환경 구축은 어렵지만, 한 번 연결해보니 Python을 시작할 준비가 됐다.

---

## 🌱 앞으로의 목표

가상환경과 개발환경에 익숙해져서, 혼자서도 Python 프로젝트를 시작하고 실행할 수 있도록 연습해야겠다.

---

# 2장. 변수와 연산자 및 기본 자료형

## 2장 1강. 변수와 동적 타이핑

변수는 데이터를 저장하기 위한 이름이다.

Python에서는 변수를 만들 때 자료형을 따로 지정하지 않아도 된다.

```python
name = "가영"
age = 29
```
위 코드에서 name에는 문자열이,
age에는 정수가 저장된다.

Python은 변수에 저장된 값에 따라 자료형을 자동으로 판단한다.

이를 동적 타이핑(Dynamic Typing) 이라고 한다.

x = 10
x = "hello"

처음에는 x가 정수였지만,
나중에는 문자열을 저장할 수도 있다.

🌟 핵심 정리
변수는 값을 저장하는 이름이다.
Python은 자료형을 미리 선언하지 않는다.
값에 따라 자료형이 자동으로 결정된다.
하나의 변수에 다른 자료형의 값을 다시 저장할 수도 있다.

---
2장 2강. 기본 자료형과 형변환
---
Python에서 자주 사용하는 기본 자료형은 다음과 같다.

integer = 10       # int
decimal = 3.14     # float
text = "hello"     # str
boolean = True     # bool
int : 정수
float : 실수
str : 문자열
bool : 참/거짓

자료형은 type()을 사용하여 확인할 수 있다.

x = 10
print(type(x))

출력 결과:

<class 'int'>
형변환

형변환은 데이터의 자료형을 다른 자료형으로 바꾸는 것이다.

x = "10"

print(int(x))
print(float(x))

문자열 "10"을 정수 10,
실수 10.0으로 변환할 수 있다.

대표적인 형변환 함수:

int() : 정수형으로 변환
float() : 실수형으로 변환
str() : 문자열로 변환
bool() : 불리언형으로 변환

🌟핵심 정리
자료형은 데이터의 종류를 의미하고,
필요에 따라 다른 자료형으로 변환할 수 있다.

---

2장 3강. 연산자

연산자는 값을 계산하거나 비교할 때 사용한다.

산술 연산자
a = 10
b = 3

print(a + b)
print(a - b)
print(a * b)
print(a / b)
print(a // b)
print(a % b)
print(a ** b)
+ : 더하기
- : 빼기
* : 곱하기
/ : 나누기
// : 몫
% : 나머지
** : 거듭제곱
비교 연산자
a = 10
b = 5

print(a > b)
print(a < b)
print(a == b)
print(a != b)

비교 결과는 True 또는 False로 나타난다.

논리 연산자
a = True
b = False

print(a and b)
print(a or b)
print(not a)
and : 둘 다 참일 때 True
or : 하나라도 참이면 True
not : 참과 거짓을 반대로 변경
2장 4강. 문자열 형식화

"문자열 형식화"는 변수의 값을 문자열 안에 넣어 표현하는 방법이다.

Python에서는 f-string을 많이 사용한다.

name = "가영"
age = 29

print(f"이름은 {name}이고 나이는 {age}살입니다.")

{} 안에 변수명을 넣으면
해당 변수의 값이 문자열에 자동으로 들어간다.

계산식도 사용할 수 있다.

price = 10000
count = 3

print(f"총 금액은 {price * count}원입니다.")

핵심 정리
f-string을 사용하면 변수와 문자열을
쉽고 읽기 좋게 조합할 수 있다.

---

# 🌟 오늘의 핵심 정리
변수는 데이터를 저장하는 이름이다.
Python은 동적 타이핑을 사용한다.
주요 자료형에는 int, float, str, bool이 있다.
필요에 따라 형변환을 할 수 있다.
연산자를 사용하여 계산과 비교를 할 수 있다.
f-string을 사용하면 문자열 안에 변수 값을 쉽게 넣을 수 있다.

---

## 📝 오늘의 한 줄
Python의 기본 문법인 변수, 자료형, 연산자와 문자열 형식화를 배우며
코드가 어떻게 값을 저장하고 처리하는지 이해했다.

---

## 🌱 앞으로의 목표
기본 문법을 단순히 외우기보다 직접 코드를 작성하면서
변수와 자료형, 연산자를 자연스럽게 사용할 수 있도록 연습하기.
