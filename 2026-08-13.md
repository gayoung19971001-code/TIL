# 📚 TIL - 프로그램 검증과 객체지향 프로그래밍

> **학습일:** 2026-08-13  
> **학습 범위**
> - 7장 1강: 예외 처리
> - 7장 2강: 디버깅 기초
> - 8장 1강: 클래스
> - 8장 2강: 상속과 캡슐화

---

# 1. 예외 처리

프로그램을 실행하다 보면 예상하지 못한 오류가 발생할 수 있다.

이때 오류가 발생하더라도 프로그램이 무조건 종료되지 않도록 처리하는 것을  
**예외 처리(Exception Handling)**라고 한다.

---

## 1-1. 구문 오류와 런타임 예외

파이썬에서 발생하는 오류는 크게 **구문 오류(SyntaxError)**와 **예외(Exception)**로 나눌 수 있다.

### 구문 오류(SyntaxError)

파이썬 문법 자체를 잘못 작성했을 때 발생한다.

```python
price = 10000

if price > 5000
    print("가격이 높습니다.")
```

`if`문 뒤에 `:`가 없기 때문에 코드가 정상적으로 실행되지 않는다.

### 런타임 예외(Exception)

문법에는 문제가 없지만 **프로그램 실행 중** 문제가 발생하는 경우이다.

```python
total_price = 10000
people = 0

price_per_person = total_price / people
```

실행하면 다음과 같은 오류가 발생한다.

```text
ZeroDivisionError: division by zero
```

### 구문 오류 vs 예외

| 구분 | 의미 |
|---|---|
| SyntaxError | 코드 문법 자체가 잘못된 경우 |
| Exception | 문법은 맞지만 실행 중 문제가 발생한 경우 |

> **핵심:**  
> SyntaxError는 실행 전에 문법에서 문제가 발견되고,  
> Exception은 프로그램 실행 도중 발생한다.

---

## 1-2. 대표적인 예외 종류

### IndexError

리스트에 존재하지 않는 인덱스에 접근할 때 발생한다.

```python
products = ["노트북", "마우스", "키보드"]

print(products[5])
```

```text
IndexError: list index out of range
```

---

### ValueError

자료형은 맞지만 사용할 수 없는 값이 들어왔을 때 발생한다.

```python
number = int("사과")
```

```text
ValueError
```

---

### TypeError

연산이나 함수에서 요구하는 자료형과 다른 자료형을 사용했을 때 발생한다.

```python
price = 10000
delivery_fee = "3000"

total_price = price + delivery_fee
```

```text
TypeError
```

---

### ZeroDivisionError

숫자를 0으로 나누려고 할 때 발생한다.

```python
total_price = 10000
people = 0

price_per_person = total_price / people
```

```text
ZeroDivisionError
```

---

## 1-3. try-except

예외가 발생할 가능성이 있는 코드는 `try`에 작성하고,  
예외가 발생했을 때 실행할 코드는 `except`에 작성한다.

```python
try:
    quantity = int(input("수량을 입력하세요: "))

except ValueError:
    print("숫자를 입력해주세요.")
```

### 기본 구조

```python
try:
    # 예외가 발생할 가능성이 있는 코드

except:
    # 예외 발생 시 실행할 코드
```

---

## 1-4. try-except-else-finally

```python
try:
    quantity = int(input("수량을 입력하세요: "))

except ValueError:
    print("숫자를 입력해주세요.")

else:
    print("정상적으로 입력되었습니다.")

finally:
    print("프로그램을 종료합니다.")
```

각 영역의 역할은 다음과 같다.

| 구문 | 역할 |
|---|---|
| `try` | 예외가 발생할 가능성이 있는 코드 실행 |
| `except` | 예외가 발생했을 때 실행 |
| `else` | 예외가 발생하지 않았을 때 실행 |
| `finally` | 예외 발생 여부와 관계없이 항상 실행 |

### 쉽게 기억하기

```text
except  → 오류가 발생하면 실행
else    → 오류가 없으면 실행
finally → 무조건 실행
```

---

## 1-5. raise

`raise`는 개발자가 특정 조건에서 **직접 예외를 발생시키는 기능**이다.

예를 들어 주문 수량은 숫자로 정상 입력되었지만 `0` 이하라면  
프로그램 문법상 오류는 아니더라도 실제 주문 데이터로는 올바르지 않다.

```python
def check_order_quantity(quantity):

    if quantity <= 0:
        raise ValueError("주문 수량은 1개 이상이어야 합니다.")

    return quantity
```

```python
order_quantity = 0
check_order_quantity(order_quantity)
```

`raise`를 사용하면 잘못된 데이터가 이후 코드에 사용되는 것을 막을 수 있다.

### 일반 예외 vs raise

| 구분 | 의미 |
|---|---|
| 일반 예외 | 파이썬 실행 과정에서 자동으로 발생 |
| `raise` | 개발자가 조건을 정해 직접 예외 발생 |

---

# 2. 디버깅 기초

**디버깅(Debugging)**은 프로그램에서 발생한 오류의 원인을 찾아 수정하는 과정이다.

단순히 오류 메시지만 확인하는 것이 아니라  
프로그램이 어떤 순서로 실행되었고 변수의 값이 어떻게 변했는지 확인하는 것이 중요하다.

---

## 2-1. 트레이스백(Traceback)

**트레이스백(Traceback)**은 오류가 발생했을 때  
어떤 함수들을 거쳐서 **어디에서 오류가 발생했는지 보여주는 기록**이다.

```python
def apply_discount(price):
    return price - discount_amount

def calculate_order():
    final_price = apply_discount(10000)
    print(final_price)

calculate_order()
```

`discount_amount`가 정의되어 있지 않기 때문에 다음과 같은 오류가 발생한다.

```text
NameError: name 'discount_amount' is not defined
```

### 트레이스백 확인 순서

1. 가장 마지막 줄에서 **예외 종류와 메시지**를 확인한다.
2. 오류가 발생한 코드 위치를 확인한다.
3. 위쪽의 함수 호출 흐름을 확인한다.
4. 오류의 실제 원인을 추적한다.

> **핵심:**  
> 트레이스백은 아래쪽의 실제 오류 메시지부터 확인한 뒤  
> 위쪽으로 올라가며 호출 흐름을 추적한다.

---

## 2-2. print()를 이용한 디버깅

가장 간단하게 사용할 수 있는 디버깅 방법이다.

중간중간 `print()`를 사용하여 변수값이나 코드 실행 위치를 확인한다.

```python
stock = 20
orders = [2, 4, 5]

for order in orders:

    stock = stock - order

    print("현재 주문:", order)
    print("남은 재고:", stock)
```

### 장점

- 간단하게 사용할 수 있다.
- 변수값을 빠르게 확인할 수 있다.
- 별도의 디버깅 도구가 필요하지 않다.

### 단점

- 코드가 길어지면 `print()`가 많아진다.
- 복잡한 프로그램의 전체 실행 흐름을 확인하기 어렵다.

---

# 3. VS Code Debugger

VS Code의 Debugger를 사용하면 프로그램을 한 번에 실행하지 않고  
**한 줄씩 실행하면서 변수값과 실행 흐름을 확인**할 수 있다.

---

## 3-1. Breakpoint

**Breakpoint(중단점)**은 프로그램 실행을 원하는 위치에서 잠시 멈추게 하는 기능이다.

프로그램이 중단점에서 멈추면 현재 변수의 값과 실행 상태를 확인할 수 있다.

---

## 3-2. Variables

Debugger의 **Variables** 영역에서는 현재 실행 시점의 변수값을 확인할 수 있다.

```python
price = 10000
discount = 1000

final_price = price - discount
```

예를 들어 중단점을 설정하면 다음과 같은 값을 확인할 수 있다.

```text
price = 10000
discount = 1000
final_price = 9000
```

---

## 3-3. Call Stack

**Call Stack**은 현재 코드가 어떤 함수 호출 과정을 거쳐 실행되고 있는지 보여준다.

예를 들어 다음과 같은 함수 호출 구조가 있다면

```text
create_order()
    ↓
calculate_payment()
    ↓
apply_discount()
```

어떤 함수에서 어떤 함수를 호출하여 현재 위치까지 왔는지 확인할 수 있다.

---

## 3-4. 디버거 실행 제어

### Step Over

현재 줄을 실행하고 다음 줄로 이동한다.

함수가 호출되더라도 **함수 내부로 들어가지 않는다.**

```text
현재 줄 실행
↓
다음 줄 이동
```

---

### Step Into

현재 줄에서 함수가 호출되면 **함수 내부로 들어간다.**

함수가 내부에서 어떻게 동작하는지 자세히 확인할 때 사용한다.

---

### Step Out

현재 실행 중인 함수의 나머지 코드를 실행한 뒤  
**현재 함수를 빠져나가 호출한 위치로 돌아간다.**

---

### Continue

현재 위치부터 코드를 계속 실행한다.

다음 Breakpoint가 있다면 그 위치에서 다시 실행을 멈춘다.

---

## 3-5. 디버깅 기능 비교

| 기능 | 역할 |
|---|---|
| Traceback | 오류가 발생한 위치와 호출 흐름 추적 |
| `print()` | 중간 변수값을 직접 출력 |
| Breakpoint | 원하는 위치에서 실행 중단 |
| Variables | 현재 변수값 확인 |
| Call Stack | 함수 호출 순서 확인 |
| Step Over | 함수 내부로 들어가지 않고 다음 줄 실행 |
| Step Into | 호출된 함수 내부로 들어가기 |
| Step Out | 현재 함수에서 빠져나오기 |
| Continue | 다음 중단점까지 계속 실행 |

### Step 기능 핵심 비교

```text
Step Over → 함수 안 들어감
Step Into → 함수 안으로 들어감
Step Out  → 함수 밖으로 나감
```

---

# 4. 객체지향 프로그래밍(OOP)

**객체지향 프로그래밍(Object-Oriented Programming)**은  
데이터와 기능을 하나의 객체로 묶어 관리하는 프로그래밍 방식이다.

관련된 데이터와 함수를 하나의 클래스로 관리할 수 있어  
코드의 재사용성과 유지보수성이 좋아진다.

---

# 5. 클래스(Class)

**클래스(Class)**는 객체를 만들기 위한 **설계도**이다.

```python
class Product:
    pass
```

클래스에는 객체가 가지게 될 **속성(Attribute)**과  
객체가 수행할 **메서드(Method)**를 정의할 수 있다.

---

# 6. 클래스와 인스턴스

## 6-1. 클래스(Class)

객체를 만들기 위한 설계도이다.

```python
class Product:
    pass
```

---

## 6-2. 인스턴스(Instance)

클래스를 이용해 실제로 만들어진 객체이다.

```python
product1 = Product()
product2 = Product()
```

여기서

```text
Product  → 클래스
product1 → 인스턴스
product2 → 인스턴스
```

이다.

---

## 6-3. 인스턴스화(Instantiation)

클래스를 이용하여 실제 인스턴스를 만드는 과정을 말한다.

```python
product1 = Product()
```

---

# 7. 생성자 __init__()

`__init__()`은 객체가 생성될 때 자동으로 실행되는  
**생성자(Constructor)**이다.

객체가 처음 가져야 할 값을 설정하는 데 사용한다.

```python
class Product:

    def __init__(self, name, price):

        self.name = name
        self.price = price
```

객체를 생성할 때 값을 전달할 수 있다.

```python
product1 = Product("노트북", 1000000)
```

---

# 8. self

`self`는 **현재 사용 중인 인스턴스 자기 자신**을 의미한다.

```python
class Product:

    def __init__(self, name, price):

        self.name = name
        self.price = price
```

여기서

```python
self.name
self.price
```

는 각각 생성된 인스턴스가 자신의 값을 따로 저장하도록 만든다.

> **핵심:**  
> `self`는 클래스 자체가 아니라 **현재 인스턴스 자신**을 가리킨다.

---

# 9. 속성(Attribute)과 메서드(Method)

## 9-1. 속성(Attribute)

객체가 가지고 있는 **데이터 또는 상태**이다.

```python
self.name
self.price
self.stock
self.hp
```

---

## 9-2. 메서드(Method)

클래스 안에 정의된 함수이며  
객체가 수행할 수 있는 **기능 또는 행동**을 나타낸다.

```python
class Character:

    def heal(self):
        self.hp += 10
```

### 쉽게 기억하기

```text
속성(Attribute) = 객체가 가진 것
메서드(Method)   = 객체가 하는 것
```

---

## 9-3. 클래스 핵심 개념 비교

| 개념 | 의미 |
|---|---|
| 클래스(Class) | 객체를 만들기 위한 설계도 |
| 인스턴스(Instance) | 클래스로 만들어진 실제 객체 |
| 인스턴스화 | 클래스로 객체를 만드는 과정 |
| `__init__` | 객체 생성 시 초기값을 설정하는 생성자 |
| `self` | 현재 인스턴스 자기 자신 |
| 속성(Attribute) | 객체가 가지고 있는 데이터 |
| 메서드(Method) | 객체가 수행하는 기능 |

---

# 10. 여러 인스턴스 생성

하나의 클래스에서 여러 개의 인스턴스를 만들 수 있다.

```python
class GameCharacter:

    def __init__(self, name, hp):

        self.name = name
        self.hp = hp
```

```python
warrior = GameCharacter("전사", 100)
wizard = GameCharacter("마법사", 70)
```

두 객체 모두 `GameCharacter` 클래스로 만들어졌지만  
각 인스턴스는 자신의 상태를 독립적으로 가진다.

```text
warrior.hp = 100
wizard.hp = 70
```

즉, **같은 설계도를 사용해도 만들어진 객체의 데이터는 서로 다를 수 있다.**

---

# 11. 상속(Inheritance)

**상속(Inheritance)**은 기존 클래스의 속성과 메서드를  
새로운 클래스가 물려받아 사용할 수 있도록 하는 기능이다.

```python
class Member:

    def login(self):
        print("로그인했습니다.")
```

```python
class VIPMember(Member):

    def use_vip_lounge(self):
        print("VIP 라운지를 이용합니다.")
```

`VIPMember`는 `Member`의 `login()`을 따로 작성하지 않아도 사용할 수 있다.

```python
vip = VIPMember()

vip.login()
vip.use_vip_lounge()
```

---

## 11-1. 부모 클래스와 자식 클래스

```python
class Animal:
    pass

class Dog(Animal):
    pass
```

```text
Animal → 부모 클래스(Parent / Super Class)
Dog    → 자식 클래스(Child / Sub Class)
```

### 상속의 장점

- 기존 기능을 재사용할 수 있다.
- 중복 코드를 줄일 수 있다.
- 공통 기능은 부모 클래스에서 관리할 수 있다.
- 자식 클래스에서 필요한 기능만 추가할 수 있다.

---

# 12. 메서드 오버라이딩(Overriding)

**메서드 오버라이딩(Method Overriding)**은  
부모 클래스에서 정의된 메서드를 자식 클래스에서 **다시 정의하는 것**이다.

```python
class Delivery:

    def deliver(self):
        print("일반 배송을 시작합니다.")
```

```python
class MorningDelivery(Delivery):

    def deliver(self):
        print("새벽 배송을 시작합니다.")
```

```python
normal = Delivery()
morning = MorningDelivery()

normal.deliver()
morning.deliver()
```

자식 클래스에서 같은 이름의 메서드를 다시 정의하면  
부모 클래스의 메서드 대신 자식 클래스의 메서드가 실행된다.

> **상속** = 기능을 물려받기  
> **오버라이딩** = 물려받은 기능을 내 방식으로 다시 정의하기

---

# 13. super()

`super()`는 **부모 클래스의 메서드를 호출**할 때 사용한다.

특히 부모 클래스의 `__init__()`을 재사용할 때 많이 사용한다.

```python
class Product:

    def __init__(self, name, price):

        self.name = name
        self.price = price
```

```python
class DiscountProduct(Product):

    def __init__(self, name, price, discount_rate):

        super().__init__(name, price)
        self.discount_rate = discount_rate
```

`super().__init__(name, price)`를 사용하면  
부모 클래스의 초기화 코드를 다시 작성하지 않아도 된다.

### super()의 장점

- 부모 클래스의 기능을 재사용할 수 있다.
- 중복 코드를 줄일 수 있다.
- 부모 클래스의 변경 사항을 자식 클래스에도 쉽게 적용할 수 있다.

---

# 14. 캡슐화(Encapsulation)

**캡슐화(Encapsulation)**는 객체 내부의 중요한 데이터를 보호하고  
외부에서는 정해진 방법을 통해서만 접근하도록 만드는 방식이다.

예를 들어 계좌의 잔액을 아무 코드에서나 마음대로 변경할 수 있다면 문제가 발생할 수 있다.

따라서 데이터를 직접 변경하기보다  
메서드를 통해 안전하게 변경하도록 설계할 수 있다.

### 캡슐화의 목적

- 데이터를 보호한다.
- 잘못된 값이 저장되는 것을 방지한다.
- 데이터 변경 규칙을 한 곳에서 관리할 수 있다.
- 객체 내부 구조를 외부에서 함부로 변경하지 못하도록 한다.

---

# 15. 정보 은닉과 Private Attribute

파이썬에서는 속성 이름 앞에 `__`를 붙여  
외부에서 직접 접근하기 어렵게 만들 수 있다.

```python
class User:

    def __init__(self, user_id, password):

        self.user_id = user_id
        self.__password = password
```

```python
user = User("user01", "python123")
```

```python
print(user.user_id)
```

은 가능하지만,

```python
print(user.__password)
```

처럼 접근하면 바로 사용할 수 없다.

---

## 15-1. Name Mangling

파이썬의 `__속성명`은 완전한 접근 금지 기능이라기보다는  
**Name Mangling**이라는 방식으로 이름을 내부적으로 변경한다.

예를 들어

```python
self.__password
```

는 내부적으로 다음과 비슷한 형태로 변경된다.

```text
_ClassName__password
```

즉, 파이썬의 Private Attribute는  
**절대 접근할 수 없게 막는 기능이 아니라 직접 접근을 어렵게 만드는 기능**이다.

---

# 16. Getter와 Setter

Private Attribute의 값을 안전하게 조회하거나 수정하기 위해  
**Getter와 Setter 메서드**를 사용할 수 있다.

---

## 16-1. Getter

객체 내부의 값을 **조회하는 메서드**이다.

```python
class ProductStock:

    def __init__(self, stock):
        self.__stock = stock

    def get_stock(self):
        return self.__stock
```

```python
product = ProductStock(10)

print(product.get_stock())
```

---

## 16-2. Setter

객체 내부 값을 **변경하는 메서드**이다.

Setter를 사용하면 값을 변경하기 전에 조건을 검사할 수 있다.

```python
class ProductStock:

    def __init__(self, stock):
        self.__stock = stock

    def get_stock(self):
        return self.__stock

    def set_stock(self, stock):

        if stock >= 0:
            self.__stock = stock

        else:
            print("재고는 음수가 될 수 없습니다.")
```

```python
product = ProductStock(10)

product.set_stock(20)
print(product.get_stock())
```

Setter를 사용하면 외부에서 무조건 값을 변경하게 하지 않고  
**검증 과정을 거친 후 값을 변경**할 수 있다.

---

# 17. 상속과 캡슐화 함께 사용하기

예를 들어 기본 계좌인 `BankAccount`를 만든 뒤  
이를 상속받는 `MinusAccount` 클래스를 만들 수 있다.

```python
class BankAccount:

    def __init__(self, owner, balance):

        self.owner = owner
        self.balance = balance

    def withdraw(self, amount):

        if self.balance >= amount:
            self.balance -= amount
```

```python
class MinusAccount(BankAccount):

    def __init__(self, owner, balance, limit):

        super().__init__(owner, balance)
        self.limit = limit

    def withdraw(self, amount):

        if self.balance + self.limit >= amount:
            self.balance -= amount
```

`MinusAccount`는 `BankAccount`의 기본 기능을 물려받으면서  
출금 기능만 자신의 조건에 맞게 **오버라이딩**할 수 있다.

---

# 18. 오늘 헷갈리기 쉬운 개념 비교

## SyntaxError vs Exception

| SyntaxError | Exception |
|---|---|
| 문법 자체가 잘못됨 | 문법은 맞음 |
| 정상 실행이 어려움 | 실행 중 발생 |
| 코드 구조 수정 필요 | 예외 처리 가능 |

---

## except vs else vs finally

| 구문 | 언제 실행? |
|---|---|
| `except` | 예외 발생 |
| `else` | 예외 없음 |
| `finally` | 무조건 실행 |

---

## 클래스 vs 인스턴스

| 클래스 | 인스턴스 |
|---|---|
| 설계도 | 실제 객체 |
| `class Product:` | `product1 = Product()` |
| 객체의 구조 정의 | 실제 데이터 보유 |

---

## 속성 vs 메서드

| 속성 | 메서드 |
|---|---|
| 객체가 가진 데이터 | 객체가 수행하는 행동 |
| 이름, 가격, 체력 | 구매, 공격, 회복 |
| `self.price` | `self.sell()` |

---

## 상속 vs 오버라이딩

| 상속 | 오버라이딩 |
|---|---|
| 부모 기능을 물려받음 | 부모 기능을 다시 정의 |
| 코드 재사용 | 자식 클래스에 맞게 기능 변경 |

---

## Getter vs Setter

| Getter | Setter |
|---|---|
| 값 조회 | 값 변경 |
| 데이터를 가져옴 | 데이터를 검증 후 수정 가능 |

---

# 🌟 오늘 최종 정리

오늘은 프로그램에서 오류가 발생했을 때 이를 처리하고 원인을 추적하는 방법부터  
객체지향 프로그래밍의 기본 구조까지 학습했다.

예외 처리에서는 `try-except`를 사용하여 실행 중 발생하는 오류를 처리할 수 있고,  
`else`와 `finally`를 이용하면 정상 실행과 프로그램 마무리 과정까지 구분할 수 있다.

또한 `raise`를 사용하면 파이썬이 자동으로 발생시키는 오류뿐만 아니라  
개발자가 직접 조건을 정하여 잘못된 데이터를 예외로 처리할 수 있다.

디버깅에서는 Traceback을 통해 오류 발생 위치와 함수 호출 흐름을 확인하고,  
Breakpoint와 VS Code Debugger를 활용하여 변수값이 어떻게 변하는지 단계별로 확인할 수 있다.

객체지향 프로그래밍에서는 클래스를 설계도로 사용하여 여러 인스턴스를 생성하고,  
`__init__`, `self`, 속성, 메서드를 통해 객체의 데이터와 기능을 하나로 관리한다.

또한 상속을 사용하면 기존 클래스의 기능을 재사용할 수 있고,  
오버라이딩을 통해 자식 클래스에서 필요한 기능만 다시 정의할 수 있다.

마지막으로 캡슐화를 통해 객체 내부 데이터를 보호하고  
Getter와 Setter를 사용하여 데이터를 보다 안전하게 관리할 수 있다.

---

# 💡 오늘 느낀 점

처음에는 예외 처리, 디버깅, 클래스처럼 각각 다른 내용처럼 느껴졌지만  
전체적으로 보면 모두 **프로그램을 더 안정적이고 관리하기 쉽게 만드는 방법**이라는 공통점이 있었다.

특히 클래스는 처음 보면 `self`, `__init__`, 인스턴스 등 새로운 용어가 많아 복잡하게 느껴지지만  
`클래스 = 설계도`, `인스턴스 = 실제 객체`, `속성 = 가진 것`, `메서드 = 하는 것`으로 연결해서 생각하니 조금 더 이해하기 쉬웠다.

디버깅 역시 단순히 오류가 나면 코드를 다시 보는 것이 아니라  
Traceback이나 Breakpoint를 이용해서 **오류가 발생한 과정을 추적하는 것**이 중요하다는 점을 알게 되었다.

---

# 📝 오늘의 한 줄

> **오류를 피하는 것보다 오류를 찾아 처리할 수 있는 코드를 만들고, 객체를 이용해 데이터를 체계적으로 관리하는 것이 중요하다.**

---

# 🌱 앞으로의 목표

- `try-except-else-finally`의 실행 순서를 확실하게 구분하기
- `raise`를 이용한 데이터 검증 코드 직접 작성해보기
- Traceback에서 실제 오류가 발생한 위치 빠르게 찾는 연습하기
- Step Over / Step Into / Step Out 차이 익숙해지기
- 클래스 / 인스턴스 / 속성 / 메서드 관계 확실하게 구분하기
- `self`와 `__init__()` 사용법 반복해서 연습하기
- 상속과 메서드 오버라이딩을 직접 코드로 작성해보기
- Getter / Setter를 이용해 데이터를 안전하게 관리하는 방법 익히기
