# 📚 TIL - SQL 서브쿼리 기초와 EXISTS / FROM·SELECT절 서브쿼리

오늘은 SQL의 **서브쿼리(Subquery)** 를 중심으로 학습했다.

특히 다음 내용을 실습했다.

- 서브쿼리의 기본 구조
- `WHERE`절 서브쿼리
- `IN / NOT IN`
- `EXISTS / NOT EXISTS`
- `FROM`절 서브쿼리
- `SELECT`절 서브쿼리
- 서브쿼리와 `JOIN`, `UNION ALL`의 차이
- 집계함수 `AVG / MAX / MIN / COUNT / SUM`을 서브쿼리에서 활용하는 방법
- 서브쿼리 작성 중 발생한 데이터 타입, 테이블명, 컬럼명 오류 해결

---

# 1. 서브쿼리(Subquery)란?

서브쿼리는 **SQL문 안에 들어가는 또 다른 SELECT문**이다.

쉽게 말하면:

```text
SELECT 안에 또 SELECT
```

또는:

```text
어떤 값을 먼저 조회하고
그 결과를 바깥 SQL에서 다시 사용
```

하는 구조이다.

예:

```sql
SELECT
    product_id,
    product_name,
    list_price
FROM products
WHERE list_price > (
    SELECT AVG(list_price)
    FROM products
);
```

안쪽:

```sql
SELECT AVG(list_price)
FROM products
```

→ 전체 상품의 평균 가격 계산

바깥:

```sql
WHERE list_price > (평균가격)
```

→ 평균보다 비싼 상품 조회

⭐ 암기

```text
서브쿼리
→ SELECT 안의 SELECT

안쪽 SELECT
→ 비교하거나 사용할 기준값 생성

바깥 SELECT
→ 그 값을 이용해 최종 결과 조회
```

---

# 2. 서브쿼리에서 집계함수 고르기

서브쿼리라고 해서 무조건 `AVG()`를 사용하는 것은 아니다.

**문제에서 무엇을 요구하는지**에 따라 함수가 달라진다.

```text
평균
→ AVG()

가장 큰 값 / 최고 / 가장 비싼
→ MAX()

가장 작은 값 / 최저
→ MIN()

개수
→ COUNT()

합계
→ SUM()
```

⭐ 문제 문장의 단어와 함수를 연결해서 생각한다.

---

# 3. 평균보다 비싼 상품 찾기

문제:

```text
평균 가격보다 비싼 상품을 찾는다.
```

평균이 필요하므로 `AVG()`를 사용한다.

```sql
SELECT
    product_id,
    product_name,
    list_price
FROM products
WHERE list_price > (
    SELECT AVG(list_price)
    FROM products
);
```

구조:

```text
1. AVG로 평균가격 계산
2. 평균가격을 바깥 WHERE로 전달
3. 평균보다 가격이 큰 상품 조회
```

⭐ 암기

```text
"평균보다"
→ AVG()를 먼저 구해야 함
→ 서브쿼리
```

---

# 4. 가장 비싼 상품 찾기

문제:

```text
가장 비싼 상품의 가격과 같은 상품을 찾는다.
```

가장 큰 가격이 필요하므로 `MAX()`를 사용한다.

```sql
SELECT
    product_id,
    product_name,
    list_price
FROM products
WHERE list_price = (
    SELECT MAX(list_price)
    FROM products
);
```

안쪽:

```sql
SELECT MAX(list_price)
FROM products
```

→ 가장 비싼 가격 계산

바깥:

```sql
WHERE list_price = (최고가격)
```

→ 최고가격과 같은 상품 조회

⭐ 암기

```text
제일 비싼 가격만 알고 싶음
→ SELECT MAX()

제일 비싼 상품까지 알고 싶음
→ WHERE 가격 = (SELECT MAX())
```

---

# 5. 가장 오래된 날짜 / 가장 최근 날짜

날짜도 크고 작은 값으로 비교할 수 있다.

```text
과거 날짜
→ 작은 값

최근 날짜
→ 큰 값
```

따라서:

```sql
SELECT MIN(order_purchase_timestamp)
FROM orders;
```

→ 가장 오래된 주문일

```sql
SELECT MAX(order_purchase_timestamp)
FROM orders;
```

→ 가장 최근 주문일

⭐ 암기

```text
가장 오래된 날짜 → MIN()
가장 최근 날짜   → MAX()
```

---

# 6. order_purchase_timestamp

```text
order
→ 주문

purchase
→ 구매

timestamp
→ 날짜 + 시간
```

따라서:

```text
order_purchase_timestamp
→ 고객이 주문한 날짜와 시간
→ 주문 발생 일시
```

예:

```text
2018-01-15 13:42:10
```

활용:

```sql
MIN(order_purchase_timestamp)
```

→ 가장 오래된 주문

```sql
MAX(order_purchase_timestamp)
```

→ 가장 최근 주문

---

# 7. IN 서브쿼리

`IN`은 어떤 값이 **서브쿼리가 반환한 여러 값 중 하나에 포함되는지** 확인한다.

예:

```sql
SELECT
    product_id,
    product_name,
    list_price
FROM products
WHERE product_id IN (
    SELECT product_id
    FROM stocks
    WHERE store_id = 1
);
```

뜻:

```text
1번 매장의 stocks에 존재하는 product_id를 먼저 찾음
↓
그 product_id와 같은 products만 조회
```

⭐ 암기

```text
IN
→ 목록 안에 포함되어 있는가?
```

---

# 8. NOT IN

`NOT IN`은 특정 값 또는 목록에 **포함되지 않는 것**을 찾는다.

예:

```sql
WHERE order_status NOT IN ('canceled', 'unavailable')
```

→ canceled와 unavailable을 제외

⭐ 암기

```text
DISTINCT
→ 중복값 제거

NOT IN
→ 특정 값 제외
```

둘은 역할이 완전히 다르다.

---

# 9. DISTINCT

`DISTINCT`는 같은 값이 여러 번 있을 때 **중복을 제거한다.**

```sql
SELECT DISTINCT customer_id
FROM orders;
```

예:

```text
원본
1
1
2
2
3

DISTINCT 결과
1
2
3
```

⭐ 암기

```text
DISTINCT
→ 중복 제거
```

---

# 10. EXISTS

`EXISTS`는 서브쿼리 조건에 맞는 **행이 하나라도 존재하는지** 확인한다.

예:

```sql
SELECT
    p.product_id,
    p.product_name
FROM products p
WHERE EXISTS (
    SELECT 1
    FROM stocks s
    WHERE s.product_id = p.product_id
);
```

뜻:

```text
products에서 상품 하나를 봄
↓
stocks에 같은 product_id가 있는지 확인
↓
있으면 TRUE
↓
상품 출력
```

⭐ 암기

```text
EXISTS
→ 하나라도 있냐?
→ 있으면 TRUE
```

---

# 11. SELECT 1의 의미

`EXISTS` 안에서는 자주:

```sql
SELECT 1
```

을 사용한다.

여기서 숫자 `1` 자체가 필요한 것은 아니다.

`EXISTS`는:

```text
무슨 값이 들어 있느냐?
```

가 아니라:

```text
조건에 맞는 행이 존재하느냐?
```

만 확인하기 때문이다.

따라서 이론적으로:

```sql
SELECT 1
```

```sql
SELECT 999
```

등도 존재 여부 판단에는 같은 역할을 할 수 있다.

하지만 관례적으로:

```sql
SELECT 1
```

을 가장 많이 사용한다.

⭐ 암기

```text
EXISTS / NOT EXISTS
→ 보통 SELECT 1

1이라는 값이 중요한 것이 아니라
행의 존재 여부만 확인
```

---

# 12. NOT EXISTS

`NOT EXISTS`는 조건에 맞는 행이 **하나도 존재하지 않을 때 TRUE**가 된다.

```text
EXISTS
→ 있으면 TRUE

NOT EXISTS
→ 없으면 TRUE
```

예:

```sql
SELECT
    c.customer_id,
    c.first_name,
    c.last_name
FROM customers c
WHERE NOT EXISTS (
    SELECT 1
    FROM orders o
    WHERE o.customer_id = c.customer_id
);
```

뜻:

```text
주문 이력이 없는 고객 조회
```

⭐ 암기

```text
있다
→ EXISTS

없다 / 한 번도 없다
→ NOT EXISTS
```

---

# 13. EXISTS / NOT EXISTS 문제 문장 구별

문제:

```text
한 번이라도 주문된 상품
```

→ `EXISTS`

```sql
WHERE EXISTS (
    SELECT 1
    FROM order_items oi
    WHERE oi.product_id = p.product_id
);
```

문제:

```text
한 번도 주문되지 않은 상품
```

→ `NOT EXISTS`

```sql
WHERE NOT EXISTS (
    SELECT 1
    FROM order_items oi
    WHERE oi.product_id = p.product_id
);
```

문제:

```text
주문 이력이 있는 고객
```

→ `EXISTS`

문제:

```text
주문 이력이 없는 고객
```

→ `NOT EXISTS`

⭐ 바로 연결

```text
"있다"
→ EXISTS

"없다"
"한 번도 없다"
"이력이 없다"
→ NOT EXISTS
```

---

# 14. EXISTS 전체 구조

기본 형태:

```sql
SELECT 바깥에서_볼_컬럼
FROM 바깥테이블 별칭
WHERE EXISTS (
    SELECT 1
    FROM 안쪽테이블 별칭
    WHERE 안쪽테이블.ID = 바깥테이블.ID
);
```

예:

```sql
SELECT
    p.product_id,
    p.product_name
FROM products p
WHERE EXISTS (
    SELECT 1
    FROM stocks s
    WHERE s.product_id = p.product_id
);
```

각 부분:

```text
바깥 SELECT
→ 최종적으로 보고 싶은 것

바깥 FROM
→ 기준 테이블

WHERE EXISTS
→ 관련 데이터가 존재하는지 검사

안쪽 SELECT 1
→ 행 존재 여부만 확인

안쪽 FROM
→ 존재 여부를 확인할 테이블

안쪽 WHERE
→ 바깥 테이블과 안쪽 테이블 연결
```

---

# 15. EXISTS 마지막 WHERE가 중요한 이유

```sql
WHERE s.product_id = p.product_id
```

여기서:

```text
s = stocks
p = products
```

따라서:

```text
stocks.product_id
=
products.product_id
```

인 행이 존재하는지 확인한다.

쉽게 말하면:

```text
"지금 보고 있는 상품이 stocks에도 있니?"
```

라는 뜻이다.

⭐ JOIN과 비교

JOIN:

```sql
ON s.product_id = p.product_id
```

EXISTS:

```sql
WHERE s.product_id = p.product_id
```

둘 다 **두 테이블의 관계를 연결하는 조건**이다.

```text
JOIN
→ ON으로 연결

EXISTS
→ 안쪽 WHERE로 연결
```

---

# 16. 서브쿼리에서 SELECT / FROM / WHERE가 반복되는 이유

예:

```sql
SELECT p.product_id, p.product_name
FROM products p
WHERE EXISTS (
    SELECT 1
    FROM stocks s
    WHERE s.product_id = p.product_id
);
```

`SELECT`, `FROM`, `WHERE`가 두 번 등장한다.

이것은 같은 문장을 중복해서 쓰는 것이 아니라:

```text
바깥 쿼리 1개
+
안쪽 서브쿼리 1개
```

가 각각 자신의 SQL 구조를 가지고 있기 때문이다.

구조:

```text
바깥 쿼리

SELECT
FROM
WHERE
    (
        안쪽 쿼리

        SELECT
        FROM
        WHERE
    )
```

⭐ 암기

```text
서브쿼리
→ 쿼리 안에 또 하나의 쿼리

따라서
SELECT / FROM / WHERE가 다시 나올 수 있음
```

---

# 17. SELECT / FROM / WHERE 작성 순서

기본 작성 순서:

```text
SELECT
FROM
WHERE
```

따라서 서브쿼리 안에서도:

```sql
SELECT 1
FROM stocks s
WHERE s.product_id = p.product_id
```

처럼 작성한다.

⭐ 암기

```text
작성 순서
SELECT → FROM → WHERE
```

---

# 18. FROM절 서브쿼리

`FROM`절 안에도 서브쿼리를 사용할 수 있다.

FROM절의 서브쿼리는 **임시 테이블처럼 사용**된다.

예:

```sql
SELECT
    store_id,
    product_count,
    total_stock
FROM (
    SELECT
        store_id,
        COUNT(product_id) AS product_count,
        SUM(quantity) AS total_stock
    FROM stocks
    GROUP BY store_id
) AS store_summary
WHERE total_stock >= 500;
```

안쪽:

```sql
SELECT
    store_id,
    COUNT(product_id),
    SUM(quantity)
FROM stocks
GROUP BY store_id
```

→ 매장별 요약 결과 생성

바깥:

```sql
WHERE total_stock >= 500
```

→ 요약 결과에서 재고 합계 500 이상만 조회

⭐ 암기

```text
FROM절 서브쿼리
→ 서브쿼리 결과를 임시 테이블처럼 사용
```

---

# 19. FROM절 서브쿼리의 별칭

FROM절 서브쿼리 뒤에는 보통 별칭을 붙인다.

```sql
) AS store_summary
```

이 별칭은 서브쿼리가 만든 결과 테이블의 이름처럼 사용한다.

⭐ 암기

```text
FROM 안의 서브쿼리
→ 임시 테이블
→ 별칭 붙이기
```

---

# 20. SELECT절 서브쿼리

서브쿼리는 SELECT 안에서도 사용할 수 있다.

예:

```sql
SELECT
    product_id,
    product_name,
    list_price,
    (
        SELECT MAX(list_price)
        FROM products
    ) AS max_price
FROM products;
```

뜻:

```text
각 상품정보 옆에
전체 상품의 최고가격을 하나의 컬럼처럼 추가
```

⭐ 암기

```text
SELECT절 서브쿼리
→ 조회 결과에 컬럼 하나 추가
```

---

# 21. SELECT절 서브쿼리 주의점

SELECT절에 들어가는 서브쿼리는 보통 **값 하나만 반환해야 한다.**

즉:

```text
1행 1열
```

결과가 필요하다.

예:

```sql
SELECT MAX(list_price)
FROM products
```

→ 값 하나

정상

하지만 서브쿼리가 여러 행을 반환하면 오류가 발생할 수 있다.

⭐ 암기

```text
SELECT절 서브쿼리
→ 보통 1행 1열
```

---

# 22. WHERE / FROM / SELECT 서브쿼리 차이

```text
WHERE절 서브쿼리
→ 조건값을 만들 때

FROM절 서브쿼리
→ 임시 테이블을 만들 때

SELECT절 서브쿼리
→ 결과 컬럼을 추가할 때
```

⭐ 핵심 암기

```text
WHERE → 조건
FROM  → 임시 테이블
SELECT → 컬럼 추가
```

---

# 23. 상관 서브쿼리

안쪽 서브쿼리가 바깥 쿼리의 값을 참조하는 경우를 **상관 서브쿼리**라고 한다.

예:

```sql
SELECT
    c.customer_id,
    c.first_name,
    (
        SELECT COUNT(*)
        FROM orders o
        WHERE o.customer_id = c.customer_id
    ) AS order_count
FROM customers c;
```

여기서:

```sql
o.customer_id = c.customer_id
```

안쪽 쿼리가 바깥 쿼리의 `c.customer_id`를 사용한다.

⭐ 암기

```text
상관 서브쿼리
→ 안쪽 쿼리가 바깥 쿼리 값을 참조
```

---

# 24. UNION ALL과 서브쿼리의 차이

SELECT가 두 번 나온다고 무조건 `UNION ALL`을 사용하는 것은 아니다.

## UNION ALL

독립된 두 SELECT 결과를 **세로로 붙인다.**

```sql
SELECT ...
FROM ...

UNION ALL

SELECT ...
FROM ...;
```

구조:

```text
결과 1
↓
결과 2
```

⭐ 암기

```text
UNION ALL
→ 결과 + 결과
→ 아래로 붙이기
```

---

## 서브쿼리

한 SELECT의 결과를 다른 SELECT가 **조건이나 재료로 사용**한다.

```sql
SELECT ...
FROM ...
WHERE price > (
    SELECT AVG(price)
    FROM ...
);
```

구조:

```text
안쪽 SELECT
→ 기준값 생성

바깥 SELECT
→ 그 기준값 사용
```

⭐ 암기

```text
서브쿼리
→ 안쪽 결과를 바깥 쿼리가 사용
```

---

# 25. UNION ALL은 언제 쓰는가?

여러 SELECT 결과를 아래로 합칠 때 사용한다.

예:

```sql
SELECT store_id, product_id, quantity
FROM stocks
WHERE store_id = 1

UNION ALL

SELECT store_id, product_id, quantity
FROM stocks
WHERE store_id = 2

UNION ALL

SELECT store_id, product_id, quantity
FROM stocks
WHERE store_id = 3;
```

⭐ 규칙

```text
SELECT가 2개
→ UNION ALL 1개

SELECT가 3개
→ UNION ALL 2개

SELECT가 4개
→ UNION ALL 3개
```

즉:

```text
SELECT 개수 - 1
=
UNION ALL 개수
```

---

# 26. UNION ALL 미완성 오류

잘못된 코드:

```sql
SELECT store_id, product_id, quantity
FROM stocks
WHERE store_id = 1

UNION ALL
```

오류 이유:

```text
UNION ALL 뒤에 이어붙일 SELECT가 없음
```

올바른 코드:

```sql
SELECT store_id, product_id, quantity
FROM stocks
WHERE store_id = 1

UNION ALL

SELECT store_id, product_id, quantity
FROM stocks
WHERE store_id = 2;
```

⭐ 암기

```text
UNION ALL은 SELECT 사이에 사용

맨 앞 X
맨 뒤에 혼자 남기기 X
```

---

# 27. UNION / JOIN / 서브쿼리 구별

## UNION ALL

```text
결과를 아래로 합치고 싶다
→ UNION ALL
```

## JOIN

```text
서로 다른 테이블의 컬럼을 옆으로 연결하고 싶다
→ JOIN
```

## 서브쿼리

```text
어떤 값을 먼저 구하고
그 값을 조건이나 기준으로 다시 사용하고 싶다
→ 서브쿼리
```

⭐ 한 줄 암기

```text
UNION ALL
→ 결과 + 결과

JOIN
→ 테이블 + 테이블

서브쿼리
→ 결과를 조건/재료로 사용
```

---

# 28. SELECT *의 의미

```sql
SELECT *
FROM orders;
```

여기서 `*`는:

```text
모든 컬럼
```

을 뜻한다.

따라서:

```text
SELECT *
→ 해당 테이블의 모든 컬럼 조회
```

필요한 컬럼만 조회하려면:

```sql
SELECT
    order_id,
    customer_id
FROM orders;
```

⭐ 암기

```text
SELECT *
→ 모든 컬럼

SELECT 컬럼명
→ 필요한 컬럼만
```

---

# 29. SQL과 Python 코드 구분

오늘 실습 중 SQL과 Python 코드가 섞여 오류가 발생했다.

Python:

```python
from collections import deque

queue = deque()

queue.append("A")
queue.append("B")
queue.append("C")

print(queue.popleft())
print(queue)
```

이 코드는 Python이다.

따라서:

```text
VS Code
Jupyter
Colab
```

등에서 실행해야 한다.

반면:

```sql
SELECT *
FROM orders;
```

는 SQL이다.

따라서:

```text
DBeaver
pgAdmin Query Tool
```

등에서 실행한다.

⭐ 암기

```text
Python
→ from collections import ...
→ print()
→ append()
→ popleft()

SQL
→ SELECT
→ FROM
→ WHERE
→ JOIN
→ UNION
```

---

# 30. Python 코드를 SQL에서 실행한 오류

DBeaver / pgAdmin에서:

```python
print(queue)
```

등을 실행하면 오류가 발생한다.

예:

```text
syntax error at or near "print"
```

또는:

```text
syntax error at or near "from"
```

원인:

```text
SQL 실행 도구에서 Python 코드를 실행함
```

해결:

```text
Python → VS Code / Jupyter
SQL → DBeaver / pgAdmin
```

---

# 31. SQL 코드를 Python에서 실행한 오류

반대로 Python 셀에서:

```sql
SELECT *
FROM orders;
```

를 실행하면:

```text
SyntaxError
```

가 발생한다.

⭐ 암기

```text
SQL을 Python에서 실행
→ Python SyntaxError

Python을 SQL에서 실행
→ SQL syntax error
```

---

# 32. pgAdmin과 PostgreSQL

```text
PostgreSQL
→ 실제 데이터베이스

pgAdmin
→ PostgreSQL을 관리하는 프로그램

DBeaver
→ 여러 데이터베이스를 관리할 수 있는 프로그램

SQL
→ 데이터베이스에 명령하는 언어
```

⭐ 쉽게

```text
🐘 PostgreSQL → 데이터베이스
🖥 pgAdmin / DBeaver → 데이터베이스를 다루는 프로그램
⌨ SQL → 명령어
```

---

# 33. pgAdmin에서 SQL 실행

pgAdmin에서는:

```text
Database 선택
→ Query Tool
→ SQL 작성
→ ▶ 실행
```

형태로 사용한다.

`Ctrl + Enter`가 환경이나 설정에 따라 바로 실행되지 않는 경우에는 상단의 **삼각형 ▶ 실행 버튼**을 사용하면 된다.

---

# 34. pracicedb 생성 오류

새로운 연습용 데이터베이스:

```text
pracicedb
```

를 생성하는 과정에서:

```text
Cannot create a database when multi-database mode is disabled.
Enable 'Show all databases' option
```

오류 발생.

원인:

```text
DBeaver 연결 설정에서
여러 데이터베이스 표시 기능이 꺼져 있음
```

해결:

```text
postgres 우클릭
→ Edit Connection
→ Show all databases 체크
→ 확인
→ 다시 Create New Database
```

이후 `pracicedb` 생성 완료.

⭐ 암기

```text
새 DB 생성 안 됨
+
multi-database mode disabled

→ Show all databases 확인
```

---

# 35. 컬럼이 존재하지 않는 오류

실습 코드:

```sql
SELECT customer_id, name
FROM customers;
```

오류:

```text
"name" 이름의 컬럼이 없습니다
```

원인:

`customers` 테이블에는 `name` 컬럼이 없고:

```text
first_name
last_name
```

으로 이름이 나뉘어 있음.

수정:

```sql
SELECT
    customer_id,
    first_name,
    last_name
FROM customers;
```

이름을 합치려면:

```sql
SELECT
    customer_id,
    first_name || ' ' || last_name AS full_name
FROM customers;
```

⭐ 암기

```text
컬럼 없음 오류
→ 실제 테이블 컬럼명 확인
```

---

# 36. relation does not exist 오류

실습 중:

```sql
INNER JOIN staff_id n
```

작성 후:

```text
staff_id 이름의 relation이 없습니다
```

오류 발생.

원인:

```text
staff_id
→ 컬럼명

staffs
→ 테이블명
```

`JOIN` 뒤에는 테이블명이 와야 한다.

잘못된 코드:

```sql
JOIN staff_id n
```

올바른 코드:

```sql
JOIN staffs s
```

⭐ 암기

```text
FROM / JOIN 뒤
→ 테이블명

점(.) 뒤
→ 컬럼명
```

---

# 37. stores 테이블 없음 오류

코드:

```sql
FROM stores s
```

실행 시:

```text
stores relation does not exist
```

오류 발생.

원인:

```text
현재 데이터베이스에 stores 테이블이 없음
```

현재 테이블 확인:

```sql
SELECT table_name
FROM information_schema.tables
WHERE table_schema = 'public'
ORDER BY table_name;
```

필요하면 테이블 생성:

```sql
CREATE TABLE stores (
    store_id INT PRIMARY KEY,
    store_name VARCHAR(100)
);
```

⭐ 암기

```text
relation does not exist
→ 테이블명이 틀렸거나
→ 실제 테이블이 없음
```

---

# 38. varchar = integer 오류

서브쿼리에서:

```sql
WHERE product_id IN (
    SELECT product_id
    FROM stocks
)
```

실행 시:

```text
character varying = integer
```

오류 발생.

뜻:

```text
비교하는 두 product_id의 자료형이 서로 다름
```

예:

```text
products.product_id
→ VARCHAR

stocks.product_id
→ INTEGER
```

자료형 확인:

```sql
SELECT
    table_name,
    column_name,
    data_type
FROM information_schema.columns
WHERE table_name IN ('products', 'stocks')
  AND column_name = 'product_id';
```

자료형을 문자형으로 맞추는 해결 방향:

```sql
ALTER TABLE stocks
ALTER COLUMN product_id TYPE VARCHAR(50)
USING product_id::VARCHAR;
```

⭐ 마법의 문장 암기

```text
ALTER TABLE
→ 테이블 구조 수정

ALTER COLUMN
→ 특정 컬럼 수정

TYPE VARCHAR(50)
→ 문자형으로 변경

USING 컬럼::VARCHAR
→ 기존 값을 문자형으로 변환
```

---

# 39. order_status 자료형 오류

문자열 조건:

```sql
WHERE order_status IN ('canceled', 'unavailable')
```

실행 시 integer 관련 오류 발생.

확인 결과:

```text
order_status → integer
```

으로 설정되어 있었음.

해결:

```sql
ALTER TABLE orders
ALTER COLUMN order_status TYPE VARCHAR(50);
```

자료형 변경 후 정상 조회됨.

⭐ 오류 암기

```text
문자열을 비교하는데 integer 오류 발생
→ 컬럼 자료형 확인
```

---

# 40. 결과가 비어 있는 것은 오류가 아님

예:

```sql
SELECT
    customer_id,
    first_name,
    last_name
FROM customers c
WHERE NOT EXISTS (
    SELECT 1
    FROM orders o
    WHERE o.customer_id = c.customer_id
);
```

결과창에 아무 행도 나오지 않았지만 오류 메시지는 없었음.

뜻:

```text
SQL 오류 X

조건에 해당하는 데이터가 0건 O
```

즉 현재 데이터에 주문 이력이 없는 고객이 없을 수 있다.

⭐ 암기

```text
에러 메시지 없음
+
결과 0건

→ 조건에 맞는 데이터가 없는 것
```

---

# 41. 오늘 암기 포인트 한방 정리

```text
서브쿼리
→ SELECT 안의 SELECT
```

```text
평균 → AVG()
최대 → MAX()
최소 → MIN()
개수 → COUNT()
합계 → SUM()
```

```text
가장 오래된 날짜 → MIN()
가장 최근 날짜   → MAX()
```

```text
IN
→ 목록에 포함

NOT IN
→ 특정 값 제외

DISTINCT
→ 중복값 제거
```

```text
EXISTS
→ 존재하면 TRUE

NOT EXISTS
→ 존재하지 않으면 TRUE
```

```text
EXISTS / NOT EXISTS
→ 보통 SELECT 1
```

```text
"있다"
→ EXISTS

"없다"
"한 번도 없다"
"이력이 없다"
→ NOT EXISTS
```

```text
WHERE절 서브쿼리
→ 조건

FROM절 서브쿼리
→ 임시 테이블

SELECT절 서브쿼리
→ 컬럼 추가
```

```text
UNION ALL
→ 결과를 아래로 붙이기

JOIN
→ 테이블을 옆으로 연결

서브쿼리
→ 조회 결과를 조건/재료로 사용
```

```text
SELECT → FROM → WHERE
```

```text
FROM / JOIN 뒤
→ 테이블명

점(.) 뒤
→ 컬럼명
```

```text
varchar = integer
→ 비교하는 컬럼의 자료형 불일치
```

```text
relation does not exist
→ 테이블 없음 또는 테이블명 오류
```

---

# 🌟 오늘 최종 정리

오늘은 SQL 안에 또 다른 SQL을 넣어 사용하는 **서브쿼리**를 학습했다.

서브쿼리의 핵심은:

```text
안쪽 SELECT로 필요한 값을 먼저 구하고
그 결과를 바깥 SQL에서 다시 사용하는 것
```

이다.

문제 문장에서:

```text
평균보다
→ AVG()

가장 비싼
→ MAX()

가장 오래된
→ MIN()

포함된
→ IN

존재하는
→ EXISTS

존재하지 않는
→ NOT EXISTS
```

처럼 단어를 SQL 기능과 연결하면 문제를 해석하기 쉬워진다.

특히 `EXISTS`와 `NOT EXISTS`는:

```text
EXISTS
→ 있으면 TRUE

NOT EXISTS
→ 없으면 TRUE
```

로 구분한다.

둘 다 안쪽 서브쿼리에서는 보통:

```sql
SELECT 1
```

을 사용하며, 이는 실제 숫자 1이 필요한 것이 아니라 **조건에 맞는 행의 존재 여부만 확인하기 위한 관례적인 표현**이다.

또한:

```text
UNION ALL
→ 여러 조회 결과를 아래로 붙이기

JOIN
→ 여러 테이블을 옆으로 연결

서브쿼리
→ 한 조회 결과를 다른 조회의 조건이나 재료로 사용
```

이라는 차이를 구분해야 한다.

오늘 실습에서는 SQL 문법 자체뿐 아니라 테이블과 컬럼의 구조, 자료형을 정확히 확인해야 한다는 것도 다시 확인했다.

```text
FROM / JOIN 뒤 → 테이블명
. 뒤 → 컬럼명

varchar = integer
→ 자료형 불일치

relation does not exist
→ 테이블 문제
```

또한 SQL과 Python은 실행 환경이 다르므로:

```text
Python
→ VS Code / Jupyter

SQL
→ DBeaver / pgAdmin
```

으로 구분해야 한다.

---

# 💡 오늘 느낀 점

서브쿼리가 처음에는 SELECT가 여러 번 나오기 때문에 `UNION ALL`이나 `JOIN`과 비슷하게 느껴져 헷갈렸다.

하지만 각각의 목적을 구분하면서 조금씩 구조가 보이기 시작했다.

특히:

```text
UNION ALL은 결과를 붙이는 것
JOIN은 테이블을 연결하는 것
서브쿼리는 결과를 다시 사용하는 것
```

이라는 차이를 기억해야겠다.

또한 `EXISTS`에서 `SELECT 1`이 왜 사용되는지, 안쪽과 바깥쪽에 `SELECT / FROM / WHERE`가 각각 존재할 수 있다는 것도 이해했다.

오늘 발생한 오류들을 보면 SQL 문법 오류보다 데이터 타입, 테이블명, 컬럼명, 실행 환경 문제도 많았기 때문에 오류 메시지를 보고 원인을 구분하는 연습도 필요하다.

---

# 📝 오늘의 한 줄

서브쿼리는 **안에서 먼저 찾고, 그 결과를 바깥에서 다시 써먹는 SQL**이다.

---

# 🌱 앞으로의 목표

서브쿼리 문제를 볼 때 바로 코드를 작성하지 않고 문제 문장에서 먼저 힌트를 찾는다.

```text
1. 무엇을 최종적으로 조회할지 확인
2. 먼저 계산하거나 찾아야 하는 값이 있는지 확인
3. 평균이면 AVG
4. 최대면 MAX
5. 최소면 MIN
6. 포함 여부면 IN
7. 존재 여부면 EXISTS / NOT EXISTS
8. 안쪽 쿼리와 바깥 쿼리가 어떤 컬럼으로 연결되는지 확인
```

특히 아래는 계속 반복해서 암기한다.

```text
EXISTS
→ 있다

NOT EXISTS
→ 없다

SELECT 1
→ 존재 여부 확인

UNION ALL
→ 아래로 합치기

JOIN
→ 옆으로 연결

서브쿼리
→ 결과를 조건으로 다시 사용
```
