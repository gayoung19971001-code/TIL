# 📚 TIL - 10장 파일 입출력과 구조화된 데이터 변환 & 외부 라이브러리를 활용한 데이터 분석 및 시각화

## 📌 오늘의 학습 내용

이번 10장에서는 크게 두 가지 내용을 학습했다.

1. **파일 입출력과 구조화된 데이터 변환**
   - 텍스트 파일 읽기/쓰기
   - CSV 파일 처리
   - JSON 데이터 처리
   - 직렬화(Serialization)와 역직렬화(Deserialization)

2. **외부 라이브러리를 활용한 데이터 분석 및 시각화**
   - NumPy, Pandas, Matplotlib
   - Pandas의 Series와 DataFrame
   - 데이터 확인 및 요약
   - Matplotlib을 활용한 그래프 시각화


---

# 1️⃣ 파일 입출력과 텍스트 파일 다루기

## 1.1 파일 입출력이란?

파일 입출력이란 프로그램에서 데이터를 **파일에 저장하거나 파일에 저장된 데이터를 읽어오는 것**을 의미한다.

파이썬에서는 `open()` 함수를 이용해 파일을 열 수 있다.

```python
open("파일명", "모드")
```

### 주요 파일 모드

| 모드 | 의미 |
|---|---|
| `"r"` | 읽기(Read) |
| `"w"` | 쓰기(Write) |
| `"a"` | 기존 내용 뒤에 추가(Append) |

---

## 1.2 `with open()` 사용하기

파일을 열면 작업이 끝난 후 파일을 닫아야 한다.

```python
file = open("test.txt", "w")
file.write("Hello")
file.close()
```

하지만 `with open()`을 사용하면 작업이 끝난 뒤 **자동으로 파일을 닫아준다.**

```python
with open("test.txt", "w", encoding="utf-8") as file:
    file.write("파일을 저장합니다.")
```

### 핵심

- `open()` → 파일 열기
- `close()` → 파일 닫기
- `with open()` → 작업 종료 후 자동으로 파일 닫기

실제 작업에서는 `with open()`을 사용하는 것이 더 안전하고 편리하다.


---

# 2️⃣ 텍스트 파일 쓰기

`write()`를 이용하면 문자열을 파일에 저장할 수 있다.

```python
with open("todo.txt", "w", encoding="utf-8") as file:
    file.write("파이썬 공부하기")
```

여러 줄을 저장하려면 `\n`을 이용한다.

```python
with open("todo.txt", "w", encoding="utf-8") as file:
    file.write("파이썬 공부하기\n")
    file.write("TIL 작성하기\n")
    file.write("복습하기\n")
```

### `\n`

`\n`은 줄바꿈을 의미한다.

```text
파이썬 공부하기
TIL 작성하기
복습하기
```


---

# 3️⃣ 텍스트 파일 읽기

파이썬에서 텍스트 파일을 읽는 대표적인 방법은 다음과 같다.

- `read()`
- `readline()`
- `readlines()`


## 3.1 `read()`

파일의 **전체 내용을 하나의 문자열**로 읽는다.

```python
with open("memo.txt", "r", encoding="utf-8") as file:
    content = file.read()
    print(content)
```

### 특징

- 파일 전체 내용을 한 번에 읽는다.
- 반환 타입은 문자열 `str`
- 비교적 작은 파일의 전체 내용을 읽을 때 편리하다.


---

## 3.2 `readline()`

파일에서 **한 줄씩 읽는다.**

```python
with open("memo.txt", "r", encoding="utf-8") as file:
    line = file.readline()
    print(line)
```

여러 번 호출하면 다음 줄을 순서대로 읽는다.

```python
with open("memo.txt", "r", encoding="utf-8") as file:
    print(file.readline())
    print(file.readline())
```


---

## 3.3 `readlines()`

파일의 모든 줄을 읽어 **리스트 형태로 반환**한다.

```python
with open("memo.txt", "r", encoding="utf-8") as file:
    lines = file.readlines()
    print(lines)
```

예시 결과

```python
[
    "첫 번째 줄\n",
    "두 번째 줄\n",
    "세 번째 줄\n"
]
```


---

## 🔍 `read()` / `readline()` / `readlines()` 차이

| 함수 | 읽는 방식 | 반환값 |
|---|---|---|
| `read()` | 전체 내용 | 문자열 |
| `readline()` | 한 줄 | 문자열 |
| `readlines()` | 모든 줄 | 리스트 |

### 💡 기억하기

```text
read()      → 전체
readline()  → 한 줄
readlines() → 모든 줄을 리스트로
```


---

# 4️⃣ 반복문을 이용한 파일 기록

리스트에 저장된 데이터를 반복문을 이용해 파일에 저장할 수 있다.

```python
participants = ["가영", "민경", "규진"]

with open("participants.txt", "w", encoding="utf-8") as file:
    for participant in participants:
        file.write(participant + "\n")
```

결과

```text
가영
민경
규진
```


## `enumerate()`를 이용해 번호 붙이기

```python
participants = ["가영", "민경", "규진"]

with open("participants.txt", "w", encoding="utf-8") as file:
    for number, participant in enumerate(participants, start=1):
        file.write(f"{number}. {participant}\n")
```

결과

```text
1. 가영
2. 민경
3. 규진
```

### 핵심

`enumerate()`를 사용하면 반복하면서 **번호와 값을 함께 가져올 수 있다.**


---

# 5️⃣ CSV 형식

## 5.1 CSV란?

CSV는 **Comma-Separated Values**의 약자로 데이터를 쉼표(`,`)로 구분하여 저장하는 파일 형식이다.

예시

```text
이름,나이,지역
가영,29,인천
민경,30,서울
규진,29,창원
```

CSV는 행과 열로 구성된 **표 형태의 데이터**를 저장하기에 적합하다.


---

## 5.2 CSV 파일 읽기

파이썬에서는 `csv` 모듈을 이용할 수 있다.

```python
import csv

with open("employees.csv", "r", encoding="utf-8", newline="") as file:
    reader = csv.reader(file)

    for row in reader:
        print(row)
```

CSV의 각 행은 리스트 형태로 읽힌다.

```python
['이름', '나이', '지역']
['가영', '29', '인천']
```


---

## 5.3 CSV 파일 쓰기

CSV 파일을 저장할 때는 `csv.writer()`를 사용할 수 있다.

```python
import csv

with open("employees.csv", "w", encoding="utf-8", newline="") as file:
    writer = csv.writer(file)

    writer.writerow(["이름", "나이", "지역"])
    writer.writerow(["가영", 29, "인천"])
```

### 주요 함수

| 함수 | 역할 |
|---|---|
| `csv.reader()` | CSV 파일 읽기 |
| `csv.writer()` | CSV 파일 쓰기 |
| `writerow()` | 한 행 작성 |
| `writerows()` | 여러 행 작성 |


---

## 5.4 2차원 리스트와 CSV

CSV는 행과 열 구조이므로 **2차원 리스트와 형태가 비슷하다.**

```python
data = [
    ["이름", "나이", "지역"],
    ["가영", 29, "인천"],
    ["민경", 30, "서울"]
]
```

각 내부 리스트가 CSV의 **한 행**에 해당한다.


---

# 6️⃣ JSON 형식

## 6.1 JSON이란?

JSON은 **JavaScript Object Notation**의 약자로 데이터를 구조화하여 저장하거나 전달할 때 사용하는 형식이다.

```json
{
    "name": "가영",
    "point": 1500,
    "grade": "VIP"
}
```

JSON은 파이썬의 **딕셔너리 구조와 매우 비슷하다.**

```python
{
    "name": "가영",
    "point": 1500
}
```


---

# 7️⃣ JSON 문자열과 Python 객체 변환

## 7.1 JSON 문자열 → Python 객체

`json.loads()`를 사용한다.

```python
import json

json_data = '{"name": "가영", "point": 1500}'

user = json.loads(json_data)

print(user)
print(type(user))
```

결과

```python
{'name': '가영', 'point': 1500}
<class 'dict'>
```


---

## 7.2 Python 객체 → JSON 문자열

`json.dumps()`를 사용한다.

```python
import json

data = {
    "name": "가영",
    "language": "Python"
}

json_string = json.dumps(data, ensure_ascii=False)

print(json_string)
```

### `ensure_ascii=False`

한글이 유니코드 형태로 변환되지 않고 **한글 그대로 저장되도록 설정**할 수 있다.


---

# 8️⃣ JSON 파일 저장 및 불러오기

## 8.1 Python 객체를 JSON 파일로 저장

`json.dump()`를 사용한다.

```python
import json

member = {
    "name": "가영",
    "point": 2000
}

with open("member.json", "w", encoding="utf-8") as file:
    json.dump(member, file, ensure_ascii=False)
```


---

## 8.2 JSON 파일을 Python 객체로 불러오기

`json.load()`를 사용한다.

```python
import json

with open("member.json", "r", encoding="utf-8") as file:
    member = json.load(file)

print(member)
```


---

## 🔍 `dump()` / `dumps()` / `load()` / `loads()` 차이

| 함수 | 변환 방향 | 대상 |
|---|---|---|
| `json.dumps()` | Python → JSON | 문자열 |
| `json.dump()` | Python → JSON | 파일 |
| `json.loads()` | JSON → Python | 문자열 |
| `json.load()` | JSON → Python | 파일 |

### 💡 핵심 암기

```text
s가 붙으면 String(문자열)

dumps → JSON 문자열 만들기
loads → JSON 문자열 읽기

s가 없으면 파일

dump → JSON 파일 저장
load → JSON 파일 읽기
```


---

# 9️⃣ 직렬화와 역직렬화

## 9.1 직렬화 Serialization

직렬화란 Python 객체를 **파일에 저장하거나 다른 곳으로 전송할 수 있는 형태로 변환하는 것**이다.

```text
Python 객체
   ↓
JSON 등의 저장 가능한 형태
```

사용 함수

```python
json.dumps()
json.dump()
```


---

## 9.2 역직렬화 Deserialization

역직렬화는 저장되거나 전송된 데이터를 다시 **Python 객체 형태로 복원하는 것**이다.

```text
JSON 데이터
   ↓
Python 객체
```

사용 함수

```python
json.loads()
json.load()
```


---

## 🔍 직렬화 vs 역직렬화

| 구분 | 의미 |
|---|---|
| 직렬화 | Python 객체 → 저장 가능한 데이터 |
| 역직렬화 | 저장된 데이터 → Python 객체 |

### 기억하기

```text
직렬화   → 저장하기 위한 변환
역직렬화 → 다시 Python에서 사용하기 위한 복원
```


---

# 🔟 CSV와 JSON 비교

| 구분 | CSV | JSON |
|---|---|---|
| 구조 | 행/열 기반 | Key-Value 기반 |
| 형태 | 표 형태 | 구조화된 데이터 |
| 특징 | 단순하고 가벼움 | 복잡한 구조 표현 가능 |
| 활용 | 엑셀, 데이터 분석 | API, 웹 데이터 |
| Python 모듈 | `csv` | `json` |

### 💡 쉽게 기억하기

```text
CSV  → 엑셀 같은 표 데이터
JSON → 딕셔너리 같은 구조화 데이터
```


---

# 1️⃣1️⃣ 외부 라이브러리

## 외부 라이브러리란?

파이썬 기본 기능 이외에 다른 개발자가 만들어 제공하는 기능을 **외부 라이브러리**라고 한다.

데이터 분석에서 대표적으로 다음 라이브러리를 사용한다.

```python
import numpy as np
import pandas as pd
import matplotlib.pyplot as plt
```

| 라이브러리 | 역할 |
|---|---|
| NumPy | 수치 계산 |
| Pandas | 데이터 분석 및 처리 |
| Matplotlib | 데이터 시각화 |


---

# 1️⃣2️⃣ 외부 패키지 설치

프로젝트에 필요한 외부 패키지는 설치한 후 사용할 수 있다.

```bash
uv add pandas numpy matplotlib
```

설치 후 Python 코드에서 `import` 한다.

```python
import numpy as np
import pandas as pd
import matplotlib.pyplot as plt
```

## 🔍 `uv add` vs `import`

| 명령 | 의미 |
|---|---|
| `uv add` | 라이브러리를 프로젝트에 설치 |
| `import` | 설치된 라이브러리를 Python 코드에서 사용 |

### 기억하기

```text
uv add → 설치
import → 사용
```


---

# 1️⃣3️⃣ Pandas 데이터 구조

Pandas에서 가장 대표적인 데이터 구조는 다음 두 가지이다.

- Series
- DataFrame


---

## 13.1 Series

Series는 **1차원 데이터 구조**이다.

```python
import pandas as pd

prices = pd.Series([5000, 8000, 10000])

print(prices)
```

하나의 열(Column)과 비슷한 형태라고 볼 수 있다.


---

## 13.2 DataFrame

DataFrame은 **행과 열로 구성된 2차원 데이터 구조**이다.

```python
import pandas as pd

data = {
    "상품명": ["노트북", "마우스", "키보드"],
    "가격": [1200000, 20000, 50000]
}

df = pd.DataFrame(data)

print(df)
```


---

## 🔍 Series vs DataFrame

| 구분 | Series | DataFrame |
|---|---|---|
| 차원 | 1차원 | 2차원 |
| 형태 | 하나의 열 | 여러 행과 열 |
| 예시 | 상품 가격 목록 | 상품명 + 가격 + 수량 |

### 💡 기억하기

```text
Series    → 1차원
DataFrame → 2차원 표
```


---

# 1️⃣4️⃣ DataFrame 확인하기

## `print()`

```python
print(df)
```

Python에서 DataFrame의 내용을 출력할 수 있다.

하지만 데이터가 크면 전체 내용을 확인하기 어려울 수 있다.


---

## Jupyter Notebook에서 확인

Jupyter Notebook에서는 변수 이름만 작성해도 표 형태로 확인할 수 있다.

```python
df
```

DataFrame이 HTML 형태의 깔끔한 표로 출력된다.


---

# 1️⃣5️⃣ 데이터 구조 확인하기

## `info()`

DataFrame의 전체적인 구조를 확인할 때 사용한다.

```python
df.info()
```

확인할 수 있는 내용

- 행 개수
- 열 개수
- 컬럼명
- 데이터 타입
- 결측치 여부
- 메모리 사용량

### 핵심

```text
info() → 데이터가 어떻게 구성되어 있는지 확인
```


---

# 1️⃣6️⃣ 숫자형 데이터 요약하기

## `describe()`

숫자형 데이터의 기본 통계량을 확인할 수 있다.

```python
df.describe()
```

주요 결과

| 항목 | 의미 |
|---|---|
| `count` | 데이터 개수 |
| `mean` | 평균 |
| `std` | 표준편차 |
| `min` | 최소값 |
| `25%` | 1사분위수 |
| `50%` | 중앙값 |
| `75%` | 3사분위수 |
| `max` | 최대값 |


---

## 🔍 `info()` vs `describe()`

| 함수 | 확인 목적 |
|---|---|
| `info()` | 데이터 구조와 타입 확인 |
| `describe()` | 숫자 데이터의 통계 요약 |

### 💡 기억하기

```text
info()     → 구조 확인
describe() → 통계 확인
```


---

# 1️⃣7️⃣ 데이터 전처리와 시각화

실제 데이터 분석에서는 데이터를 바로 그래프로 만들기보다 먼저 데이터를 확인하고 정리하는 과정이 필요하다.

```text
데이터 불러오기
        ↓
데이터 구조 확인
        ↓
필요한 데이터 선택
        ↓
데이터 정제
        ↓
분석
        ↓
시각화
        ↓
결과 해석
```

이와 같은 과정을 **데이터 분석(Data Analysis)**이라고 한다.


---

# 1️⃣8️⃣ Matplotlib을 활용한 데이터 시각화

Matplotlib은 Python에서 데이터를 그래프로 표현할 때 사용하는 대표적인 시각화 라이브러리이다.

```python
import matplotlib.pyplot as plt
```


---

# 1️⃣9️⃣ 기본 선 그래프

```python
import matplotlib.pyplot as plt

days = [1, 2, 3, 4, 5]
orders = [20, 30, 25, 40, 35]

plt.plot(days, orders)
plt.show()
```

`plt.plot()`을 사용하면 선 그래프를 생성할 수 있다.


---

# 2️⃣0️⃣ 그래프 제목과 축 이름 설정하기

## 그래프 제목

```python
plt.title("Monthly Sales")
```

## X축 이름

```python
plt.xlabel("Month")
```

## Y축 이름

```python
plt.ylabel("Sales")
```

## 격자 표시

```python
plt.grid(True)
```

## 그래프 출력

```python
plt.show()
```


---

# 2️⃣1️⃣ Matplotlib 주요 함수

| 함수 | 역할 |
|---|---|
| `plt.plot()` | 선 그래프 생성 |
| `plt.title()` | 그래프 제목 |
| `plt.xlabel()` | X축 이름 |
| `plt.ylabel()` | Y축 이름 |
| `plt.legend()` | 범례 표시 |
| `plt.grid()` | 격자 표시 |
| `plt.show()` | 그래프 출력 |


---

# 2️⃣2️⃣ 그래프의 데이터 포인트 표시하기

`marker`를 사용하면 각 데이터 값의 위치를 그래프 위에 표시할 수 있다.

```python
plt.plot(x, y, marker="o")
```

예시

```text
●────●────●────●
```

각 데이터가 어디에 위치하는지 더 명확하게 확인할 수 있다.


---

# 2️⃣3️⃣ 범례 추가하기

그래프에서 각 선이 어떤 데이터를 의미하는지 알려주기 위해 `label`과 `legend()`를 사용한다.

```python
plt.plot(
    x,
    y,
    label="Monthly Sales"
)

plt.legend()
```

### 핵심

```text
label    → 그래프 이름 설정
legend() → 설정한 그래프 이름 표시
```


---

# 2️⃣4️⃣ Pandas DataFrame과 Matplotlib 함께 사용하기

Pandas에서 만든 DataFrame의 데이터를 Matplotlib에서 바로 사용할 수 있다.

```python
import pandas as pd
import matplotlib.pyplot as plt

user_data = {
    "월": ["1월", "2월", "3월", "4월", "5월"],
    "사용자수": [2500, 4200, 3000, 5100, 4700]
}

df_users = pd.DataFrame(user_data)
```

그래프 생성

```python
plt.plot(
    df_users["월"],
    df_users["사용자수"],
    marker="o",
    label="Monthly Sales"
)

plt.title("Monthly New Users")
plt.xlabel("Month")
plt.ylabel("Users")
plt.grid(True)
plt.legend()
plt.show()
```

### 핵심

Pandas에서 원하는 열을 선택한 후 Matplotlib의 X축과 Y축에 넣어 그래프로 표현할 수 있다.

```python
df_users["월"]
df_users["사용자수"]
```


---

# 2️⃣5️⃣ 실전 판매 데이터 시각화

예를 들어 월별 판매량을 DataFrame으로 만든 뒤 그래프로 표현할 수 있다.

```python
import pandas as pd
import matplotlib.pyplot as plt

order_data = {
    "월": ["1월", "2월", "3월", "4월", "5월"],
    "판매량": [3200, 4200, 3000, 5100, 4700]
}

df_orders = pd.DataFrame(order_data)

plt.plot(
    df_orders["월"],
    df_orders["판매량"],
    marker="o",
    label="Monthly Sales"
)

plt.title("Sparta Lecture Sales Report")
plt.xlabel("Month")
plt.ylabel("Sales")
plt.grid(True)
plt.legend()
plt.show()
```

그래프를 이용하면 숫자만 볼 때보다 **월별 판매량의 증가와 감소 추세를 빠르게 확인할 수 있다.**


---

# 2️⃣6️⃣ 데이터 분석과 시각화 전체 흐름

이번 강의에서 배운 내용을 하나의 과정으로 연결하면 다음과 같다.

```text
CSV / JSON / 텍스트 파일
        ↓
Python으로 데이터 불러오기
        ↓
Pandas DataFrame으로 변환
        ↓
info()로 데이터 구조 확인
        ↓
describe()로 통계 확인
        ↓
필요한 열 선택
        ↓
Matplotlib으로 시각화
        ↓
데이터 패턴 및 의미 분석
```

즉, 파일 입출력과 데이터 분석, 시각화는 각각 따로 존재하는 것이 아니라 하나의 데이터 분석 과정으로 연결된다.


---

# 🔎 헷갈리는 개념 한 번에 비교

## 1. 파일 읽기 함수

| 함수 | 의미 | 반환 형태 |
|---|---|---|
| `read()` | 전체 내용 읽기 | 문자열 |
| `readline()` | 한 줄 읽기 | 문자열 |
| `readlines()` | 모든 줄 읽기 | 리스트 |

```text
read      → 전체
readline  → 한 줄
readlines → 전체 줄 리스트
```


---

## 2. JSON 함수

| 함수 | 의미 |
|---|---|
| `json.dumps()` | Python 객체 → JSON 문자열 |
| `json.dump()` | Python 객체 → JSON 파일 |
| `json.loads()` | JSON 문자열 → Python 객체 |
| `json.load()` | JSON 파일 → Python 객체 |

```text
s가 있으면 문자열(String)
s가 없으면 파일
```


---

## 3. CSV vs JSON

| CSV | JSON |
|---|---|
| 표 형태 | 구조화된 형태 |
| 행/열 중심 | Key-Value 중심 |
| 데이터 분석에 많이 사용 | API·웹 데이터에 많이 사용 |


---

## 4. Series vs DataFrame

| Series | DataFrame |
|---|---|
| 1차원 | 2차원 |
| 하나의 열과 유사 | 행과 열로 구성된 표 |


---

## 5. `info()` vs `describe()`

| 함수 | 의미 |
|---|---|
| `info()` | 데이터 구조·타입·결측치 확인 |
| `describe()` | 숫자 데이터 통계 요약 |


---

## 6. `uv add` vs `import`

| 명령 | 의미 |
|---|---|
| `uv add` | 외부 라이브러리 설치 |
| `import` | 설치된 라이브러리 사용 |


---

# 🧠 10장 핵심 암기 포인트

1. `with open()`을 사용하면 파일 작업이 끝난 후 자동으로 파일을 닫아준다.
2. `read()`는 전체 내용을 읽는다.
3. `readline()`은 한 줄을 읽는다.
4. `readlines()`는 모든 줄을 리스트로 읽는다.
5. CSV는 **행과 열로 이루어진 표 형태 데이터**이다.
6. JSON은 **Key-Value 기반의 구조화된 데이터**이다.
7. `dump/load`는 파일과 관련된다.
8. `dumps/loads`는 문자열과 관련된다.
9. 직렬화는 Python 객체를 저장 가능한 형태로 변환하는 것이다.
10. 역직렬화는 저장된 데이터를 Python 객체로 복원하는 것이다.
11. NumPy는 수치 계산에 사용한다.
12. Pandas는 데이터를 분석하고 처리할 때 사용한다.
13. Matplotlib은 데이터를 그래프로 시각화할 때 사용한다.
14. Series는 1차원 데이터 구조이다.
15. DataFrame은 행과 열을 가진 2차원 데이터 구조이다.
16. `info()`는 데이터 구조를 확인한다.
17. `describe()`는 숫자형 데이터의 통계 정보를 확인한다.
18. `plt.plot()`은 선 그래프를 생성한다.
19. `label`은 그래프의 이름을 지정한다.
20. `legend()`는 지정한 이름을 범례로 표시한다.


---

# 🌟 오늘 최종 정리

이번 10장에서는 데이터를 Python 프로그램 내부에서만 사용하는 것이 아니라 **파일로 저장하고 다시 불러오는 방법부터 데이터를 분석하고 시각화하는 과정까지** 학습했다.

텍스트 파일을 다룰 때는 `open()`과 `with open()`을 사용하며, 파일을 읽는 방법에는 `read()`, `readline()`, `readlines()`가 있다.

```text
read()      → 전체 내용
readline()  → 한 줄
readlines() → 전체 줄을 리스트로
```

구조화된 데이터를 저장하는 대표적인 방식으로 CSV와 JSON이 있다.

CSV는 행과 열 중심의 표 형태 데이터이고, JSON은 Python의 딕셔너리처럼 Key-Value 구조를 가진 데이터이다.

JSON에서는 함수 이름이 비슷하기 때문에 다음과 같이 기억하면 좋다.

```text
dump  → JSON 파일 저장
load  → JSON 파일 읽기

dumps → JSON 문자열 만들기
loads → JSON 문자열 읽기
```

또한 데이터를 분석하기 위해 Pandas를 이용하여 Series와 DataFrame을 만들 수 있으며, `info()`와 `describe()`를 통해 데이터의 구조와 통계 정보를 확인할 수 있다.

```text
Series    → 1차원
DataFrame → 2차원

info()     → 데이터 구조 확인
describe() → 통계 정보 확인
```

마지막으로 Pandas에서 정리한 데이터를 Matplotlib으로 전달해 그래프로 표현하는 방법을 학습했다.

이번 장의 전체 흐름을 정리하면 다음과 같다.

```text
파일에서 데이터 가져오기
        ↓
데이터를 구조화하기
        ↓
Pandas DataFrame 만들기
        ↓
데이터 구조와 통계 확인하기
        ↓
필요한 데이터 선택하기
        ↓
Matplotlib으로 시각화하기
        ↓
데이터의 패턴과 의미 해석하기
```

이번 장부터는 지금까지 배운 Python 문법들이 실제 데이터 분석 과정에서 어떻게 활용되는지 연결되기 시작했다.


# 💡 오늘 느낀 점

이전까지는 리스트, 딕셔너리, 반복문, 함수처럼 Python의 기본 문법을 각각 따로 배우는 느낌이 강했는데, 이번 장에서는 그 내용들이 실제 데이터 파일을 다루는 과정에서 어떻게 사용되는지 조금씩 연결되기 시작했다.

특히 CSV와 JSON은 단순히 파일 확장자를 외우는 것이 아니라 데이터가 어떤 구조로 저장되어 있는지 이해하는 것이 중요하다는 것을 알게 되었다.

Pandas와 Matplotlib도 각각의 함수를 따로 외우기보다는 **데이터를 가져오고 → 확인하고 → 분석하고 → 시각화하는 전체 흐름**으로 이해하는 것이 더 중요하다고 느꼈다.

앞으로 실제 데이터를 분석할 때도 무작정 코드를 작성하기보다 먼저 데이터의 구조를 확인하고, 필요한 내용을 정리한 다음 시각화하는 순서로 접근해야겠다.


# 📝 오늘의 한 줄

**데이터 분석은 데이터를 불러오는 것에서 시작해, 구조를 이해하고 분석한 뒤 시각화하여 의미를 찾는 과정이다.**


# 🌱 앞으로의 목표

- `read()`, `readline()`, `readlines()`의 차이를 확실하게 구분하기
- `json.dump()`, `json.dumps()`, `json.load()`, `json.loads()`를 헷갈리지 않기
- CSV와 JSON의 데이터 구조 차이를 이해하기
- 파일 입출력 코드를 직접 작성해보기
- Pandas의 Series와 DataFrame 구조에 익숙해지기
- 데이터를 받으면 먼저 `info()`와 `describe()`로 확인하는 습관 만들기
- Matplotlib의 기본 그래프 함수 직접 작성해보기
- CSV/JSON → DataFrame → 분석 → 시각화까지 하나의 흐름으로 연습하기
