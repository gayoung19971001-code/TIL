# 📚 TIL - 파이썬 내장 함수와 표준 라이브러리 활용

**날짜: 2026-08-14**

## 📖 오늘 학습 내용

- 9장 1강: 내장 함수
- 9장 2강: 표준 라이브러리

---

# 1. 파이썬 내장 함수

파이썬의 **내장 함수(Built-in Function)** 는 별도의 `import` 없이 바로 사용할 수 있는 함수이다.

데이터의 자료형을 확인하거나, 여러 조건을 판단하거나, 숫자를 계산하고 데이터를 가공할 때 자주 사용한다.

---

## 1.1 `isinstance()` - 자료형 확인

`isinstance()`는 어떤 값이 특정 자료형에 해당하는지 확인하는 함수이다.

기본 형태:

    isinstance(값, 자료형)

예제:

    order_count = 4

    if isinstance(order_count, int):
        print("주문 수량은 정수입니다.")
    else:
        print("주문 수량은 숫자로 입력해주세요.")

결과:

    주문 수량은 정수입니다.

조건에 맞으면 `True`, 아니면 `False`를 반환한다.

    age_1 = 20
    age_2 = "20"

    print(isinstance(age_1, int))
    print(isinstance(age_2, int))

결과:

    True
    False

### 💡 핵심

`type()`으로도 자료형을 확인할 수 있지만, 조건문에서 자료형을 검사할 때는 `isinstance()`를 활용할 수 있다.

---

# 2. `all()`과 `any()` - 여러 조건 판단

여러 개의 조건을 한 번에 검사할 때 사용하는 함수이다.

---

## 2.1 `all()`

`all()`은 모든 값이 참인지 확인한다.

    task_status = [True, True, False]

    print(all(task_status))

결과:

    False

### 핵심

- 모두 `True` → `True`
- 하나라도 `False` → `False`

즉,

> **all = 전부 만족하는가?**

---

## 2.2 `any()`

`any()`는 여러 값 중 하나라도 참인지 확인한다.

    user_agreements = [True, True, False]

    print(any(user_agreements))

결과:

    True

### 핵심

- 하나라도 `True` → `True`
- 모두 `False` → `False`

즉,

> **any = 하나라도 만족하는가?**

---

## 🔥 `all()` vs `any()`

| 함수 | 의미 | True가 되는 조건 |
|---|---|---|
| `all()` | 모두 만족하는가? | 모든 값이 True |
| `any()` | 하나라도 만족하는가? | 하나 이상 True |

예제:

    conditions = [True, True, False]

    print(all(conditions))
    print(any(conditions))

결과:

    False
    True

---

## ⚠️ 빈 리스트에서의 `all()`과 `any()`

    print(all([]))
    print(any([]))

결과:

    True
    False

- `all([])` → `True`
- `any([])` → `False`

빈 리스트에서는 결과가 일반적인 직관과 다르게 느껴질 수 있으므로 주의해야 한다.

---

# 3. 데이터가 비어 있는지 확인하기

파이썬에서는 리스트와 같은 객체 자체를 조건식으로 사용할 수 있다.

    document_checks = []

    if document_checks:
        print("서류 확인을 진행합니다.")
    else:
        print("서류가 비어 있습니다.")

빈 리스트는 조건식에서 `False`로 판단된다.

### 대표적인 False 값

- `False`
- `None`
- `0`
- `0.0`
- `""`
- `[]`
- `{}`
- `()`

즉, 데이터가 비어 있는지를 확인할 때 다음처럼 사용할 수 있다.

    if data:
        print("데이터가 있습니다.")
    else:
        print("데이터가 없습니다.")

---

# 4. 숫자 데이터 요약 함수

데이터의 개수, 합계, 반올림 등을 구할 때 기본 내장 함수를 사용할 수 있다.

---

## 4.1 `len()`

`len()`은 데이터의 요소 개수를 반환한다.

    order_amounts = [12000, 24000, 17000, 20000]

    print(len(order_amounts))

결과:

    4

### 핵심

> `len()` = 데이터 개수

---

## 4.2 `sum()`

`sum()`은 숫자 데이터의 합계를 구한다.

    order_amounts = [12000, 24000, 17000, 20000]

    print(sum(order_amounts))

### 핵심

> `sum()` = 숫자들의 총합

---

## 4.3 `round()`

`round()`는 숫자를 반올림한다.

    average_amount = 18324.567

    print(round(average_amount))
    print(round(average_amount, 2))

결과:

    18325
    18324.57

기본 형태:

    round(숫자, 소수점_자리수)

### 핵심

- `round(3.7)` → 정수 단위 반올림
- `round(3.14159, 2)` → 소수점 둘째 자리까지 표시

---

# 5. 평균과 중앙값

평균과 중앙값을 계산할 때는 파이썬 표준 라이브러리인 `statistics`를 사용할 수 있다.

    import statistics

---

## 5.1 `statistics.mean()` - 평균

`mean()`은 데이터의 평균값을 계산한다.

    response_times = [15, 8, 7, 9, 46]

    average_time = statistics.mean(response_times)

    print(average_time)

평균은 모든 값을 더한 뒤 데이터 개수로 나눈 값이다.

---

## 5.2 `statistics.median()` - 중앙값

`median()`은 데이터를 정렬했을 때 가운데에 위치하는 값을 반환한다.

    response_times = [15, 8, 7, 9, 46]

    median_time = statistics.median(response_times)

    print(median_time)

---

## 🔥 평균 vs 중앙값

| 구분 | 평균 `mean()` | 중앙값 `median()` |
|---|---|---|
| 의미 | 전체 값을 더해 개수로 나눈 값 | 정렬했을 때 가운데 값 |
| 이상치 영향 | 큼 | 비교적 적음 |
| 활용 | 전체적인 평균 수준 | 이상치가 있는 데이터 |

예를 들어 고객 응답 시간이 대부분 5~10분이지만 한 명만 200분이라면 평균은 크게 올라갈 수 있다.

이런 경우에는 중앙값을 함께 확인하는 것이 데이터의 일반적인 수준을 이해하는 데 도움이 된다.

---

# 6. `map()` - 데이터 변환

`map()`은 iterable의 **각 요소에 동일한 함수를 적용**할 때 사용한다.

기본 형태:

    map(함수, 데이터)

예제:

    product_prices = [10000, 15000, 20000]

    final_prices = list(
        map(lambda price: price + 3000, product_prices)
    )

    print(final_prices)

결과:

    [13000, 18000, 23000]

`map()`의 결과는 `map` 객체이므로 결과를 리스트로 확인하고 싶다면 `list()`로 변환해야 한다.

    list(map(...))

### 핵심

> `map()` = 데이터를 하나씩 바꾼다.

---

# 7. `filter()` - 조건에 맞는 데이터 추출

`filter()`는 조건이 `True`인 데이터만 골라낸다.

기본 형태:

    filter(조건함수, 데이터)

예제:

    stock_counts = [4, 15, 8, 20, 12]

    available_stocks = list(
        filter(lambda stock: stock >= 10, stock_counts)
    )

    print(available_stocks)

결과:

    [15, 20, 12]

---

## 🔥 `map()` vs `filter()`

| 함수 | 역할 |
|---|---|
| `map()` | 모든 데이터를 변환 |
| `filter()` | 조건에 맞는 데이터만 선택 |

예제 데이터:

    scores = [60, 80, 90, 50]

모든 점수에 5점을 더하고 싶다면:

    list(map(lambda x: x + 5, scores))

70점 이상만 가져오고 싶다면:

    list(filter(lambda x: x >= 70, scores))

### 핵심

> `map()` = 바꾼다  
> `filter()` = 골라낸다

---

# 8. `enumerate()` - 인덱스 자동 부여

`enumerate()`는 반복문에서 **인덱스와 값**을 동시에 사용할 수 있게 해준다.

    tasks = ["회의 준비", "보고서 작성", "코드 공부"]

    for number, task in enumerate(tasks, start=1):
        print(f"{number}. {task}")

결과:

    1. 회의 준비
    2. 보고서 작성
    3. 코드 공부

기본 형태:

    enumerate(iterable, start=0)

기본 인덱스는 `0`부터 시작하지만 `start=1`을 사용하면 1부터 시작할 수 있다.

### 핵심

> `enumerate()` = 번호 + 값

---

# 9. `zip()` - 여러 데이터 묶기

`zip()`은 두 개 이상의 리스트에서 같은 위치의 데이터를 하나씩 묶어준다.

    products = ["노트북", "키보드", "마우스"]
    prices = [1200000, 50000, 30000]

    for product, price in zip(products, prices):
        print(product, price)

결과:

    노트북 1200000
    키보드 50000
    마우스 30000

기본 형태:

    zip(iterable1, iterable2)

서로 관련된 여러 데이터를 동시에 반복 처리할 때 유용하다.

### 핵심

> `zip()` = 같은 위치의 데이터를 묶는다.

---

# 10. `sorted()` - 데이터 정렬

`sorted()`는 데이터를 정렬한 **새로운 리스트를 반환**한다.

    order_amounts = [24000, 12000, 30000, 18000]

    print(sorted(order_amounts))

결과:

    [12000, 18000, 24000, 30000]

기본적으로 오름차순으로 정렬된다.

내림차순으로 정렬하려면 `reverse=True`를 사용한다.

    print(sorted(order_amounts, reverse=True))

결과:

    [30000, 24000, 18000, 12000]

---

## ⚠️ `sorted()`는 원본을 변경하지 않는다.

    numbers = [3, 1, 2]

    result = sorted(numbers)

    print(numbers)
    print(result)

결과:

    [3, 1, 2]
    [1, 2, 3]

원본 `numbers`는 그대로 유지되고 정렬된 새로운 리스트가 생성된다.

### 핵심

> `sorted()` = 정렬된 새 리스트 반환

---

# 11. `min()`과 `max()`

`min()`은 최솟값, `max()`는 최댓값을 반환한다.

    scores = [75, 90, 80, 95, 70]

    print(min(scores))
    print(max(scores))

결과:

    70
    95

### 핵심

- `min()` → 가장 작은 값
- `max()` → 가장 큰 값

---

# 12. 파이썬 표준 라이브러리

**표준 라이브러리(Standard Library)** 는 파이썬을 설치하면 기본적으로 함께 제공되는 기능들의 모음이다.

내장 함수와 달리 대부분 `import`한 뒤 사용한다.

대표적인 표준 라이브러리:

    import datetime
    import time
    import math
    import statistics
    import json
    import random
    import os
    import sys

---

# 13. 내장 함수 vs 표준 라이브러리

| 구분 | 내장 함수 | 표준 라이브러리 |
|---|---|---|
| 사용 방법 | 바로 사용 가능 | 대부분 `import` 필요 |
| 예시 | `len()`, `sum()`, `sorted()` | `math`, `random`, `json` |
| 목적 | 기본적이고 자주 쓰는 기능 | 분야별로 확장된 기능 |

내장 함수:

    print(len([1, 2, 3]))

별도의 `import`가 필요하지 않다.

표준 라이브러리:

    import math

    print(math.sqrt(16))

먼저 `math`를 불러와야 한다.

---

# 14. `datetime` - 날짜와 시간

날짜와 시간을 다룰 때 사용하는 표준 라이브러리이다.

    import datetime

    now = datetime.datetime.now()

    print(now)

`datetime.datetime.now()`를 사용하면 현재 날짜와 시간을 가져올 수 있다.

---

## 14.1 `strftime()` - 날짜/시간 형식 지정

`strftime()`은 날짜와 시간을 원하는 문자열 형식으로 변환할 때 사용한다.

    formatted = now.strftime("%Y-%m-%d %H:%M:%S")

    print(formatted)

대표적인 포맷:

| 코드 | 의미 |
|---|---|
| `%Y` | 4자리 연도 |
| `%m` | 월 |
| `%d` | 일 |
| `%H` | 시간 |
| `%M` | 분 |
| `%S` | 초 |
| `%A` | 요일 이름 |

예:

    now.strftime("%Y-%m-%d")

결과 예시:

    2026-08-14

### 핵심

> `strftime()` = 날짜/시간을 원하는 형식의 문자열로 만든다.

---

# 15. `time` - 시간 관련 기능

시간 측정이나 프로그램 실행 지연 등에 사용할 수 있다.

    import time

---

## 15.1 `time.sleep()`

프로그램의 실행을 일정 시간 동안 멈춘다.

    print("시작")

    time.sleep(2)

    print("2초 후 실행")

`time.sleep(2)`는 프로그램을 약 2초 동안 멈춘다.

---

## 15.2 `time.perf_counter()`

프로그램이나 코드의 실행 시간을 측정할 때 사용할 수 있다.

    import time

    start = time.perf_counter()

    # 실행할 코드

    end = time.perf_counter()

    print(end - start)

### 핵심

- `time.sleep()` → 잠깐 멈추기
- `time.perf_counter()` → 실행 시간 측정

---

# 16. `math` - 수학 계산

수학 연산과 관련된 여러 기능을 제공한다.

    import math

자주 사용하는 함수:

- `math.ceil()`
- `math.floor()`
- `math.sqrt()`
- `math.pi`

---

## 16.1 `math.ceil()` - 올림

    print(math.ceil(3.14))

결과:

    4

---

## 16.2 `math.floor()` - 내림

    print(math.floor(3.99))

결과:

    3

---

## 16.3 `math.sqrt()` - 제곱근

    print(math.sqrt(25))

결과:

    5.0

---

## 🔥 `round()` vs `ceil()` vs `floor()`

| 함수 | 의미 |
|---|---|
| `round()` | 반올림 |
| `math.ceil()` | 올림 |
| `math.floor()` | 내림 |

예제:

    import math

    value = 3.7

    print(round(value))
    print(math.ceil(value))
    print(math.floor(value))

결과:

    4
    4
    3

### 핵심

> `round()` = 반올림  
> `ceil()` = 무조건 위로  
> `floor()` = 무조건 아래로

---

# 17. `json` - 데이터 형식 변환

JSON은 프로그램이나 시스템 사이에서 데이터를 주고받을 때 많이 사용되는 데이터 형식이다.

    import json

---

## 17.1 `json.dumps()`

파이썬 객체를 JSON 문자열로 변환한다.

    product = {
        "name": "무선 마우스",
        "price": 25000,
        "in_stock": True
    }

    json_text = json.dumps(
        product,
        ensure_ascii=False
    )

    print(json_text)

파이썬의 `dict`가 JSON 형식의 문자열로 변환된다.

---

## 17.2 `json.loads()`

JSON 문자열을 다시 파이썬 객체로 변환한다.

    restored_product = json.loads(json_text)

    print(restored_product)

---

## 🔥 `json.dumps()` vs `json.loads()`

| 함수 | 변환 방향 |
|---|---|
| `json.dumps()` | Python → JSON 문자열 |
| `json.loads()` | JSON 문자열 → Python |

### 기억하기

> `dumps()` = 파이썬 데이터를 JSON 문자열로 내보내기  
> `loads()` = JSON 문자열을 파이썬으로 불러오기

---

# 18. `random` - 무작위 데이터

랜덤한 값을 생성하거나 데이터를 무작위로 선택할 때 사용하는 라이브러리이다.

    import random

---

## 18.1 `random.sample()`

여러 데이터 중 지정한 개수만큼 **중복 없이 선택**한다.

    participants = ["민수", "영희", "철수", "수진"]

    winners = random.sample(participants, 2)

    print(winners)

`participants`에서 2명을 무작위로 선택한다.

---

## 18.2 자주 사용하는 `random` 함수

| 함수 | 역할 |
|---|---|
| `random.randint(a, b)` | a~b 사이 정수 생성 |
| `random.random()` | 0 이상 1 미만 실수 생성 |
| `random.choice()` | 데이터 하나 선택 |
| `random.shuffle()` | 데이터 순서 섞기 |
| `random.sample()` | 여러 개를 중복 없이 선택 |

### `random.choice()`

    menus = ["김밥", "라면", "돈까스"]

    print(random.choice(menus))

하나만 무작위로 선택한다.

### `random.sample()`

    print(random.sample(menus, 2))

여러 개를 **중복 없이** 선택한다.

---

## 🔥 `choice()` vs `sample()`

| 함수 | 특징 |
|---|---|
| `random.choice()` | 요소 하나 선택 |
| `random.sample()` | 여러 요소를 중복 없이 선택 |

---

# 19. `os` - 운영체제와 파일 시스템

`os`는 운영체제와 파일, 폴더, 경로 등을 다룰 때 사용하는 표준 라이브러리이다.

    import os

---

## 19.1 `os.getcwd()`

현재 작업 중인 폴더 경로를 확인한다.

    print(os.getcwd())

---

## 19.2 `os.path.join()`

여러 경로를 운영체제에 맞게 하나의 경로로 결합한다.

    file_path = os.path.join(
        "data",
        "sales.csv"
    )

    print(file_path)

---

## 19.3 `os.path.exists()`

파일이나 폴더가 실제로 존재하는지 확인한다.

    print(os.path.exists(file_path))

존재하면 `True`, 존재하지 않으면 `False`를 반환한다.

### 핵심

- `os.getcwd()` → 현재 폴더
- `os.path.join()` → 경로 결합
- `os.path.exists()` → 존재 여부 확인

---

# 20. `sys` - 파이썬 실행 환경

`sys`는 현재 실행 중인 파이썬 환경과 관련된 정보를 확인할 때 사용한다.

    import sys

---

## 20.1 `sys.version`

현재 사용 중인 파이썬 버전을 확인한다.

    print(sys.version)

### 핵심

> `sys` = 현재 파이썬 실행 환경 관련 정보

---

# 21. 주요 함수 한눈에 정리

| 함수 | 역할 |
|---|---|
| `isinstance()` | 자료형 확인 |
| `all()` | 모든 조건이 참인지 확인 |
| `any()` | 하나라도 참인지 확인 |
| `len()` | 데이터 개수 |
| `sum()` | 합계 |
| `round()` | 반올림 |
| `map()` | 데이터 변환 |
| `filter()` | 조건에 맞는 데이터 추출 |
| `enumerate()` | 인덱스와 값 함께 사용 |
| `zip()` | 여러 데이터 묶기 |
| `sorted()` | 데이터 정렬 |
| `min()` | 최솟값 |
| `max()` | 최댓값 |

---

# 22. 주요 표준 라이브러리 한눈에 정리

| 라이브러리 | 주요 역할 |
|---|---|
| `datetime` | 날짜와 시간 |
| `time` | 시간 측정, 실행 지연 |
| `math` | 수학 연산 |
| `statistics` | 평균, 중앙값 등 통계 |
| `json` | JSON 데이터 변환 |
| `random` | 무작위 값 생성 |
| `os` | 운영체제, 파일, 경로 |
| `sys` | 파이썬 실행 환경 |

---

# 23. 헷갈리는 개념 비교

## 🔥 `all()` vs `any()`

    all() → 모두 True인가?
    any() → 하나라도 True인가?

---

## 🔥 `map()` vs `filter()`

    map() → 데이터를 바꾼다
    filter() → 조건에 맞는 데이터를 골라낸다

---

## 🔥 `enumerate()` vs `zip()`

    enumerate() → 인덱스 + 값
    zip() → 여러 데이터의 같은 위치 값끼리 묶기

---

## 🔥 `round()` vs `ceil()` vs `floor()`

    round() → 반올림
    ceil()  → 올림
    floor() → 내림

---

## 🔥 `json.dumps()` vs `json.loads()`

    dumps() → Python → JSON 문자열
    loads() → JSON 문자열 → Python

---

## 🔥 내장 함수 vs 표준 라이브러리

    내장 함수
    → import 없이 바로 사용
    → len(), sum(), map(), filter(), sorted() 등

    표준 라이브러리
    → 대부분 import 후 사용
    → math, datetime, random, json, os 등

---

# 🌟 오늘 최종 정리

오늘은 파이썬에서 기본적으로 제공하는 **내장 함수와 표준 라이브러리**를 학습했다.

내장 함수는 별도의 `import` 없이 바로 사용할 수 있다.

대표적인 함수는 다음과 같다.

    len()
    sum()
    round()
    isinstance()
    all()
    any()
    map()
    filter()
    enumerate()
    zip()
    sorted()
    min()
    max()

표준 라이브러리는 파이썬 설치 시 함께 제공되지만 대부분 사용하기 전에 `import`가 필요하다.

오늘 학습한 대표적인 표준 라이브러리는 다음과 같다.

    datetime
    time
    math
    statistics
    json
    random
    os
    sys

내장 함수와 표준 라이브러리를 활용하면 직접 모든 기능을 처음부터 구현하지 않아도 되기 때문에 코드를 더 간결하고 효율적으로 작성할 수 있다.

특히 오늘 학습한 내용에서는 비슷해 보이는 기능들의 차이를 확실하게 구분하는 것이 중요하다.

    all()       → 모두 만족
    any()       → 하나라도 만족

    map()       → 데이터 변환
    filter()    → 조건에 맞는 데이터 추출

    enumerate() → 인덱스 + 값
    zip()       → 데이터끼리 묶기

    round()     → 반올림
    ceil()      → 올림
    floor()     → 내림

    dumps()     → Python → JSON
    loads()     → JSON → Python

또한 `sorted()`는 원본 데이터를 직접 변경하는 것이 아니라 **정렬된 새로운 리스트를 반환한다는 점**도 기억해야 한다.

---

# 💡 오늘 느낀 점

처음에는 함수와 라이브러리의 종류가 많아서 전부 외워야 할 것처럼 느껴졌지만, 실제로는 각각의 이름을 무작정 암기하기보다 **어떤 상황에서 어떤 기능을 사용하는지 이해하는 것이 더 중요하다는 생각이 들었다.**

특히 `map()`과 `filter()`, `all()`과 `any()`처럼 비슷해 보이는 기능은 따로 외우는 것보다 서로 비교해서 보면 차이가 훨씬 명확하게 이해된다.

앞으로 데이터 분석을 하면서 반복적으로 사용하다 보면 자연스럽게 익숙해질 것 같다.

---

# 📝 오늘의 한 줄

> **파이썬을 잘 활용한다는 것은 모든 기능을 직접 만드는 것이 아니라, 이미 준비된 함수와 라이브러리를 상황에 맞게 잘 사용하는 것이다.**

---

# 🌱 앞으로의 목표

내장 함수와 표준 라이브러리의 이름을 단순히 암기하기보다 실제 데이터 분석 과정에서 직접 사용하면서 익히자.

특히 `map()`과 `filter()`, `all()`과 `any()`, `enumerate()`와 `zip()`처럼 헷갈리기 쉬운 함수들의 차이를 확실히 구분하고, 필요한 상황에서 바로 떠올릴 수 있도록 반복해서 연습하자.
