[2026-08-26_TIL_2장_데이터정제.md](https://github.com/user-attachments/files/31447023/2026-08-26_TIL_2._.md)
# 📚 TIL - 2장 데이터 정제 요소 / 결측치·이상치·중복데이터 처리

## 📌 오늘 배운 내용

오늘은 데이터 분석 전에 반드시 거쳐야 하는 **데이터 정제(Data Cleaning)** 과정을 학습했다.

실제 데이터에는 다음과 같은 문제가 자주 포함된다.

- 결측치(Missing Value)
- 중복 데이터(Duplicate Data)
- 이상치(Outlier)
- 잘못 입력된 값
- 분석 목적과 맞지 않는 데이터

이러한 문제를 그대로 둔 채 분석하면 결과가 왜곡될 수 있으므로, 분석 전에 데이터를 정리하고 처리하는 과정이 필요하다.

---

# 1. 데이터 정제 요소

## 1-1. 데이터 정제란?

데이터 정제는 분석에 사용할 데이터를 보다 정확하고 일관된 상태로 만드는 과정이다.

대표적인 데이터 정제 요소는 다음과 같다.

- 결측치 처리
- 중복 데이터 처리
- 이상치 처리
- 잘못된 데이터 수정
- 데이터 형식 통일

데이터 정제에서 중요한 것은 단순히 값을 삭제하거나 수정하는 것이 아니라,

> 왜 해당 데이터를 삭제하거나 수정했는지 근거를 남기는 것이다.

처리 방법에 따라 분석 결과가 달라질 수 있기 때문에 데이터의 분포와 의미를 먼저 확인해야 한다.

---

# 2. 완전탐색(Brute Force)

## 2-1. 완전탐색이란?

완전탐색은 가능한 모든 경우를 하나씩 확인하여 원하는 결과를 찾는 방법이다.

예를 들어 여러 숫자 중 두 숫자를 골라 합이 특정 값이 되는 조합을 찾을 경우, 가능한 모든 조합을 직접 확인할 수 있다.

```python
numbers = [2, 4, 6, 7]

for i in range(len(numbers)):
    for j in range(i + 1, len(numbers)):
        if numbers[i] + numbers[j] == 9:
            print(numbers[i], numbers[j])
```

### 완전탐색 특징

- 모든 경우를 확인한다.
- 구현이 비교적 단순하다.
- 경우의 수가 적을 때 효과적이다.
- 데이터가 많아지면 실행 시간이 오래 걸릴 수 있다.

---

# 3. 결측치(Missing Value)

## 3-1. 결측치란?

결측치는 데이터에서 값이 비어 있거나 존재하지 않는 상태를 의미한다.

Pandas에서는 주로 다음과 같이 표현된다.

```python
NaN
None
```

예시

```python
import pandas as pd

survey = pd.DataFrame({
    'customer': ['가영', '민경', '규진', '재웅'],
    'age': [28, 30, 29, 34],
    'satisfaction': [5, None, 4, None]
})
```

`satisfaction` 열의 일부 값이 비어 있으므로 결측치가 존재한다.

---

## 3-2. 결측치 확인 - isna()

결측치 여부는 `isna()`로 확인할 수 있다.

```python
survey['satisfaction'].isna()
```

- `True` : 결측치
- `False` : 값이 존재함

---

# 4. 결측치 처리 방법 4가지

대표적인 결측치 처리 방법은 다음과 같다.

1. 삭제
2. 평균값 대체
3. 중앙값 대체
4. 최빈값 대체

---

## 4-1. 결측치 삭제 - dropna()

결측치가 존재하는 행을 삭제할 때 사용한다.

```python
df.dropna()
```

특정 열을 기준으로 삭제할 수도 있다.

```python
df.dropna(subset=['age'])
```

### 삭제가 적절한 경우

- 결측치가 매우 적을 때
- 삭제해도 전체 분석에 큰 영향을 주지 않을 때
- 임의의 값으로 채우는 것이 더 큰 왜곡을 만들 수 있을 때

결측치 비율이 높은데 무조건 삭제하면 데이터 손실이 커질 수 있으므로 주의해야 한다.

---

## 4-2. 결측치 채우기 - fillna()

결측치를 다른 값으로 채울 때는 `fillna()`를 사용한다.

```python
df['age'] = df['age'].fillna(값)
```

---

## 4-3. 평균값 대체

수치형 데이터의 결측치를 평균값으로 대체할 수 있다.

```python
df['age'] = df['age'].fillna(df['age'].mean())
```

### 특징

- 수치형 데이터에 사용
- 데이터 분포가 비교적 고를 때 활용 가능
- 이상치가 많으면 평균값 자체가 영향을 받을 수 있음

---

## 4-4. 중앙값 대체

중앙값은 데이터를 크기 순으로 정렬했을 때 가운데 위치한 값이다.

```python
df['age'] = df['age'].fillna(df['age'].median())
```

### 특징

- 수치형 데이터에 사용
- 평균보다 이상치 영향을 적게 받음
- 소득, 매출, 가격처럼 극단값이 존재하는 데이터에 유용

---

## 4-5. 최빈값 대체

최빈값은 가장 자주 등장하는 값이다.

```python
df['region'] = df['region'].fillna(df['region'].mode()[0])
```

### 특징

- 범주형 데이터에서 자주 사용
- 가장 많이 등장한 값을 이용해 결측치를 채움

---

## 4-6. 결측치 처리 방법 비교

| 처리 방법 | 특징 | 적합한 경우 |
|---|---|---|
| 삭제 | 결측 행 제거 | 결측치가 적을 때 |
| 평균값 대체 | 평균으로 채움 | 수치형, 이상치 영향이 적을 때 |
| 중앙값 대체 | 중앙값으로 채움 | 수치형, 이상치가 있을 때 |
| 최빈값 대체 | 가장 자주 등장한 값으로 채움 | 범주형 데이터 |

---

## 4-7. 결측치 처리 근거 기록

실무에서는 단순히 처리만 하는 것이 아니라 **왜 그렇게 처리했는지 기록하는 것**이 중요하다.

예시

```text
age 열의 결측치는 전체 데이터의 1% 이하이므로 삭제했다.

income 열은 이상치가 많아 평균 대신 중앙값으로 대체했다.

region 열은 범주형 데이터이므로 최빈값으로 대체했다.
```

---

# 5. 이상치(Outlier)

## 5-1. 이상치란?

이상치는 대부분의 데이터와 지나치게 차이가 나는 값을 의미한다.

예시

```text
20, 21, 19, 22, 23, 200
```

대부분의 값이 20 근처인데 `200`만 지나치게 크므로 이상치일 가능성이 있다.

이상치는 무조건 오류라고 단정할 수 없으며, 실제로 의미 있는 값일 수도 있다.

---

# 6. IQR 방식

## 6-1. IQR이란?

IQR은 **Interquartile Range**의 약자로 사분위 범위를 의미한다.

- Q1 : 하위 25%
- Q2 : 중앙값 50%
- Q3 : 상위 75%

공식

```text
IQR = Q3 - Q1
```

---

## 6-2. IQR 이상치 기준

하한값

```text
Q1 - 1.5 × IQR
```

상한값

```text
Q3 + 1.5 × IQR
```

따라서 다음 조건에 해당하면 이상치 후보로 판단한다.

```text
값 < Q1 - 1.5 × IQR
```

또는

```text
값 > Q3 + 1.5 × IQR
```

---

## 6-3. Pandas에서 IQR 계산

```python
Q1 = df['금액'].quantile(0.25)
Q3 = df['금액'].quantile(0.75)

IQR = Q3 - Q1

lower_bound = Q1 - 1.5 * IQR
upper_bound = Q3 + 1.5 * IQR
```

이상치 추출

```python
outliers = df[
    (df['금액'] < lower_bound) |
    (df['금액'] > upper_bound)
]
```

### IQR 핵심

```text
Q1 = 25%
Q3 = 75%

IQR = Q3 - Q1

하한 = Q1 - 1.5 × IQR
상한 = Q3 + 1.5 × IQR

범위 밖 값 = 이상치 후보
```

---

# 7. Z-score 방식

## 7-1. Z-score란?

Z-score는 특정 값이 평균에서 몇 개의 표준편차만큼 떨어져 있는지를 나타내는 값이다.

쉽게 말하면,

> 평균에서 얼마나 멀리 떨어져 있는가

를 나타낸다.

---

## 7-2. Z-score 이상치 기준

일반적으로 다음 기준을 많이 사용한다.

```text
|Z-score| > 3
```

즉, Z-score의 절댓값이 3보다 크면 이상치 후보로 판단한다.

예시

```python
from scipy import stats
import numpy as np

z_scores = np.abs(stats.zscore(df['금액']))

outliers = df[z_scores > 3]
```

---

# 8. IQR vs Z-score

| 구분 | IQR | Z-score |
|---|---|---|
| 기준 | 사분위수 | 평균, 표준편차 |
| 대표 기준 | 1.5 × IQR | 절댓값 3 이상 |
| 이상치 영향 | 비교적 적음 | 평균·표준편차가 이상치 영향을 받을 수 있음 |
| 적합한 데이터 | 비대칭 데이터에도 비교적 안정적 | 정규분포에 가까운 데이터 |

---

# 9. 이상치 처리 전략

이상치를 발견했다고 무조건 삭제하는 것은 좋지 않다.

대표적인 처리 방법은 다음과 같다.

1. 제거
2. Winsorizing
3. 유지

---

## 9-1. 이상치 제거

명백한 입력 오류이거나 분석에 불필요한 값이면 삭제할 수 있다.

```python
df = df[
    (df['금액'] >= lower_bound) &
    (df['금액'] <= upper_bound)
]
```

---

## 9-2. Winsorizing

Winsorizing은 이상치를 삭제하지 않고 **경계값으로 바꾸는 방법**이다.

예를 들어 상한값이 20,000인데 실제 값이 50,000이라면

```text
50,000 → 20,000
```

으로 변경한다.

Pandas에서는 `clip()`을 사용할 수 있다.

```python
df['금액'] = df['금액'].clip(
    lower=lower_bound,
    upper=upper_bound
)
```

### 장점

- 데이터를 삭제하지 않음
- 이상치의 영향을 줄일 수 있음

---

## 9-3. 이상치 유지

이상치처럼 보여도 실제 의미가 있는 값이라면 그대로 유지할 수 있다.

예를 들어 대부분의 고객이 10만 원 이하를 구매했지만 VIP 고객이 1,000만 원을 구매했다면, 이는 오류가 아니라 실제 고객 특성일 수 있다.

따라서 이상치 처리는 다음 순서로 생각한다.

```text
이상치 발견
↓
왜 이상치인지 확인
↓
실제 오류인지 판단
↓
삭제 / Winsorizing / 유지 결정
```

---

## 9-4. 이상치 처리 방법 비교

| 방법 | 설명 | 사용 상황 |
|---|---|---|
| 제거 | 이상치 데이터 삭제 | 명백한 오류 |
| Winsorizing | 경계값으로 변경 | 데이터는 유지하면서 영향 감소 |
| 유지 | 그대로 사용 | 실제 의미가 있는 값 |

---

# 10. 중복 데이터(Duplicate Data)

## 10-1. 중복 데이터란?

중복 데이터는 동일한 정보가 데이터에 여러 번 들어 있는 상태를 의미한다.

예시

```text
order_id  customer
1001      가영
1002      민경
1002      민경
1003      규진
```

`1002` 주문이 두 번 들어가 있으므로 중복이다.

중복 데이터를 그대로 사용하면 주문 수, 고객 수, 매출 등이 실제보다 크게 계산될 수 있다.

---

## 10-2. 중복 데이터 발생 원인

- 데이터 입력 실수
- 여러 파일 병합 과정
- ETL 과정
- 크롤링
- 시스템 오류
- 동일 데이터 재수집

---

# 11. duplicated()

## 11-1. 중복 여부 확인

Pandas에서는 `duplicated()`를 이용하여 중복 여부를 확인할 수 있다.

```python
df.duplicated()
```

결과

```text
False
False
True
False
```

- `True` : 중복
- `False` : 중복 아님

---

## 11-2. 특정 열 기준 중복 확인

```python
df.duplicated(subset=['order_id'])
```

`order_id`를 기준으로 중복 여부를 판단한다.

---

# 12. duplicated()의 keep 옵션

## keep='first'

```python
df.duplicated(
    subset=['order_id'],
    keep='first'
)
```

- 첫 번째 값은 `False`
- 이후 중복값은 `True`

즉, 첫 번째 데이터를 남기는 방식이다.

---

## keep='last'

```python
df.duplicated(
    subset=['order_id'],
    keep='last'
)
```

마지막 데이터를 남기고 앞의 중복값을 `True`로 판단한다.

---

## keep=False

```python
df.duplicated(
    subset=['order_id'],
    keep=False
)
```

중복된 데이터 전체를 `True`로 표시한다.

### keep 옵션 정리

| 옵션 | 의미 |
|---|---|
| `keep='first'` | 첫 번째 남김 |
| `keep='last'` | 마지막 남김 |
| `keep=False` | 중복된 데이터 전부 중복으로 판단 |

---

# 13. drop_duplicates()

## 13-1. 중복 데이터 제거

중복 데이터를 실제로 제거할 때는 `drop_duplicates()`를 사용한다.

```python
df.drop_duplicates()
```

---

## 13-2. 특정 열 기준 중복 제거

```python
df.drop_duplicates(
    subset=['customer_id']
)
```

`customer_id`를 기준으로 중복을 제거한다.

---

## 13-3. keep 옵션

첫 번째 값 유지

```python
df.drop_duplicates(
    subset=['customer_id'],
    keep='first'
)
```

마지막 값 유지

```python
df.drop_duplicates(
    subset=['customer_id'],
    keep='last'
)
```

중복된 데이터 전체 제거

```python
df.drop_duplicates(
    subset=['customer_id'],
    keep=False
)
```

---

# 14. subset의 의미

`subset`은 **어떤 열을 기준으로 중복을 판단할지 지정하는 옵션**이다.

예시

```python
df.drop_duplicates(
    subset=['order_id']
)
```

→ 주문번호 기준 중복 제거

반대로 `subset`을 사용하지 않으면

```python
df.drop_duplicates()
```

행 전체 값이 동일한 경우에만 중복으로 판단한다.

---

# 15. 중복 처리 전후 검증

중복 데이터를 제거한 뒤에는 반드시 처리 결과가 맞는지 확인하는 것이 좋다.

```python
before_count = len(df)

clean_df = df.drop_duplicates(
    subset=['order_id']
)

after_count = len(clean_df)

removed_count = before_count - after_count

print("처리 전:", before_count)
print("처리 후:", after_count)
print("제거된 행:", removed_count)
```

---

## 15-1. len()과 shape

행 개수 확인

```python
len(df)
```

데이터 크기 확인

```python
df.shape
```

예시

```text
(1000, 5)
```

의미

```text
1000행 5열
```

중복 제거 후 `len()` 또는 `shape`를 이용하면 실제로 데이터 개수가 줄었는지 확인할 수 있다.

---

# 16. 중복 데이터 처리 흐름

```text
중복 여부 확인
↓
duplicated()

기준 열 설정
↓
subset

어떤 값을 남길지 결정
↓
keep

중복 제거
↓
drop_duplicates()

처리 결과 검증
↓
len() / shape
```

---

# 🔥 헷갈리는 개념 비교

## isna() vs duplicated()

```text
isna()
→ 결측치인지 확인

duplicated()
→ 중복인지 확인
```

둘 다 결과가 `True / False` 형태로 나타난다.

---

## dropna() vs fillna()

```text
dropna()
→ 결측치 삭제

fillna()
→ 결측치를 다른 값으로 채움
```

---

## dropna() vs drop_duplicates()

```text
dropna()
→ 결측치 삭제

drop_duplicates()
→ 중복 데이터 삭제
```

둘 다 `drop`이 들어가지만 삭제 대상이 다르다.

---

## duplicated() vs drop_duplicates()

```text
duplicated()
→ 중복 여부 확인

drop_duplicates()
→ 중복 데이터 실제 제거
```

쉽게 기억하면

```text
duplicated()
→ "중복이야?"

drop_duplicates()
→ "중복 없애!"
```

---

## 평균 vs 중앙값 vs 최빈값

```text
평균(mean)
→ 전체 합 ÷ 개수
→ 이상치 영향을 많이 받음

중앙값(median)
→ 정렬했을 때 가운데 값
→ 이상치 영향을 비교적 적게 받음

최빈값(mode)
→ 가장 많이 등장한 값
→ 범주형 데이터에 자주 사용
```

---

## IQR vs Z-score

```text
IQR
→ 사분위수 기준
→ Q1, Q3 사용
→ 비대칭 데이터에서도 비교적 안정적

Z-score
→ 평균과 표준편차 기준
→ 평균에서 얼마나 떨어졌는지 확인
→ 정규분포에 가까운 데이터에 적합
```

---

## 이상치 탐지 vs 이상치 처리

```text
이상치 탐지
→ 이상치를 찾는 과정
→ IQR / Z-score

이상치 처리
→ 찾은 이상치를 어떻게 할지 결정
→ 제거 / Winsorizing / 유지
```

---

# 🧠 오늘의 암기 포인트

## 1. 데이터 정제 핵심

```text
결측치
중복 데이터
이상치
```

분석 전에 반드시 확인한다.

---

## 2. 결측치 확인

```python
isna()
```

```text
True = 결측치
False = 정상값
```

---

## 3. 결측치 삭제

```python
dropna()
```

```text
drop = 삭제
na = 결측치
```

---

## 4. 결측치 채우기

```python
fillna()
```

```text
fill = 채우기
na = 결측치
```

---

## 5. 결측치 처리 기준

```text
결측치가 매우 적음
→ 삭제 고려

수치형
→ 평균 / 중앙값

이상치가 많은 수치형
→ 중앙값 고려

범주형
→ 최빈값 고려
```

단, 실제 처리 방법은 데이터 분포와 의미를 보고 결정한다.

---

## 6. 평균 / 중앙값 / 최빈값 함수

```python
mean()
median()
mode()
```

---

## 7. IQR 공식 ★ 필수 암기

```text
IQR = Q3 - Q1
```

```text
하한 = Q1 - 1.5 × IQR
상한 = Q3 + 1.5 × IQR
```

```text
값 < 하한
또는
값 > 상한

→ 이상치 후보
```

---

## 8. quantile()

```python
df['컬럼'].quantile(0.25)
```

→ Q1

```python
df['컬럼'].quantile(0.75)
```

→ Q3

---

## 9. Z-score

```text
평균에서 몇 표준편차 떨어져 있는가
```

대표적인 이상치 기준

```text
|Z-score| > 3
```

---

## 10. Winsorizing

```text
이상치 삭제 X
이상치를 상한값 / 하한값으로 변경
```

Pandas에서는

```python
clip()
```

을 활용할 수 있다.

---

## 11. 중복 확인

```python
duplicated()
```

→ 중복 확인

---

## 12. 중복 삭제

```python
drop_duplicates()
```

→ 중복 제거

---

## 13. subset

```python
subset=['order_id']
```

→ 어떤 열을 기준으로 중복을 판단할지 지정

---

## 14. keep 옵션 ★

```text
keep='first'
→ 첫 번째 남김

keep='last'
→ 마지막 남김

keep=False
→ 중복된 값을 전부 중복으로 처리
```

---

## 15. subset을 쓰지 않으면

```python
df.drop_duplicates()
```

→ 행 전체가 동일한 경우를 중복으로 판단한다.

특정 열 기준이라면

```python
df.drop_duplicates(
    subset=['customer_id']
)
```

---

## 16. 처리 후 검증

```python
len(df)
df.shape
```

중복이나 결측치를 처리한 뒤에는 데이터 개수가 원하는 대로 변경됐는지 확인한다.

---

## 17. 오늘 함수 한 번에 암기

```python
# 결측치 확인
isna()

# 결측치 삭제
dropna()

# 결측치 대체
fillna()

# 평균 / 중앙값 / 최빈값
mean()
median()
mode()

# 사분위수
quantile()

# 중복 확인
duplicated()

# 중복 제거
drop_duplicates()

# 이상치 경계값 처리
clip()

# 데이터 크기 확인
len()
shape
```

---

# 🌟 오늘 최종 정리

오늘은 데이터 분석 전에 반드시 수행해야 하는 **데이터 정제 과정**을 학습했다.

대표적인 데이터 문제는 **결측치, 이상치, 중복 데이터**이다.

결측치는 `isna()`로 확인하고, `dropna()`로 삭제하거나 `fillna()`를 이용해 평균·중앙값·최빈값 등으로 대체할 수 있다.

이상치는 IQR과 Z-score 방식으로 탐지할 수 있다.

IQR은 다음 공식을 사용한다.

```text
IQR = Q3 - Q1

하한 = Q1 - 1.5 × IQR
상한 = Q3 + 1.5 × IQR
```

범위를 벗어나는 값을 이상치 후보로 판단한다.

Z-score는 평균에서 몇 개의 표준편차만큼 떨어져 있는지 확인하며, 일반적으로 절댓값이 3보다 큰 값을 이상치 후보로 볼 수 있다.

이상치는 무조건 삭제하는 것이 아니라 실제 데이터의 의미를 확인한 뒤 **제거 / Winsorizing / 유지** 중 적절한 방법을 선택한다.

중복 데이터는 `duplicated()`로 확인하고 `drop_duplicates()`로 제거한다.

`subset`으로 중복 판단 기준 열을 지정하고, `keep='first'`, `keep='last'`, `keep=False`를 이용해 어떤 데이터를 남길지 결정할 수 있다.

마지막으로 데이터 정제에서 가장 중요한 것은 단순히 값을 없애는 것이 아니라,

> 왜 이 데이터를 이렇게 처리했는지 설명할 수 있어야 한다는 것이다.

---

# 💡 오늘 느낀 점

데이터 분석은 단순히 그래프를 만들거나 계산하는 것보다, 분석 전에 데이터를 제대로 정리하는 과정이 훨씬 중요하다는 것을 알게 되었다.

특히 결측치나 이상치를 발견했다고 무조건 삭제하는 것이 아니라 데이터의 크기, 분포, 의미를 확인한 뒤 적절한 처리 방법을 선택해야 한다는 점이 중요했다.

또 `dropna()`, `fillna()`, `duplicated()`, `drop_duplicates()`처럼 이름이 비슷한 함수들이 많기 때문에 단순히 함수 이름을 외우는 것보다 **확인하는 함수인지, 실제 데이터를 변경하는 함수인지** 구분해서 기억해야겠다.

---

# 📝 오늘의 한 줄

**좋은 분석은 좋은 데이터에서 시작하고, 좋은 데이터는 올바른 정제에서 시작한다.**

---

# 🌱 앞으로의 목표

결측치, 이상치, 중복 데이터를 발견했을 때 바로 삭제부터 하는 것이 아니라 먼저 데이터의 의미와 분포를 확인하는 습관을 들여야겠다.

특히 다음 함수들은 코드를 보는 즉시 역할이 떠오르도록 반복해서 익힐 것이다.

```python
isna()
dropna()
fillna()

mean()
median()
mode()
quantile()

duplicated()
drop_duplicates()

clip()

len()
shape
```

그리고 IQR 공식은 계산 순서까지 바로 떠올릴 수 있도록 확실하게 암기한다.

```text
Q1 / Q3 구하기
↓
IQR = Q3 - Q1
↓
하한 = Q1 - 1.5 × IQR
상한 = Q3 + 1.5 × IQR
↓
범위 밖 데이터 = 이상치 후보
```
