# 📚 TIL - SQL 데이터 집계와 테이블 결합

오늘은 SQL에서 데이터를 집계하고, 그룹별로 분석하고, 여러 테이블을 결합하는 방법을 학습했다.

특히 오늘은 `GROUP BY`, `HAVING`, `UNION`, `JOIN`을 중심으로 공부했고, JOIN 실습을 하면서 테이블명 / 컬럼명 / 별칭(alias) / 연결키를 구분하는 연습을 했다.

---

# 1. 데이터 집계 함수

여러 행의 데이터를 하나의 값으로 요약할 때 집계 함수를 사용한다.

## COUNT()

행의 개수를 센다.

```sql
SELECT COUNT(*)
FROM orders;
```

### COUNT(*) vs COUNT(column)

```sql
COUNT(*)
```

→ 전체 행의 개수를 센다.

```sql
COUNT(column)
```

→ 해당 컬럼에서 `NULL`이 아닌 값만 센다.

⭐ 암기

```text
COUNT(*)      → 전체 행 개수
COUNT(column) → NULL 제외하고 개수
```

---

## SUM()

숫자 데이터를 모두 더한다.

```sql
SELECT SUM(quantity)
FROM order_items;
```

→ 전체 주문 수량의 합

---

## AVG()

평균을 구한다.

```sql
SELECT AVG(quantity)
FROM order_items;
```

→ 평균 주문 수량

---

## MAX()

가장 큰 값을 구한다.

```sql
SELECT MAX(list_price)
FROM products;
```

→ 가장 비싼 상품 가격

---

## MIN()

가장 작은 값을 구한다.

```sql
SELECT MIN(list_price)
FROM products;
```

→ 가장 저렴한 상품 가격

---

## ⭐ 집계 함수 암기

```text
COUNT → 개수
SUM   → 합계
AVG   → 평균
MAX   → 최댓값
MIN   → 최솟값
```

---

# 2. GROUP BY

`GROUP BY`는 같은 값을 가진 행들을 하나의 그룹으로 묶을 때 사용한다.

예를 들어 직원별 주문 수를 알고 싶다면:

```sql
SELECT
    staff_id,
    COUNT(*) AS order_count
FROM orders
GROUP BY staff_id;
```

이렇게 작성한다.

### 동작 방식

```text
staff_id가 같은 주문끼리 묶기
        ↓
각 그룹의 주문 개수 계산
```

---

## GROUP BY 사용 시 주의점

SELECT에 집계 함수가 아닌 컬럼이 있다면 일반적으로 `GROUP BY`에도 적어줘야 한다.

```sql
SELECT
    staff_id,
    COUNT(*)
FROM orders
GROUP BY staff_id;
```

⭐ 암기

```text
GROUP BY
→ 같은 값끼리 그룹 만들기
```

---

# 3. WHERE와 HAVING 차이

둘 다 조건을 걸 때 사용하지만 적용되는 시점이 다르다.

## WHERE

그룹을 만들기 전에 개별 행을 필터링한다.

```sql
SELECT *
FROM stocks
WHERE quantity > 0;
```

→ 재고가 0보다 큰 행만 먼저 선택

---

## HAVING

`GROUP BY`로 그룹을 만든 뒤 그룹 결과에 조건을 건다.

```sql
SELECT
    store_id,
    SUM(quantity) AS total_stock
FROM stocks
GROUP BY store_id
HAVING SUM(quantity) >= 100;
```

→ 매장별 재고 합계를 구한 후  
→ 합계가 100 이상인 매장만 조회

---

## WHERE + GROUP BY + HAVING

```sql
SELECT
    store_id,
    SUM(quantity) AS total_stock
FROM stocks
WHERE quantity > 0
GROUP BY store_id
HAVING SUM(quantity) >= 100;
```

동작 흐름:

```text
WHERE
→ 개별 행 필터링

GROUP BY
→ 그룹 생성

HAVING
→ 만들어진 그룹 필터링
```

⭐ 암기

```text
WHERE  → 행 조건
HAVING → 그룹 조건
```

---

# 4. SQL 실행 흐름

SQL은 작성하는 순서와 실제로 처리되는 흐름이 다르다.

작성 순서:

```text
SELECT
FROM
WHERE
GROUP BY
HAVING
ORDER BY
```

논리적인 처리 흐름:

```text
FROM
→ WHERE
→ GROUP BY
→ HAVING
→ SELECT
→ ORDER BY
```

⭐ 암기

```text
FROM → WHERE → GROUP BY → HAVING → SELECT → ORDER BY
```

---

# 5. UNION

`UNION`은 여러 SELECT 결과를 세로로 이어 붙인다.

예:

```sql
SELECT
    store_id,
    product_id,
    quantity
FROM stocks
WHERE store_id = 1

UNION

SELECT
    store_id,
    product_id,
    quantity
FROM stocks
WHERE store_id = 2;
```

결과:

```text
1번 매장 데이터
↓
2번 매장 데이터
```

---

# 6. UNION vs UNION ALL

## UNION

중복된 행을 제거한다.

```sql
SELECT ...
UNION
SELECT ...;
```

---

## UNION ALL

중복을 제거하지 않고 모두 가져온다.

```sql
SELECT ...
UNION ALL
SELECT ...;
```

중복 제거 작업을 하지 않기 때문에 일반적으로 `UNION ALL`이 더 빠를 수 있다.

⭐ 암기

```text
UNION
→ 중복 제거

UNION ALL
→ 중복 포함
```

---

# 7. UNION 사용 조건

UNION으로 합치는 SELECT문은 서로 구조가 비슷해야 한다.

예를 들어:

```sql
SELECT store_id, product_id, quantity
FROM stocks

UNION ALL

SELECT store_id, product_id, quantity
FROM stocks;
```

각 SELECT문의:

```text
컬럼 개수
컬럼 위치
데이터 타입
```

이 서로 호환되어야 한다.

---

# 8. JOIN

`JOIN`은 서로 관련된 여러 테이블을 가로로 연결할 때 사용한다.

⭐ 핵심

```text
UNION → 세로로 붙이기
JOIN  → 가로로 붙이기
```

---

# 9. 기본 JOIN 구조

```sql
SELECT
    o.order_id,
    c.first_name,
    c.last_name
FROM orders o
JOIN customers c
    ON o.customer_id = c.customer_id;
```

구조를 나누면:

```text
SELECT
→ 어떤 컬럼을 볼지

FROM
→ 기준 테이블

JOIN
→ 붙일 테이블

ON
→ 어떤 컬럼을 기준으로 연결할지
```

⭐ 오늘 가장 중요한 암기

```text
SELECT → 보고 싶은 컬럼
FROM   → 기준 테이블
JOIN   → 붙일 테이블
ON     → 두 테이블을 연결하는 컬럼
```

---

# 10. 테이블 별칭 Alias

테이블 이름이 길기 때문에 짧은 별명을 붙여 사용할 수 있다.

```sql
FROM orders o
```

이때:

```text
orders → 실제 테이블 이름
o      → 테이블 별칭
```

아래 두 코드는 같은 의미이다.

```sql
FROM orders o
```

```sql
FROM orders AS o
```

---

## 별칭은 내가 정할 수 있다

예:

```sql
FROM orders abc
```

그러면:

```sql
abc.order_id
```

처럼 사용할 수 있다.

하지만 실무에서는 알아보기 쉽게 사용하는 것이 좋다.

```text
orders      → o
customers   → c
products    → p
order_items → oi
stocks      → s
staffs      → s
```

⭐ 암기

```text
별칭은 변수 같은 임시 이름
한 번 정했으면 쿼리 안에서 동일하게 사용
```

---

# 11. 컬럼 앞의 별칭 구별 방법

예:

```sql
SELECT
    oi.order_id,
    p.product_name,
    oi.quantity
FROM order_items oi
JOIN products p
    ON oi.product_id = p.product_id;
```

여기서:

```text
oi = order_items
p  = products
```

컬럼 앞에 어떤 별칭을 붙일지는  
**그 컬럼이 원래 어느 테이블에 속해 있는지**를 보면 된다.

```text
order_id
→ order_items에 있음
→ oi.order_id

product_name
→ products에 있음
→ p.product_name

quantity
→ order_items에 있음
→ oi.quantity
```

⭐ 암기

```text
별칭을 보고 컬럼을 고르는 게 아니라
컬럼의 원래 테이블을 보고 별칭을 붙인다.
```

---

# 12. product_id가 헷갈렸던 이유

`product_id`는 여러 테이블에 같이 존재할 수 있다.

예:

```text
order_items.product_id
products.product_id
stocks.product_id
```

왜냐하면 테이블들을 연결하기 위한 ID이기 때문이다.

예:

```sql
ON oi.product_id = p.product_id
```

이 경우:

```sql
oi.product_id
```

와

```sql
p.product_id
```

는 JOIN된 결과에서는 같은 상품 ID를 의미한다.

⭐ 암기

```text
product_id
→ JOIN 연결키라 여러 테이블에 존재할 수 있음
```

---

# 13. quantity가 헷갈렸던 이유

`quantity`는 어느 테이블의 수량인지에 따라 의미가 달라진다.

## order_items의 quantity

```sql
oi.quantity
```

→ 해당 주문에서 상품을 몇 개 구매했는지

즉:

```text
주문 수량
```

---

## stocks의 quantity

```sql
s.quantity
```

→ 현재 재고로 몇 개 보유하고 있는지

즉:

```text
현재 재고 수량
```

---

## products

`products`는 상품 자체 정보를 가진 테이블이다.

주로:

```text
product_id
product_name
category
price
```

같은 정보가 들어간다.

⭐ 핵심 암기

```text
p = products
→ 상품 자체 정보

oi = order_items
→ 주문 상세

s = stocks
→ 재고
```

그리고:

```text
oi.quantity → 주문 수량
s.quantity  → 현재 재고 수량
```

---

# 14. 상품 테이블 / 주문상세 / 재고 구별

## products

```text
"이 상품이 뭐야?"
```

예:

```text
상품 ID
상품명
카테고리
가격
```

---

## order_items

```text
"이 주문에서 이 상품을 어떻게 샀어?"
```

예:

```text
주문 ID
상품 ID
주문 수량
```

---

## stocks

```text
"현재 이 상품이 몇 개 남아 있어?"
```

예:

```text
매장 ID
상품 ID
현재 재고 수량
```

⭐ 암기

```text
상품 정보 → p
주문 정보 → oi
재고 정보 → s
```

---

# 15. ON의 의미

ON은 두 테이블을 **무슨 값을 기준으로 연결할 것인지** 정한다.

예:

```sql
FROM orders o
JOIN staffs s
    ON o.staff_id = s.staff_id;
```

의미:

```text
orders의 staff_id
=
staffs의 staff_id

같은 직원 ID를 가진 행끼리 연결
```

---

## JOIN과 ON을 생각하는 방법

```text
JOIN → 누구를 붙일까?
ON   → 무엇을 기준으로 붙일까?
```

예:

```text
JOIN staffs
→ staff_id로 연결

JOIN products
→ product_id로 연결

JOIN customers
→ customer_id로 연결
```

단, 무조건 이름이 같은 ID를 사용하는 것이 아니라  
**실제로 두 테이블을 연결하는 관계 컬럼을 사용해야 한다.**

---

# 16. ON 조건 좌우 순서

아래 두 개는 같은 의미이다.

```sql
ON oi.product_id = p.product_id
```

```sql
ON p.product_id = oi.product_id
```

왜냐하면:

```text
A = B
B = A
```

가 같은 조건이기 때문이다.

⭐ 암기

```text
ON에서 = 좌우 순서는 중요하지 않음

중요한 건
"어떤 컬럼끼리 연결하느냐"
```

---

# 17. FROM / JOIN 뒤에는 테이블명

오늘 실습에서 가장 헷갈렸던 부분.

잘못 작성한 예:

```sql
FROM orders o
INNER JOIN staff_id n
```

여기서 `staff_id`는 테이블명이 아니라 **컬럼명**이다.

올바른 코드:

```sql
FROM orders o
INNER JOIN staffs s
    ON o.staff_id = s.staff_id;
```

⭐ 암기

```text
FROM 뒤 → 테이블 이름
JOIN 뒤 → 테이블 이름

. 뒤 → 컬럼 이름
```

예:

```text
orders  → 테이블
staffs  → 테이블

order_id   → 컬럼
staff_id   → 컬럼
first_name → 컬럼
last_name  → 컬럼
```

---

# 18. JOIN 실습 오류

문제:

주문번호, 주문일자, 담당 직원 이름과 성 조회

처음 작성:

```sql
SELECT
    o.order_id,
    o.order_date,
    s.first_name,
    s.last_name
FROM orders o
INNER JOIN staffs s
    ON o.order_id = s.order_id;
```

문제점:

`orders`와 `staffs`를 연결해야 하는데 `order_id`를 사용했다.

`staffs` 테이블에는 주문번호가 아니라 직원 ID가 있기 때문에:

```sql
ON o.staff_id = s.staff_id
```

로 연결해야 한다.

수정:

```sql
SELECT
    o.order_id,
    o.order_date,
    s.first_name,
    s.last_name
FROM orders o
INNER JOIN staffs s
    ON o.staff_id = s.staff_id;
```

⭐ 헷갈렸던 원인

```text
order_id
→ 주문 번호

staff_id
→ 주문과 직원을 연결하는 번호
```

---

# 19. JOIN 실습 문제 정답

## 주문번호 + 상품명 + 주문수량

```sql
SELECT
    oi.order_id,
    p.product_name,
    oi.quantity
FROM order_items oi
INNER JOIN products p
    ON oi.product_id = p.product_id;
```

### 해석

```text
oi.order_id
→ 주문번호

p.product_name
→ 상품명

oi.quantity
→ 주문수량

oi.product_id = p.product_id
→ 상품 ID를 기준으로 두 테이블 연결
```

---

# 20. INNER JOIN

`INNER JOIN`은 양쪽 테이블에 모두 존재하는 데이터만 가져온다.

```sql
FROM orders o
INNER JOIN customers c
    ON o.customer_id = c.customer_id;
```

`INNER`는 생략 가능하다.

```sql
JOIN customers c
```

와:

```sql
INNER JOIN customers c
```

는 같은 의미이다.

⭐ 암기

```text
JOIN = INNER JOIN
```

---

# 21. INNER JOIN에서 테이블 순서

예:

```sql
FROM stocks s
JOIN products p
    ON s.product_id = p.product_id
```

와:

```sql
FROM products p
JOIN stocks s
    ON p.product_id = s.product_id
```

INNER JOIN에서는 매칭되는 데이터만 가져오기 때문에  
일반적으로 결과 행은 동일하다.

단, SELECT 컬럼의 순서가 다르면 출력되는 컬럼 위치는 달라질 수 있다.

⭐ 암기

```text
INNER JOIN
→ 테이블 순서를 바꿔도 매칭되는 데이터는 보통 같음
```

---

# 22. LEFT JOIN

왼쪽 테이블의 데이터는 모두 가져온다.

오른쪽 테이블에서 일치하는 데이터가 없으면 `NULL`이 들어간다.

```sql
FROM products p
LEFT JOIN stocks s
    ON p.product_id = s.product_id;
```

의미:

```text
products 전체 유지
+
stocks에서 매칭되는 데이터 붙이기
```

⭐ 암기

```text
LEFT JOIN
→ 왼쪽 테이블이 기준
```

---

# 23. RIGHT JOIN

오른쪽 테이블의 데이터를 모두 가져온다.

```sql
FROM products p
RIGHT JOIN stocks s
    ON p.product_id = s.product_id;
```

⭐ 암기

```text
RIGHT JOIN
→ 오른쪽 테이블이 기준
```

---

# 24. FULL OUTER JOIN

양쪽 테이블의 모든 데이터를 가져온다.

```sql
FROM products p
FULL OUTER JOIN stocks s
    ON p.product_id = s.product_id;
```

또는:

```sql
FULL JOIN
```

으로 작성할 수 있다.

일치하지 않는 데이터는 반대쪽 컬럼에 `NULL`이 들어간다.

⭐ JOIN 종류 암기

```text
INNER JOIN
→ 양쪽 모두 있는 데이터

LEFT JOIN
→ 왼쪽 전체 + 오른쪽 매칭

RIGHT JOIN
→ 오른쪽 전체 + 왼쪽 매칭

FULL OUTER JOIN
→ 양쪽 전체
```

---

# 25. INNER JOIN vs LEFT JOIN 순서

INNER JOIN은 테이블 순서를 바꿔도 결과가 거의 같지만  
LEFT JOIN은 순서가 매우 중요하다.

예:

```sql
FROM products p
LEFT JOIN stocks s
```

→ products 기준

반대로:

```sql
FROM stocks s
LEFT JOIN products p
```

→ stocks 기준

따라서 결과가 달라질 수 있다.

⭐ 암기

```text
INNER JOIN
→ 순서 영향 적음

LEFT JOIN / RIGHT JOIN
→ 기준 테이블이 달라지므로 순서 중요
```

---

# 26. SELECT 컬럼 구분

예:

```sql
SELECT
    p.first_name,
    p.last_name
```

이렇게 작성하면 결과는 두 개의 컬럼으로 출력된다.

```text
first_name | last_name
```

`,`는 컬럼을 구분한다.

⭐ 암기

```text
,  → 컬럼을 따로 출력
```

---

# 27. 이름 합치기

두 컬럼을 하나의 문자열로 합치고 싶으면 `||`를 사용한다.

```sql
SELECT
    p.first_name || ' ' || p.last_name AS full_name
FROM customers p;
```

결과:

```text
full_name
---------
가영 김
```

⭐ 암기

```text
,  → 컬럼 따로
|| → 문자열 합치기
```

---

# 28. 별칭을 일부 컬럼에만 써도 되는가?

예:

```sql
SELECT
    p.first_name,
    last_name
FROM customers p;
```

다른 테이블에 같은 컬럼명이 없다면 실행될 수 있다.

하지만 JOIN에서는 여러 테이블에 같은 컬럼명이 있을 수 있기 때문에:

```sql
SELECT
    p.first_name,
    p.last_name
```

처럼 앞에 테이블 별칭을 붙이는 것이 안전하다.

⭐ 권장

```text
JOIN 사용 시
→ 컬럼 앞에 테이블 별칭 붙이기
```

---

# 29. SQL 대소문자

SQL 키워드는 일반적으로 대소문자를 구분하지 않는다.

아래는 같은 의미이다.

```sql
SELECT
FROM
JOIN
ON
```

```sql
select
from
join
on
```

따라서 DBeaver에서 자동으로:

```sql
ON
```

이:

```sql
on
```

으로 바뀌어도 문제가 없다.

⭐ 암기

```text
SELECT = select
FROM = from
JOIN = join
ON = on
```

대문자 사용은 가독성을 위한 스타일이다.

---

# 30. UNION vs JOIN 최종 비교

| 구분 | UNION | JOIN |
|---|---|---|
| 방향 | 세로 | 가로 |
| 목적 | 여러 SELECT 결과 합치기 | 여러 테이블 연결 |
| 기준 | 컬럼 구조 | 연결키 |
| 대표 문법 | UNION / UNION ALL | INNER / LEFT / RIGHT / FULL |

⭐ 암기

```text
UNION
→ 아래로 붙인다

JOIN
→ 옆으로 붙인다
```

---

# 31. 오늘 헷갈렸던 포인트 정리

## ① 테이블명과 컬럼명

```text
orders
staffs
products
order_items
stocks
customers

→ 테이블
```

```text
order_id
staff_id
product_id
quantity
product_name
first_name
last_name

→ 컬럼
```

---

## ② FROM / JOIN / ON

```text
FROM
→ 기준 테이블

JOIN
→ 붙일 테이블

ON
→ 연결할 컬럼
```

---

## ③ 별칭

```text
o  → orders
oi → order_items
p  → products
s  → stocks 또는 staffs
c  → customers
```

별칭 자체는 내가 정할 수 있다.

단, 한 번 정했으면 그 쿼리 안에서 일관되게 사용한다.

---

## ④ quantity

```text
oi.quantity
→ 주문 수량

s.quantity
→ 현재 재고 수량
```

---

## ⑤ product_id

```text
product_id
→ 여러 테이블을 연결하는 키라
여러 테이블에 존재할 수 있음
```

---

## ⑥ JOIN 조건

```text
JOIN staffs
→ staff_id로 연결

JOIN products
→ product_id로 연결

JOIN customers
→ customer_id로 연결
```

단, 실제 테이블 관계를 확인하고 연결해야 한다.

---

# 🌟 오늘 최종 정리

오늘은 SQL의 집계와 테이블 결합을 학습했다.

집계 함수는 여러 행의 데이터를 하나의 값으로 요약할 때 사용하며:

```text
COUNT → 개수
SUM   → 합계
AVG   → 평균
MAX   → 최댓값
MIN   → 최솟값
```

으로 정리할 수 있다.

`GROUP BY`는 같은 값을 가진 데이터를 그룹으로 묶으며, `WHERE`는 그룹 이전의 개별 행을 필터링하고 `HAVING`은 그룹 결과에 조건을 건다.

```text
WHERE
→ 행 필터링

GROUP BY
→ 그룹 생성

HAVING
→ 그룹 필터링
```

여러 데이터를 합치는 방법에서는:

```text
UNION → 세로 결합
JOIN  → 가로 결합
```

이라는 차이가 있다.

JOIN에서는:

```text
SELECT → 보고 싶은 컬럼
FROM   → 기준 테이블
JOIN   → 붙일 테이블
ON     → 두 테이블의 연결키
```

구조를 기억하는 것이 중요하다.

특히 오늘 실습에서는 테이블명과 컬럼명을 혼동했으며, `JOIN staff_id`처럼 컬럼명을 테이블 위치에 작성하는 오류가 있었다.

이를 통해:

```text
FROM / JOIN 뒤 → 테이블명
점(.) 뒤       → 컬럼명
```

이라는 기준을 익혔다.

또한 테이블 별칭은 변수처럼 사용할 수 있지만, 컬럼 앞의 별칭은 그 컬럼이 실제로 어느 테이블에 속해 있는지 확인해서 작성해야 한다.

```text
p = products       → 상품 정보
oi = order_items   → 주문 상세
s = stocks         → 재고
```

그리고 가장 헷갈렸던 `quantity`는:

```text
oi.quantity → 주문 수량
s.quantity  → 현재 재고 수량
```

으로 구분한다.

---

# 💡 오늘 느낀 점

JOIN 문법 자체보다 어떤 테이블에 어떤 컬럼이 들어 있는지 구분하는 부분에서 많이 헷갈렸다.

특히 `product_id`, `quantity`, `staff_id`처럼 여러 테이블의 관계를 연결하거나 비슷한 이름을 가진 컬럼을 볼 때 앞에 어떤 별칭을 붙여야 하는지 혼동했다.

하지만 문제를 풀면서 단순히 별칭을 외우는 것이 아니라:

```text
이 컬럼이 원래 어느 테이블에 있는가?
```

를 먼저 생각하면 된다는 것을 이해했다.

또한 JOIN에서 `ON`은 단순히 아무 ID를 적는 것이 아니라 두 테이블을 실제로 연결하는 공통 키를 적는 부분이라는 것도 확실히 알게 되었다.

---

# 📝 오늘의 한 줄

JOIN은 **테이블을 붙이고, ON은 그 테이블들을 연결할 공통키를 정한다.**

---

# 🌱 앞으로의 목표

JOIN 문제를 볼 때 바로 코드를 작성하기보다 먼저:

```text
1. 어떤 테이블이 필요한지 확인
2. 각 컬럼이 어느 테이블에 있는지 확인
3. 기준 테이블을 FROM에 작성
4. 붙일 테이블을 JOIN에 작성
5. 두 테이블의 공통 연결키를 ON에 작성
6. SELECT에서 필요한 컬럼 앞에 정확한 별칭 붙이기
```

순서로 생각하는 습관을 들인다.

특히 아래는 계속 반복해서 암기한다.

```text
p  = products      → 상품 정보
oi = order_items   → 주문 상세
s  = stocks        → 재고

oi.quantity → 주문 수량
s.quantity  → 현재 재고 수량

FROM / JOIN → 테이블명
ON          → 연결키
. 뒤        → 컬럼명
```

---
# 📚 TIL - SQL 데이터 집계와 분석 실습 암기 정리
---

오늘은 `COUNT`, `SUM`, `AVG`, `MIN`, `MAX` 같은 집계함수와 `GROUP BY`, `WHERE`, `HAVING`, `ORDER BY`를 이용해 데이터를 요약하고 그룹별로 분석하는 실습을 진행했다.

---

# 1. COUNT(*)와 COUNT(컬럼명)

## COUNT(*)

전체 행의 개수를 센다.

```sql
SELECT COUNT(*)
FROM orders;
```

⭐ 암기

```text
COUNT(*)
→ 전체 행 개수
→ NULL이 있어도 행 자체를 셈
```

---

## COUNT(컬럼명)

해당 컬럼에서 `NULL`이 아닌 값만 센다.

```sql
SELECT COUNT(order_delivered_customer_date)
FROM orders;
```

⭐ 암기

```text
COUNT(컬럼명)
→ NULL 제외한 값 개수
```

---

## COUNT(*) vs COUNT(컬럼명)

예:

```sql
SELECT
    COUNT(*) AS total_order_count,
    COUNT(order_delivered_customer_date) AS delivered_date_count
FROM orders;
```

해석:

```text
COUNT(*)
→ 전체 주문 수

COUNT(order_delivered_customer_date)
→ 배송 완료 날짜가 있는 주문 수
```

둘의 값이 다를 수 있는 이유:

```text
order_delivered_customer_date에 NULL이 있는 주문이 있기 때문
```

⭐ 핵심 암기

```text
COUNT(*)      → 전체 행
COUNT(컬럼명) → NULL 제외
```

---

# 2. 집계함수 기본

집계함수는 여러 행의 데이터를 하나의 값으로 요약할 때 사용한다.

## SUM()

합계를 구한다.

```sql
SELECT SUM(price)
FROM order_items;
```

→ 전체 가격 합계

---

## AVG()

평균을 구한다.

```sql
SELECT AVG(price)
FROM order_items;
```

→ 평균 가격

---

## MIN()

가장 작은 값을 구한다.

```sql
SELECT MIN(price)
FROM order_items;
```

→ 최소 가격

---

## MAX()

가장 큰 값을 구한다.

```sql
SELECT MAX(price)
FROM order_items;
```

→ 최대 가격

---

## ⭐ 집계함수 암기

```text
COUNT → 개수
SUM   → 합계
AVG   → 평균
MIN   → 최솟값
MAX   → 최댓값
```

---

# 3. 여러 집계값을 한 번에 조회

## order_items.price 요약

```sql
SELECT
    COUNT(*) AS item_count,
    SUM(price) AS total_price,
    AVG(price) AS avg_price,
    MIN(price) AS min_price,
    MAX(price) AS max_price
FROM order_items;
```

결과 의미:

```text
item_count  → 주문상품 행 수
total_price → 가격 합계
avg_price   → 평균 가격
min_price   → 최소 가격
max_price   → 최대 가격
```

---

## 배송비 집계

```sql
SELECT
    SUM(freight_value) AS total_freight,
    AVG(freight_value) AS avg_freight,
    MIN(freight_value) AS min_freight,
    MAX(freight_value) AS max_freight
FROM order_items;
```

---

## 결제금액 집계

```sql
SELECT
    SUM(payment_value) AS total_payment,
    AVG(payment_value) AS avg_payment,
    MIN(payment_value) AS min_payment,
    MAX(payment_value) AS max_payment
FROM order_payments;
```

---

# 4. GROUP BY

`GROUP BY`는 같은 값을 가진 행들을 하나의 그룹으로 묶을 때 사용한다.

예:

```sql
SELECT
    order_status,
    COUNT(*) AS order_count
FROM orders
GROUP BY order_status;
```

의미:

```text
order_status가 같은 주문끼리 묶은 뒤
각 상태별 주문 건수를 계산
```

---

## GROUP BY가 필요한 상황

문제에 이런 표현이 나오면 `GROUP BY`를 떠올린다.

```text
~별로
~별
각 ~마다
그룹별
```

예:

```text
주문 상태별
결제 방식별
판매자별
리뷰 점수별
```

⭐ 암기

```text
"~별로" 나오면 GROUP BY 생각
```

---

# 5. 주문 상태별 주문 건수

```sql
SELECT
    order_status,
    COUNT(*) AS order_count
FROM orders
GROUP BY order_status
ORDER BY order_count DESC;
```

해석:

```text
order_status별로 주문 건수를 집계하고
주문 건수가 많은 상태부터 정렬
```

⭐ 암기

```text
GROUP BY order_status
→ 주문 상태별로 묶기

COUNT(*)
→ 각 그룹의 주문 개수
```

---

# 6. 결제 방식별 집계

```sql
SELECT
    payment_type,
    COUNT(*) AS payment_count,
    SUM(payment_value) AS total_payment,
    AVG(payment_value) AS avg_payment
FROM order_payments
GROUP BY payment_type
ORDER BY total_payment DESC;
```

해석:

```text
payment_type별로

결제 건수
총 결제 금액
평균 결제 금액

을 계산
```

⭐ 암기

```text
payment_type별
→ GROUP BY payment_type
```

---

# 7. 리뷰 점수별 리뷰 건수

```sql
SELECT
    review_score,
    COUNT(*) AS review_count
FROM order_reviews
GROUP BY review_score
ORDER BY review_score ASC;
```

해석:

```text
review_score별 리뷰 개수 확인
낮은 점수부터 정렬
```

---

# 8. seller_id별 가격 집계

```sql
SELECT
    seller_id,
    COUNT(*) AS item_count,
    AVG(price) AS avg_price,
    MIN(price) AS min_price,
    MAX(price) AS max_price
FROM order_items
GROUP BY seller_id;
```

해석:

```text
seller_id별로

주문상품 건수
평균 가격
최소 가격
최대 가격

을 구함
```

⭐ 암기

```text
seller_id별
→ GROUP BY seller_id
```

---

# 9. WHERE와 HAVING 차이

둘 다 조건을 걸 때 사용하지만 적용되는 대상이 다르다.

## WHERE

개별 행을 먼저 필터링한다.

```sql
WHERE freight_value >= 10
```

→ 배송비가 10 이상인 행만 남김

---

## HAVING

`GROUP BY`로 그룹을 만든 뒤 그룹 결과를 필터링한다.

```sql
HAVING AVG(freight_value) >= 20
```

→ 평균 배송비가 20 이상인 그룹만 남김

---

## ⭐ 핵심 차이

```text
WHERE
→ 개별 행 조건
→ GROUP BY 전에 적용

HAVING
→ 그룹/집계 결과 조건
→ GROUP BY 후에 적용
```

---

# 10. HAVING 기본

리뷰 점수별 리뷰 수를 구한 뒤 리뷰가 10,000건 이상인 그룹만 조회:

```sql
SELECT
    review_score,
    COUNT(*) AS review_count
FROM order_reviews
GROUP BY review_score
HAVING COUNT(*) >= 10000
ORDER BY review_count DESC;
```

해석:

```text
1. review_score별로 그룹 생성
2. 각 그룹의 리뷰 수 계산
3. 리뷰 수가 10,000 이상인 그룹만 남김
```

⭐ 암기

```text
집계 결과에 조건
→ HAVING
```

---

# 11. 평균값에 HAVING 사용

```sql
SELECT
    payment_type,
    AVG(payment_value) AS avg_payment
FROM order_payments
GROUP BY payment_type
HAVING AVG(payment_value) >= 100;
```

의미:

```text
결제 방식별 평균 결제 금액을 계산한 뒤
평균이 100 이상인 결제 방식만 남김
```

---

# 12. WHERE → GROUP BY → HAVING 흐름

2018-01-01 이후 주문만 대상으로:

```sql
SELECT
    order_status,
    COUNT(*) AS order_count
FROM orders
WHERE order_purchase_timestamp >= '2018-01-01'
GROUP BY order_status
HAVING COUNT(*) >= 100
ORDER BY order_count DESC;
```

처리 흐름:

```text
WHERE
→ 2018년 이후 주문만 남김

GROUP BY
→ order_status별로 묶음

HAVING
→ 주문 건수가 100 이상인 그룹만 남김

ORDER BY
→ 주문 건수가 많은 순으로 정렬
```

⭐ 암기

```text
WHERE → GROUP BY → HAVING
```

---

# 13. WHERE COUNT(*)를 쓰면 안 되는 이유

잘못된 예:

```sql
WHERE COUNT(*) >= 100
```

이렇게 쓰면 안 된다.

이유:

```text
WHERE는 그룹을 만들기 전에 실행됨

COUNT(*)는 GROUP BY 이후
그룹이 만들어진 뒤 계산되는 집계값
```

따라서:

```sql
HAVING COUNT(*) >= 100
```

으로 작성해야 한다.

⭐ 암기

```text
집계함수 조건
→ WHERE X
→ HAVING O
```

---

# 14. HAVING에 여러 조건 사용

결제 방식별로:

- 결제 건수 1,000건 이상
- 평균 결제 금액 100 이상

두 조건을 모두 만족시키려면:

```sql
SELECT
    payment_type,
    COUNT(*) AS payment_count,
    SUM(payment_value) AS total_payment,
    AVG(payment_value) AS avg_payment
FROM order_payments
GROUP BY payment_type
HAVING COUNT(*) >= 1000
   AND AVG(payment_value) >= 100
ORDER BY total_payment DESC;
```

⭐ 암기

```text
HAVING에서도 AND / OR 사용 가능
```

---

# 15. WHERE + HAVING 같이 쓰기

배송비가 10 이상인 주문상품만 대상으로:

- seller_id별 평균 배송비 계산
- 평균 배송비 20 이상인 판매자만 조회

```sql
SELECT
    seller_id,
    AVG(freight_value) AS avg_freight
FROM order_items
WHERE freight_value >= 10
GROUP BY seller_id
HAVING AVG(freight_value) >= 20
ORDER BY avg_freight DESC;
```

해석:

```text
WHERE freight_value >= 10
→ 개별 주문상품 중 배송비 10 이상만

GROUP BY seller_id
→ 판매자별 그룹 생성

HAVING AVG(freight_value) >= 20
→ 평균 배송비 20 이상인 판매자만
```

⭐ 매우 중요

```text
WHERE
→ 원본 데이터에서 먼저 거르기

GROUP BY
→ 그룹 만들기

HAVING
→ 만들어진 그룹 결과 거르기
```

---

# 16. ORDER BY와 집계 결과

집계한 결과도 `ORDER BY`로 정렬할 수 있다.

예:

```sql
ORDER BY total_payment DESC
```

→ 총 결제 금액이 큰 그룹부터

```sql
ORDER BY order_count DESC
```

→ 주문 건수가 많은 그룹부터

```sql
ORDER BY review_score ASC
```

→ 리뷰 점수가 낮은 순서부터

⭐ 암기

```text
ASC  → 오름차순
DESC → 내림차순
```

---

# 17. 별칭 AS

집계 결과는 별칭을 붙이면 보기 편하다.

```sql
COUNT(*) AS order_count
```

```sql
AVG(price) AS avg_price
```

```sql
SUM(payment_value) AS total_payment
```

⭐ 암기

```text
AS
→ 결과 컬럼에 새 이름 붙이기
```

---

# 18. 집계함수가 아닌 컬럼과 GROUP BY

예:

```sql
SELECT
    payment_type,
    AVG(payment_value)
FROM order_payments
GROUP BY payment_type;
```

여기서:

```text
payment_type
→ 집계함수 아님

AVG(payment_value)
→ 집계함수
```

집계함수가 아닌 컬럼인 `payment_type`은:

```sql
GROUP BY payment_type
```

에 작성해야 한다.

⭐ 암기

```text
SELECT에 집계함수가 아닌 컬럼이 있으면
그 컬럼은 보통 GROUP BY에 들어간다.
```

---

# 19. GROUP BY가 필요한 이유

예:

```sql
SELECT
    seller_id,
    AVG(price)
FROM order_items
GROUP BY seller_id;
```

의미:

```text
seller_id마다 평균 가격을 따로 계산
```

GROUP BY가 없다면:

```text
전체 order_items의 평균만 계산
```

⭐ 쉽게 기억

```text
전체 평균
→ GROUP BY 없음

판매자별 평균
→ GROUP BY seller_id
```

---

# 20. SQL 집계 문제 해석 순서

집계 문제를 보면 바로 코드부터 쓰지 말고 아래 순서로 생각한다.

```text
1. 어떤 테이블을 볼지
2. 무엇을 기준으로 묶을지
3. 어떤 값을 집계할지
4. 개별 행 조건이 있는지
5. 그룹 결과 조건이 있는지
6. 어떤 순서로 정렬할지
```

예:

```text
배송비 10 이상인 주문상품 중
seller_id별 평균 배송비를 구하고
평균 배송비가 20 이상인 판매자만
평균 배송비가 높은 순으로 조회
```

생각 순서:

```text
테이블
→ order_items

개별 행 조건
→ freight_value >= 10

그룹 기준
→ seller_id

집계값
→ AVG(freight_value)

그룹 조건
→ AVG(freight_value) >= 20

정렬
→ avg_freight DESC
```

결과:

```sql
SELECT
    seller_id,
    AVG(freight_value) AS avg_freight
FROM order_items
WHERE freight_value >= 10
GROUP BY seller_id
HAVING AVG(freight_value) >= 20
ORDER BY avg_freight DESC;
```

---

# 21. 문제 문장에서 SQL 찾는 방법

문제 문장을 보고 키워드를 연결한다.

```text
전체 몇 건
→ COUNT(*)

합계
→ SUM()

평균
→ AVG()

최소
→ MIN()

최대
→ MAX()

~별로
→ GROUP BY

~이상인 원본 데이터
→ WHERE

집계 결과가 ~이상인 그룹
→ HAVING

큰 순서
→ ORDER BY ... DESC

작은 순서
→ ORDER BY ... ASC
```

---

# 22. 실습 중 발생한 데이터 타입 오류

문제:

```sql
AND order_status IN ('canceled', 'unavailable')
```

실행 시:

```text
integer 자료형에 대한 잘못된 입력
'canceled'
```

오류 발생.

확인:

```sql
SELECT
    column_name,
    data_type
FROM information_schema.columns
WHERE table_name = 'orders'
  AND column_name = 'order_status';
```

결과:

```text
order_status → integer
```

원인은 `order_status`가 문자형이 아니라 `integer`로 잘못 설정되어 있었기 때문.

해결:

```sql
ALTER TABLE orders
ALTER COLUMN order_status TYPE VARCHAR(50);
```

이후 문자열 조건이 정상 조회됨.

⭐ 오류 암기

```text
문자열 비교인데 integer 오류
→ 컬럼 자료형 먼저 확인
```

---

# 23. DBeaver 연결 오류

실습 중:

```text
Cannot obtain session
No active connection
```

오류 발생.

이 오류는 SQL 문법 문제가 아니라 DB 연결 문제이다.

해결:

```text
postgres 연결 상태 확인
→ Auto 또는 다시 Connect
→ 필요하면 새 SQL Script 열기
```

⭐ 오류 암기

```text
No active connection
→ SQL 오류 X
→ DB 연결 오류
```

---

# 24. 오늘 핵심 암기 세트

```text
COUNT(*)      → 전체 행 개수
COUNT(column) → NULL 제외 개수

SUM → 합계
AVG → 평균
MIN → 최솟값
MAX → 최댓값
```

```text
GROUP BY
→ 같은 값끼리 묶기
→ "~별로" 나오면 생각
```

```text
WHERE
→ 개별 행 필터링
→ GROUP BY 전에 적용
```

```text
HAVING
→ 그룹/집계 결과 필터링
→ GROUP BY 후에 적용
```

```text
WHERE → GROUP BY → HAVING
```

```text
ASC  → 오름차순
DESC → 내림차순
```

```text
집계함수 조건
→ WHERE X
→ HAVING O
```

```text
SELECT에 집계함수가 아닌 컬럼이 있으면
→ GROUP BY에 넣는지 확인
```

---

# 🌟 오늘 최종 정리

오늘 실습의 핵심은 데이터를 단순히 조회하는 것을 넘어서 여러 행을 요약하고 그룹별로 분석하는 것이다.

집계함수는 다음과 같이 구분한다.

```text
COUNT → 개수
SUM   → 합계
AVG   → 평균
MIN   → 최솟값
MAX   → 최댓값
```

특정 기준별로 집계하려면 `GROUP BY`를 사용한다.

```text
판매자별
결제 방식별
주문 상태별
리뷰 점수별

→ GROUP BY
```

조건을 걸 때는 대상에 따라 `WHERE`와 `HAVING`을 구분해야 한다.

```text
WHERE
→ 개별 행

HAVING
→ 그룹/집계 결과
```

가장 중요한 실행 흐름은:

```text
WHERE
→ GROUP BY
→ HAVING
```

이다.

문제 문장에서:

```text
"~별로"
→ GROUP BY

"원본 데이터 중 ~인 것"
→ WHERE

"집계한 결과가 ~인 그룹"
→ HAVING
```

으로 해석하면 된다.

---

# 💡 오늘 느낀 점

집계함수 자체는 어렵지 않았지만 `WHERE`와 `HAVING`을 어느 상황에서 사용해야 하는지가 가장 중요한 부분이었다.

특히 문제를 풀 때 단순히 `100 이상`이라는 조건만 보고 WHERE를 쓰는 것이 아니라, 그 값이 원본 행의 값인지 집계 결과인지 먼저 구분해야 한다는 것을 알게 되었다.

또한 `GROUP BY`는 단순한 문법이 아니라 문제에서 "~별로"라는 표현을 SQL로 바꾸는 핵심이라는 것을 이해했다.

---

# 📝 오늘의 한 줄

`WHERE`는 묶기 전에 거르고, `GROUP BY`로 묶고, `HAVING`은 묶은 뒤 거른다.

---

# 🌱 앞으로의 목표

집계 문제를 볼 때 아래 순서로 생각하는 습관을 들인다.

```text
1. 테이블 확인
2. "~별로" 기준 확인
3. 집계함수 확인
4. WHERE 조건 확인
5. GROUP BY 작성
6. HAVING 조건 확인
7. ORDER BY 정렬 확인
```

특히 아래 세 줄은 계속 반복해서 암기한다.

```text
WHERE → 행
GROUP BY → 그룹
HAVING → 그룹 조건
```
