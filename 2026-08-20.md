# 📚 TIL - SQL 데이터 집계와 테이블 결합

오늘은 SQL에서 데이터를 집계하고, 그룹별로 분석하고, 여러 테이블을 결합하는 방법을 학습했다.

특히 오늘은 `GROUP BY`, `HAVING`, `UNION`, `JOIN`을 중심으로 공부했고, JOIN 실습을 하면서 테이블명 / 컬럼명 / 별칭(alias) / 연결키를 구분하는 연습을 했다.

---

# 1. 데이터 집계 함수

여러 행의 데이터를 하나의 값으로 요약할 때 집계 함수를 사용한다.

## COUNT()

행의 개수를 센다.

```sql
SELECT COUNT(*)
FROM orders;
```

### COUNT(*) vs COUNT(column)

```sql
COUNT(*)
```

→ 전체 행의 개수를 센다.

```sql
COUNT(column)
```

→ 해당 컬럼에서 `NULL`이 아닌 값만 센다.

⭐ 암기

```text
COUNT(*)      → 전체 행 개수
COUNT(column) → NULL 제외하고 개수
```

---

## SUM()

숫자 데이터를 모두 더한다.

```sql
SELECT SUM(quantity)
FROM order_items;
```

→ 전체 주문 수량의 합

---

## AVG()

평균을 구한다.

```sql
SELECT AVG(quantity)
FROM order_items;
```

→ 평균 주문 수량

---

## MAX()

가장 큰 값을 구한다.

```sql
SELECT MAX(list_price)
FROM products;
```

→ 가장 비싼 상품 가격

---

## MIN()

가장 작은 값을 구한다.

```sql
SELECT MIN(list_price)
FROM products;
```

→ 가장 저렴한 상품 가격

---

## ⭐ 집계 함수 암기

```text
COUNT → 개수
SUM   → 합계
AVG   → 평균
MAX   → 최댓값
MIN   → 최솟값
```

---

# 2. GROUP BY

`GROUP BY`는 같은 값을 가진 행들을 하나의 그룹으로 묶을 때 사용한다.

예를 들어 직원별 주문 수를 알고 싶다면:

```sql
SELECT
    staff_id,
    COUNT(*) AS order_count
FROM orders
GROUP BY staff_id;
```

이렇게 작성한다.

### 동작 방식

```text
staff_id가 같은 주문끼리 묶기
        ↓
각 그룹의 주문 개수 계산
```

---

## GROUP BY 사용 시 주의점

SELECT에 집계 함수가 아닌 컬럼이 있다면 일반적으로 `GROUP BY`에도 적어줘야 한다.

```sql
SELECT
    staff_id,
    COUNT(*)
FROM orders
GROUP BY staff_id;
```

⭐ 암기

```text
GROUP BY
→ 같은 값끼리 그룹 만들기
```

---

# 3. WHERE와 HAVING 차이

둘 다 조건을 걸 때 사용하지만 적용되는 시점이 다르다.

## WHERE

그룹을 만들기 전에 개별 행을 필터링한다.

```sql
SELECT *
FROM stocks
WHERE quantity > 0;
```

→ 재고가 0보다 큰 행만 먼저 선택

---

## HAVING

`GROUP BY`로 그룹을 만든 뒤 그룹 결과에 조건을 건다.

```sql
SELECT
    store_id,
    SUM(quantity) AS total_stock
FROM stocks
GROUP BY store_id
HAVING SUM(quantity) >= 100;
```

→ 매장별 재고 합계를 구한 후  
→ 합계가 100 이상인 매장만 조회

---

## WHERE + GROUP BY + HAVING

```sql
SELECT
    store_id,
    SUM(quantity) AS total_stock
FROM stocks
WHERE quantity > 0
GROUP BY store_id
HAVING SUM(quantity) >= 100;
```

동작 흐름:

```text
WHERE
→ 개별 행 필터링

GROUP BY
→ 그룹 생성

HAVING
→ 만들어진 그룹 필터링
```

⭐ 암기

```text
WHERE  → 행 조건
HAVING → 그룹 조건
```

---

# 4. SQL 실행 흐름

SQL은 작성하는 순서와 실제로 처리되는 흐름이 다르다.

작성 순서:

```text
SELECT
FROM
WHERE
GROUP BY
HAVING
ORDER BY
```

논리적인 처리 흐름:

```text
FROM
→ WHERE
→ GROUP BY
→ HAVING
→ SELECT
→ ORDER BY
```

⭐ 암기

```text
FROM → WHERE → GROUP BY → HAVING → SELECT → ORDER BY
```

---

# 5. UNION

`UNION`은 여러 SELECT 결과를 세로로 이어 붙인다.

예:

```sql
SELECT
    store_id,
    product_id,
    quantity
FROM stocks
WHERE store_id = 1

UNION

SELECT
    store_id,
    product_id,
    quantity
FROM stocks
WHERE store_id = 2;
```

결과:

```text
1번 매장 데이터
↓
2번 매장 데이터
```

---

# 6. UNION vs UNION ALL

## UNION

중복된 행을 제거한다.

```sql
SELECT ...
UNION
SELECT ...;
```

---

## UNION ALL

중복을 제거하지 않고 모두 가져온다.

```sql
SELECT ...
UNION ALL
SELECT ...;
```

중복 제거 작업을 하지 않기 때문에 일반적으로 `UNION ALL`이 더 빠를 수 있다.

⭐ 암기

```text
UNION
→ 중복 제거

UNION ALL
→ 중복 포함
```

---

# 7. UNION 사용 조건

UNION으로 합치는 SELECT문은 서로 구조가 비슷해야 한다.

예를 들어:

```sql
SELECT store_id, product_id, quantity
FROM stocks

UNION ALL

SELECT store_id, product_id, quantity
FROM stocks;
```

각 SELECT문의:

```text
컬럼 개수
컬럼 위치
데이터 타입
```

이 서로 호환되어야 한다.

---

# 8. JOIN

`JOIN`은 서로 관련된 여러 테이블을 가로로 연결할 때 사용한다.

⭐ 핵심

```text
UNION → 세로로 붙이기
JOIN  → 가로로 붙이기
```

---

# 9. 기본 JOIN 구조

```sql
SELECT
    o.order_id,
    c.first_name,
    c.last_name
FROM orders o
JOIN customers c
    ON o.customer_id = c.customer_id;
```

구조를 나누면:

```text
SELECT
→ 어떤 컬럼을 볼지

FROM
→ 기준 테이블

JOIN
→ 붙일 테이블

ON
→ 어떤 컬럼을 기준으로 연결할지
```

⭐ 오늘 가장 중요한 암기

```text
SELECT → 보고 싶은 컬럼
FROM   → 기준 테이블
JOIN   → 붙일 테이블
ON     → 두 테이블을 연결하는 컬럼
```

---

# 10. 테이블 별칭 Alias

테이블 이름이 길기 때문에 짧은 별명을 붙여 사용할 수 있다.

```sql
FROM orders o
```

이때:

```text
orders → 실제 테이블 이름
o      → 테이블 별칭
```

아래 두 코드는 같은 의미이다.

```sql
FROM orders o
```

```sql
FROM orders AS o
```

---

## 별칭은 내가 정할 수 있다

예:

```sql
FROM orders abc
```

그러면:

```sql
abc.order_id
```

처럼 사용할 수 있다.

하지만 실무에서는 알아보기 쉽게 사용하는 것이 좋다.

```text
orders      → o
customers   → c
products    → p
order_items → oi
stocks      → s
staffs      → s
```

⭐ 암기

```text
별칭은 변수 같은 임시 이름
한 번 정했으면 쿼리 안에서 동일하게 사용
```

---

# 11. 컬럼 앞의 별칭 구별 방법

예:

```sql
SELECT
    oi.order_id,
    p.product_name,
    oi.quantity
FROM order_items oi
JOIN products p
    ON oi.product_id = p.product_id;
```

여기서:

```text
oi = order_items
p  = products
```

컬럼 앞에 어떤 별칭을 붙일지는  
**그 컬럼이 원래 어느 테이블에 속해 있는지**를 보면 된다.

```text
order_id
→ order_items에 있음
→ oi.order_id

product_name
→ products에 있음
→ p.product_name

quantity
→ order_items에 있음
→ oi.quantity
```

⭐ 암기

```text
별칭을 보고 컬럼을 고르는 게 아니라
컬럼의 원래 테이블을 보고 별칭을 붙인다.
```

---

# 12. product_id가 헷갈렸던 이유

`product_id`는 여러 테이블에 같이 존재할 수 있다.

예:

```text
order_items.product_id
products.product_id
stocks.product_id
```

왜냐하면 테이블들을 연결하기 위한 ID이기 때문이다.

예:

```sql
ON oi.product_id = p.product_id
```

이 경우:

```sql
oi.product_id
```

와

```sql
p.product_id
```

는 JOIN된 결과에서는 같은 상품 ID를 의미한다.

⭐ 암기

```text
product_id
→ JOIN 연결키라 여러 테이블에 존재할 수 있음
```

---

# 13. quantity가 헷갈렸던 이유

`quantity`는 어느 테이블의 수량인지에 따라 의미가 달라진다.

## order_items의 quantity

```sql
oi.quantity
```

→ 해당 주문에서 상품을 몇 개 구매했는지

즉:

```text
주문 수량
```

---

## stocks의 quantity

```sql
s.quantity
```

→ 현재 재고로 몇 개 보유하고 있는지

즉:

```text
현재 재고 수량
```

---

## products

`products`는 상품 자체 정보를 가진 테이블이다.

주로:

```text
product_id
product_name
category
price
```

같은 정보가 들어간다.

⭐ 핵심 암기

```text
p = products
→ 상품 자체 정보

oi = order_items
→ 주문 상세

s = stocks
→ 재고
```

그리고:

```text
oi.quantity → 주문 수량
s.quantity  → 현재 재고 수량
```

---

# 14. 상품 테이블 / 주문상세 / 재고 구별

## products

```text
"이 상품이 뭐야?"
```

예:

```text
상품 ID
상품명
카테고리
가격
```

---

## order_items

```text
"이 주문에서 이 상품을 어떻게 샀어?"
```

예:

```text
주문 ID
상품 ID
주문 수량
```

---

## stocks

```text
"현재 이 상품이 몇 개 남아 있어?"
```

예:

```text
매장 ID
상품 ID
현재 재고 수량
```

⭐ 암기

```text
상품 정보 → p
주문 정보 → oi
재고 정보 → s
```

---

# 15. ON의 의미

ON은 두 테이블을 **무슨 값을 기준으로 연결할 것인지** 정한다.

예:

```sql
FROM orders o
JOIN staffs s
    ON o.staff_id = s.staff_id;
```

의미:

```text
orders의 staff_id
=
staffs의 staff_id

같은 직원 ID를 가진 행끼리 연결
```

---

## JOIN과 ON을 생각하는 방법

```text
JOIN → 누구를 붙일까?
ON   → 무엇을 기준으로 붙일까?
```

예:

```text
JOIN staffs
→ staff_id로 연결

JOIN products
→ product_id로 연결

JOIN customers
→ customer_id로 연결
```

단, 무조건 이름이 같은 ID를 사용하는 것이 아니라  
**실제로 두 테이블을 연결하는 관계 컬럼을 사용해야 한다.**

---

# 16. ON 조건 좌우 순서

아래 두 개는 같은 의미이다.

```sql
ON oi.product_id = p.product_id
```

```sql
ON p.product_id = oi.product_id
```

왜냐하면:

```text
A = B
B = A
```

가 같은 조건이기 때문이다.

⭐ 암기

```text
ON에서 = 좌우 순서는 중요하지 않음

중요한 건
"어떤 컬럼끼리 연결하느냐"
```

---

# 17. FROM / JOIN 뒤에는 테이블명

오늘 실습에서 가장 헷갈렸던 부분.

잘못 작성한 예:

```sql
FROM orders o
INNER JOIN staff_id n
```

여기서 `staff_id`는 테이블명이 아니라 **컬럼명**이다.

올바른 코드:

```sql
FROM orders o
INNER JOIN staffs s
    ON o.staff_id = s.staff_id;
```

⭐ 암기

```text
FROM 뒤 → 테이블 이름
JOIN 뒤 → 테이블 이름

. 뒤 → 컬럼 이름
```

예:

```text
orders  → 테이블
staffs  → 테이블

order_id   → 컬럼
staff_id   → 컬럼
first_name → 컬럼
last_name  → 컬럼
```

---

# 18. JOIN 실습 오류

문제:

주문번호, 주문일자, 담당 직원 이름과 성 조회

처음 작성:

```sql
SELECT
    o.order_id,
    o.order_date,
    s.first_name,
    s.last_name
FROM orders o
INNER JOIN staffs s
    ON o.order_id = s.order_id;
```

문제점:

`orders`와 `staffs`를 연결해야 하는데 `order_id`를 사용했다.

`staffs` 테이블에는 주문번호가 아니라 직원 ID가 있기 때문에:

```sql
ON o.staff_id = s.staff_id
```

로 연결해야 한다.

수정:

```sql
SELECT
    o.order_id,
    o.order_date,
    s.first_name,
    s.last_name
FROM orders o
INNER JOIN staffs s
    ON o.staff_id = s.staff_id;
```

⭐ 헷갈렸던 원인

```text
order_id
→ 주문 번호

staff_id
→ 주문과 직원을 연결하는 번호
```

---

# 19. JOIN 실습 문제 정답

## 주문번호 + 상품명 + 주문수량

```sql
SELECT
    oi.order_id,
    p.product_name,
    oi.quantity
FROM order_items oi
INNER JOIN products p
    ON oi.product_id = p.product_id;
```

### 해석

```text
oi.order_id
→ 주문번호

p.product_name
→ 상품명

oi.quantity
→ 주문수량

oi.product_id = p.product_id
→ 상품 ID를 기준으로 두 테이블 연결
```

---

# 20. INNER JOIN

`INNER JOIN`은 양쪽 테이블에 모두 존재하는 데이터만 가져온다.

```sql
FROM orders o
INNER JOIN customers c
    ON o.customer_id = c.customer_id;
```

`INNER`는 생략 가능하다.

```sql
JOIN customers c
```

와:

```sql
INNER JOIN customers c
```

는 같은 의미이다.

⭐ 암기

```text
JOIN = INNER JOIN
```

---

# 21. INNER JOIN에서 테이블 순서

예:

```sql
FROM stocks s
JOIN products p
    ON s.product_id = p.product_id
```

와:

```sql
FROM products p
JOIN stocks s
    ON p.product_id = s.product_id
```

INNER JOIN에서는 매칭되는 데이터만 가져오기 때문에  
일반적으로 결과 행은 동일하다.

단, SELECT 컬럼의 순서가 다르면 출력되는 컬럼 위치는 달라질 수 있다.

⭐ 암기

```text
INNER JOIN
→ 테이블 순서를 바꿔도 매칭되는 데이터는 보통 같음
```

---

# 22. LEFT JOIN

왼쪽 테이블의 데이터는 모두 가져온다.

오른쪽 테이블에서 일치하는 데이터가 없으면 `NULL`이 들어간다.

```sql
FROM products p
LEFT JOIN stocks s
    ON p.product_id = s.product_id;
```

의미:

```text
products 전체 유지
+
stocks에서 매칭되는 데이터 붙이기
```

⭐ 암기

```text
LEFT JOIN
→ 왼쪽 테이블이 기준
```

---

# 23. RIGHT JOIN

오른쪽 테이블의 데이터를 모두 가져온다.

```sql
FROM products p
RIGHT JOIN stocks s
    ON p.product_id = s.product_id;
```

⭐ 암기

```text
RIGHT JOIN
→ 오른쪽 테이블이 기준
```

---

# 24. FULL OUTER JOIN

양쪽 테이블의 모든 데이터를 가져온다.

```sql
FROM products p
FULL OUTER JOIN stocks s
    ON p.product_id = s.product_id;
```

또는:

```sql
FULL JOIN
```

으로 작성할 수 있다.

일치하지 않는 데이터는 반대쪽 컬럼에 `NULL`이 들어간다.

⭐ JOIN 종류 암기

```text
INNER JOIN
→ 양쪽 모두 있는 데이터

LEFT JOIN
→ 왼쪽 전체 + 오른쪽 매칭

RIGHT JOIN
→ 오른쪽 전체 + 왼쪽 매칭

FULL OUTER JOIN
→ 양쪽 전체
```

---

# 25. INNER JOIN vs LEFT JOIN 순서

INNER JOIN은 테이블 순서를 바꿔도 결과가 거의 같지만  
LEFT JOIN은 순서가 매우 중요하다.

예:

```sql
FROM products p
LEFT JOIN stocks s
```

→ products 기준

반대로:

```sql
FROM stocks s
LEFT JOIN products p
```

→ stocks 기준

따라서 결과가 달라질 수 있다.

⭐ 암기

```text
INNER JOIN
→ 순서 영향 적음

LEFT JOIN / RIGHT JOIN
→ 기준 테이블이 달라지므로 순서 중요
```

---

# 26. SELECT 컬럼 구분

예:

```sql
SELECT
    p.first_name,
    p.last_name
```

이렇게 작성하면 결과는 두 개의 컬럼으로 출력된다.

```text
first_name | last_name
```

`,`는 컬럼을 구분한다.

⭐ 암기

```text
,  → 컬럼을 따로 출력
```

---

# 27. 이름 합치기

두 컬럼을 하나의 문자열로 합치고 싶으면 `||`를 사용한다.

```sql
SELECT
    p.first_name || ' ' || p.last_name AS full_name
FROM customers p;
```

결과:

```text
full_name
---------
가영 김
```

⭐ 암기

```text
,  → 컬럼 따로
|| → 문자열 합치기
```

---

# 28. 별칭을 일부 컬럼에만 써도 되는가?

예:

```sql
SELECT
    p.first_name,
    last_name
FROM customers p;
```

다른 테이블에 같은 컬럼명이 없다면 실행될 수 있다.

하지만 JOIN에서는 여러 테이블에 같은 컬럼명이 있을 수 있기 때문에:

```sql
SELECT
    p.first_name,
    p.last_name
```

처럼 앞에 테이블 별칭을 붙이는 것이 안전하다.

⭐ 권장

```text
JOIN 사용 시
→ 컬럼 앞에 테이블 별칭 붙이기
```

---

# 29. SQL 대소문자

SQL 키워드는 일반적으로 대소문자를 구분하지 않는다.

아래는 같은 의미이다.

```sql
SELECT
FROM
JOIN
ON
```

```sql
select
from
join
on
```

따라서 DBeaver에서 자동으로:

```sql
ON
```

이:

```sql
on
```

으로 바뀌어도 문제가 없다.

⭐ 암기

```text
SELECT = select
FROM = from
JOIN = join
ON = on
```

대문자 사용은 가독성을 위한 스타일이다.

---

# 30. UNION vs JOIN 최종 비교

| 구분 | UNION | JOIN |
|---|---|---|
| 방향 | 세로 | 가로 |
| 목적 | 여러 SELECT 결과 합치기 | 여러 테이블 연결 |
| 기준 | 컬럼 구조 | 연결키 |
| 대표 문법 | UNION / UNION ALL | INNER / LEFT / RIGHT / FULL |

⭐ 암기

```text
UNION
→ 아래로 붙인다

JOIN
→ 옆으로 붙인다
```

---

# 31. 오늘 헷갈렸던 포인트 정리

## ① 테이블명과 컬럼명

```text
orders
staffs
products
order_items
stocks
customers

→ 테이블
```

```text
order_id
staff_id
product_id
quantity
product_name
first_name
last_name

→ 컬럼
```

---

## ② FROM / JOIN / ON

```text
FROM
→ 기준 테이블

JOIN
→ 붙일 테이블

ON
→ 연결할 컬럼
```

---

## ③ 별칭

```text
o  → orders
oi → order_items
p  → products
s  → stocks 또는 staffs
c  → customers
```

별칭 자체는 내가 정할 수 있다.

단, 한 번 정했으면 그 쿼리 안에서 일관되게 사용한다.

---

## ④ quantity

```text
oi.quantity
→ 주문 수량

s.quantity
→ 현재 재고 수량
```

---

## ⑤ product_id

```text
product_id
→ 여러 테이블을 연결하는 키라
여러 테이블에 존재할 수 있음
```

---

## ⑥ JOIN 조건

```text
JOIN staffs
→ staff_id로 연결

JOIN products
→ product_id로 연결

JOIN customers
→ customer_id로 연결
```

단, 실제 테이블 관계를 확인하고 연결해야 한다.

---

# 🌟 오늘 최종 정리

오늘은 SQL의 집계와 테이블 결합을 학습했다.

집계 함수는 여러 행의 데이터를 하나의 값으로 요약할 때 사용하며:

```text
COUNT → 개수
SUM   → 합계
AVG   → 평균
MAX   → 최댓값
MIN   → 최솟값
```

으로 정리할 수 있다.

`GROUP BY`는 같은 값을 가진 데이터를 그룹으로 묶으며, `WHERE`는 그룹 이전의 개별 행을 필터링하고 `HAVING`은 그룹 결과에 조건을 건다.

```text
WHERE
→ 행 필터링

GROUP BY
→ 그룹 생성

HAVING
→ 그룹 필터링
```

여러 데이터를 합치는 방법에서는:

```text
UNION → 세로 결합
JOIN  → 가로 결합
```

이라는 차이가 있다.

JOIN에서는:

```text
SELECT → 보고 싶은 컬럼
FROM   → 기준 테이블
JOIN   → 붙일 테이블
ON     → 두 테이블의 연결키
```

구조를 기억하는 것이 중요하다.

특히 오늘 실습에서는 테이블명과 컬럼명을 혼동했으며, `JOIN staff_id`처럼 컬럼명을 테이블 위치에 작성하는 오류가 있었다.

이를 통해:

```text
FROM / JOIN 뒤 → 테이블명
점(.) 뒤       → 컬럼명
```

이라는 기준을 익혔다.

또한 테이블 별칭은 변수처럼 사용할 수 있지만, 컬럼 앞의 별칭은 그 컬럼이 실제로 어느 테이블에 속해 있는지 확인해서 작성해야 한다.

```text
p = products       → 상품 정보
oi = order_items   → 주문 상세
s = stocks         → 재고
```

그리고 가장 헷갈렸던 `quantity`는:

```text
oi.quantity → 주문 수량
s.quantity  → 현재 재고 수량
```

으로 구분한다.

---

# 💡 오늘 느낀 점

JOIN 문법 자체보다 어떤 테이블에 어떤 컬럼이 들어 있는지 구분하는 부분에서 많이 헷갈렸다.

특히 `product_id`, `quantity`, `staff_id`처럼 여러 테이블의 관계를 연결하거나 비슷한 이름을 가진 컬럼을 볼 때 앞에 어떤 별칭을 붙여야 하는지 혼동했다.

하지만 문제를 풀면서 단순히 별칭을 외우는 것이 아니라:

```text
이 컬럼이 원래 어느 테이블에 있는가?
```

를 먼저 생각하면 된다는 것을 이해했다.

또한 JOIN에서 `ON`은 단순히 아무 ID를 적는 것이 아니라 두 테이블을 실제로 연결하는 공통 키를 적는 부분이라는 것도 확실히 알게 되었다.

---

# 📝 오늘의 한 줄

JOIN은 **테이블을 붙이고, ON은 그 테이블들을 연결할 공통키를 정한다.**

---

# 🌱 앞으로의 목표

JOIN 문제를 볼 때 바로 코드를 작성하기보다 먼저:

```text
1. 어떤 테이블이 필요한지 확인
2. 각 컬럼이 어느 테이블에 있는지 확인
3. 기준 테이블을 FROM에 작성
4. 붙일 테이블을 JOIN에 작성
5. 두 테이블의 공통 연결키를 ON에 작성
6. SELECT에서 필요한 컬럼 앞에 정확한 별칭 붙이기
```

순서로 생각하는 습관을 들인다.

특히 아래는 계속 반복해서 암기한다.

```text
p  = products      → 상품 정보
oi = order_items   → 주문 상세
s  = stocks        → 재고

oi.quantity → 주문 수량
s.quantity  → 현재 재고 수량

FROM / JOIN → 테이블명
ON          → 연결키
. 뒤        → 컬럼명
```
