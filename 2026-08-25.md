# 📚 TIL - 데이터 전처리 & 시각화 1장 3강 후반 ~ 1장 6강

## 📌 오늘 학습 범위

- 1장 3강: 데이터 결합 - `join()`부터 마무리
- 1장 4강: 데이터 집계 - `groupby()` & `pivot_table()`
- 1장 5강: 데이터 정렬 & 유용한 메서드
- 1장 6강: EDA

---

# 1장 3강 - 데이터 결합 마무리

## 1. `join()` - 인덱스 기반 결합

`join()`은 **인덱스를 기준으로 DataFrame을 결합**할 때 사용하는 메서드이다.

```python
result = employees.join(scores, how="left")
```

### 핵심

- `join()` → 인덱스를 기준으로 결합
- 기본적으로 왼쪽 DataFrame을 기준으로 결합
- `how` 옵션으로 결합 방식을 지정할 수 있음
- 인덱스가 고객번호, 상품번호 등 의미 있는 키일 때 사용하기 편함

### 예시

```python
import pandas as pd

employees = pd.DataFrame({
    "name": ["김철수", "이영희", "박민수"]
}, index=[101, 102, 103])

scores = pd.DataFrame({
    "score": [85, 92, 78]
}, index=[101, 102, 103])

result = employees.join(scores, how="left")

print(result)
```

### `how` 옵션

```python
how="left"
```

→ 왼쪽 DataFrame의 인덱스를 모두 유지

```python
how="inner"
```

→ 양쪽에 공통으로 존재하는 인덱스만 유지

### 암기

- `join()` → 인덱스 기준 결합
- `left` → 왼쪽 기준
- `inner` → 공통 인덱스만

---

## 2. `concat()` / `merge()` / `join()` 비교

### `concat()`

DataFrame을 **단순히 이어 붙일 때** 사용한다.

```python
pd.concat([df1, df2], axis=0)
```

- `axis=0` → 위아래로 붙이기
- `axis=1` → 좌우로 붙이기

### 암기

`concat()` → 그냥 이어 붙이기

---

### `merge()`

공통 컬럼을 기준으로 데이터를 결합한다.

```python
pd.merge(
    orders,
    customers,
    on="customer_id",
    how="left"
)
```

### 특징

- 공통 컬럼 기준
- SQL의 `JOIN`과 비슷함
- `on`, `left_on`, `right_on` 등을 사용할 수 있음

### 암기

`merge()` → 공통 컬럼 기준 결합

---

### `join()`

인덱스를 기준으로 결합한다.

```python
customers.join(orders, how="left")
```

### 암기

`join()` → 인덱스 기준 결합

---

## 3. 결합 메서드 한눈에 보기

| 메서드 | 기준 | 대표 사용 상황 |
|---|---|---|
| `concat()` | 행/열 방향 | 여러 데이터를 단순 연결 |
| `merge()` | 공통 컬럼 | 고객ID, 상품ID 등 공통 키로 연결 |
| `join()` | 인덱스 | 인덱스를 키로 사용하는 데이터 결합 |

### 초간단 암기

- 그냥 붙이기 → `concat()`
- 컬럼 키로 결합 → `merge()`
- 인덱스로 결합 → `join()`

---

## 4. `append()` 주의

과거 Pandas에서는 `append()`를 사용하여 데이터를 추가했지만 현재는 제거되었다.

```python
# 과거 방식
df1.append(df2)
```

현재는 `pd.concat()`을 사용한다.

```python
pd.concat([df1, df2])
```

### 암기

`append()` → 제거됨  
대신 → `pd.concat()`

---

# 1장 4강 - 데이터 집계: `groupby()` & `pivot_table()`

## 1. `groupby()` - 그룹별 데이터 집계

`groupby()`는 특정 컬럼의 값을 기준으로 데이터를 그룹으로 묶은 뒤 계산할 때 사용한다.

### 기본 구조

```python
df.groupby("기준컬럼")["계산할컬럼"].sum()
```

### 예시

```python
total_sales = df.groupby("region")["sales"].sum()

print(total_sales)
```

지역별로 데이터를 묶은 뒤 매출 합계를 계산한다.

---

## 2. `groupby()`에서 자주 사용하는 집계 함수

```python
df.groupby("region")["sales"].sum()
df.groupby("region")["sales"].mean()
df.groupby("region")["sales"].count()
df.groupby("region")["sales"].min()
df.groupby("region")["sales"].max()
```

### 의미

- `sum()` → 합계
- `mean()` → 평균
- `count()` → 개수
- `min()` → 최솟값
- `max()` → 최댓값

### 암기

`groupby()` → 묶고 계산

---

## 3. `reset_index()`

`groupby()`를 사용하면 그룹 기준 컬럼이 인덱스로 들어가는 경우가 있다.

일반 컬럼 형태로 다시 돌리고 싶다면:

```python
result = (
    df.groupby("region")["sales"]
    .sum()
    .reset_index()
)
```

### 암기

`reset_index()` → 인덱스를 다시 일반 컬럼으로

### 자주 쓰는 흐름

```python
df.groupby("기준")["값"].sum().reset_index()
```

---

## 4. `agg()` - 여러 집계를 한 번에

`agg()`는 여러 집계 함수를 동시에 적용할 때 사용한다.

### 예시

```python
df.groupby("store")["sales"].agg([
    "sum",
    "mean",
    "count"
])
```

또는 컬럼마다 다른 집계 함수를 적용할 수 있다.

```python
result = df.groupby("store").agg({
    "sales": "sum",
    "quantity": "mean",
    "order_id": "count"
})
```

### 암기

`agg()` → 여러 집계 함수를 한 번에

---

## 5. `pivot_table()` - 데이터 재구성 및 요약

`pivot_table()`은 데이터를 **행과 열 기준으로 재구성하면서 집계**할 때 사용한다.

### 기본 구조

```python
pd.pivot_table(
    df,
    index="행기준",
    columns="열기준",
    values="집계할값",
    aggfunc="sum",
    fill_value=0
)
```

### 예시

```python
pivot = pd.pivot_table(
    df,
    index="day",
    columns="payment",
    values="sales",
    aggfunc="sum",
    fill_value=0
)

print(pivot)
```

---

## 6. `pivot_table()` 주요 옵션

- `index` → 행 기준
- `columns` → 열 기준
- `values` → 계산할 값
- `aggfunc` → 집계 함수
- `fill_value` → 빈 값 채우기

### 예

```python
aggfunc="mean"
```

→ 평균 계산

```python
fill_value=0
```

→ 결측값을 0으로 채움

---

## 7. `groupby()` vs `pivot_table()`

### `groupby()`

- 데이터를 특정 기준으로 묶음
- 그룹별 합계, 평균, 개수 등을 계산
- 분석 과정에서 자주 사용

### `pivot_table()`

- 그룹별 집계
- 행과 열 형태로 재구성
- 보고서나 요약표 형태로 보기 좋음

### 암기

`groupby()` → 묶어서 계산  
`pivot_table()` → 묶어서 계산 + 표 재구성

---

# 1장 5강 - 데이터 정렬 & 유용한 메서드

## 1. `sort_values()` - 컬럼 값 기준 정렬

특정 컬럼의 값을 기준으로 데이터를 정렬할 때 사용한다.

```python
df.sort_values("sales")
```

기본값은 오름차순이다.

---

## 2. 내림차순 정렬

```python
df.sort_values(
    "sales",
    ascending=False
)
```

### 암기

- `ascending=True` → 오름차순
- `ascending=False` → 내림차순

---

## 3. 여러 컬럼 기준 정렬

```python
df.sort_values(
    ["region", "sales"],
    ascending=[True, False]
)
```

의미:

- `region` → 오름차순
- `sales` → 내림차순

---

## 4. `sort_index()` - 인덱스 기준 정렬

인덱스 값을 기준으로 데이터를 정렬한다.

```python
df.sort_index()
```

내림차순:

```python
df.sort_index(ascending=False)
```

---

## 5. `sort_values()` vs `sort_index()`

| 메서드 | 정렬 기준 |
|---|---|
| `sort_values()` | 컬럼 값 |
| `sort_index()` | 인덱스 |

### 초간단 암기

- values → 값
- index → 인덱스

---

## 6. `lambda` - 익명 함수

`lambda`는 이름 없이 짧게 작성하는 함수이다.

```python
lambda x: x * 0.9
```

일반 함수:

```python
def discount(x):
    return x * 0.9
```

같은 내용을 `lambda`로 작성하면:

```python
lambda x: x * 0.9
```

---

## 7. `apply()`와 `lambda`

`apply()`는 각 데이터 값에 함수를 적용한다.

```python
df["discount_price"] = df["price"].apply(
    lambda x: x * 0.9
)
```

### 예시

```python
df["delivery_type"] = df["delivery_days"].apply(
    lambda x: "지연" if x >= 20 else "정상"
)
```

### 암기

`lambda` → 이름 없는 짧은 함수  
`apply()` → 각 값에 함수 적용

---

## 8. `split()` - 문자열 분리

문자열 데이터를 특정 구분자를 기준으로 나눈다.

```python
df["product_code"].str.split("-")
```

예를 들어:

```text
FOOD-001
FOOD-002
HOME-001
```

`-` 기준으로 나눌 수 있다.

---

## 9. `expand=True`

문자열을 나눈 결과를 여러 컬럼으로 만들고 싶다면:

```python
df[["category", "number"]] = (
    df["product_code"]
    .str.split("-", expand=True)
)
```

### 암기

`split()` → 문자열 나누기  
`expand=True` → 나눈 값을 여러 컬럼으로 펼치기

---

## 10. 날짜 규칙 생성

반복되는 날짜 규칙을 만들 수 있다.

```python
from datetime import datetime
from dateutil.rrule import rrule, WEEKLY

for date in rrule(
    freq=WEEKLY,
    dtstart=datetime(2026, 1, 1),
    count=5
):
    print(date)
```

### 주요 개념

- `freq` → 반복 주기
- `dtstart` → 시작 날짜
- `count` → 생성 개수

### 암기

`rrule` → 반복 날짜 규칙 생성

---

# 1장 6강 - EDA

## 1. EDA란?

EDA는 `Exploratory Data Analysis`의 약자로 **탐색적 데이터 분석**을 의미한다.

본격적인 분석이나 모델링 전에 데이터를 먼저 살펴보면서 구조와 특징을 파악하는 과정이다.

### EDA의 목적

- 데이터 구조 확인
- 데이터 타입 확인
- 결측치 확인
- 이상치 확인
- 데이터 분포 확인
- 그룹별 차이 확인
- 분석 방향 설정
- 새로운 질문 만들기

---

## 2. EDA가 필요한 이유

평균 하나만 보고 데이터를 판단하면 실제 데이터의 특징을 놓칠 수 있다.

예를 들어 두 그룹의 평균이 같더라도:

- 한 그룹은 값들이 평균 주변에 몰려 있을 수 있고
- 다른 그룹은 일부 매우 큰 값 때문에 평균이 높아졌을 수 있다.

따라서 EDA에서는 평균뿐 아니라:

- 최솟값
- 최댓값
- 분포
- 이상치
- 그룹별 차이

등을 함께 확인해야 한다.

---

## 3. `head()`

데이터의 앞부분을 확인한다.

```python
df.head()
```

기본적으로 앞 5개 행을 보여준다.

```python
df.head(3)
```

→ 앞 3개 행 확인

---

## 4. `tail()`

데이터의 마지막 부분을 확인한다.

```python
df.tail()
```

기본적으로 마지막 5개 행을 보여준다.

---

## 5. `shape`

전체 행과 열 개수를 확인한다.

```python
df.shape
```

결과 예:

```text
(500, 20)
```

의미:

- 500행
- 20열

### 암기

`shape` → `(행 수, 열 수)`

---

## 6. `columns`

전체 컬럼명을 확인한다.

```python
df.columns
```

---

## 7. `dtypes`

컬럼별 데이터 타입을 확인한다.

```python
df.dtypes
```

---

## 8. `info()`

DataFrame의 전체 구조를 확인한다.

```python
df.info()
```

확인할 수 있는 내용:

- 전체 행 개수
- 컬럼명
- 결측치 여부
- 데이터 타입
- 메모리 사용량

### 암기

`info()` → 전체 구조 확인

---

## 9. `describe()`

수치형 컬럼의 요약 통계를 확인한다.

```python
df.describe()
```

확인할 수 있는 값:

- `count` → 데이터 개수
- `mean` → 평균
- `std` → 표준편차
- `min` → 최솟값
- `25%` → 1사분위수
- `50%` → 중앙값
- `75%` → 3사분위수
- `max` → 최댓값

### 암기

`describe()` → 수치형 데이터 요약 통계

---

## 10. 결측치 확인

```python
df.isnull().sum()
```

또는:

```python
df.isna().sum()
```

### 의미

- `isnull()` / `isna()` → 결측치인지 확인
- `sum()` → 결측치 개수 계산

### 암기

결측치 개수 확인:

```python
df.isnull().sum()
```

---

## 11. `value_counts()`

범주형 데이터에서 각 값이 몇 번 등장하는지 확인한다.

```python
df["product_category"].value_counts()
```

예를 들어:

```text
전자제품    100
식품         80
의류         50
```

처럼 값별 개수를 확인할 수 있다.

### 암기

`value_counts()` → 값별 개수

---

## 12. `unique()`와 `nunique()`

### `unique()`

중복을 제외한 고유값 목록을 확인한다.

```python
df["region"].unique()
```

### `nunique()`

중복을 제외한 고유값의 개수를 확인한다.

```python
df["region"].nunique()
```

### 암기

`unique()` → 고유값 목록  
`nunique()` → 고유값 개수

---

# EDA 기본 프로세스

## 1단계: Question - 질문 만들기

분석하고 싶은 질문을 먼저 생각한다.

예:

- 어떤 고객군이 이탈률이 높은가?
- 어떤 지역의 매출이 가장 높은가?
- 어떤 상품군의 판매량이 많은가?

---

## 2단계: Exploration - 탐색

데이터를 확인한다.

```python
df.head()
df.info()
df.describe()
df.value_counts()
```

필요한 그룹별 집계도 수행한다.

```python
df.groupby("region")["sales"].mean()
```

---

## 3단계: Insight - 인사이트

탐색 결과에서 의미 있는 특징을 찾는다.

예:

- 특정 지역의 평균 매출이 높음
- 특정 고객군의 이탈률이 높음
- 배송기간이 길수록 고객평점이 낮음

---

## 4단계: New Question - 새로운 질문

분석 결과를 바탕으로 새로운 질문을 만든다.

예:

“왜 이 지역의 매출이 높은가?”

“왜 특정 고객군의 이탈률이 높은가?”

이렇게 질문 → 탐색 → 인사이트 → 새로운 질문의 흐름을 반복한다.

---

# EDA 기본 확인 순서

## 1. 데이터 앞뒤 확인

```python
df.head()
df.tail()
```

## 2. 데이터 크기 확인

```python
df.shape
```

## 3. 컬럼 확인

```python
df.columns
```

## 4. 데이터 타입 확인

```python
df.dtypes
```

## 5. 전체 구조 확인

```python
df.info()
```

## 6. 요약 통계 확인

```python
df.describe()
```

## 7. 결측치 확인

```python
df.isnull().sum()
```

## 8. 범주형 값 분포 확인

```python
df["컬럼명"].value_counts()
```

## 9. 그룹별 비교

```python
df.groupby("기준컬럼")["값컬럼"].mean()
```

---

# ⚠️ 헷갈리기 쉬운 개념 비교

## `concat()` vs `merge()` vs `join()`

| 메서드 | 기준 | 암기 |
|---|---|---|
| `concat()` | 행/열 | 그냥 붙이기 |
| `merge()` | 컬럼 | 공통 키로 결합 |
| `join()` | 인덱스 | 인덱스로 결합 |

---

## `groupby()` vs `pivot_table()`

| 메서드 | 역할 |
|---|---|
| `groupby()` | 그룹으로 묶어서 계산 |
| `pivot_table()` | 그룹 집계 + 행/열 표 재구성 |

---

## `sort_values()` vs `sort_index()`

| 메서드 | 기준 |
|---|---|
| `sort_values()` | 컬럼 값 |
| `sort_index()` | 인덱스 |

---

## `unique()` vs `nunique()`

| 메서드 | 결과 |
|---|---|
| `unique()` | 고유값 목록 |
| `nunique()` | 고유값 개수 |

---

## `set_index()` vs `reset_index()`

| 메서드 | 역할 |
|---|---|
| `set_index()` | 특정 컬럼을 인덱스로 지정 |
| `reset_index()` | 인덱스를 다시 일반 컬럼으로 변경 |

예:

```python
df.set_index("customer_id")
```

→ `customer_id` 컬럼을 인덱스로 지정

```python
df.reset_index()
```

→ 인덱스를 일반 컬럼으로 되돌림

---

# 🧠 누적 암기 포인트

## 데이터 기본 확인

```python
df.head()
df.tail()
df.shape
df.columns
df.dtypes
df.info()
df.describe()
```

### 암기

- `head()` → 앞부분
- `tail()` → 뒷부분
- `shape` → 행/열 개수
- `columns` → 컬럼명
- `dtypes` → 컬럼별 자료형
- `info()` → 전체 구조
- `describe()` → 수치형 요약 통계

---

## 고유값 / 빈도

```python
df["col"].unique()
df["col"].nunique()
df["col"].value_counts()
```

### 암기

- `unique()` → 고유값 목록
- `nunique()` → 고유값 개수
- `value_counts()` → 값별 개수

---

## 인덱스

```python
df.set_index("id")
df.reset_index()
```

### 암기

- `set_index()` → 컬럼을 인덱스로
- `reset_index()` → 인덱스를 일반 컬럼으로

---

## 데이터 결합

```python
pd.concat()
pd.merge()
df.join()
```

### 암기

- `concat()` → 단순 연결
- `merge()` → 컬럼 기준 결합
- `join()` → 인덱스 기준 결합

---

## 데이터 집계

```python
df.groupby()
df.groupby().agg()
pd.pivot_table()
```

### 암기

- `groupby()` → 묶어서 계산
- `agg()` → 여러 집계 함수 적용
- `pivot_table()` → 행/열 요약표

---

## 데이터 정렬

```python
df.sort_values()
df.sort_index()
```

### 암기

- `sort_values()` → 값 기준
- `sort_index()` → 인덱스 기준
- `ascending=False` → 내림차순

---

## 값 변환

```python
lambda x: ...
df["col"].apply(lambda x: ...)
```

### 암기

- `lambda` → 이름 없는 짧은 함수
- `apply()` → 각 값에 함수 적용

---

## 문자열 분리

```python
df["col"].str.split("-")
```

여러 컬럼으로 분리:

```python
df["col"].str.split("-", expand=True)
```

### 암기

- `split()` → 문자열 나누기
- `expand=True` → 나눈 값을 컬럼으로 펼치기

---

# 🔥 문제 문장을 보고 바로 떠올릴 메서드

“앞의 데이터 보여줘”
→ `head()`

“마지막 데이터 보여줘”
→ `tail()`

“몇 행 몇 열?”
→ `shape`

“컬럼 이름 확인”
→ `columns`

“자료형 확인”
→ `dtypes`

“전체 구조 확인”
→ `info()`

“평균, 최소, 최대 등 통계 확인”
→ `describe()`

“고유값 목록”
→ `unique()`

“고유값 개수”
→ `nunique()`

“값별 개수”
→ `value_counts()`

“특정 컬럼을 인덱스로 지정”
→ `set_index()`

“인덱스를 다시 컬럼으로”
→ `reset_index()`

“데이터 그냥 이어 붙이기”
→ `concat()`

“공통 컬럼으로 데이터 연결”
→ `merge()`

“인덱스를 기준으로 데이터 연결”
→ `join()`

“지역별 / 상품별 / 고객별로 묶기”
→ `groupby()`

“합계와 평균을 한 번에 계산”
→ `agg()`

“행과 열 형태의 요약표 만들기”
→ `pivot_table()`

“값이 큰 순서대로 정렬”
→ `sort_values(ascending=False)`

“인덱스 순서로 정렬”
→ `sort_index()`

“각 값에 계산 적용”
→ `apply()`

“간단한 함수를 한 줄로 작성”
→ `lambda`

“문자열을 구분자로 나누기”
→ `split()`

---

# ⭐ 꼭 외울 핵심 비교

```text
concat = 붙이기
merge = 컬럼으로 결합
join = 인덱스로 결합

groupby = 묶어서 계산
agg = 여러 계산 한 번에
pivot_table = 묶어서 계산 + 표 재구성

sort_values = 값 정렬
sort_index = 인덱스 정렬

unique = 고유값 목록
nunique = 고유값 개수
value_counts = 값별 개수

set_index = 인덱스 설정
reset_index = 인덱스 되돌리기

lambda = 짧은 함수
apply = 함수 적용

split = 문자열 분리
```

---

# 🌟 오늘 최종 정리

이번 범위에서는 데이터 결합부터 집계, 정렬, 문자열 처리, 데이터 변환, EDA까지 Pandas에서 데이터를 실제 분석 가능한 형태로 다루는 방법을 학습했다.

데이터 결합에서는 `concat()`, `merge()`, `join()`의 차이를 배웠다. `concat()`은 데이터를 단순히 이어 붙이고, `merge()`는 공통 컬럼을 기준으로 결합하며, `join()`은 인덱스를 기준으로 결합한다.

데이터 집계에서는 `groupby()`로 특정 기준에 따라 데이터를 그룹화한 뒤 합계, 평균, 개수 등을 계산할 수 있었고, `agg()`를 이용하면 여러 집계 함수를 한 번에 적용할 수 있었다. `pivot_table()`은 그룹별 집계 결과를 행과 열 기준의 표 형태로 재구성할 때 사용할 수 있다.

데이터 정렬에서는 `sort_values()`가 컬럼 값을 기준으로 정렬하고 `sort_index()`가 인덱스를 기준으로 정렬한다는 차이를 익혔다. 또한 `lambda`와 `apply()`를 이용하여 각 데이터 값에 간단한 함수를 적용하고, `split()`을 이용하여 문자열 데이터를 여러 부분으로 나누는 방법도 학습했다.

EDA에서는 분석을 시작하기 전에 데이터의 구조, 자료형, 결측치, 요약 통계, 분포, 그룹별 차이 등을 확인하는 과정이 중요하다는 것을 배웠다. EDA는 단순히 데이터를 출력하는 것이 아니라 질문을 만들고 데이터를 탐색한 뒤 인사이트를 발견하고 다시 새로운 질문을 만드는 반복 과정이라는 점이 핵심이다.

---

# 💡 오늘 느낀 점

Pandas에서 사용하는 메서드가 많아지면서 모든 문법을 그대로 외우는 것보다 문제에서 무엇을 요구하는지 먼저 파악하고 그 행동에 맞는 메서드를 연결하는 것이 더 중요하다고 느꼈다.

특히 `concat()`, `merge()`, `join()`이나 `groupby()`, `pivot_table()`처럼 비슷해 보이는 기능은 단순히 이름을 외우는 것보다 각각 무엇을 기준으로 데이터를 처리하는지를 이해해야 헷갈리지 않을 것 같다.

EDA도 단순히 `describe()`나 `info()`를 실행하는 것이 아니라 결과에서 이상한 점을 찾고 “왜 이런 결과가 나왔을까?”라는 새로운 질문으로 이어가는 과정이라는 점을 기억해야겠다.

---

# 📝 오늘의 한 줄

**데이터 분석은 메서드를 외우는 것보다 무엇을 기준으로 묶고, 연결하고, 정렬하고, 질문할지를 판단하는 것이 먼저다.**

---

# 🌱 앞으로의 목표

문제를 읽었을 때 필요한 메서드를 바로 떠올릴 수 있도록 결합, 집계, 정렬, 전처리, EDA별 핵심 메서드를 반복해서 연습하고, 비슷한 기능들의 차이를 비교하면서 익히겠다.

특히 아래 연결은 바로 떠올릴 수 있도록 반복해서 암기한다.

```text
그냥 붙이기 → concat
컬럼으로 결합 → merge
인덱스로 결합 → join

그룹별 계산 → groupby
여러 집계 → agg
요약표 → pivot_table

값 정렬 → sort_values
인덱스 정렬 → sort_index

고유값 목록 → unique
고유값 개수 → nunique
값별 개수 → value_counts

인덱스 지정 → set_index
인덱스 해제 → reset_index

짧은 함수 → lambda
함수 적용 → apply
문자열 분리 → split
```
