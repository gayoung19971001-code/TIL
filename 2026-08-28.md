# 2026-08-28 TIL — 기초 통계

## 학습 범위

- 1장 1강: 통계란 무엇인가 — 기술통계와 추론통계
- 1장 2강: 중심경향치 — 평균·중앙값·최빈값
- 1장 3강: 산포도 — 범위·분산·표준편차·IQR·변동계수
- 2장 1강: 확률의 기초

---

## 1장 1강. 통계란 무엇인가 — 기술통계와 추론통계

### 1. 통계란?

통계는 데이터를 수집하고 정리·분석하여 의미 있는 결과를 얻는 과정이다. 단순히 숫자를 모으는 것이 아니라, 데이터에서 규칙과 경향을 발견하여 판단이나 의사결정에 활용하는 것이 핵심이다.

예를 들어 배송 시간 데이터가 여러 개 있을 때 평균 배송 시간을 계산하면 전체적인 배송 수준을 한눈에 파악할 수 있다.

```python
delivery_hours = [20, 21, 22, 25, 24]
average_hours = sum(delivery_hours) / len(delivery_hours)

print("평균 배송 시간:", average_hours, "시간")
```

### 2. 기술통계와 추론통계

#### 기술통계(Descriptive Statistics)

수집된 데이터를 보기 쉽게 정리하고 요약하는 방법이다.

- 평균, 중앙값, 최빈값
- 분산, 표준편차
- 표와 그래프

```python
scores = [4, 5, 4, 4, 4]
average_score = sum(scores) / len(scores)

print("조사 고객의 평균 만족도:", average_score)
```

위 코드는 조사한 5명의 평균 만족도를 계산하므로 기술통계에 해당한다.

#### 추론통계(Inferential Statistics)

표본에서 얻은 결과를 이용해 전체 모집단의 특성을 추측하거나 일반화하는 방법이다.

예를 들어 학생 100명 중 10명의 시험 점수를 조사한 뒤, 그 결과로 전체 학생의 성적 수준을 추정하는 것이 추론통계다.

### 3. 모집단과 표본

- **모집단(Population)**: 조사하고 싶은 전체 대상
- **표본(Sample)**: 모집단에서 실제로 선택하여 조사한 일부 대상

```python
all_users = ["김민지", "서울고객", "신규고객", "기존고객", "재구매고객"]
sample_users = all_users[:2]

print("모집단:", all_users)
print("표본:", sample_users)
```

모집단 전체를 조사하면 정확도가 높지만 시간과 비용이 많이 든다. 따라서 모집단을 잘 대표하는 표본을 선택하여 조사하는 경우가 많다.

> 표본이 모집단을 제대로 대표하지 못하면 분석 결과가 왜곡될 수 있다.

### 4. 통계 분석의 기본 흐름

1. 문제 정의: 무엇을 알고 싶은지 정한다.
2. 데이터 수집: 문제 해결에 필요한 데이터를 모은다.
3. 기술통계: 평균, 중앙값, 분산 등으로 데이터를 요약한다.
4. 추론통계: 표본의 결과를 바탕으로 모집단의 특성을 추정한다.
5. 결과 해석 및 의사결정: 분석 결과가 무엇을 의미하는지 판단하고 행동으로 연결한다.

```text
문제 정의 → 데이터 수집 → 기술통계 → 추론통계 → 결과 해석
```

---

## 1장 2강. 중심경향치 — 평균·중앙값·최빈값

중심경향치는 데이터가 어느 값을 중심으로 모여 있는지 보여주는 대표값이다.

### 1. 평균(Mean)

모든 값을 더한 뒤 데이터 개수로 나눈 값이다.

\[
\text{평균}=\frac{\text{데이터의 합}}{\text{데이터의 개수}}
\]

```python
import pandas as pd

ratings = pd.Series([4, 5, 4, 4, 4])
print(ratings.mean())
```

#### 가중평균

값마다 중요도가 다를 때 각 값에 가중치를 적용해 계산한다.

```python
import numpy as np

scores = [80, 90]
weights = [0.4, 0.6]
weighted_mean = np.average(scores, weights=weights)

print(weighted_mean)
```

평균은 모든 값을 반영한다는 장점이 있지만, 지나치게 크거나 작은 이상치에 영향을 많이 받는다.

### 2. 중앙값(Median)

데이터를 크기순으로 정렬했을 때 가운데에 위치하는 값이다.

- 데이터 개수가 홀수: 가운데 값
- 데이터 개수가 짝수: 가운데 두 값의 평균

```python
salaries = pd.Series([3200, 3400, 3700, 4000, 15000])

print("평균:", salaries.mean())
print("중앙값:", salaries.median())
```

연봉이나 집값처럼 이상치가 큰 데이터에서는 평균보다 중앙값이 전체적인 수준을 더 잘 나타낼 수 있다.

### 3. 최빈값(Mode)

데이터에서 가장 자주 등장하는 값이다. 상품 결제 수단, 선호 브랜드처럼 범주형 데이터를 요약할 때 유용하다.

```python
payment_methods = pd.Series([
    "카드", "카드", "계좌이체", "카드", "간편결제"
])

print(payment_methods.mode())
```

- 최빈값은 여러 개일 수 있다.
- 모든 값의 빈도가 같으면 대표적인 최빈값이 없다고 볼 수 있다.
- Pandas의 `mode()`는 결과를 `Series`로 반환한다.

### 4. 왜도(Skewness)

왜도는 데이터 분포가 어느 방향으로 치우쳐 있는지 나타낸다.

| 왜도 | 분포 형태 | 일반적인 관계 |
| --- | --- | --- |
| 양수 | 오른쪽 꼬리가 김 | 평균 > 중앙값 |
| 0에 가까움 | 대칭 분포 | 평균 ≈ 중앙값 |
| 음수 | 왼쪽 꼬리가 김 | 평균 < 중앙값 |

```python
purchase_amounts = pd.Series([1, 4, 5, 5, 7, 8, 60, 100])
print(purchase_amounts.skew())
```

### 5. 백분위수와 사분위수

- **백분위수(Percentile)**: 데이터를 100등분했을 때 특정 위치의 값
- **Q1**: 25번째 백분위수
- **Q2**: 50번째 백분위수이며 중앙값과 같음
- **Q3**: 75번째 백분위수

```python
import numpy as np

delivery_hours = [20, 25, 30, 35, 40, 45, 60]

q1 = np.percentile(delivery_hours, 25)
q2 = np.percentile(delivery_hours, 50)
q3 = np.percentile(delivery_hours, 75)

print(q1, q2, q3)
```

---

## 1장 3강. 산포도 — 범위·분산·표준편차·IQR·변동계수

산포도는 데이터가 중심값 주변에 얼마나 퍼져 있는지를 나타낸다. 평균이 같아도 데이터가 퍼진 정도는 서로 다를 수 있으므로 대표값과 산포도를 함께 확인해야 한다.

### 1. 범위(Range)

최댓값과 최솟값의 차이다.

\[
\text{범위}=\text{최댓값}-\text{최솟값}
\]

```python
delivery_days = pd.Series([2, 3, 4, 6, 8])
data_range = delivery_days.max() - delivery_days.min()

print(data_range)
```

- 계산이 간단하다.
- 최댓값과 최솟값만 사용하므로 이상치의 영향을 크게 받는다.

### 2. 분산(Variance)

각 데이터가 평균에서 떨어진 거리인 편차를 제곱한 뒤 평균을 낸 값이다.

\[
\text{분산}=\frac{\sum(x_i-\bar{x})^2}{n}
\]

편차를 그대로 더하면 양수와 음수가 상쇄되므로 제곱하여 계산한다. 제곱을 사용하기 때문에 분산의 단위는 원래 데이터 단위의 제곱이 된다.

```python
data = pd.Series([2, 4, 4, 4, 5, 5, 7, 9])

print("모집단 분산:", data.var(ddof=0))
print("표본분산:", data.var(ddof=1))
```

#### 모집단 분산과 표본분산

- 모집단 분산: 분모가 `N`, `ddof=0`
- 표본분산: 분모가 `N-1`, `ddof=1`

Pandas의 `var()`는 기본적으로 표본분산을 계산하고, NumPy의 `np.var()`는 기본적으로 모집단 분산을 계산한다.

### 3. 표준편차(Standard Deviation)

분산에 제곱근을 씌운 값이다.

\[
\text{표준편차}=\sqrt{\text{분산}}
\]

```python
customer_counts = pd.Series([98, 99, 100, 101, 102])

print("평균:", customer_counts.mean())
print("표준편차:", customer_counts.std(ddof=0))
```

- 값이 작으면 데이터가 평균 주변에 모여 있다.
- 값이 크면 데이터가 평균에서 넓게 퍼져 있다.
- 원래 데이터와 단위가 같아 분산보다 해석하기 쉽다.

### 4. IQR(Interquartile Range)

Q3에서 Q1을 뺀 값으로, 데이터 가운데 50%가 퍼져 있는 범위를 나타낸다.

\[
IQR=Q3-Q1
\]

```python
orders = pd.Series([20, 22, 24, 25, 28, 30, 100])

q1 = orders.quantile(0.25)
q3 = orders.quantile(0.75)
iqr = q3 - q1

print(q1, q3, iqr)
```

범위와 달리 양쪽 극단값을 직접 사용하지 않아 이상치에 비교적 강하다.

#### IQR을 이용한 이상치 판별

\[
\text{하한}=Q1-1.5\times IQR
\]

\[
\text{상한}=Q3+1.5\times IQR
\]

하한보다 작거나 상한보다 큰 값은 이상치 후보로 판단한다.

```python
lower_bound = q1 - 1.5 * iqr
upper_bound = q3 + 1.5 * iqr

outliers = orders[(orders < lower_bound) | (orders > upper_bound)]
print(outliers)
```

### 5. 변동계수(Coefficient of Variation, CV)

표준편차를 평균으로 나눈 뒤 백분율로 나타낸 값이다.

\[
CV=\frac{\text{표준편차}}{\text{평균}}\times100
\]

```python
product_a = pd.Series([98, 99, 100, 101, 102])
cv_a = product_a.std(ddof=0) / product_a.mean() * 100

print(cv_a)
```

단위나 평균의 크기가 다른 데이터끼리 상대적인 변동성을 비교할 때 유용하다. CV가 작을수록 평균 대비 데이터가 더 안정적으로 모여 있다고 해석할 수 있다.

### 6. 산포도 비교

| 지표 | 계산 방식 | 단위 | 이상치 영향 | 주요 용도 |
| --- | --- | --- | --- | --- |
| 범위 | 최댓값 - 최솟값 | 원래 단위 | 매우 큼 | 전체 폭을 빠르게 확인 |
| 분산 | 편차 제곱의 평균 | 원래 단위² | 큼 | 변동성의 수학적 계산 |
| 표준편차 | 분산의 제곱근 | 원래 단위 | 큼 | 평균 주변의 퍼짐 해석 |
| IQR | Q3 - Q1 | 원래 단위 | 비교적 작음 | 이상치가 있는 데이터 분석 |
| 변동계수 | 표준편차 ÷ 평균 × 100 | % | 큼 | 규모·단위가 다른 자료 비교 |

---

## 2장 1강. 확률의 기초

### 1. 확률의 정의

확률은 어떤 사건이 발생할 가능성을 0부터 1 사이의 값으로 나타낸 것이다.

- 확률이 0: 절대 발생하지 않음
- 확률이 1: 반드시 발생함
- 확률이 0.5: 발생 가능성이 50%

모든 결과가 동일한 가능성을 가진다면 다음과 같이 계산한다.

\[
P(A)=\frac{\text{사건 A가 발생하는 경우의 수}}{\text{전체 가능한 경우의 수}}
\]

```python
total_visitors = 500
buyers = 40

purchase_probability = buyers / total_visitors
print(purchase_probability)
```

### 2. 확률의 기본 법칙

#### 여사건

사건 A가 발생하지 않을 확률이다.

\[
P(A^c)=1-P(A)
\]

#### 덧셈법칙

둘 중 하나 이상이 발생할 확률이다.

\[
P(A\cup B)=P(A)+P(B)-P(A\cap B)
\]

두 사건이 서로 동시에 발생할 수 없는 배반사건이라면 교집합이 0이므로 다음과 같다.

\[
P(A\cup B)=P(A)+P(B)
\]

#### 곱셈법칙

두 사건이 모두 발생할 확률이다.

\[
P(A\cap B)=P(A)\times P(B|A)
\]

두 사건이 서로 독립이라면 다음과 같이 계산한다.

\[
P(A\cap B)=P(A)\times P(B)
\]

#### 독립사건과 종속사건

- 독립사건: 한 사건의 발생이 다른 사건의 확률에 영향을 주지 않음
- 종속사건: 한 사건의 발생이 다른 사건의 확률에 영향을 줌

### 3. 조건부확률

조건부확률은 사건 B가 발생했다는 조건 아래에서 사건 A가 발생할 확률이다.

\[
P(A|B)=\frac{P(A\cap B)}{P(B)}
\]

예를 들어 전체 구매자가 아니라 광고를 본 고객 중 구매한 고객의 비율을 계산한다면 조건부확률에 해당한다.

```python
ad_viewers = 1000
paid_users_among_viewers = 300

p_paid_given_ad = paid_users_among_viewers / ad_viewers
print(p_paid_given_ad)
```

### 4. 베이즈 정리

새로운 정보를 얻었을 때 기존 확률을 갱신하는 방법이다.

\[
P(A|B)=\frac{P(B|A)P(A)}{P(B)}
\]

- `P(A)`: 사전확률 — 새로운 정보를 얻기 전의 확률
- `P(B|A)`: 가능도 — A일 때 B가 발생할 확률
- `P(B)`: 증거확률 — B가 발생할 전체 확률
- `P(A|B)`: 사후확률 — 새로운 정보 B를 반영한 뒤 갱신된 확률

#### 예시: 불량품 검사

- 전체 제품의 불량률: 10%
- 불량품을 검사기가 불량으로 판정할 확률: 90%
- 정상 제품을 불량으로 잘못 판정할 확률: 10%

검사 결과가 불량이라고 나왔더라도 실제 불량일 확률이 반드시 90%인 것은 아니다. 전체 제품에서 불량품이 차지하는 비율과 오탐 확률까지 함께 고려해야 한다.

```python
p_defective = 0.10
p_positive_given_defective = 0.90
p_positive_given_normal = 0.10
p_normal = 1 - p_defective

p_positive = (
    p_positive_given_defective * p_defective
    + p_positive_given_normal * p_normal
)

p_defective_given_positive = (
    p_positive_given_defective * p_defective / p_positive
)

print(p_defective_given_positive)
```

---

## 헷갈리기 쉬운 개념 비교

### 기술통계 vs 추론통계

| 구분 | 기술통계 | 추론통계 |
| --- | --- | --- |
| 목적 | 수집한 데이터 요약 | 표본으로 모집단 추정 |
| 대상 | 현재 확보한 데이터 | 모집단 전체 |
| 예시 | 평균 만족도 4.2점 | 전체 고객 만족도도 높을 것이라고 추정 |

### 평균 vs 중앙값 vs 최빈값

| 지표 | 의미 | 적합한 상황 |
| --- | --- | --- |
| 평균 | 모든 값을 더해 개수로 나눈 값 | 이상치가 크지 않은 수치형 데이터 |
| 중앙값 | 정렬했을 때 가운데 값 | 연봉·매출처럼 이상치가 큰 데이터 |
| 최빈값 | 가장 자주 등장한 값 | 결제수단·브랜드 등 범주형 데이터 |

### IQR vs Z-score

IQR과 Z-score는 같은 개념이 아니다.

| 구분 | IQR | Z-score |
| --- | --- | --- |
| 기준 | Q1과 Q3 | 평균과 표준편차 |
| 의미 | 가운데 50%의 범위 | 값이 평균에서 표준편차 몇 개만큼 떨어졌는지 |
| 이상치 기준 예시 | 하한·상한 밖의 값 | 일반적으로 절댓값이 3보다 큰 값 |
| 특징 | 이상치와 비대칭 분포에 비교적 강함 | 평균과 표준편차의 영향을 받음 |

### 독립사건 vs 배반사건

- 독립사건: 두 사건이 서로의 발생 확률에 영향을 주지 않는다. 동시에 발생할 수 있다.
- 배반사건: 두 사건이 동시에 발생할 수 없다.

두 개념은 서로 다르므로 혼동하지 않아야 한다.

---

## 암기 포인트

1. **기술통계는 데이터를 요약하고, 추론통계는 표본으로 모집단을 추정한다.**
2. **모집단은 전체, 표본은 전체에서 선택한 일부다.**
3. **평균은 이상치에 약하고 중앙값은 이상치에 비교적 강하다.**
4. **최빈값은 가장 자주 등장하는 값이며 여러 개일 수 있다.**
5. **양의 왜도는 오른쪽 꼬리, 음의 왜도는 왼쪽 꼬리다.**
6. **Q2는 50번째 백분위수이며 중앙값과 같다.**
7. **범위 = 최댓값 - 최솟값**
8. **분산은 편차 제곱의 평균, 표준편차는 분산의 제곱근이다.**
9. **IQR = Q3 - Q1**
10. **IQR 이상치 기준은 `Q1 - 1.5×IQR` 미만 또는 `Q3 + 1.5×IQR` 초과다.**
11. **CV = 표준편차 ÷ 평균 × 100**
12. **확률은 0부터 1 사이의 값이다.**
13. **여사건의 확률은 `1 - 원래 사건의 확률`이다.**
14. **조건부확률은 이미 주어진 조건 안에서 다시 비율을 계산하는 것이다.**
15. **베이즈 정리는 새로운 증거를 반영해 기존 확률을 갱신한다.**

### 함수 암기표

| 목적 | 코드 |
| --- | --- |
| 평균 | `Series.mean()` |
| 가중평균 | `np.average(data, weights=weights)` |
| 중앙값 | `Series.median()` |
| 최빈값 | `Series.mode()` |
| 왜도 | `Series.skew()` |
| 백분위수 | `np.percentile(data, 백분율)` |
| 분위수 | `Series.quantile(0.25)` |
| 분산 | `Series.var(ddof=0 또는 1)` |
| 표준편차 | `Series.std(ddof=0 또는 1)` |
| 범위 | `Series.max() - Series.min()` |

---

## 🌟 최종정리

통계는 데이터를 단순히 숫자로 계산하는 것이 아니라, 데이터를 요약하고 그 결과를 바탕으로 합리적인 판단을 내리는 과정이다. 기술통계는 평균·중앙값·최빈값과 산포도 등을 이용해 현재 데이터를 설명하고, 추론통계는 표본에서 얻은 결과를 모집단으로 확장한다. 대표값만으로는 데이터의 전체 모습을 알기 어려우므로 범위·분산·표준편차·IQR과 같은 산포도를 함께 확인해야 한다. 확률과 조건부확률, 베이즈 정리는 불확실한 상황에서 새로운 정보를 반영해 판단하는 기초가 된다.

## 💡 느낀점

평균 하나만 확인하면 데이터를 제대로 이해했다고 생각하기 쉽지만, 이상치나 분포에 따라 평균이 실제 모습을 왜곡할 수도 있다는 점을 알게 되었다. 중앙값과 IQR처럼 이상치에 비교적 강한 지표를 상황에 맞게 선택하는 것이 중요하며, 확률도 단순 계산보다 어떤 조건과 가정에서 나온 값인지 먼저 확인해야 한다.

## 📝 한줄

데이터를 숫자 하나로 요약하기 전에, 그 숫자가 어떤 데이터와 가정 위에 서 있는지 먼저 확인하자.

## 🌱 목표

평균·중앙값·최빈값과 범위·분산·표준편차·IQR을 데이터 특성에 맞게 선택하고, 조건부확률과 베이즈 정리를 실제 데이터 분석 문제에 적용할 수 있도록 반복해서 연습한다.
