# 📚 TIL - 데이터 전처리 & Pandas 기초 ~ 데이터 결합

## 📌 오늘 학습 범위

오늘은 데이터 전처리와 Pandas의 기본 개념부터 시작해서 데이터 확인, 필터링, 인덱스 설정, CSV 불러오기, 그리고 `concat()`과 `merge()`를 활용한 데이터 결합까지 학습했다.

진도 범위는 다음과 같다.

- 1장 1강: Pandas란?
- 1장 2강: 데이터 확인
- 1장 3강: 데이터 결합
  - `concat()`
  - `merge()`
  - `join(): 인덱스 기반 결합` 직전까지

---

# 1. 데이터 전처리란?

데이터 전처리(Data Preprocessing)는 분석이나 모델링을 하기 전에 데이터를 분석 가능한 형태로 정리하고 정제하는 과정이다.

단순히 이상한 값을 삭제하는 것이 아니라, 데이터가 왜 그렇게 수집되었는지 확인하고 분석 목적에 맞게 데이터를 정리하는 것이 중요하다.

예를 들어 고객 나이 데이터가 다음과 같다고 가정한다.

```python
ages = [25, 31, 28, 999, 35, -1]
```

여기서 `999`, `-1`은 정상적인 나이라고 보기 어렵다.

이 값을 그대로 평균 계산에 사용하면 결과가 크게 왜곡된다.

```python
average_age = sum(ages) / len(ages)
print(average_age)
```

실제 결과:

```text
186.16666666666666
```

이상치를 제거한 데이터:

```python
clean_ages = [25, 31, 28, 35]

average_age = sum(clean_ages) / len(clean_ages)

print(clean_ages)
print(average_age)
```

결과:

```text
[25, 31, 28, 35]
29.75
```

즉, 데이터 전처리를 하지 않으면 실제 상황과 전혀 맞지 않는 결과가 나올 수 있다.

---

## 💡 이상치가 평균에 미치는 영향

### 예제 1: 고객 나이

```python
import pandas as pd

age_raw = pd.Series([25, 30, 28, 999, 32, -1])
age_clean = pd.Series([25, 30, 28, 32])

print('age_raw:', age_raw.mean())
print('age_clean:', age_clean.mean())
```

결과:

```text
age_raw: 185.5
age_clean: 28.75
```

이상치가 포함되면 평균이 크게 왜곡된다.

---

### 예제 2: 구매 금액

```python
purchase_raw = pd.Series([45000, 52000, 48000, 55000, 50000, 1000000])
purchase_clean = pd.Series([45000, 52000, 48000, 55000, 50000])

print('이상치 포함 평균 구매 금액:', purchase_raw.mean())
print('이상치 제외 평균 구매 금액:', purchase_clean.mean())
```

이 문제는 정상 구매금액 사이에 매우 큰 값인 `1000000`이 포함되었을 때 평균이 얼마나 영향을 받는지 확인하는 예제이다.

---

# 2. Pandas란?

Pandas는 Python에서 표 형태의 데이터를 다루기 위한 대표적인 라이브러리이다.

Excel이나 CSV처럼 행과 열로 이루어진 데이터를 불러오고, 정리하고, 분석할 수 있다.

Pandas는 일반적으로 다음과 같이 불러온다.

```python
import pandas as pd
```

여기서 `pd`는 pandas를 짧게 부르기 위한 별칭이다.

---

# 3. DataFrame과 Series

Pandas에는 대표적으로 두 가지 데이터 구조가 있다.

## DataFrame

DataFrame은 행과 열로 구성된 2차원 표 형태의 데이터이다.

예시:

```python
import pandas as pd

orders = pd.DataFrame({
    'order_id': [101, 102, 103],
    'product': ['노트북', '마우스', '키보드'],
    'price': [1200000, 35000, 89000]
})

print(orders)
```

---

## Series

Series는 1차원 데이터 구조이다.

DataFrame에서 하나의 컬럼만 선택하면 일반적으로 Series가 된다.

```python
product_series = orders['product']

print(product_series)
print(type(product_series))
```

---

## DataFrame과 Series 차이

```python
df['product']
```

결과:

```text
Series
```

반면,

```python
df[['product']]
```

결과:

```text
DataFrame
```

### ⭐ 암기

```text
df['컬럼']   → Series
df[['컬럼']] → DataFrame
```

대괄호가 한 번이면 Series, 두 번이면 DataFrame으로 이해하면 된다.

---

# 4. CSV 파일 불러오기

Pandas에서는 `read_csv()`를 사용하여 CSV 파일을 불러온다.

```python
import pandas as pd

df = pd.read_csv("ecommerce_sales_analytics_5000.csv")
```

`read_csv`는 이름 그대로 다음과 같이 이해할 수 있다.

```text
read = 읽다
csv = CSV 파일
```

즉,

```python
pd.read_csv()
```

는 Pandas를 이용하여 CSV 파일을 읽어오는 기능이다.

---

# 5. CSV 파일 저장하기

DataFrame을 CSV 파일로 저장하려면 `to_csv()`를 사용한다.

```python
df.to_csv("processed_orders.csv", index=False)
```

`index=False`를 사용하면 DataFrame의 인덱스가 별도의 컬럼으로 CSV에 저장되지 않는다.

### ⭐ 암기

```text
pd.read_csv() → CSV 파일 불러오기
df.to_csv()   → CSV 파일 저장하기
```

---

# 6. 데이터 기본 확인

CSV 파일을 불러온 뒤에는 데이터가 정상적으로 들어왔는지 먼저 확인해야 한다.

## head()

데이터 앞부분 확인

```python
df.head()
```

기본적으로 앞에서부터 5개의 행을 보여준다.

---

## tail()

데이터 뒷부분 확인

```python
df.tail()
```

기본적으로 마지막 5개의 행을 보여준다.

---

## shape

행과 열의 개수 확인

```python
df.shape
```

결과 예시:

```text
(5000, 12)
```

의미:

```text
5000행, 12열
```

### ⭐ 암기

```text
shape = (행 개수, 열 개수)
```

---

## columns

컬럼 이름 확인

```python
df.columns
```

---

## dtypes

각 컬럼의 데이터 타입 확인

```python
df.dtypes
```

---

## info()

데이터의 전체적인 구조 확인

```python
df.info()
```

`info()`를 통해 다음과 같은 정보를 확인할 수 있다.

- 행 개수
- 컬럼 개수
- 컬럼명
- 결측치 여부
- 데이터 타입

---

# 7. 데이터 타입 변환

데이터를 불러왔을 때 숫자가 문자열로 저장되어 있거나 날짜가 일반 문자열로 저장되어 있을 수 있다.

## astype()

자료형 변경

```python
df['price'] = df['price'].astype(int)
```

---

## pd.to_datetime()

날짜 타입으로 변환

```python
df['sale_date'] = pd.to_datetime(df['sale_date'])
```

### ⭐ 암기

```text
astype()       → 자료형 변경
pd.to_datetime() → 날짜형으로 변환
```

---

# 8. 데이터 분포 확인

## describe()

수치형 데이터의 기본 통계량을 확인한다.

```python
df.describe()
```

주요 항목:

```text
count → 데이터 개수
mean  → 평균
std   → 표준편차
min   → 최솟값
25%   → 1사분위수
50%   → 중앙값
75%   → 3사분위수
max   → 최댓값
```

---

## value_counts()

각 값이 몇 번 등장하는지 확인한다.

```python
df['product_category'].value_counts()
```

---

## nunique()

서로 다른 값의 개수를 확인한다.

```python
df['product_category'].nunique()
```

### ⭐ value_counts() vs nunique()

```text
value_counts()
→ 각각의 값이 몇 번 등장했는지 확인

nunique()
→ 서로 다른 값이 총 몇 개인지 확인
```

예를 들어:

```text
A, A, B, C, C
```

이라면,

```text
value_counts()

A    2
B    1
C    2
```

그리고,

```text
nunique()

3
```

---

# 9. 데이터 선택과 필터링

## loc

`loc`는 라벨 또는 컬럼명을 기준으로 데이터를 선택한다.

```python
df.loc[5:9, ['payment_method', 'delivery_days', 'customer_rating']]
```

---

## iloc

`iloc`는 실제 위치 번호를 기준으로 데이터를 선택한다.

```python
df.iloc[5:10, [8, 9, 10]]
```

---

## loc와 iloc 차이

```text
loc  → 라벨 기준
iloc → 위치 번호 기준
```

슬라이싱에서도 차이가 있다.

```text
iloc → 끝 번호 미포함
loc  → 끝 값 포함
```

예를 들어 5~9번 행을 선택하려면:

```python
df.iloc[5:10]
```

또는

```python
df.loc[5:9]
```

를 사용한다.

### ⭐ 암기

```text
iloc = Python 슬라이싱처럼 끝 미포함
loc  = 끝값 포함
```

실무에서는 세부 문법을 매번 전부 외우기보다 필요할 때 검색해서 확인하는 경우가 많다.

따라서 우선은 다음 정도를 기억하는 것이 중요하다.

```text
loc  = 이름/라벨 기준
iloc = 위치 기준
```

---

# 10. Boolean Indexing

조건을 이용하여 원하는 데이터만 선택할 수 있다.

```python
df[df['point'] >= 3000]
```

여러 조건을 사용할 경우 Pandas에서는 `and`, `or` 대신 `&`, `|`를 사용한다.

```python
df[(조건1) & (조건2)]
```

```python
df[(조건1) | (조건2)]
```

### ⭐ 암기

```text
& → AND
| → OR
```

---

# 11. Index 이해하기

Index는 각 행을 구분하는 라벨이다.

기본적으로 DataFrame은 다음과 같은 인덱스를 가진다.

```text
0
1
2
3
4
...
```

---

## set_index()

특정 컬럼을 인덱스로 설정한다.

```python
df_indexed = df.set_index('customer_id')

df_indexed.head()
```

---

## reset_index()

설정했던 인덱스를 다시 일반 컬럼으로 되돌린다.

```python
df_restored = df_indexed.reset_index()

df_restored.head()
```

### ⭐ 암기

```text
set_index()   → 특정 컬럼을 인덱스로 설정
reset_index() → 인덱스를 다시 일반 컬럼으로 복구
```

---

# 12. concat(): 데이터 단순 연결

`concat()`은 여러 DataFrame을 단순하게 이어붙일 때 사용한다.

예를 들어 7월 주문과 8월 주문 데이터를 합치는 경우 사용할 수 있다.

```python
import pandas as pd

july_orders = pd.DataFrame({
    "order_id": [101, 102],
    "product": ["노트북", "마우스"]
})

august_orders = pd.DataFrame({
    "order_id": [103, 104],
    "product": ["키보드", "모니터"]
})

all_orders = pd.concat(
    [july_orders, august_orders],
    axis=0,
    ignore_index=True
)

print(all_orders)
```

결과:

```text
   order_id product
0       101   노트북
1       102   마우스
2       103   키보드
3       104   모니터
```

---

# 13. axis 이해하기

`axis`는 데이터를 어느 방향으로 연결할지 정하는 옵션이다.

## axis=0

위아래로 붙인다.

```python
pd.concat([df1, df2], axis=0)
```

의미:

```text
df1
↓
df2
```

행 방향으로 데이터가 늘어난다.

---

## axis=1

옆으로 붙인다.

```python
pd.concat([df1, df2], axis=1)
```

열 방향으로 데이터가 늘어난다.

### ⭐ 암기

```text
axis=0 → 위아래
axis=1 → 옆으로
```

---

# 14. ignore_index=True

두 DataFrame을 그냥 합치면 각각 가지고 있던 기존 인덱스가 그대로 유지될 수 있다.

예를 들어:

```text
df1

0
1

df2

0
1
```

그냥 합치면:

```text
0
1
0
1
```

처럼 인덱스가 중복될 수 있다.

이때:

```python
ignore_index=True
```

를 사용하면 새로운 인덱스를 다시 만들어준다.

결과:

```text
0
1
2
3
```

### ⭐ 암기

```text
ignore_index=True
→ 기존 인덱스를 무시하고 0부터 다시 번호 매기기
```

---

# 15. concat() 실습

온라인 주문과 오프라인 주문 데이터를 위아래로 합쳤다.

```python
import pandas as pd

df_online = pd.read_csv('./ecommerce_orders_online.csv')
df_offline = pd.read_csv('./ecommerce_orders_offline.csv')

print(df_online.shape, df_offline.shape)

df_orders_all = pd.concat(
    [df_online, df_offline],
    axis=0,
    ignore_index=True
)

print(df_orders_all.shape)
```

결과:

```text
(2500, 12) (2500, 12)
(5000, 12)
```

즉,

```text
온라인 주문 2500행
+
오프라인 주문 2500행
=
전체 주문 5000행
```

열은 동일하게 12개이다.

---

# 16. df_orders_all은 무엇인가?

`df_orders_all`은 Pandas에서 정해진 명령어가 아니다.

사용자가 직접 만든 변수명이다.

```python
df_orders_all = pd.concat(...)
```

의 의미는:

```text
합쳐진 전체 주문 데이터를
df_orders_all이라는 변수에 저장한다.
```

변수 이름은 다른 이름으로 바꿔도 실행 자체에는 문제가 없다.

예:

```python
banana = pd.concat([df_online, df_offline])
```

하지만 실제 코드에서는 데이터를 알아보기 쉽도록 의미 있는 변수명을 사용한다.

---

# 17. merge(): 키를 기준으로 데이터 결합

`merge()`는 서로 다른 DataFrame에서 공통으로 존재하는 컬럼을 기준으로 데이터를 연결할 때 사용한다.

SQL의 JOIN과 비슷한 개념이다.

예:

```python
result = pd.merge(
    orders,
    users,
    on='user_id',
    how='left'
)
```

---

## on

어떤 컬럼을 기준으로 데이터를 연결할지 지정한다.

```python
on='user_id'
```

즉,

```text
user_id가 같은 데이터를 찾아서 연결
```

---

## how

어떤 데이터를 남길지 정하는 옵션이다.

주요 JOIN 방식:

```text
inner
left
right
outer
```

---

## inner

양쪽 데이터에 모두 존재하는 값만 남긴다.

```python
pd.merge(df1, df2, on='id', how='inner')
```

---

## left

왼쪽 DataFrame의 데이터는 모두 유지하고 오른쪽에서 일치하는 데이터를 가져온다.

```python
pd.merge(df1, df2, on='id', how='left')
```

---

## right

오른쪽 DataFrame의 데이터는 모두 유지하고 왼쪽에서 일치하는 데이터를 가져온다.

```python
pd.merge(df1, df2, on='id', how='right')
```

---

## outer

양쪽 DataFrame의 데이터를 모두 유지한다.

```python
pd.merge(df1, df2, on='id', how='outer')
```

---

# 18. concat()과 merge() 차이

| 구분 | concat() | merge() |
|---|---|---|
| 목적 | 데이터 단순 연결 | 공통 키를 기준으로 결합 |
| 기준 | 위치/축 | 특정 컬럼 |
| 주요 옵션 | axis, ignore_index | on, how |
| 예시 | 월별 주문 데이터 합치기 | 주문 정보 + 고객 정보 연결 |
| SQL 느낌 | 단순 이어붙이기 | JOIN |

### ⭐ 핵심 암기

```text
concat = 그냥 이어붙인다
merge  = 공통 키를 찾아서 붙인다
```

---

# ⚠️ 오늘 발생한 오류와 해결

## 1. FileNotFoundError

오류:

```text
FileNotFoundError
No such file or directory
```

원인:

```python
df = pd.read_csv("ecommerce_sales_analytics_5000.csv")
```

를 실행했지만 현재 작업폴더에 CSV 파일이 없었다.

현재 작업폴더 확인:

```python
import os

print(os.getcwd())
print(os.listdir())
```

파일 존재 여부 확인:

```python
print(os.path.exists("ecommerce_sales_analytics_5000.csv"))
```

결과:

```text
False
```

해결:

CSV 파일을 현재 작업폴더인

```text
C:\data-process
```

안으로 복사했다.

다시 확인:

```python
print(os.path.exists("ecommerce_sales_analytics_5000.csv"))
```

결과:

```text
True
```

이후 강의 코드 그대로 사용 가능해졌다.

```python
df = pd.read_csv("ecommerce_sales_analytics_5000.csv")
```

### ⭐ 오류 암기

```text
FileNotFoundError
→ 파일 위치/경로부터 확인
```

---

## 2. Windows 경로 SyntaxWarning

전체 경로를 다음과 같이 입력했을 때:

```python
df = pd.read_csv("C:\data-process-visualization\ecommerce_sales_analytics_5000.csv")
```

다음과 같은 경고가 발생했다.

```text
SyntaxWarning: invalid escape sequence
```

Windows 경로에 있는 `\`가 escape sequence로 해석될 수 있기 때문이다.

해결 방법 1:

```python
df = pd.read_csv(
    r"C:\data-process-visualization\ecommerce_sales_analytics_5000.csv"
)
```

문자열 앞에 `r`을 붙인다.

해결 방법 2:

```python
df = pd.read_csv(
    "C:/data-process-visualization/ecommerce_sales_analytics_5000.csv"
)
```

`\` 대신 `/`를 사용할 수도 있다.

---

## 3. NameError: pd is not defined

오류:

```text
NameError: name 'pd' is not defined
```

원인:

```python
pd.Series(...)
```

를 사용하기 전에 Pandas를 불러오지 않았다.

해결:

```python
import pandas as pd
```

를 먼저 실행한다.

### ⭐ 암기

새 노트북이나 커널 재시작 후에는 필요한 라이브러리를 다시 import해야 한다.

---

## 4. SyntaxError: unterminated string literal

오류 원인:

`print()`문에서 따옴표 범위를 잘못 설정했다.

문제가 된 형태:

```python
print('purchase_clean = pd.Series([...])
:', purchase_clean.mean())
```

문자열을 시작한 따옴표가 제대로 닫히지 않아 오류가 발생했다.

올바른 형태:

```python
print('purchase_clean:', purchase_clean.mean())
```

또는:

```python
print('이상치 제외 평균 구매 금액:', purchase_clean.mean())
```

### ⭐ 암기

```text
따옴표 안 → 화면에 보여줄 문자
따옴표 밖 → 실제 실행할 코드
```

예:

```python
print('설명:', 변수.mean())
```

---

## 5. KeyError: customer_id가 없음

오류:

```text
KeyError: "None of ['customer_id'] are in the columns"
```

문제가 된 코드:

```python
df_indexed = df.set_index('customer_id')
```

현재 `df.columns`를 확인했더니:

```text
Index(['employee_id', 'name', 'department'], dtype='str')
```

즉 현재 `df`는 쇼핑몰 데이터가 아니라 다른 예제용 직원 DataFrame으로 덮어쓰기 된 상태였다.

원인:

```python
df = pd.DataFrame(...)
```

형태로 같은 변수명 `df`를 다시 사용하면서 기존 CSV DataFrame이 사라졌다.

해결:

```python
df = pd.read_csv("ecommerce_sales_analytics_5000.csv")
```

로 원본 데이터를 다시 불러왔다.

그 후:

```python
df_indexed = df.set_index('customer_id')
```

정상 실행되었다.

### ⭐ 암기

```text
df = 새로운데이터
```

를 실행하면 기존 `df`는 덮어쓰기 된다.

원하는 컬럼이 갑자기 없으면:

```python
print(df.columns)
```

부터 확인한다.

---

## 6. NameError: df_indexed가 정의되지 않음

오류:

```text
NameError: name 'df_indexed' is not defined
```

원인:

앞 단계인

```python
df_indexed = df.set_index('customer_id')
```

가 `KeyError`로 실패했기 때문에 `df_indexed`라는 변수가 생성되지 않았다.

즉:

```text
첫 번째 코드 실패
↓
변수 생성 실패
↓
다음 코드에서 변수 사용
↓
NameError
```

앞 단계 오류를 해결한 후 다시 실행하니 정상 작동했다.

---

## 7. loc에서 KeyError 발생

문제가 된 컬럼명:

```python
'customer_rating '
```

실제 컬럼명:

```python
'customer_rating'
```

컬럼명 뒤에 공백 한 칸이 들어가 있어 `KeyError`가 발생했다.

해결:

```python
print(
    df.loc[
        5:9,
        ['payment_method', 'delivery_days', 'customer_rating']
    ]
)
```

### ⭐ 암기

컬럼명 오류가 의심되면:

```python
print(df.columns)
```

로 정확한 컬럼명을 확인한다.

---

# 🧠 오늘 암기 포인트

## 1. 상황 → 떠올릴 함수

```text
CSV 파일 읽기
→ pd.read_csv()

데이터 앞부분 보기
→ head()

데이터 크기 확인
→ shape

컬럼명 확인
→ columns

전체 구조 확인
→ info()

통계 확인
→ describe()

값별 개수 확인
→ value_counts()

고유값 개수 확인
→ nunique()

라벨 기준 선택
→ loc

위치 기준 선택
→ iloc

특정 컬럼을 인덱스로
→ set_index()

인덱스를 다시 일반 컬럼으로
→ reset_index()

데이터 단순 연결
→ concat()

공통 키를 기준으로 연결
→ merge()
```

---

## 2. Series vs DataFrame

```text
df['컬럼']
→ Series

df[['컬럼']]
→ DataFrame
```

---

## 3. loc vs iloc

```text
loc
→ 라벨 기준
→ 슬라이싱 끝 포함

iloc
→ 위치 기준
→ 슬라이싱 끝 미포함
```

---

## 4. value_counts vs nunique

```text
value_counts()
→ 각각 몇 번 나왔는지

nunique()
→ 서로 다른 값이 몇 개인지
```

---

## 5. set_index vs reset_index

```text
set_index()
→ 컬럼을 인덱스로 변경

reset_index()
→ 인덱스를 다시 일반 컬럼으로 복구
```

---

## 6. concat 핵심

```text
concat
→ 데이터 단순 연결

axis=0
→ 위아래로 연결

axis=1
→ 옆으로 연결

ignore_index=True
→ 기존 인덱스를 무시하고 0부터 다시 번호 부여
```

---

## 7. merge 핵심

```text
merge
→ 공통 키를 기준으로 데이터 결합

on
→ 어떤 컬럼을 기준으로 합칠지

how
→ 어떤 데이터를 남길지
```

JOIN 방식:

```text
inner → 양쪽에 모두 있는 값
left  → 왼쪽 데이터 기준
right → 오른쪽 데이터 기준
outer → 양쪽 데이터 모두
```

---

## 8. concat vs merge

```text
concat
→ 그냥 이어붙이기

merge
→ 같은 키를 찾아서 연결하기
```

---

## 9. 오류가 났을 때 먼저 확인할 것

```text
FileNotFoundError
→ 파일 경로 확인

KeyError
→ 컬럼명 확인

NameError
→ 변수 또는 import가 생성/실행됐는지 확인

SyntaxError
→ 괄호, 따옴표, 쉼표 확인
```

---

# 🌟 오늘 최종 정리

오늘은 Pandas를 이용해 데이터를 불러오고 확인하는 가장 기본적인 흐름부터 데이터 결합까지 학습했다.

전체 흐름은 다음과 같이 정리할 수 있다.

```text
CSV 불러오기
↓
데이터 구조 확인
↓
자료형 및 분포 확인
↓
필요한 행/열 선택
↓
인덱스 설정
↓
여러 DataFrame 연결
```

특히 오늘 가장 중요했던 개념은 다음과 같다.

```text
read_csv
→ CSV 읽기

DataFrame
→ 2차원 표

Series
→ 1차원 데이터

loc
→ 라벨 기준

iloc
→ 위치 기준

concat
→ 데이터를 단순히 이어붙임

merge
→ 공통 키를 기준으로 데이터를 연결
```

세부 문법을 모두 외우는 것보다 먼저 중요한 것은 문제에서 무엇을 하려는지 파악하고 필요한 기능을 떠올리는 것이다.

예를 들어:

```text
"두 데이터를 위아래로 합쳐라"
→ concat

"customer_id가 같은 데이터를 연결하라"
→ merge

"CSV 파일을 불러와라"
→ read_csv

"앞부분을 확인하라"
→ head
```

처럼 상황과 기능을 연결해서 기억하는 것이 중요하다.

---

# 💡 오늘 느낀 점

처음 보는 메서드와 옵션이 많아서 코드를 한 번에 외우려고 하면 어렵게 느껴졌다.

하지만 `axis`, `ignore_index`, `shape` 같은 단어도 코드 전체를 암기하는 것이 아니라 각 기능이 무엇을 의미하는지 이해하면 조금씩 읽히기 시작했다.

또한 오늘 발생한 오류 대부분은 Pandas 자체의 어려운 문제라기보다 파일 경로, 변수 덮어쓰기, 컬럼명의 공백, 따옴표 위치처럼 작은 부분에서 발생했다.

오류가 발생했을 때 무작정 코드를 다시 쓰기보다 에러 이름을 보고 원인을 하나씩 확인하는 습관이 중요하다는 것을 알게 되었다.

---

# 📝 오늘의 한 줄

**Pandas 문법을 통째로 외우기보다, “무엇을 하고 싶은지 → 어떤 기능을 쓰는지”를 연결해서 기억하자.**

---

# 🌱 앞으로의 목표

다음 학습에서는 `join()`을 포함해 데이터 결합 방법을 더 익히고, `concat`, `merge`, `join`의 차이를 실제 데이터 예제로 구분할 수 있도록 연습한다.

또한 새로운 메서드가 등장했을 때 모든 문법을 외우려고 하기보다, 먼저 해당 메서드의 역할과 언제 사용하는지를 이해하는 방식으로 학습한다.


  
