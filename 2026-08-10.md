# 📚 TIL - Python 자료구조와 조건문

## 오늘 배운 내용

- 리스트(List)와 튜플(Tuple)
- 인덱싱(Indexing)과 슬라이싱(Slicing)
- 시퀀스 자료구조의 공통 연산
- 패킹(Packing)과 언패킹(Unpacking)
- 딕셔너리(Dictionary)
- 집합(Set)
- 자료구조 간 변환
- 조건문 `if`, `elif`, `else`
- 논리 연산자 `and`, `or`

---

## 1. List vs Tuple

리스트와 튜플은 모두 여러 데이터를 순서대로 저장하는 **시퀀스 자료구조**이다.

가장 큰 차이는 **데이터를 변경할 수 있는지 여부**이다.

| List | Tuple |
|---|---|
| `[]` 사용 | `()` 사용 |
| 변경 가능 (Mutable) | 변경 불가 (Immutable) |
| 추가/수정/삭제 가능 | 생성 후 수정 불가 |

```python
list_data = [10, 20, 30]
list_data[0] = 100

tuple_data = (10, 20, 30)
# tuple_data[0] = 100  → TypeError
```

**핵심**
- List = 변경 가능
- Tuple = 변경 불가

---

## 2. Indexing vs Slicing

### Indexing

인덱싱은 특정 위치의 **데이터 하나**를 가져오는 방법이다.

```python
data = [10, 20, 30, 40, 50]

data[0]   # 10
data[-1]  # 50
```

인덱스는 앞에서 접근할 때 `0`부터 시작하고,
뒤에서 접근할 때 `-1`부터 시작한다.

```text
값          10   20   30   40   50
앞 인덱스     0    1    2    3    4
뒤 인덱스    -5   -4   -3   -2   -1
```

### 주의할 점

사람이 말하는 **몇 번째**와 파이썬의 **인덱스 번호**는 다르다.

- 첫 번째 값 → 인덱스 `0`
- 두 번째 값 → 인덱스 `1`
- 마지막 값 → 인덱스 `-1`

---

### Slicing

슬라이싱은 데이터의 **일정 범위**를 가져오는 방법이다.

```python
data[start:end:step]
```

가장 중요한 규칙은 다음과 같다.

> **시작 인덱스는 포함하고, 끝 인덱스는 포함하지 않는다.**

```python
data = [10, 20, 30, 40, 50]

data[1:4]
# [20, 30, 40]
```

인덱스 `1`부터 시작해서
인덱스 `4`의 **직전까지** 가져온다.

즉 실제로 가져오는 인덱스는 `1, 2, 3`이다.

### 몇 번째 데이터와 인덱스 구분

두 번째부터 네 번째 데이터:

```python
data[1:4]
# [20, 30, 40]
```

첫 번째부터 네 번째 데이터:

```python
data[:4]
# [10, 20, 30, 40]
```

### Step

슬라이싱에서는 세 번째 값으로 이동 간격을 지정할 수 있다.

```python
data[::2]
# 2칸 간격으로 가져오기
```

```python
data[::-1]
# 전체 데이터를 역순으로 가져오기
```

### 슬라이싱 핵심

```text
[start : end : step]

start → 포함
end   → 미포함
step  → 이동 간격
```

---

## 3. 시퀀스 자료구조의 공통 연산

### 연결 `+`

```python
[1, 2] + [3, 4]
# [1, 2, 3, 4]
```

### 반복 `*`

```python
[1, 2] * 3
# [1, 2, 1, 2, 1, 2]
```

### 길이 확인 `len()`

```python
len([10, 20, 30])
# 3
```

### 포함 여부 `in`, `not in`

```python
20 in [10, 20, 30]
# True
```

```python
40 not in [10, 20, 30]
# True
```

| 연산자 | 의미 |
|---|---|
| `in` | 값이 포함되어 있는가? |
| `not in` | 값이 포함되어 있지 않은가? |

---

## 4. Packing vs Unpacking

### Packing

여러 개의 값을 하나로 묶는 것을 패킹이라고 한다.

```python
data = 10, 20, 30

print(data)
# (10, 20, 30)
```

여러 값이 하나의 튜플로 묶인다.

### Unpacking

묶여 있는 데이터를 각각의 변수에 나누는 것을 언패킹이라고 한다.

```python
data = (10, 20, 30)

a, b, c = data

print(a)  # 10
print(b)  # 20
print(c)  # 30
```

### `*`를 이용한 언패킹

```python
first, *others = [10, 20, 30, 40]

print(first)
# 10

print(others)
# [20, 30, 40]
```

`*변수`를 사용하면 **남은 값을 한꺼번에 받을 수 있다.**

**핵심**
- Packing = 여러 값을 하나로 묶기
- Unpacking = 묶인 값을 각각 나누기
- `*변수` = 남은 값 전부 받기

---

## 5. Dictionary

딕셔너리는 데이터를 **Key : Value** 형태로 저장하는 자료구조이다.

```python
user = {
    "name": "sparta",
    "age": 20
}
```

- `"name"` → Key
- `"sparta"` → Value

리스트는 인덱스로 값을 찾지만,
딕셔너리는 **Key를 이용해서 Value를 찾는다.**

```python
user["name"]
# 'sparta'
```

### List vs Dictionary

| List | Dictionary |
|---|---|
| 인덱스로 접근 | Key로 접근 |
| `data[0]` | `data["name"]` |
| 순서대로 데이터 관리 | Key와 Value의 관계로 관리 |

딕셔너리는 **변경 가능한 자료구조(Mutable)**이다.

```python
user["age"] = 21
user["job"] = "student"
```

같은 Key에 새로운 값을 저장하면 기존 값이 변경된다.

---

## 6. Dictionary 주요 메서드

```python
menu = {
    "coffee": 4000,
    "tea": 4500
}
```

### `keys()`

Key만 가져온다.

```python
menu.keys()
```

### `values()`

Value만 가져온다.

```python
menu.values()
```

### `items()`

Key와 Value를 한 쌍으로 가져온다.

```python
menu.items()
```

| 메서드 | 가져오는 값 |
|---|---|
| `keys()` | Key |
| `values()` | Value |
| `items()` | Key + Value |

---

## 7. `[]` vs `get()`

둘 다 딕셔너리에서 값을 가져올 때 사용하지만,
존재하지 않는 Key를 조회했을 때 차이가 있다.

```python
menu["juice"]
```

존재하지 않는 Key라면 `KeyError`가 발생한다.

반면:

```python
menu.get("juice")
```

존재하지 않는 Key라면 `None`을 반환한다.

기본값도 지정할 수 있다.

```python
menu.get("juice", 5000)
# 5000
```

| `[]` | `.get()` |
|---|---|
| Key가 없으면 오류 | Key가 없어도 오류 X |
| `KeyError` 발생 | `None` 또는 지정한 기본값 반환 |

---

## 8. Set

집합(Set)의 가장 중요한 특징은 다음 두 가지이다.

> **중복 없음 + 순서 없음**

```python
numbers = {1, 2, 2, 3, 3}

print(numbers)
# {1, 2, 3}
```

중복된 값이 자동으로 제거된다.

Set은 순서가 없는 자료구조이기 때문에
리스트처럼 인덱싱이나 슬라이싱을 사용할 수 없다.

```python
# numbers[0]  → 불가능
```

---

## 9. Set 연산

```python
A = {1, 2, 3}
B = {3, 4, 5}
```

### 교집합 `&`

두 집합에 모두 포함된 값

```python
A & B
# {3}
```

### 합집합 `|`

두 집합의 모든 값

```python
A | B
# {1, 2, 3, 4, 5}
```

### 차집합 `-`

A에는 있지만 B에는 없는 값

```python
A - B
# {1, 2}
```

### 포함 여부 확인

```python
3 in A
# True
```

---

## 10. 자료구조 간 변환

`list()`, `tuple()`, `set()`을 사용하여
자료구조를 서로 변환할 수 있다.

```python
data = ["a", "b", "b", "c"]

unique_data = set(data)
```

결과:

```python
{"a", "b", "c"}
```

Set으로 변환하면 중복값이 제거된다.

다시 리스트로 바꿀 수도 있다.

```python
result = list(unique_data)
```

즉 중복 제거가 필요한 경우:

```text
List → Set → 중복 제거 → List
```

형태로 사용할 수 있다.

### 주의

Set은 순서가 없는 자료구조이기 때문에
`set()`으로 변환하면 **기존 데이터의 순서는 보장되지 않는다.**

---

# 11. 조건문 `if`

조건문은 특정 조건이 `True`일 때 코드를 실행한다.

```python
age = 25

if age >= 20:
    print("성인")
```

조건식 뒤에는 반드시 `:`을 작성한다.

---

## 12. 들여쓰기(Indentation)

파이썬은 들여쓰기를 이용하여
어떤 코드가 조건문 안에 포함되는지 구분한다.

```python
if age >= 20:
    print("성인")
```

일반적으로 **공백 4칸**을 사용한다.

들여쓰기가 올바르지 않으면 오류가 발생할 수 있다.

---

## 13. `if / elif / else`

```python
if 조건1:
    실행문

elif 조건2:
    실행문

else:
    실행문
```

- `if` → 첫 번째 조건 확인
- `elif` → 앞 조건이 False일 경우 추가 조건 확인
- `else` → 앞의 모든 조건이 False일 경우 실행

`else`에는 별도의 조건식을 작성하지 않는다.

---

## 14. 조건문의 실행 순서

`if - elif - else`는 **위에서 아래로 조건을 확인한다.**

그리고 처음으로 `True`가 된 블록을 실행하면
나머지 조건은 확인하지 않고 종료한다.

예를 들어:

```python
score = 95

if score >= 80:
    print("B학점")

elif score >= 90:
    print("A학점")
```

결과는:

```text
B학점
```

95는 90 이상이지만,
그 전에 있는 `score >= 80` 조건도 True이기 때문이다.

따라서 다음과 같이 작성해야 한다.

```python
if score >= 90:
    print("A학점")

elif score >= 80:
    print("B학점")

else:
    print("C학점 이하")
```

### 핵심

> 조건 범위가 겹치는 경우 **더 구체적인 조건을 먼저 작성한다.**

---

## 15. 중첩 조건문

조건문 안에 또 다른 조건문을 작성할 수도 있다.

```python
if is_logged_in:
    if has_permission:
        print("관리자 페이지")
```

하지만 중첩 조건문이 너무 많아지면
코드가 복잡하고 읽기 어려워질 수 있다.

---

## 16. `and` vs `or`

여러 조건을 하나의 조건식으로 연결할 때 사용한다.

### `and`

모든 조건이 True여야 전체 결과가 True이다.

```python
if is_logged_in and has_permission:
    print("관리자 페이지")
```

### `or`

조건 중 하나라도 True이면 전체 결과가 True이다.

```python
if age <= 7 or age >= 65:
    print("무료 입장")
```

| 연산자 | 의미 |
|---|---|
| `and` | 모든 조건을 만족 |
| `or` | 하나라도 만족 |

중첩 조건문을 여러 번 사용하는 대신
`and`, `or`를 사용하면 조건식을 더 간결하게 작성할 수 있다.

---

## 17. 단락 회로 평가 (Short-Circuit Evaluation)

논리 연산에서는 앞의 조건만 확인해도 결과가 결정되는 경우
뒤의 조건을 확인하지 않을 수 있다.

### `and`

```python
False and 조건
```

앞의 값이 이미 False이므로
전체 결과는 무조건 False이다.

따라서 뒤 조건을 확인할 필요가 없다.

### `or`

```python
True or 조건
```

앞의 값이 이미 True이므로
전체 결과는 무조건 True이다.

따라서 뒤 조건을 확인할 필요가 없다.

---

# 오늘 헷갈렸던 부분

## 1. 사람의 '몇 번째'와 파이썬 인덱스

사람은 첫 번째를 `1`이라고 세지만,
파이썬 인덱스는 `0`부터 시작한다.

```text
첫 번째 값 → index 0
두 번째 값 → index 1
세 번째 값 → index 2
```

뒤에서부터 접근할 때는 `-1`부터 시작한다.

```text
마지막 값 → index -1
뒤에서 두 번째 값 → index -2
```

---

## 2. 슬라이싱의 시작과 끝

```python
data[1:4]
```

여기서 `1`은 포함하지만 `4`는 포함하지 않는다.

따라서 실제로 가져오는 인덱스는:

```text
1, 2, 3
```

> **슬라이싱 = 시작 포함 / 끝 미포함**

---

## 3. Indexing vs Slicing

| Indexing | Slicing |
|---|---|
| 데이터 하나 | 데이터 범위 |
| `data[1]` | `data[1:4]` |

---

## 4. List vs Tuple

| List | Tuple |
|---|---|
| 변경 가능 | 변경 불가 |
| Mutable | Immutable |

---

## 5. Packing vs Unpacking

| Packing | Unpacking |
|---|---|
| 여러 값을 하나로 묶음 | 묶인 값을 각각 나눔 |
| `data = 10, 20, 30` | `a, b, c = data` |

---

## 6. `keys()` vs `values()` vs `items()`

| 메서드 | 의미 |
|---|---|
| `keys()` | Key만 |
| `values()` | Value만 |
| `items()` | Key와 Value 모두 |

---

## 7. `[]` vs `.get()`

| `[]` | `.get()` |
|---|---|
| 없는 Key 조회 시 오류 | 없는 Key 조회 시 오류 없음 |
| `KeyError` | `None` 또는 기본값 |

---

## 8. List vs Set

| List | Set |
|---|---|
| 순서 있음 | 순서 없음 |
| 중복 가능 | 중복 불가 |
| 인덱싱 가능 | 인덱싱 불가 |

---

## 9. `and` vs `or`

| `and` | `or` |
|---|---|
| 모두 만족 | 하나라도 만족 |
| 모두 True여야 True | 하나만 True여도 True |

---

# 오늘의 핵심 정리

1. 파이썬의 인덱스는 앞에서 `0`, 뒤에서 `-1`부터 시작한다.
2. 슬라이싱은 **시작 포함, 끝 미포함**이다.
3. 리스트는 변경 가능하고 튜플은 변경할 수 없다.
4. 딕셔너리는 Key를 이용하여 Value를 찾는다.
5. Set은 중복을 허용하지 않고 순서가 없다.
6. `if - elif - else`는 위에서 아래로 확인하고 처음 True인 블록을 실행한다.
7. 조건 범위가 겹친다면 더 구체적인 조건부터 작성해야 한다.
8. `and`는 모든 조건을 만족해야 하고, `or`는 하나만 만족해도 된다.

---

## 💡 오늘 느낀 점

기초 강의에서 한 번 접했던 내용들이 다시 나왔지만,
이번에는 단순히 문법을 따라 쓰는 것보다
**비슷한 개념들의 차이를 정확히 구분하는 것이 중요하다는 점**을 느꼈다.

특히 인덱싱에서 사람이 말하는 `몇 번째`와 파이썬의 인덱스 번호가 다르고,
슬라이싱은 **시작은 포함하지만 끝은 포함하지 않는다**는 규칙이 처음에는 헷갈렸다.

List와 Tuple, Indexing과 Slicing, Packing과 Unpacking처럼
비슷해 보이는 개념들도 각각의 차이를 비교해서 정리하니
어떤 상황에서 사용하는지 훨씬 이해하기 쉬웠다.

앞으로도 새로운 문법을 단순히 외우기보다
**기존에 배운 개념과 비교하면서 차이점을 중심으로 이해하는 방식**으로 공부해야겠다.

---

## 🌟 오늘 최종 정리

- List는 변경 가능, Tuple은 변경 불가
- 인덱싱은 값 하나, 슬라이싱은 범위 선택
- 앞 인덱스는 `0`부터, 뒤 인덱스는 `-1`부터 시작
- 슬라이싱은 **시작 포함 / 끝 미포함**
- Packing은 여러 값을 묶는 것, Unpacking은 묶인 값을 나누는 것
- Dictionary는 `Key : Value` 구조이며 Key를 이용해 값을 찾음
- `keys()`, `values()`, `items()`는 각각 Key, Value, Key-Value 쌍을 가져옴
- `[]`는 없는 Key 조회 시 오류가 발생하지만 `.get()`은 안전하게 조회 가능
- Set은 **중복 없음 + 순서 없음**
- `if / elif / else`는 위에서 아래로 조건을 확인함
- 조건이 겹치는 경우 더 구체적인 조건을 먼저 작성
- `and`는 모두 만족, `or`는 하나라도 만족하면 True

---
##핵심
---
1. 파이썬 인덱스는 앞에서 `0`, 뒤에서 `-1`부터 시작한다.
2. 슬라이싱은 **시작 포함 / 끝 미포함**이다.
3. List는 변경 가능하고, Tuple은 변경할 수 없다.
4. Dictionary는 `Key`로 `Value`를 찾고, Set은 중복을 허용하지 않는다.
5. `if - elif - else`는 위에서 아래로 검사하며 처음 True가 된 블록만 실행한다.
6. `and`는 모든 조건을 만족해야 하고, `or`는 하나만 만족해도 된다.
   
---

## 📝 오늘의 한 줄

**비슷한 문법일수록 외우기보다 차이점을 비교해서 이해하자.**

---

## 🌱 앞으로의 목표

앞으로 새로운 문법을 배울 때
단순히 사용법만 외우지 않고 기존에 배운 개념과 비교하면서 정리해보기.

특히 인덱싱, 슬라이싱, 조건문처럼
문제에서 표현을 조금만 바꿔도 헷갈릴 수 있는 부분은
직접 예제를 만들어보면서 익숙해지기.

코드를 보고 결과를 예상하는 연습도 함께 하면서
문법을 실제 문제에 적용할 수 있도록 연습하자.
