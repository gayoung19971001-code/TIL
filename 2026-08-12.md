# 📚 TIL - 4장 2강: 반복문

## 1. 반복문이란?

반복문은 **같거나 비슷한 작업을 여러 번 반복해서 실행할 때 사용하는 문법**이다.

데이터 분석에서는 수백~수천 개의 데이터를 하나씩 처리해야 하는 경우가 많기 때문에 반복문이 자주 사용된다.

```python
fruits = ["사과", "바나나", "포도"]

for fruit in fruits:
    print(fruit)
```

실행 결과:

```text
사과
바나나
포도
```

---

## 2. `for` 문

`for` 문은 **반복 횟수가 정해져 있거나 리스트처럼 여러 데이터를 하나씩 처리할 때** 주로 사용한다.

기본 구조:

```python
for 변수 in 반복가능한데이터:
    실행할 코드
```

예시:

```python
products = ["노트북", "마우스", "키보드"]

for product in products:
    print(product)
```

### 핵심

```python
for product in products:
```

는

> `products`에서 데이터를 하나씩 꺼내 `product`에 넣는다.

라는 의미이다.

---

## 3. `range()` 함수

`range()`는 **숫자 범위를 만들어 반복 횟수를 지정할 때** 사용한다.

```python
for i in range(5):
    print(i)
```

실행 결과:

```text
0
1
2
3
4
```

### ⭐ 핵심 규칙

`range(5)`는 `0 ~ 4`까지 생성한다.

> **시작값 포함, 끝값 미포함**

### `range()` 사용법

```python
range(끝)
```

```python
range(5)
# 0, 1, 2, 3, 4
```

```python
range(시작, 끝)
```

```python
range(1, 6)
# 1, 2, 3, 4, 5
```

```python
range(시작, 끝, 증가값)
```

```python
range(1, 6, 2)
# 1, 3, 5
```

예:

```python
for i in range(2, 10, 3):
    print(i)
```

실행 결과:

```text
2
5
8
```

---

## 4. `while` 문

`while` 문은 **특정 조건이 `True`인 동안 계속 반복**한다.

```python
count = 1

while count <= 5:
    print(count)
    count += 1
```

실행 결과:

```text
1
2
3
4
5
```

### 실행 흐름

```text
조건 확인
↓
True
↓
코드 실행
↓
다시 조건 확인
↓
False가 되면 종료
```

---

## 5. `for` vs `while`

| 구분 | `for` | `while` |
|---|---|---|
| 기준 | 데이터 / 횟수 | 조건 |
| 사용 상황 | 반복 횟수를 알고 있을 때 | 특정 조건까지 반복할 때 |
| 종료 | 데이터 처리가 끝나면 | 조건이 False가 되면 |

쉽게 기억하면:

- `for` = 몇 번 할지 알고 있음
- `while` = 어떤 조건까지 할지 알고 있음

---

## 6. 무한 루프

`while`문의 조건이 계속 `True`라면 반복문이 끝나지 않는다.

```python
while True:
    print("계속 실행")
```

이를 **무한 루프(Infinite Loop)**라고 한다.

강제로 종료할 때는 일반적으로:

```text
Ctrl + C
```

를 사용할 수 있다.

---

## 7. `break`

`break`는 **반복문 자체를 즉시 종료**한다.

```python
for num in range(1, 6):
    if num == 4:
        break

    print(num)
```

실행 결과:

```text
1
2
3
```

> 🛑 `break` = 반복문 전체 종료

---

## 8. `continue`

`continue`는 **현재 반복만 건너뛰고 다음 반복으로 이동**한다.

```python
for num in range(1, 6):
    if num % 2 == 0:
        continue

    print(num)
```

실행 결과:

```text
1
3
5
```

> ⏭️ `continue` = 이번 차례만 건너뛰기

---

## 9. `break` vs `continue`

| 구분 | 의미 |
|---|---|
| `break` | 반복문 전체 종료 |
| `continue` | 현재 반복만 건너뛰고 다음 반복 진행 |

```text
break = 끝
continue = 건너뛰기
```

---

## 10. 중첩 반복문

반복문 안에 또 다른 반복문이 들어가는 것을 **중첩 반복문(Nested Loop)**이라고 한다.

```python
for dan in range(2, 10):
    for times in range(1, 10):
        print(dan * times)
```

### 핵심

> **바깥 반복문 1번 실행 → 안쪽 반복문 전체 실행**

구구단처럼 여러 차원의 데이터를 처리할 때 사용할 수 있다.

---

## 11. 데이터 사이언스에서 반복문 활용

예를 들어 여러 이미지에서 OCR 작업을 수행한다면:

```text
이미지 1 → OCR
이미지 2 → OCR
이미지 3 → OCR
...
이미지 2000 → OCR
```

사람이 하나씩 처리하지 않고 반복문을 사용하여 자동화할 수 있다.

반복문은 **많은 데이터를 자동으로 반복 처리하기 위한 기본 도구**이다.

---

# 🌟 오늘 최종 정리

- `for` : 정해진 횟수나 데이터를 순서대로 반복
- `range()` : 숫자 범위를 만들어 반복 횟수 지정
- `range(시작, 끝, 증가값)`
- **시작값 포함 / 끝값 미포함**
- `while` : 조건이 `True`인 동안 반복
- `break` : 반복문 전체 종료
- `continue` : 현재 반복만 건너뛰기
- 중첩 반복문 : 반복문 안에 또 다른 반복문
- 데이터 분석에서는 많은 데이터를 자동 처리할 때 반복문을 활용한다.

# 💡 오늘 느낀 점

반복문은 여러 문법이 있어서 처음에는 복잡해 보이지만, 결국 같은 작업을 여러 번 자동으로 실행하기 위한 방법이라는 점은 같다. 특히 `break`와 `continue`의 차이를 확실하게 구분하는 것이 중요하다.

# 📝 오늘의 한 줄

**for는 횟수, while은 조건, break는 끝, continue는 건너뛰기!**

# 🌱 앞으로의 목표

반복문 문법을 외우기보다 코드가 어떤 순서로 실행되는지 직접 따라가면서 이해하고, 여러 데이터를 반복 처리하는 상황에 활용할 수 있도록 연습하자.

---
# 📚 TIL - 4장 3강: 종합 제어 실습

## 1. 제어문과 반복문의 조합

실제 프로그램에서는 `if`, `for`, `while` 등을 따로 사용하기보다 함께 조합해서 사용한다.

대표적인 구조:

```python
for 데이터 in 데이터목록:
    if 조건:
        실행할 코드
```

즉,

> **반복문으로 데이터를 하나씩 확인하고 → 조건문으로 원하는 데이터만 골라낸다.**

---

## 2. 데이터 필터링

필터링(Filtering)은 많은 데이터에서 **특정 조건을 만족하는 데이터만 골라내는 것**이다.

```python
user_database = [
    {"name": "김가영", "level": "VIP", "active": True, "point": 1500},
    {"name": "홍길동", "level": "Gold", "active": True, "point": 800},
    {"name": "이철수", "level": "VIP", "active": False, "point": 2300}
]
```

```python
for user in user_database:
    if user["active"]:
        if user["level"] == "VIP":
            print(user["name"])
```

### 핵심

```text
for = 하나씩 확인
if = 조건 검사
```

---

## 3. 중첩 조건문

조건문 안에 또 다른 조건문이 들어가는 것을 **중첩 조건문**이라고 한다.

```python
if user["active"]:
    if user["level"] == "VIP":
        print("활동 중인 VIP 회원")
```

```text
1차 조건 검사
↓
통과
↓
2차 조건 검사
↓
최종 선택
```

---

## 4. 누적 연산

반복하면서 값을 계속 더하는 것을 **누적 연산(Accumulation)**이라고 한다.

```python
total = 0

for number in numbers:
    total += number
```

`+=`는 다음과 같다.

```python
total += number
```

=

```python
total = total + number
```

---

## 5. 조건에 맞는 값만 누적하기

```python
active_user_count = 0

for user in user_database:
    if user["active"]:
        active_user_count += 1
```

활성 회원을 발견할 때마다 `1`씩 증가한다.

VIP 회원 포인트만 합산할 수도 있다.

```python
total_vip_points = 0

for user in user_database:
    if user["active"]:
        if user["level"] == "VIP":
            total_vip_points += user["point"]
```

---

## 6. 최댓값 구하기

```python
student_scores = [85, 92, 78, 66, 99, 71, 88, 85]

highest_score = student_scores[0]

for score in student_scores:
    if score > highest_score:
        highest_score = score
```

최댓값은 데이터를 하나씩 비교하면서 더 큰 값을 발견할 때마다 갱신한다.

---

## 7. 최솟값 구하기

```python
lowest_score = student_scores[0]

for score in student_scores:
    if score < lowest_score:
        lowest_score = score
```

```text
최댓값 → >
최솟값 → <
```

---

## 8. 첫 번째 데이터를 초기값으로 사용하는 이유

최댓값과 최솟값을 구할 때는 실제 데이터의 첫 번째 값을 기준으로 사용하는 것이 안전하다.

```python
highest_score = student_scores[0]
lowest_score = student_scores[0]
```

예를 들어:

```python
numbers = [-10, -20, -5]
```

최댓값을 `0`으로 초기화하면 실제 데이터에는 없는 `0`이 최댓값이 되는 문제가 발생할 수 있다.

---

## 9. 합계와 평균

합계:

```python
score_sum = 0

for score in student_scores:
    score_sum += score
```

평균:

```python
score_average = score_sum / len(student_scores)
```

평균 공식:

```text
평균 = 전체 합계 ÷ 데이터 개수
```

---

## 10. `random.randint()`

`random`은 무작위 값을 만들기 위한 모듈이다.

```python
import random

computer_number = random.randint(1, 100)
```

`randint(1, 100)`은 **1과 100을 모두 포함**한다.

### `range()`와 비교

```text
range(1, 100)
→ 1 ~ 99

random.randint(1, 100)
→ 1 ~ 100
```

---

## 11. `input()` 함수

`input()`은 사용자에게 값을 입력받을 때 사용한다.

```python
user_input = input("숫자를 입력하세요: ")
```

`input()`의 결과는 기본적으로 **문자열(str)**이다.

숫자로 사용하려면:

```python
user_input = int(input("숫자를 입력하세요: "))
```

처럼 변환한다.

```text
input() → 문자열
int(input()) → 정수
```

---

## 12. Up-Down 게임

```python
import random

computer_number = random.randint(1, 100)
try_count = 0

while True:
    guess = int(input("숫자를 입력하세요: "))
    try_count += 1

    if guess == computer_number:
        print("정답입니다!")
        break

    elif guess < computer_number:
        print("Up!")

    else:
        print("Down!")
```

### 실행 흐름

```text
while True
↓
사용자 입력
↓
if / elif / else로 판단
↓
틀리면 반복
↓
맞히면 break
```

---

## 13. 무한 루프 탈출

`while True`는 계속 실행되기 때문에 반드시 종료 조건이 필요하다.

```python
if guess == computer_number:
    break
```

실수로 무한 루프가 발생하면:

```text
Ctrl + C
```

또는 Jupyter Notebook의 **Interrupt Kernel**을 이용해 실행을 중단할 수 있다.

---

## 14. 문법 연결 구조

```text
데이터
↓
for로 하나씩 확인
↓
if로 조건 검사
↓
필요한 데이터 선택
↓
+= 로 누적
↓
최댓값 / 최솟값 / 평균 계산
```

게임에서는:

```text
while True
↓
input()
↓
if / elif / else
↓
정답이면 break
```

---

# 🌟 오늘 최종 정리

- `for + if`를 사용하면 조건에 맞는 데이터를 필터링할 수 있다.
- `+=`를 사용하면 값이나 개수를 누적할 수 있다.
- 최댓값과 최솟값은 실제 데이터의 첫 번째 값을 초기값으로 사용하는 것이 안전하다.
- 합계는 `0`부터 시작한다.
- 평균은 `합계 / len(데이터)`로 계산한다.
- `random.randint()`는 무작위 정수를 생성한다.
- `input()`의 결과는 문자열이므로 필요하면 `int()`로 변환한다.
- `while True`는 무한 반복이므로 적절한 종료 조건과 `break`가 필요하다.

# 💡 오늘 느낀 점

앞에서 따로 배웠던 `if`, `for`, `while`이 실제 프로그램에서는 하나의 흐름으로 연결된다는 것을 알게 되었다. 반복하면서 데이터를 확인하고, 조건을 판단하고, 필요한 값만 누적하는 구조를 이해하는 것이 중요하다.

# 📝 오늘의 한 줄

**반복문으로 하나씩 보고, 조건문으로 골라내고, 필요한 값은 누적한다!**

# 🌱 앞으로의 목표

문법을 하나씩 외우는 것보다 여러 문법이 함께 사용될 때 코드가 어떤 순서로 실행되는지 따라가며 이해하는 연습을 하자.

---

# 📚 TIL - 5장 1강: 함수 정의와 매개변수

## 1. 함수(Function)란?

함수는 **특정 작업을 수행하는 코드를 하나로 묶어둔 것**이다.

```python
def say_hello():
    print("안녕하세요!")
```

함수는 정의만 해서는 실행되지 않고 호출해야 한다.

```python
say_hello()
```

기본 구조:

```python
def 함수이름():
    실행할 코드
```

---

## 2. 함수 이름

파이썬에서는 함수 이름에 일반적으로 **snake_case**를 사용한다.

```python
calculate_average()
get_user_info()
print_result()
```

함수 이름은 가능하면 기능을 알 수 있도록 작성한다.

---

## 3. 매개변수(Parameter)

매개변수는 **함수를 정의할 때 데이터를 받을 수 있도록 만들어 놓은 변수**이다.

```python
def welcome(name):
    print(f"{name}님, 환영합니다!")
```

여기서 `name`이 매개변수이다.

> 매개변수 = 데이터를 받을 자리

---

## 4. 인수(Argument)

인수는 함수를 호출할 때 실제로 전달하는 값이다.

```python
welcome("가영")
```

여기서 `"가영"`이 인수이다.

### ⭐ 매개변수 vs 인수

| 구분 | 의미 |
|---|---|
| Parameter | 함수를 정의할 때 받을 자리 |
| Argument | 함수를 호출할 때 전달하는 실제 값 |

```text
매개변수 = 받을 자리
인수 = 실제 넣는 값
```

---

## 5. 위치 인수(Positional Argument)

위치 인수는 **매개변수가 정의된 순서대로 값을 전달**한다.

```python
def introduce_self(name, age):
    print(f"이름: {name}, 나이: {age}")
```

```python
introduce_self("가영", 28)
```

```text
name ← "가영"
age ← 28
```

위치 인수에서는 **순서가 중요하다.**

---

## 6. 키워드 인수(Keyword Argument)

매개변수 이름을 직접 지정해서 값을 전달할 수도 있다.

```python
introduce_self(name="가영", age=28)
```

순서를 바꿔도 사용할 수 있다.

```python
introduce_self(age=28, name="가영")
```

```text
위치 인수 → 순서로 구분
키워드 인수 → 이름으로 구분
```

---

## 7. 기본값 매개변수(Default Parameter)

매개변수에 기본값을 지정할 수 있다.

```python
def introduce(name, age=20):
    print(f"이름은 {name}, 나이는 {age}세입니다.")
```

```python
introduce("가영")
```

`age`를 생략하면 `20`이 사용된다.

직접 값을 넣으면:

```python
introduce("가영", 28)
```

`28`이 사용된다.

---

## 8. 기본값 매개변수의 위치

기본값이 없는 매개변수를 먼저 작성해야 한다.

✅ 올바른 코드:

```python
def introduce(name, age=20):
    pass
```

❌ 잘못된 코드:

```python
def introduce(age=20, name):
    pass
```

> **필수 매개변수 먼저 → 기본값 매개변수 나중**

---

## 9. 가변 인수 `*args`

몇 개의 인수가 전달될지 모를 때 사용할 수 있다.

```python
def add_all(*numbers):
    total = sum(numbers)
    return total
```

```python
add_all(1, 2, 3)
```

결과:

```text
6
```

`*args`로 받은 데이터는 **튜플(tuple)** 형태로 처리된다.

---

## 10. 키워드 가변 인수 `**kwargs`

키워드 형태의 여러 값을 받을 때 사용한다.

```python
def user_info(**data):
    print(data)
```

```python
user_info(
    name="가영",
    level=5,
    active=True
)
```

결과는 딕셔너리 형태이다.

```python
{
    "name": "가영",
    "level": 5,
    "active": True
}
```

---

## 11. `*args` vs `**kwargs`

| 구분 | 데이터 형태 |
|---|---|
| `*args` | 여러 값을 튜플로 받음 |
| `**kwargs` | `key=value` 형태를 딕셔너리로 받음 |

```text
*args → Tuple
**kwargs → Dictionary
```

---

## 12. `return`

`return`은 함수에서 계산한 **결과값을 함수 밖으로 반환**한다.

```python
def add(a, b):
    return a + b
```

```python
result = add(10, 5)
```

`result`에는 `15`가 저장된다.

---

## 13. `print()` vs `return`

| `print()` | `return` |
|---|---|
| 화면에 출력 | 값을 반환 |
| 보여주는 역할 | 다른 코드에서 활용 가능 |

```python
def add(a, b):
    print(a + b)
```

는 결과를 보여주는 것이고,

```python
def add(a, b):
    return a + b
```

는 결과값을 함수 밖으로 전달한다.

---

## 14. 여러 값 반환하기

```python
def calculate_values(a, b):
    plus = a + b
    minus = a - b

    return plus, minus
```

```python
result1, result2 = calculate_values(10, 5)
```

결과:

```text
result1 = 15
result2 = 5
```

여러 값을 반환하면 내부적으로 **튜플 형태**로 묶인다.

---

## 15. `return`과 함수 종료

`return`을 만나면 함수 실행은 즉시 끝난다.

```python
def test(a):
    if a > 10:
        return "큰 수"

    return "작은 수"
```

```text
break → 반복문 종료
return → 함수 종료
```

---

## 16. 가변 인수를 이용한 평균 계산

```python
def get_average(*scores):
    if len(scores) == 0:
        return 0

    total = sum(scores)
    avg = total / len(scores)

    return avg
```

```python
get_average(80, 90, 100)
```

결과:

```text
90
```

---

## 17. 함수를 사용하는 이유

함수를 사용하면:

- 반복되는 코드 감소
- 코드 재사용 가능
- 코드 가독성 향상
- 유지보수 편리
- 기능별 코드 분리

가 가능하다.

```python
def add(a, b):
    return a + b

def subtract(a, b):
    return a - b

def multiply(a, b):
    return a * b
```

---

# 🌟 오늘 최종 정리

- 함수는 특정 기능을 수행하는 코드를 하나로 묶은 것이다.
- `def`로 정의하고 `함수이름()`으로 호출한다.
- Parameter는 받을 자리, Argument는 실제 넣는 값이다.
- 위치 인수는 순서대로 값을 전달한다.
- 키워드 인수는 매개변수 이름을 지정한다.
- 기본값이 없는 매개변수는 기본값 매개변수보다 앞에 작성한다.
- `*args`는 여러 값을 튜플로 받는다.
- `**kwargs`는 여러 키워드 인수를 딕셔너리로 받는다.
- `return`은 결과를 함수 밖으로 반환하고 함수 실행을 종료한다.

# 💡 오늘 느낀 점

지금까지는 코드를 직접 위에서부터 작성했다면 함수부터는 자주 사용하는 기능을 하나의 도구처럼 만들어 사용할 수 있게 되었다. 매개변수와 인수는 각각 '받을 자리'와 '실제로 넣는 값'으로 구분하면 이해하기 쉬웠다.

# 📝 오늘의 한 줄

**함수는 코드를 하나의 재사용 가능한 도구로 만들어주는 기능이다!**

# 🌱 앞으로의 목표

함수 문법을 외우기보다 입력값을 받고 → 처리하고 → `return`으로 결과를 돌려주는 흐름을 이해하자.

---

# 📚 TIL - 5장 2강: 변수 스코프와 람다 함수

## 1. 변수 스코프(Scope)

스코프는 **변수를 사용할 수 있는 유효 범위**를 의미한다.

대표적으로:

- 지역 변수(Local Variable)
- 전역 변수(Global Variable)

가 있다.

---

## 2. 전역 변수(Global Variable)

함수 바깥에서 선언된 변수이다.

```python
message = "전역 변수"

def test():
    print(message)

test()
print(message)
```

전역 변수는 비교적 넓은 영역에서 사용할 수 있다.

---

## 3. 지역 변수(Local Variable)

함수 안에서 선언된 변수이다.

```python
def test():
    message = "지역 변수"
    print(message)
```

지역 변수는 해당 함수 내부에서만 사용할 수 있다.

```text
함수 밖 → Global
함수 안 → Local
```

---

## 4. 변수 가림(Shadowing)

전역 변수와 지역 변수의 이름이 같으면 함수 내부에서는 지역 변수가 우선된다.

```python
message = "전역"

def test():
    message = "지역"
    print(message)

test()
print(message)
```

결과:

```text
지역
전역
```

---

## 5. LEGB 규칙

파이썬은 변수를 찾을 때 다음 순서로 검색한다.

```text
L → E → G → B
```

- L : Local
- E : Enclosing
- G : Global
- B : Built-in

즉:

```text
Local
↓
Enclosing
↓
Global
↓
Built-in
```

가장 가까운 범위부터 찾는다.

---

## 6. `global` 키워드

함수 안에서 전역 변수의 값을 직접 수정할 때 사용한다.

```python
visitor_count = 10

def add_visitor():
    global visitor_count
    visitor_count += 1

add_visitor()

print(visitor_count)
```

결과:

```text
11
```

하지만 `global`을 지나치게 사용하면 변수 변경 위치를 추적하기 어려우므로 필요한 경우에만 사용하는 것이 좋다.

---

## 7. 람다 함수(lambda)

람다는 간단한 함수를 짧게 표현하는 방법이다.

일반 함수:

```python
def calculate_price(price):
    return price * 0.9
```

람다:

```python
lambda price: price * 0.9
```

기본 구조:

```python
lambda 매개변수: 표현식
```

---

## 8. 일반 함수 vs 람다

| 일반 함수 | lambda |
|---|---|
| `def` 사용 | `lambda` 사용 |
| 복잡한 기능 가능 | 간단한 표현식에 적합 |
| 여러 줄 작성 가능 | 한 줄 표현에 적합 |

```text
복잡하면 def
간단하면 lambda
```

---

## 9. `map()`

`map()`은 **모든 데이터에 같은 함수를 적용하여 값을 변환**한다.

```python
prices = [1000, 2000, 3000, 4000]

result = list(
    map(lambda price: price * 1.1, prices)
)
```

구조:

```python
map(함수, 데이터)
```

> `map()` = 모든 데이터를 변환

---

## 10. `filter()`

`filter()`는 **조건에 맞는 데이터만 선택**한다.

```python
heights = [120, 150, 170, 180, 125]

result = list(
    filter(lambda height: height >= 160, heights)
)
```

결과:

```python
[170, 180]
```

`filter()`에서는:

```text
True → 남김
False → 제거
```

---

## 11. `map()` vs `filter()`

| 함수 | 역할 |
|---|---|
| `map()` | 값을 변환 |
| `filter()` | 조건에 맞는 값 선택 |

```text
map = 바꾼다
filter = 거른다
```

예:

```python
numbers = [1, 2, 3, 4, 5]
```

```python
list(map(lambda x: x * 10, numbers))
```

결과:

```python
[10, 20, 30, 40, 50]
```

```python
list(filter(lambda x: x >= 3, numbers))
```

결과:

```python
[3, 4, 5]
```

---

## 12. `map()`과 `filter()` 연결

```python
numbers = [1, 2, 3, 4, 5]

mapped = map(lambda x: x * 10, numbers)
filtered = filter(lambda x: x >= 30, mapped)

result = list(filtered)
```

결과:

```python
[30, 40, 50]
```

흐름:

```text
원본 데이터
↓
map()
↓
값 변환
↓
filter()
↓
조건에 맞는 값 선택
```

---

## 13. 리스트 컴프리헨션과 비교

`map()`:

```python
result = list(map(lambda x: x * 2, numbers))
```

리스트 컴프리헨션:

```python
result = [x * 2 for x in numbers]
```

`filter()`:

```python
result = list(filter(lambda x: x >= 3, numbers))
```

리스트 컴프리헨션:

```python
result = [x for x in numbers if x >= 3]
```

---

## 14. 일급 객체(First-class Object)

파이썬에서는 **함수도 하나의 값처럼 사용할 수 있다.**

함수를:

- 변수에 저장
- 다른 함수에 전달
- 함수의 결과로 반환

할 수 있다.

```python
def normal_delivery(product):
    return f"{product}: 일반 배송"

delivery = normal_delivery
```

---

## 15. 함수 이름 vs 함수 실행

```python
normal_delivery
```

→ 함수 자체

```python
normal_delivery()
```

→ 함수를 실행

이 차이는 고차 함수를 사용할 때 중요하다.

---

## 16. 고차 함수(Higher-order Function)

고차 함수는 **다른 함수를 인수로 받거나 함수를 반환하는 함수**이다.

```python
def normal_point(price):
    return price * 0.01

def vip_point(price):
    return price * 0.05

def calculate_point(point_function, price):
    return point_function(price)
```

```python
calculate_point(normal_point, 10000)
```

---

## 17. 일급 객체 vs 고차 함수

```text
일급 객체
→ 함수를 값처럼 다룰 수 있는 특징

고차 함수
→ 함수를 인수로 받거나 반환하는 함수
```

---

# 🌟 오늘 최종 정리

- Scope는 변수를 사용할 수 있는 범위이다.
- 함수 안의 변수는 Local, 함수 밖의 변수는 Global이다.
- 파이썬은 `Local → Enclosing → Global → Built-in` 순서로 변수를 찾는다.
- `global`을 사용하면 함수 내부에서 전역 변수를 수정할 수 있다.
- `lambda`는 간단한 함수를 짧게 표현한다.
- `map()`은 모든 데이터를 변환한다.
- `filter()`는 조건에 맞는 데이터만 선택한다.
- 파이썬에서 함수는 일급 객체이다.
- 함수를 인수로 받거나 반환하는 함수는 고차 함수이다.
- `func`는 함수 자체, `func()`는 함수 실행이다.

# 💡 오늘 느낀 점

이번 강의부터 함수가 단순히 코드를 묶는 기능을 넘어 다양하게 활용되기 시작했다. `lambda`, `map()`, `filter()`도 결국 함수를 데이터 처리에 활용하는 방법이라고 생각하면 이해하기 쉬웠다.

# 📝 오늘의 한 줄

**변수는 LEGB 순서로 찾고, map은 바꾸고, filter는 골라낸다!**

# 🌱 앞으로의 목표

Local/Global, map/filter, 함수/함수()처럼 비슷해서 헷갈리는 개념을 확실하게 구분하고 데이터가 어떤 순서로 처리되는지 따라가는 연습을 하자.

---

# 📚 TIL - 6장 1강: 모듈과 패키지

## 1. 모듈(Module)

모듈은 파이썬 코드를 기능별로 나누어 저장한 **`.py` 파일 하나**이다.

```text
project/
├── main.py
├── calculator.py
├── user.py
└── database.py
```

즉:

> 📄 **파이썬 파일 하나 = 모듈**

---

## 2. 모듈을 사용하는 이유

모듈을 사용하면:

- 코드 분리
- 코드 재사용
- 가독성 향상
- 유지보수 편리
- 기능별 관리

가 가능하다.

---

## 3. 사용자 정의 모듈

`calculator.py`

```python
def add(a, b):
    return a + b

def subtract(a, b):
    return a - b
```

`main.py`

```python
import calculator

print(calculator.add(10, 5))
```

결과:

```text
15
```

---

## 4. 패키지(Package)

패키지는 **여러 모듈을 폴더 단위로 묶어 관리하는 구조**이다.

```text
shop/
├── main.py
├── account/
│   ├── profile.py
│   └── user.py
└── payment/
    ├── card.py
    └── point.py
```

```text
모듈 = .py 파일
패키지 = 여러 모듈을 담은 폴더
```

---

## 5. 모듈 vs 패키지

| 구분 | 모듈 | 패키지 |
|---|---|---|
| 단위 | 파일 | 폴더 |
| 예 | `calculator.py` | `payment/` |
| 목적 | 기능별 코드 분리 | 여러 모듈 관리 |

---

## 6. `import`

모듈 전체를 가져올 때 사용한다.

```python
import math

print(math.sqrt(16))
```

결과:

```text
4.0
```

기본 사용법:

```python
모듈이름.기능()
```

---

## 7. `from`

모듈에서 특정 기능만 가져올 수 있다.

```python
from math import sqrt

print(sqrt(16))
```

`math.`을 붙이지 않아도 된다.

---

## 8. `as`

모듈이나 기능에 별명을 붙인다.

```python
import math as m

print(m.sqrt(16))
```

데이터 분석에서 자주 보는 형태:

```python
import pandas as pd
import numpy as np
import matplotlib.pyplot as plt
```

---

## 9. `import` vs `from` vs `as`

| 문법 | 의미 |
|---|---|
| `import` | 모듈 전체 가져오기 |
| `from` | 특정 기능 가져오기 |
| `as` | 별명 붙이기 |

---

## 10. 표준 라이브러리(Standard Library)

파이썬에서 기본적으로 제공하는 라이브러리이다.

대표적으로:

```text
random
datetime
sys
```

가 있다.

---

## 11. `random`

무작위 값을 생성할 때 사용한다.

```python
import random

number = random.randint(1, 10)

print(number)
```

`randint(1, 10)`은 `1`과 `10`을 모두 포함한다.

---

## 12. `datetime`

날짜와 시간을 처리한다.

```python
import datetime

current_time = datetime.datetime.now()

print(current_time)
```

문자열 형식으로 변환:

```python
current_time.strftime("%Y-%m-%d %H:%M:%S")
```

대표적인 형식:

```text
%Y → 연도
%m → 월
%d → 일
%H → 시
%M → 분
%S → 초
```

---

## 13. `sys`

파이썬 실행 환경과 시스템 관련 정보를 확인할 수 있다.

```python
import sys

print(sys.version)
```

---

## 14. 표준 라이브러리 vs 사용자 정의 모듈

```text
표준 라이브러리
→ 파이썬에서 기본 제공

사용자 정의 모듈
→ 내가 직접 만든 .py 파일
```

둘 다 `import`를 사용해서 불러올 수 있다.

---

## 15. 여러 파일로 기능 분리하기

```text
project/
├── main.py
├── calculator.py
├── order.py
├── payment.py
└── event.py
```

`calculator.py`

```python
def calculate_price(price, quantity):
    return price * quantity
```

`main.py`

```python
import calculator

total_price = calculator.calculate_price(5000, 3)

print(total_price)
```

---

## 16. 모듈 별명 사용

```python
import calculator as calc
```

```python
calc.add(10, 5)
```

---

## 17. 랜덤 쿠폰 번호 만들기

```python
import random
import datetime

current_time = datetime.datetime.now()
random_number = random.randint(1000, 9999)

date_text = current_time.strftime("%Y%m%d")

coupon = f"{date_text}-{random_number}"

print(coupon)
```

예:

```text
20260812-4821
```

---

## 18. 함수 → 모듈 → 패키지

```text
여러 줄의 코드
↓
함수 Function
↓
모듈 Module (.py)
↓
패키지 Package (폴더)
```

프로그램이 커질수록 기능을 단계별로 나누어 관리한다.

---

# 🌟 오늘 최종 정리

- 모듈은 함수나 클래스 등의 코드를 담고 있는 `.py` 파일이다.
- 패키지는 여러 모듈을 묶어 관리하는 폴더 구조이다.
- `import`는 모듈 전체를 가져온다.
- `from`은 특정 기능을 가져온다.
- `as`는 별명을 붙인다.
- `random`, `datetime`, `sys`는 대표적인 파이썬 표준 라이브러리이다.
- 직접 만든 `.py` 파일도 다른 파일에서 `import`할 수 있다.
- 프로그램은 코드 → 함수 → 모듈 → 패키지 순서로 구조화할 수 있다.

# 💡 오늘 느낀 점

지금까지 함수를 이용해 코드를 기능별로 나눴다면, 이번에는 함수를 파일 자체로 분리해서 관리하는 방법을 배웠다. `import pandas as pd`처럼 이전에 사용했던 코드도 결국 모듈을 가져와 별명을 붙이는 같은 원리라는 점이 연결되었다.

# 📝 오늘의 한 줄

**함수를 파일로 묶으면 모듈, 모듈을 폴더로 묶으면 패키지!**

# 🌱 앞으로의 목표

`import`, `from`, `as`의 차이를 확실하게 구분하고 하나의 긴 코드를 작성하기보다 기능별로 함수를 만들고 모듈로 나누어 사용하는 구조에 익숙해지자.

---

# 📚 TIL - 6장 2강: uv를 활용한 의존성 관리

## 1. 의존성(Dependency)

의존성은 **프로젝트가 실행되기 위해 필요한 외부 패키지나 라이브러리**를 의미한다.

예를 들어:

```python
import pandas
import matplotlib
```

를 사용하는 프로젝트는 `pandas`, `matplotlib`에 의존한다고 볼 수 있다.

---

## 2. 오픈소스 생태계

파이썬에서는 다른 개발자가 만든 패키지를 설치하여 사용할 수 있다.

대표적인 예:

```text
pandas → 데이터 분석
matplotlib → 데이터 시각화
numpy → 수치 계산
```

모든 기능을 직접 만들 필요 없이 필요한 패키지를 가져와 사용할 수 있다.

---

## 3. PyPI

**PyPI(Python Package Index)**는 파이썬 패키지들이 등록되어 있는 저장소이다.

쉽게 생각하면:

> 🏪 **파이썬 패키지 앱스토어**

와 비슷하다.

---

## 4. 패키지의 의존성

하나의 패키지가 또 다른 패키지를 필요로 할 수도 있다.

```text
내 프로젝트
↓
pandas
↓
pandas가 필요로 하는 다른 패키지
```

프로젝트가 커질수록 이러한 의존성 관계를 관리하는 것이 중요하다.

---

## 5. `uv`

`uv`는 **파이썬 프로젝트의 패키지와 의존성, 개발 환경을 관리하는 도구**이다.

예:

```bash
uv add pandas
```

`pandas`를 프로젝트의 의존성으로 추가할 수 있다.

---

## 6. `pip` vs `uv`

기존 패키지 설치:

```bash
pip install pandas
```

`uv`:

```bash
uv add pandas
```

간단히 구분하면:

```text
pip
→ 패키지 설치

uv
→ 패키지 설치 + 프로젝트 의존성 및 환경 관리
```

---

## 7. 프로젝트 생성

```bash
uv init my_dependency_project
```

기본적인 프로젝트 구조가 만들어진다.

```text
my_dependency_project/
├── main.py
├── pyproject.toml
└── ...
```

---

## 8. `pyproject.toml`

`pyproject.toml`은 **파이썬 프로젝트의 설정 및 의존성 정보를 저장하는 파일**이다.

예:

```toml
dependencies = [
    "pandas",
    "matplotlib"
]
```

쉽게:

> 📋 **이 프로젝트에 무엇이 필요한지 기록하는 명세서**

---

## 9. `uv add`

새로운 패키지를 프로젝트에 추가할 때 사용한다.

```bash
uv add pandas
```

여러 패키지를 동시에 추가할 수도 있다.

```bash
uv add pandas matplotlib
```

개념적으로:

```text
uv add
↓
패키지 추가
+
pyproject.toml 갱신
+
uv.lock 갱신
+
프로젝트 환경에 설치
```

---

## 10. 선언적 의존성 관리

`pyproject.toml`에 필요한 패키지를 기록해두는 방식이다.

```toml
dependencies = [
    "pandas",
    "matplotlib"
]
```

개발자는 어떤 패키지가 필요한지 선언하고 실제 설치 및 관리는 도구가 처리한다.

---

## 11. `uv.lock`

`uv.lock`은 프로젝트에서 실제로 사용하는 **구체적인 패키지 버전 정보를 기록하는 잠금 파일**이다.

> 🔒 **동일한 개발 환경을 재현하기 위한 파일**

---

## 12. `pyproject.toml` vs `uv.lock`

| 파일 | 역할 |
|---|---|
| `pyproject.toml` | 프로젝트가 어떤 패키지를 필요로 하는지 정의 |
| `uv.lock` | 실제 사용할 구체적인 버전을 기록 |

쉽게:

```text
pyproject.toml
→ "pandas가 필요해!"

uv.lock
→ "정확히 이 버전의 pandas를 사용할게!"
```

### ⭐ 핵심

```text
pyproject.toml = 요구사항
uv.lock = 확정된 환경
```

---

## 13. 버전 관리가 필요한 이유

같은 코드라도 패키지 버전이 다르면 동작이 달라지거나 오류가 발생할 수 있다.

```text
개발자 A
→ pandas 버전 A
→ 정상 실행

개발자 B
→ pandas 버전 B
→ 오류 발생
```

따라서 프로젝트에서 사용한 패키지 버전을 기록해두는 것이 중요하다.

---

## 14. 가상환경 `.venv`

가상환경은 **프로젝트마다 독립적인 파이썬 환경을 만드는 것**이다.

```text
컴퓨터
│
├── 프로젝트 A
│   └── pandas 버전 A
│
└── 프로젝트 B
    └── pandas 버전 B
```

같은 컴퓨터에서도 프로젝트마다 다른 버전의 패키지를 사용할 수 있다.

> 🏠 가상환경 = 프로젝트 전용 파이썬 방

---

## 15. `uv sync`

`uv sync`는 프로젝트 설정을 기준으로 **현재 개발 환경을 맞춰주는 명령어**이다.

```bash
uv sync
```

`pyproject.toml`과 `uv.lock`을 기준으로 필요한 패키지를 설치하고 환경을 구성한다.

쉽게:

> 🔄 **이 프로젝트 설정대로 내 환경을 맞춰줘!**

---

## 16. 다른 컴퓨터에서 환경 복원

프로젝트를 전달받았다고 가정한다.

```text
project/
├── main.py
├── pyproject.toml
└── uv.lock
```

이후:

```bash
uv sync
```

를 실행하면 필요한 패키지를 설치하여 개발 환경을 복원할 수 있다.

```text
프로젝트 받기
↓
uv sync
↓
필요한 패키지 설치
↓
개발 환경 복원
```

---

## 17. `.venv`를 다시 만들 수 있는 이유

`.venv`가 삭제되어도:

```text
pyproject.toml
+
uv.lock
```

이 남아 있다면:

```bash
uv sync
```

를 통해 다시 환경을 구성할 수 있다.

---

## 18. 프로젝트 공유

프로젝트를 공유할 때 중요한 것은:

```text
소스 코드
pyproject.toml
uv.lock
```

이다.

상대방은:

```bash
uv sync
```

를 실행해 동일한 환경을 구성할 수 있다.

---

## 19. PyPI → uv → 프로젝트

전체 관계:

```text
PyPI
│
│ 패키지 제공
↓
uv
│
│ 설치 및 의존성 관리
↓
내 프로젝트
├── pyproject.toml
└── uv.lock
```

```text
PyPI → 패키지가 있는 곳
uv → 패키지를 관리하는 도구
pyproject.toml → 필요한 패키지 기록
uv.lock → 구체적인 버전 기록
```

---

## 20. `uv add` vs `uv sync`

| 명령어 | 역할 |
|---|---|
| `uv add` | 새로운 패키지 추가 |
| `uv sync` | 프로젝트 설정과 현재 환경 동기화 |

```text
uv add = 추가 ➕
uv sync = 환경 맞추기 🔄
```

---

## 21. `pip install` vs `uv add` vs `uv sync`

```text
pip install pandas
→ pandas 설치

uv add pandas
→ pandas를 프로젝트 의존성으로 추가하고 관리

uv sync
→ 이미 기록되어 있는 의존성을 기준으로 환경 복원
```

---

## 22. 데이터 사이언스에서 의존성 관리

데이터 분석에서는 많은 라이브러리를 사용한다.

```text
pandas
numpy
matplotlib
seaborn
scikit-learn
```

따라서 어떤 라이브러리를 어떤 버전으로 사용했는지 관리하는 것이 중요하다.

다른 사람이 프로젝트를 실행했을 때도 동일한 환경에서 분석 결과를 재현할 수 있어야 한다.

---

## 23. 6장 1강과 6장 2강 연결

```text
6장 1강
→ 코드 구조 관리
→ 함수 / 모듈 / 패키지

6장 2강
→ 실행 환경 관리
→ 의존성 / uv / pyproject.toml / uv.lock
```

---

## 24. 전체 흐름

```text
프로젝트 생성
↓
uv init
↓
패키지가 필요함
↓
uv add pandas matplotlib
↓
pyproject.toml
필요한 패키지 기록
↓
uv.lock
구체적인 버전 기록
↓
다른 컴퓨터에서 프로젝트 받음
↓
uv sync
↓
동일한 개발 환경 구성
```

---

# 🌟 오늘 최종 정리

- Dependency는 프로젝트 실행에 필요한 외부 패키지나 라이브러리이다.
- PyPI는 파이썬 패키지가 등록되어 있는 저장소이다.
- `uv`는 파이썬 프로젝트의 의존성과 환경을 관리하는 도구이다.
- `uv add`는 새로운 패키지를 프로젝트에 추가한다.
- `pyproject.toml`은 프로젝트 정보와 필요한 패키지를 기록한다.
- `uv.lock`은 실제 사용할 구체적인 패키지 버전을 기록한다.
- `.venv`는 프로젝트별 독립적인 가상환경이다.
- `uv sync`는 프로젝트 설정에 맞게 개발 환경을 구성하거나 복원한다.
- `pyproject.toml = 필요한 것`
- `uv.lock = 실제 확정 버전`
- `uv add = 추가`
- `uv sync = 환경 맞추기`

# 💡 오늘 느낀 점

처음에는 `pyproject.toml`, `uv.lock`, `uv`처럼 낯선 이름이 많아 어렵게 느껴졌지만 결국 프로젝트에 어떤 패키지가 필요한지 기록하고, 다른 컴퓨터에서도 동일한 환경을 만들기 위한 관리 방법이라는 것을 알게 되었다.

# 📝 오늘의 한 줄

**pyproject.toml은 필요한 패키지를 기록하고, uv.lock은 정확한 버전을 잠근다! 🔒**

# 🌱 앞으로의 목표

패키지를 단순히 설치하는 데서 끝내지 않고 `uv add → pyproject.toml → uv.lock → uv sync`가 어떻게 연결되는지 이해하고, 앞으로 데이터 분석 프로젝트에서도 재현 가능한 개발 환경을 만드는 습관을 익히자.
